---
schema_version: 1
work_item: 1.2.4-1.3.9
notion_eap: 1.2.4, 1.2.5, 1.3.1, 1.3.2, 1.3.3, 1.3.4, 1.3.5, 1.3.6, 1.3.7, 1.3.8, 1.3.9
status: ready
base_ref: chore/1-2-4-1-3-9-tooling-qualidade
base_commit: c5baa6197aaf78ef50f059b65a25e28cc75ad2db
generated_at: 2026-08-24
---

# Context Packet — bloco 1.2.4-1.3.9 · Tooling e qualidade

> Produzido por Claude, não pelo contrato Codex `site-context-packet`. A recuperação externa já havia
> sido feita nesta sessão antes da transição de estado, e delegar significaria re-buscar as mesmas
> linhas do Notion. O desvio foi declarado a João em 2026-08-24. Formato e limites do contrato são
> respeitados.

## Scope

Sprint 0 restante: as 11 EAP em `A fazer` do Sprint 0 no Notion. Fecha o baseline técnico antes de
qualquer clone visual. Não inclui clone, conteúdo, formulário nem deploy.

O backlog local afirma 12 tasks; a contagem real medida contra o Notion é 11. Ver `F-2`.

## Source registry

| id | tipo | acesso | lido em |
|---|---|---|---|
| `S-1` | Notion data source | `collection://2f0e72ec-ef53-4e08-a466-312de7eea7d2`, SQL sobre `Sprint LIKE 'Sprint 0%'` | 2026-08-24 |
| `S-2` | repositório | `package.json`, `tsconfig.node.json`, `eslint.config.js`, `pnpm-workspace.yaml`, `.gitignore` em `c5baa61` | 2026-08-24 |
| `S-3` | repositório | `.claude/rules/architecture.md`, `.claude/rules/frontend.md`, `.claude/rules/testing.md` em `c5baa61` | 2026-08-24 |
| `S-4` | repositório | `scripts/validate-agent-workflow.mjs` em `c5baa61`, 295 linhas | 2026-08-24 |
| `S-5` | Google Drive | busca `fullText contains 'ADR-SITE-001'`, `title contains 'ADR-SITE'` | 2026-08-24 |

`S-5` retornou vazio. Não há planejamento canônico no Drive para este bloco; `ADR-SITE-001` existe
apenas como referência de campo no Notion e é criado por `1.3.8`. Fonte marcada como `empty` após
tentativa real, não como `unavailable`.

## Key facts

- `F-1` — As 11 EAP em `A fazer` do Sprint 0 são `1.2.4`, `1.2.5`, `1.3.1`, `1.3.2`, `1.3.3`, `1.3.4`, `1.3.5`, `1.3.6`, `1.3.7`, `1.3.8`, `1.3.9`. Toda dependência externa ao bloco (`1.2.1`, `1.2.3`) está `Concluída`. `S-1`
- `F-2` — `docs/superpowers/backlog.md` declara 12 tasks para este tema; `S-1` mostra 11. Divergência de contagem local, não de escopo.
- `F-3` — Nenhuma EAP do roadmap instala `tailwindcss`. `1.2.5` instala `tailwind-merge` e `1.3.1` instala `prettier-plugin-tailwindcss`; ambas pressupõem Tailwind presente. `S-1`
- `F-4` — `tsconfig.node.json` tem `"include": ["vite.config.ts"]`, então `scripts/*.mjs` e qualquer arquivo de configuração novo ficam fora de `tsc -b`. `S-2`
- `F-5` — `pnpm-workspace.yaml` tem `engineStrict: true` e `engines.node` é `>=24.19.0 <25`. Runner de CI fora dessa faixa mata todo script pnpm com `ERR_PNPM_UNSUPPORTED_ENGINE`. `S-2`
- `F-6` — `eslint.config.js` já implementa catracas de direção de import por caminho para `src/content`, `src/lib`, `src/components` e `src/components/ui`. Os diretórios ainda não existem. `S-2`
- `F-7` — `architecture.md` desenha `app components/{layout,sections,ui} content integrations lib assets` e exige "diretório não nasce vazio". O critério de `1.3.5` no Notion pede `features/contact`, `config`, `tests` e `docs`, ausentes da rule. `S-1` `S-3`
- `F-8` — `scripts/validate-agent-workflow.mjs` tem 295 linhas e zero anotação JSDoc. Sob `checkJs` com `strict` e `noUncheckedIndexedAccess` produzirá erro; `.mjs` sob `erasableSyntaxOnly` não aceita sintaxe de tipo, então a correção sai por JSDoc ou guarda em runtime. `S-4`

## Resolved divergences

Decisões de João em 2026-08-24, registradas antes do planejamento.

| # | Divergência | Decisão |
|---|---|---|
| 1 | Move de `docs/1.1.6-…md` para `historico/` sujava a árvore e não pertencia ao bloco | Commit próprio no início da branch, declarado no PR. Feito em `c5baa61`. |
| 2 | `F-3`, Tailwind sem EAP | Instalar `tailwindcss` dentro de `1.2.5`, sem escrever no Notion. Vira débito D-08. |
| 3 | `F-7`, estrutura de `1.3.5` | `.claude/rules/architecture.md` é a autoridade; a árvore do Notion vira débito D-09. |
| 4 | `F-4`, zona cega do typecheck | `pnpm check` engloba `agent:check`; D-06 fecha dentro de `1.3.6`. |
| 5 | Navegadores do Playwright, não especificados no Notion | Só Chromium. WebKit vira débito D-10 com gatilho no Sprint 5. |
| 6 | `1.3.5` criar `src/components/` antes do clone | Não. `1.3.5` cria só `lib/` e `content/`; `components/` nasce no Sprint 2 com consumidor real. |

## Constraints

- Lei 1: clone antes de redesign. Nenhum componente com decisão estética nasce neste bloco.
- `architecture.md`: diretório só existe com consumidor real.
- Node fixado em `24.19.0` por `.nvmrc`; `engineStrict` transforma desvio em erro (`F-5`).
- `verbatimModuleSyntax` e `erasableSyntaxOnly` ativos: import somente-tipo exige `type`; `enum` e afins quebram o build.
- Não há test runner hoje; `1.3.2` é quem cria o primeiro.
- Notion e Drive são somente leitura neste bloco. Escrita exige autorização nova (Lei 6).

## Acceptance signals

Critério de aceite por EAP, copiado de `S-1` em forma comprimida. Excede o teto de 8 fatos do
contrato de propósito: são o alvo de prova de `/executar-site`, não fatos de contexto, e ficam em
seção própria justamente para não competir com `Key facts`.

| EAP | Sinal de aceite |
|---|---|
| `1.2.4` | `.env` real, `node_modules` e artefato de build não versionados; `.env.example` sem segredo |
| `1.2.5` | Dependências instaladas; nenhuma lib de estado, fetch ou UI pesada adicionada |
| `1.3.1` | `pnpm format:check` passa e a formatação é determinística |
| `1.3.2` | Teste mínimo roda por `pnpm test` e falha corretamente com asserção inválida |
| `1.3.3` | Smoke test abre a home e valida resposta/renderização |
| `1.3.4` | Suite E2E executa auditoria axe da home e reporta violações |
| `1.3.5` | Estrutura mínima existe e responsabilidades estão documentadas |
| `1.3.6` | `pnpm check` executa a cadeia completa e retorna zero |
| `1.3.7` | Workflow roda em PR e bloqueia merge quando gate obrigatório falha |
| `1.3.8` | Novo dev clona, instala, inicia, testa e entende as decisões sem contexto oral |
| `1.3.9` | `pnpm check` e `pnpm e2e` passam em ambiente limpo e CI está verde |

## Open questions

Nenhuma aberta. As seis divergências acima foram decididas por João antes do planejamento.

## Staleness triggers

- Mudança de `Status` de qualquer das 11 EAP em `S-1`, ou criação de EAP nova no Sprint 0.
- Commit novo em `main` que toque `package.json`, `tsconfig*.json`, `eslint.config.js` ou `pnpm-workspace.yaml`.
- Edição de `.claude/rules/architecture.md`, que invalida `F-7` e a decisão 3.
- Aparecimento de planejamento canônico no Drive para `ADR-SITE-001`, que invalida `S-5`.
- Reconciliação do débito D-01, que pode reabrir o escopo de tasks stale do roadmap.
