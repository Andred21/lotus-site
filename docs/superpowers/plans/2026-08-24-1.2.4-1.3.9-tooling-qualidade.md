# Tooling e qualidade — bloco 1.2.4-1.3.9 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar o Sprint 0 entregando formatação determinística, testes unitários e E2E, auditoria de acessibilidade, arquitetura de pastas com consumidor real, gate único de qualidade, CI e documentação de decisão — sem escrever uma linha do clone visual.

**Architecture:** Onze EAP em quatro fases. A fase A instala ambiente e dependências; a B cria `src/lib/` e `src/content/` com consumidor real em `src/App.tsx`; a C instala o ferramental que verifica esse código; a D amarra tudo em `pnpm check`, roda em CI e documenta. Cada EAP vira exatamente um commit.

**Tech Stack:** Vite 8, React 19, TypeScript 6 (strict), Tailwind 4, Prettier, Vitest + Testing Library, Playwright (Chromium), `@axe-core/playwright`, GitHub Actions, pnpm 11, Node 24.19.0.

**Spec:** `docs/superpowers/specs/2026-08-24-1.2.4-1.3.9-tooling-qualidade-design.md`
**Context packet:** `docs/superpowers/context-packets/1.2.4-1.3.9.md`
**Branch:** `chore/1-2-4-1-3-9-tooling-qualidade` — nunca commitar em `main`.

## Global Constraints

- Gerenciador é **pnpm**. Nunca `npm`, nunca `yarn`.
- Node fixado em `24.19.0` por `.nvmrc`. `engineStrict: true` no `pnpm-workspace.yaml` transforma runtime fora de `>=24.19.0 <25` em `ERR_PNPM_UNSUPPORTED_ENGINE`.
- TypeScript roda com `strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`, `erasableSyntaxOnly` e `verbatimModuleSyntax`. Import somente-tipo exige a palavra `type`. `enum` e outras sintaxes com semântica de runtime quebram o build.
- Proibido `any` e proibido cast para calar o compilador. Erro de tipo se resolve com guarda ou com tipo correto.
- Um commit por EAP, Conventional Commits com a EAP no escopo: `feat(1.2.4): ...`.
- Nenhum gate é declarado verde sem execução real e saída observada.
- Pacote recusado por `minimumReleaseAge` libera exceção em `minimumReleaseAgeExclude` no `pnpm-workspace.yaml`, nunca por desligar a política.
- Nenhuma escrita em Notion, Drive ou Figma. Nenhum `git push`. Nenhum `gh pr create`. O fechamento é de `/fechar-site`.
- Não fixar versão à mão no `package.json`: instalar pelo `pnpm add` e deixar a resolução acontecer.

---

## File Structure

**Criados:**

| Arquivo                    | Responsabilidade                                                 |
| -------------------------- | ---------------------------------------------------------------- |
| `.editorconfig`            | convenções de editor alinhadas ao Prettier                       |
| `.env.example`             | contrato de variáveis de ambiente, sem segredo                   |
| `.prettierrc`              | configuração do Prettier e do plugin Tailwind                    |
| `.prettierignore`          | o que o Prettier não formata                                     |
| `src/lib/cn.ts`            | junção de classes: clsx + tailwind-merge. Helper puro, sem React |
| `src/lib/cn.test.ts`       | teste unitário de `cn`                                           |
| `src/content/site.ts`      | strings institucionais. Não importa React                        |
| `vitest.config.ts`         | runner unitário, ambiente jsdom                                  |
| `playwright.config.ts`     | runner E2E, só Chromium                                          |
| `tsconfig.e2e.json`        | projeto TypeScript de `e2e/`                                     |
| `e2e/home.spec.ts`         | smoke da home                                                    |
| `e2e/a11y.spec.ts`         | auditoria axe da home, reporta sem reprovar                      |
| `.github/workflows/ci.yml` | gates em pull request                                            |
| `docs/adr/ADR-SITE-001.md` | decisões de fundação                                             |

**Modificados:**

| Arquivo                               | Mudança                                   |
| ------------------------------------- | ----------------------------------------- |
| `.gitignore`                          | artefatos de env e de teste               |
| `package.json`                        | dependências e scripts de qualidade       |
| `vite.config.ts`                      | plugin do Tailwind                        |
| `src/index.css`                       | `@import "tailwindcss"`                   |
| `src/App.tsx`                         | consome `cn` e `site`                     |
| `tsconfig.json`                       | referência ao projeto de E2E              |
| `tsconfig.node.json`                  | `allowJs`, `checkJs` e `include` ampliado |
| `scripts/validate-agent-workflow.mjs` | tipos via JSDoc e guardas                 |
| `README.md`                           | fluxo de desenvolvimento                  |
| `docs/superpowers/backlog.md`         | débitos novos                             |

---

## Task 1: 1.2.4 · gitignore, editorconfig e env

**Files:**

- Modify: `.gitignore`
- Create: `.editorconfig`
- Create: `.env.example`

**Interfaces:**

- Consumes: nada.
- Produces: `.editorconfig` com `indent_size = 2` e `end_of_line = lf`, que a Task 4 replica no `.prettierrc`.

- [ ] **Step 1: Adicionar artefatos de ambiente e de teste ao `.gitignore`**

`node_modules`, `dist` e logs já estão cobertos. A entrada `*.local` existente casa `.env.local` mas **não** casa `.env` puro. Acrescentar ao fim do arquivo:

```gitignore

# Environment
.env
.env.*.local

# Test artifacts
playwright-report/
test-results/
coverage/
```

- [ ] **Step 2: Verificar que `.env` seria ignorado**

```bash
printf 'VITE_FOO=bar\n' > .env
git check-ignore -v .env
rm .env
```

Esperado: linha citando `.gitignore` e o padrão `.env`. Se não imprimir nada, o padrão não pegou — corrigir antes de seguir.

- [ ] **Step 3: Criar `.editorconfig`**

```editorconfig
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
```

`trim_trailing_whitespace = false` em Markdown porque dois espaços no fim da linha são quebra de linha significativa.

- [ ] **Step 4: Criar `.env.example`**

Não existe integração até o Sprint 3, então não há variável real a declarar. O arquivo documenta o contrato:

```dotenv
# Contrato de variáveis de ambiente — lotus-site
#
# Vite só expõe ao cliente variáveis com prefixo VITE_. Tudo que recebe esse
# prefixo vai para o bundle e é público: nunca coloque segredo aqui nem em .env.
#
# Segredo real (chave de API, token de formulário) vive no provedor de deploy
# ou no cofre do CI, nunca no repositório.
#
# Este arquivo é versionado e serve de referência. Copie para .env, que é
# ignorado pelo Git, ao precisar de valores locais.
#
# Nenhuma variável é necessária para rodar o projeto hoje. As integrações
# chegam no Sprint 3 e cada uma acrescenta sua linha aqui, sem valor real.
```

- [ ] **Step 5: Provar que o contrato de aceite vale**

```bash
git status --short
git check-ignore -v node_modules dist
grep -c 'VITE_[A-Z_]*=' .env.example
```

Esperado: `.gitignore`, `.editorconfig` e `.env.example` aparecendo como novos ou modificados; `node_modules` e `dist` confirmados como ignorados; contagem `0` de variáveis com valor atribuído no `.env.example`, provando ausência de segredo.

- [ ] **Step 6: Rodar o gate do harness**

```bash
pnpm agent:check
```

Esperado: `Agent workflow contract OK`.

- [ ] **Step 7: Commit**

```bash
git add .gitignore .editorconfig .env.example
git commit -m "feat(1.2.4): padronizar gitignore, editorconfig e contrato de env

.env e artefatos de Playwright/coverage passam a ser ignorados. .editorconfig
alinha editor ao Prettier que chega em 1.3.1. .env.example documenta o contrato
do prefixo VITE_ sem declarar variável nem segredo."
```

---

## Task 2: 1.2.5 · dependências mínimas de runtime e Tailwind

**Files:**

- Modify: `package.json`
- Modify: `vite.config.ts`
- Modify: `src/index.css:1`

**Interfaces:**

- Consumes: nada.
- Produces: `clsx` e `tailwind-merge` para a Task 3; `tailwindcss` para o plugin de Prettier da Task 4; `zod` e `lucide-react` disponíveis para sprints futuros.

**Nota de escopo:** o Notion lista apenas Zod, clsx, tailwind-merge e Lucide React. `tailwindcss` e `@tailwindcss/vite` entram por decisão explícita de João em 2026-08-24, porque nenhuma EAP do roadmap instala Tailwind e duas dependem dele. Isso vai declarado no corpo do commit.

- [ ] **Step 1: Instalar as dependências de runtime**

```bash
pnpm add zod clsx tailwind-merge lucide-react
```

Esperado: quatro pacotes em `dependencies`. Se algum for recusado por `minimumReleaseAge`, acrescentar a exceção em `minimumReleaseAgeExclude` no `pnpm-workspace.yaml` — nunca desligar a política.

- [ ] **Step 2: Instalar o Tailwind**

```bash
pnpm add -D tailwindcss @tailwindcss/vite
```

Tailwind 4 configura por CSS. **Não** criar `tailwind.config.js`.

- [ ] **Step 3: Registrar o plugin no Vite**

`vite.config.ts` passa a ser:

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

- [ ] **Step 4: Importar o Tailwind no CSS**

`src/index.css` começa hoje em `:root {`. Inserir na primeira linha, antes de tudo:

```css
@import 'tailwindcss';
```

`@import` precisa vir antes de qualquer regra, senão o CSS é inválido. Os tokens em `:root` e o restante do arquivo continuam intactos: o clone do Sprint 2 vai reescrevê-los, não esta task.

- [ ] **Step 5: Provar que o Tailwind está ativo**

```bash
pnpm build
```

Esperado: build conclui sem erro. Confirmar que o Tailwind entrou no CSS gerado:

```bash
grep -c 'box-sizing:border-box' dist/assets/*.css
```

Esperado: `1` ou mais. Zero significa que o plugin não rodou — investigar antes de seguir, não commitar.

A verificação mira o preflight, o reset que o `@import 'tailwindcss'` sempre emite. Não adianta
procurar utilitário como `.flex` aqui: o Tailwind 4 só gera classe efetivamente usada, e nenhuma
classe Tailwind existe no código antes da Task 3.

- [ ] **Step 6: Confirmar que nada pesado entrou**

```bash
node -e "const p=require('./package.json');console.log(Object.keys(p.dependencies).sort().join(' '))"
```

Esperado exatamente: `clsx lucide-react react react-dom tailwind-merge zod`. Nenhuma biblioteca de estado, cliente HTTP ou kit de UI, conforme o critério da EAP e a Lei 7.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-lock.yaml pnpm-workspace.yaml vite.config.ts src/index.css
git commit -m "feat(1.2.5): instalar dependências mínimas de runtime e Tailwind

Zod, clsx, tailwind-merge e Lucide React conforme a EAP. tailwindcss e
@tailwindcss/vite entram além do texto do Notion por decisão de João em
2026-08-24: nenhuma EAP do roadmap instala Tailwind, mas 1.2.5 pede
tailwind-merge e 1.3.1 pede prettier-plugin-tailwindcss. Registrado como
débito D-08 para reconciliação futura do roadmap.

Tailwind 4 configura por CSS; nenhum tailwind.config.js é criado."
```

---

## Task 3: 1.3.5 · arquitetura inicial de pastas

**Files:**

- Create: `src/lib/cn.ts`
- Create: `src/content/site.ts`
- Modify: `src/App.tsx`

**Interfaces:**

- Consumes: `clsx` e `tailwind-merge` da Task 2.
- Produces:
  - `cn(...inputs: ClassValue[]): string` exportado de `src/lib/cn.ts` — alvo do teste da Task 5.
  - `site` exportado de `src/content/site.ts`, objeto `as const` com `name: 'Lotus OTEC'` e `locale: 'es-CL'`. `site.name` é o texto que as Tasks 6 e 7 procuram na home.

**Nota de escopo:** `src/components/`, `src/app/` e `src/integrations/` **não** nascem aqui. `architecture.md` exige consumidor real e a Lei 1 proíbe inventar aparência antes do clone. Decisão de João em 2026-08-24, registrada como débito D-09.

- [ ] **Step 1: Criar `src/lib/cn.ts`**

```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

/**
 * Junta classes condicionais e resolve conflito entre utilitários Tailwind,
 * mantendo a última classe vencedora.
 */
export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs))
}
```

`type ClassValue` precisa da palavra `type`: `verbatimModuleSyntax` está ligado.

- [ ] **Step 2: Criar `src/content/site.ts`**

```ts
/**
 * Conteúdo institucional. Não importa React, componente nem integração —
 * a direção de dependências é catraca de lint em eslint.config.js.
 */
export const site = {
  name: 'Lotus OTEC',
  locale: 'es-CL',
} as const
```

- [ ] **Step 3: Fazer `src/App.tsx` consumir os dois**

Diretório com consumidor real é o critério da rule. Duas mudanças cirúrgicas no arquivo existente, sem tocar no resto do scaffold.

Acrescentar aos imports do topo:

```tsx
import { cn } from './lib/cn'
import { site } from './content/site'
```

Trocar o bloco do título — hoje `<h1>Get started</h1>` seguido do parágrafo — por:

```tsx
<div>
  <h1 className={cn('text-balance', count > 0 && 'underline')}>{site.name}</h1>
  <p>
    Edit <code>src/App.tsx</code> and save to test <code>HMR</code>
  </p>
</div>
```

Isso exercita `cn` de verdade — com uma classe estática e uma condicional — e coloca `site.name` na home, que é o que o smoke da Task 6 vai procurar.

- [ ] **Step 4: Verificar que as catracas de import aceitam a estrutura**

```bash
pnpm lint
```

Esperado: zero erro. `eslint.config.js` já restringe imports em `src/lib/**` e `src/content/**`; se `cn.ts` ou `site.ts` violassem a direção de dependências, falharia aqui.

- [ ] **Step 5: Verificar typecheck e build**

```bash
pnpm build
```

Esperado: `tsc -b` e `vite build` sem erro.

- [ ] **Step 6: Confirmar visualmente que o nome renderiza**

```bash
pnpm build && grep -c 'Lotus OTEC' dist/assets/*.js
```

Esperado: `1` ou mais.

- [ ] **Step 7: Commit**

```bash
git add src/lib/cn.ts src/content/site.ts src/App.tsx
git commit -m "feat(1.3.5): criar lib e content com consumidor real

src/lib/cn.ts junta classes via clsx + tailwind-merge; src/content/site.ts
guarda as strings institucionais; App.tsx consome os dois, então nenhum
diretório nasce vazio.

components/, app/ e integrations/ ficam para o Sprint 2, quando houver
consumidor real: criar componente agora significaria inventar aparência
antes do clone, contra a Lei 1. Decisão de João em 2026-08-24, débito D-09."
```

---

## Task 4: 1.3.1 · Prettier e ordenação de classes Tailwind

**Files:**

- Create: `.prettierrc`
- Create: `.prettierignore`
- Modify: `package.json`

**Interfaces:**

- Consumes: `tailwindcss` da Task 2, `.editorconfig` da Task 1.
- Produces: scripts `format` e `format:check`. A Task 8 encadeia `format:check`? **Não** — o Notion define `check` como lint + typecheck + unit + build. `format:check` fica fora e roda no CI da Task 9 como passo próprio.

- [ ] **Step 1: Instalar Prettier e o plugin**

```bash
pnpm add -D prettier prettier-plugin-tailwindcss
```

- [ ] **Step 2: Criar `.prettierrc`**

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "endOfLine": "lf",
  "plugins": ["prettier-plugin-tailwindcss"],
  "tailwindStylesheet": "./src/index.css"
}
```

`semi: false` e `singleQuote: true` são o estilo que o scaffold já usa — conferir em `src/main.tsx`, que não tem ponto e vírgula. `tailwindStylesheet` é obrigatório no Tailwind 4: não existe `tailwind.config.js` para o plugin encontrar sozinho. Se a versão instalada do plugin não reconhecer a opção, ela terá outro nome — consultar o README do pacote instalado antes de inventar.

- [ ] **Step 3: Criar `.prettierignore`**

```gitignore
dist
coverage
playwright-report
test-results
pnpm-lock.yaml
public/icons.svg
```

- [ ] **Step 4: Adicionar os scripts**

Em `package.json`, dentro de `scripts`:

```json
    "format": "prettier --write .",
    "format:check": "prettier --check ."
```

- [ ] **Step 5: Rodar o check e observar a falha esperada**

```bash
pnpm format:check
```

Esperado: **FALHA**, listando arquivos com formatação divergente. Isso é o resultado correto neste passo — o repositório nunca passou por Prettier.

- [ ] **Step 6: Formatar tudo**

```bash
pnpm format
```

- [ ] **Step 7: Provar determinismo**

```bash
pnpm format:check
pnpm format && git diff --stat
```

Esperado: primeiro comando passa; o segundo produz diff vazio, provando que rodar de novo não muda nada. Determinismo é o critério de aceite literal da EAP.

- [ ] **Step 8: Confirmar que a formatação não quebrou nada**

```bash
pnpm lint && pnpm build
```

Esperado: ambos verdes. Se o Prettier reformatou `src/`, `tsc` e ESLint precisam continuar felizes.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat(1.3.1): configurar Prettier e ordenação de classes Tailwind

prettier-plugin-tailwindcss com tailwindStylesheet apontando para
src/index.css, já que Tailwind 4 não tem arquivo de configuração.
format:check é determinístico: segunda passada não produz diff.

eslint-config-prettier não entra: a configuração atual não tem regra
estilística conflitante e dependência sem necessidade viola a Lei 7."
```

---

## Task 5: 1.3.2 · Vitest e Testing Library

**Files:**

- Create: `vitest.config.ts`
- Create: `src/lib/cn.test.ts`
- Modify: `package.json`
- Modify: `tsconfig.node.json:35`

**Interfaces:**

- Consumes: `cn` de `src/lib/cn.ts` (Task 3).
- Produces: script `test` = `vitest run` e `test:watch` = `vitest`. A Task 8 encadeia `test` dentro de `check`.

- [ ] **Step 1: Instalar o runner**

```bash
pnpm add -D vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/dom
```

Testing Library entra porque a EAP pede testes de componente. Este bloco não cria componente — quem usar será o Sprint 2 —, mas a dependência é parte do critério de aceite da task.

- [ ] **Step 2: Criar `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.{ts,tsx}'],
    exclude: ['e2e/**', 'node_modules/**', 'dist/**'],
  },
})
```

`exclude` com `e2e/**` é obrigatório: sem ele o Vitest tenta executar os specs de Playwright da Task 6 e falha com erro confuso.

- [ ] **Step 3: Colocar `vitest.config.ts` sob typecheck**

`tsconfig.node.json` termina hoje com `"include": ["vite.config.ts"]`. Trocar por:

```json
  "include": ["vite.config.ts", "vitest.config.ts"]
```

Sem isso o arquivo nasce na zona cega do `tsc -b`, que é exatamente o débito D-06 — fechado por completo na Task 8.

- [ ] **Step 4: Adicionar os scripts**

Em `package.json`, dentro de `scripts`:

```json
    "test": "vitest run",
    "test:watch": "vitest"
```

- [ ] **Step 5: Escrever o teste que falha**

Criar `src/lib/cn.test.ts` com a asserção **deliberadamente errada**, para provar que o runner reprova:

```ts
import { describe, expect, it } from 'vitest'
import { cn } from './cn'

describe('cn', () => {
  it('junta classes e descarta as condicionais falsas', () => {
    expect(cn('a', false && 'b', 'c')).toBe('a b c')
  })
})
```

`cn('a', false && 'b', 'c')` produz `'a c'`, então esperar `'a b c'` tem de falhar.

- [ ] **Step 6: Rodar e observar o vermelho**

```bash
pnpm test
```

Esperado: **FALHA**, com `expected 'a c' to be 'a b c'` e saída de saída diferente de zero. Copiar essa linha: ela é a evidência do critério "falha corretamente quando a asserção é inválida" e vai no corpo do commit.

- [ ] **Step 7: Corrigir a asserção e cobrir o merge de Tailwind**

Substituir o conteúdo de `src/lib/cn.test.ts` por:

```ts
import { describe, expect, it } from 'vitest'
import { cn } from './cn'

describe('cn', () => {
  it('junta classes e descarta as condicionais falsas', () => {
    expect(cn('a', false && 'b', 'c')).toBe('a c')
  })

  it('resolve conflito de utilitário Tailwind pela última classe', () => {
    expect(cn('p-2', 'p-4')).toBe('p-4')
  })
})
```

O segundo caso é o que prova que `tailwind-merge` está de fato ligado, e não só `clsx`.

- [ ] **Step 8: Rodar e observar o verde**

```bash
pnpm test
```

Esperado: `2 passed`, saída zero.

- [ ] **Step 9: Confirmar typecheck e lint**

```bash
pnpm exec tsc -b && pnpm lint
```

Esperado: ambos verdes. `src/lib/cn.test.ts` está sob `tsconfig.app.json`, que inclui `src`, então o teste também é typechecked.

- [ ] **Step 10: Commit**

```bash
git add vitest.config.ts src/lib/cn.test.ts package.json tsconfig.node.json
git commit -m "feat(1.3.2): configurar Vitest e Testing Library

Ambiente jsdom, include restrito a src/**/*.test.* e exclude de e2e/ para o
Vitest não tentar rodar spec de Playwright.

Falha provada antes do verde: com a asserção invertida o runner reprovou com
\"expected 'a c' to be 'a b c'\" e saída diferente de zero.

vitest.config.ts entra no include de tsconfig.node.json na mesma task, senão
nasceria fora do typecheck."
```

---

## Task 6: 1.3.3 · Playwright

**Files:**

- Create: `playwright.config.ts`
- Create: `tsconfig.e2e.json`
- Create: `e2e/home.spec.ts`
- Modify: `package.json`
- Modify: `tsconfig.json`
- Modify: `tsconfig.node.json`

**Interfaces:**

- Consumes: `site.name` renderizado em `src/App.tsx` (Task 3) — o smoke procura o texto `Lotus OTEC`.
- Produces: script `e2e` = `playwright test`; `playwright.config.ts` com `use.baseURL` em `http://localhost:5173`, que a Task 7 reaproveita.

**Nota:** só Chromium, por decisão de João em 2026-08-24. WebKit e Firefox viram débito D-10 com gatilho no Sprint 5.

- [ ] **Step 1: Instalar o Playwright e o navegador**

```bash
pnpm add -D @playwright/test
pnpm exec playwright install --with-deps chromium
```

`--with-deps` instala as bibliotecas de sistema do Chromium. Em WSL sem `sudo` o comando pode falhar; nesse caso rodar `pnpm exec playwright install chromium` e registrar a limitação — **não** desistir do gate silenciosamente.

- [ ] **Step 2: Criar `playwright.config.ts`**

```ts
import { defineConfig, devices } from '@playwright/test'

const PORT = 5173
const baseURL = `http://localhost:${PORT}`

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: Boolean(process.env.CI),
  retries: process.env.CI ? 2 : 0,
  reporter: process.env.CI ? [['html', { open: 'never' }]] : [['list']],
  use: {
    baseURL,
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'pnpm dev --port ' + PORT,
    url: baseURL,
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
```

`reuseExistingServer: !process.env.CI` reaproveita um `pnpm dev` já aberto na máquina local e força servidor limpo no CI. `timeout: 120_000` cobre a primeira subida fria do Vite.

- [ ] **Step 3: Criar `tsconfig.e2e.json`**

Os specs de E2E rodam em Node mas usam a API `page`, que precisa de tipos de DOM. Não cabem em `tsconfig.app.json`, cujo `include` é `src`, nem em `tsconfig.node.json`, cujo `lib` não tem DOM. Projeto próprio:

```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.e2e.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "skipLibCheck": true,

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["e2e"]
}
```

- [ ] **Step 4: Referenciar o projeto novo e incluir a configuração**

`tsconfig.json` passa a ser:

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.e2e.json" }
  ]
}
```

E `tsconfig.node.json` ganha a configuração do Playwright no `include`:

```json
  "include": ["vite.config.ts", "vitest.config.ts", "playwright.config.ts"]
```

- [ ] **Step 5: Adicionar o script**

Em `package.json`, dentro de `scripts`:

```json
    "e2e": "playwright test"
```

- [ ] **Step 6: Escrever o smoke**

Criar `e2e/home.spec.ts`:

```ts
import { expect, test } from '@playwright/test'

test('home responde 200 e renderiza o nome institucional', async ({ page }) => {
  const response = await page.goto('/')

  expect(response?.status()).toBe(200)
  await expect(
    page.getByRole('heading', { level: 1, name: 'Lotus OTEC' }),
  ).toBeVisible()
})
```

O teste afirma resposta e renderização, que é o critério da EAP. Não afirma nada sobre aparência: o clone visual é do Sprint 2.

- [ ] **Step 7: Rodar o E2E**

```bash
pnpm e2e
```

Esperado: `1 passed`. O Playwright sobe o `pnpm dev` sozinho pelo `webServer`.

- [ ] **Step 8: Provar que o smoke reprova de verdade**

Trocar temporariamente `name: 'Lotus OTEC'` por `name: 'Lotus OTECX'` no spec e rodar:

```bash
pnpm e2e
```

Esperado: **FALHA** por timeout do locator. Reverter a mudança em seguida e rodar `pnpm e2e` de novo para confirmar o verde. Teste que nunca foi visto falhando não prova nada.

- [ ] **Step 9: Confirmar typecheck**

```bash
pnpm exec tsc -b
```

Esperado: verde, agora com três projetos. Se `tsconfig.e2e.json` estiver mal formado, falha aqui.

- [ ] **Step 10: Commit**

```bash
git add playwright.config.ts tsconfig.e2e.json tsconfig.json tsconfig.node.json e2e/home.spec.ts package.json
git commit -m "feat(1.3.3): configurar Playwright com smoke da home

Só Chromium, por decisão de João em 2026-08-24; WebKit e Firefox viram débito
D-10 com gatilho no Sprint 5.

webServer sobe o pnpm dev e reuseExistingServer fica ligado só fora do CI.
Specs vivem em e2e/, fora de src/, com tsconfig.e2e.json próprio: precisam de
lib DOM, que tsconfig.node.json não tem.

Falha provada trocando o nome esperado no locator; timeout observado antes de
reverter."
```

---

## Task 7: 1.3.4 · axe sobre o Playwright

**Files:**

- Create: `e2e/a11y.spec.ts`
- Modify: `package.json`

**Interfaces:**

- Consumes: `playwright.config.ts` e o `baseURL` da Task 6.
- Produces: anexo `axe-home.json` no relatório do Playwright.

**Nota de escopo:** neste bloco a auditoria **reporta** violação sem reprovar o build. O alvo é a home do scaffold Vite, que morre no Sprint 2; transformar violação em falha agora significaria consertar acessibilidade de uma página descartável. O gate que reprova nasce no Sprint 4, que é o sprint de acessibilidade. Decisão de design aprovada por João em 2026-08-24, registrada como débito D-11.

- [ ] **Step 1: Instalar o axe**

```bash
pnpm add -D @axe-core/playwright
```

- [ ] **Step 2: Escrever o spec de acessibilidade**

Criar `e2e/a11y.spec.ts`:

```ts
import AxeBuilder from '@axe-core/playwright'
import { expect, test } from '@playwright/test'

test('home produz auditoria axe e reporta violações', async ({
  page,
}, testInfo) => {
  await page.goto('/')

  const results = await new AxeBuilder({ page })
    .withTags(['wcag2a', 'wcag2aa'])
    .analyze()

  await testInfo.attach('axe-home.json', {
    body: JSON.stringify(results.violations, null, 2),
    contentType: 'application/json',
  })

  for (const violation of results.violations) {
    console.warn(
      `[axe] ${violation.id} (${violation.impact}): ${violation.nodes.length} nó(s)`,
    )
  }

  // Este bloco prova que a auditoria executa, não que a página passa. O alvo
  // ainda é a home do scaffold Vite. O gate que reprova violação nasce no
  // Sprint 4 — débito D-11.
  expect(Array.isArray(results.violations)).toBe(true)
})
```

- [ ] **Step 3: Rodar a suite completa**

```bash
pnpm e2e
```

Esperado: `2 passed` — o smoke da Task 6 e a auditoria. As linhas `[axe] ...` aparecem no console se houver violação.

- [ ] **Step 4: Conferir que a auditoria realmente rodou**

```bash
pnpm e2e --reporter=html
pnpm exec playwright show-report --host 127.0.0.1
```

Esperado: relatório abrindo com o anexo `axe-home.json` no teste de acessibilidade. Registrar quantas violações a home do scaffold produziu — esse número entra no corpo do commit e é o baseline que o Sprint 4 vai atacar. Encerrar o servidor do relatório com `Ctrl+C`.

- [ ] **Step 5: Confirmar typecheck e lint**

```bash
pnpm exec tsc -b && pnpm lint
```

Esperado: ambos verdes.

- [ ] **Step 6: Commit**

```bash
git add e2e/a11y.spec.ts package.json pnpm-lock.yaml
git commit -m "feat(1.3.4): integrar axe ao Playwright

@axe-core/playwright audita a home com tags wcag2a e wcag2aa e anexa
axe-home.json ao relatório.

A auditoria reporta violação mas não reprova o build neste bloco: o alvo ainda
é a home do scaffold Vite, descartada no Sprint 2. O gate que reprova nasce no
Sprint 4. Decisão registrada como débito D-11."
```

---

## Task 8: 1.3.6 · pnpm check e fechamento de D-06

**Files:**

- Modify: `package.json`
- Modify: `tsconfig.node.json`
- Modify: `scripts/validate-agent-workflow.mjs`

**Interfaces:**

- Consumes: `agent:check`, `lint`, `typecheck`, `test` (Task 5) e `build`.
- Produces: script `check`, consumido pelo CI da Task 9 e pela prova final da Task 11.

- [ ] **Step 1: Criar o script `check`**

Em `package.json`, dentro de `scripts`:

```json
    "check": "pnpm agent:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build"
```

`agent:check` entra por decisão de João em 2026-08-24: o contrato do harness passa a ser verificado junto com o produto. `e2e` fica de fora, conforme o critério da EAP no Notion, que define `check` como lint + typecheck + testes unitários + build.

- [ ] **Step 2: Rodar e observar o estado atual**

```bash
pnpm check
```

Esperado: verde. Se falhar aqui, o problema vem de uma task anterior — resolver antes de mexer no `tsconfig`.

- [ ] **Step 3: Ligar `checkJs` e ampliar o `include`**

`tsconfig.node.json` ganha duas opções em `compilerOptions`, logo abaixo de `"skipLibCheck": true`:

```json
    "allowJs": true,
    "checkJs": true,
```

E o `include` passa a ser:

```json
  "include": [
    "vite.config.ts",
    "vitest.config.ts",
    "playwright.config.ts",
    "scripts/**/*.mjs"
  ]
```

`eslint.config.js` fica deliberadamente de fora: é configuração de ferramenta com tipagem própria pesada, e incluí-la é escopo que a EAP não pede. Registrar em `1.3.8` como adiado consciente.

- [ ] **Step 4: Rodar o typecheck e coletar os erros**

```bash
pnpm exec tsc -b
```

Esperado: **FALHA**. `scripts/validate-agent-workflow.mjs` tem 295 linhas e zero anotação; sob `strict` e `noUncheckedIndexedAccess` os erros previstos são:

- `const errors = []` é inferido `never[]`, então todo `errors.push(...)` falha.
- `process.argv[flagIndex + 1]` é `string | undefined`, e `root` alimenta `join(root, rel)`, que exige `string`.
- `const values = {}` não tem assinatura de índice, então `values[pair[1]] = ...` falha.
- `match[1]`, `pair[1]`, `pair[2]`, `front[1]` e `front[2]` são possivelmente `undefined`.
- `parseFrontmatter` devolve `{}`, então `front.workflow_mode` e as demais propriedades não existem no tipo.

Guardar a saída completa: ela guia o passo seguinte e vira evidência.

- [ ] **Step 5: Tipar o script por JSDoc**

`.mjs` sob `erasableSyntaxOnly` não aceita sintaxe de tipo, então a correção sai por JSDoc e por guarda em runtime. Proibido `any` e proibido cast.

As mudanças, uma a uma:

```js
/** @type {string[]} */
const errors = []
```

```js
const flagIndex = process.argv.indexOf('--root')
const rootArg = flagIndex === -1 ? '.' : process.argv[flagIndex + 1]
if (rootArg === undefined) {
  console.error('--root requires a directory')
  process.exit(2)
}
const root = rootArg
```

Isso substitui o bloco atual de `flagIndex` e a linha `const root = ...`, e mantém o mesmo comportamento observável: sem valor após `--root`, erro e saída `2`.

```js
/**
 * @param {string} text
 * @returns {Record<string, string> | null}
 */
const parseFrontmatter = (text) => {
  const match = text.match(/^---\n([\s\S]*?)\n---/)
  const block = match?.[1]
  if (block === undefined) return null
  /** @type {Record<string, string>} */
  const values = {}
  for (const line of block.split('\n')) {
    const pair = line.match(/^([a-z_]+):\s*(.*)$/)
    const key = pair?.[1]
    const value = pair?.[2]
    if (key !== undefined && value !== undefined) values[key] = value.trim()
  }
  return values
}
```

```js
/**
 * @param {string | undefined} value
 * @returns {boolean}
 */
const isNull = (value) =>
  value === undefined || value === '' || value === 'null'
```

Em `checkState`, `front.workflow_state` passa a ser `string | undefined`. As chamadas `VALID_STATES.includes(front.workflow_state)` e `ORDERED_STATES.indexOf(effective)` precisam de valor definido. Logo após o `if (!front)`, acrescentar:

```js
const current = front.workflow_state
if (current === undefined) {
  errors.push('state: workflow_state ausente')
  return
}
```

e apagar a declaração `const current = front.workflow_state` que hoje aparece mais abaixo, para não duplicar.

`effective` usa `front.resume_state`, também `string | undefined`:

```js
const resume = front.resume_state
const effective =
  current === 'blocked' &&
  resume !== undefined &&
  ORDERED_STATES.includes(resume)
    ? resume
    : current
```

Em `checkRules`, o `match` sobre o frontmatter recebe o mesmo tratamento:

```js
    const front = content.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/)
    const head = front?.[1]
    const body = front?.[2]
    if (head === undefined || body === undefined) {
      errors.push(`rules: ${path} sem frontmatter`)
      continue
    }
    if (!head.includes('paths:')) errors.push(`rules: ${path} sem paths:`)
    if (body.trim().length === 0) errors.push(`rules: ${path} sem corpo`)
```

Se `tsc` acusar erro além destes, resolver com o mesmo padrão — guarda explícita e JSDoc. **Se o volume for grande a ponto de dominar o commit, parar e reportar em vez de decidir sozinho fatiar a task.**

- [ ] **Step 6: Rodar o typecheck de novo**

```bash
pnpm exec tsc -b
```

Esperado: verde.

- [ ] **Step 7: Provar que o script não mudou de comportamento**

```bash
pnpm agent:check
```

Esperado: `Agent workflow contract OK` — a mesma saída de antes da tipagem. Confirmar também que a validação ainda reprova de verdade:

```bash
pnpm exec node scripts/validate-agent-workflow.mjs --root /tmp; echo "saída: $?"
```

Esperado: lista de `missing:` e saída `1`. Se passar num diretório vazio, a tipagem quebrou a lógica — regressão, não conclusão.

- [ ] **Step 8: Rodar a cadeia completa**

```bash
pnpm check
```

Esperado: verde do começo ao fim, incluindo `agent:check`.

- [ ] **Step 9: Commit**

```bash
git add package.json tsconfig.node.json scripts/validate-agent-workflow.mjs
git commit -m "feat(1.3.6): padronizar pnpm check e fechar D-06

check = agent:check + lint + typecheck + test + build. O contrato do harness
passa a ser verificado junto com o produto, por decisão de João em 2026-08-24.
e2e fica fora, conforme o critério da EAP.

tsconfig.node.json ganha allowJs e checkJs e passa a incluir vitest.config.ts,
playwright.config.ts e scripts/**/*.mjs, fechando D-06.

validate-agent-workflow.mjs foi tipado por JSDoc e guarda em runtime, sem any
e sem cast. Comportamento inalterado: agent:check segue OK e a validação segue
reprovando em diretório vazio com saída 1.

eslint.config.js fica fora do include de propósito; registrado em 1.3.8."
```

---

## Task 9: 1.3.7 · CI no GitHub Actions

**Files:**

- Create: `.github/workflows/ci.yml`

**Interfaces:**

- Consumes: scripts `check` (Task 8), `format:check` (Task 4) e `e2e` (Task 6); `.nvmrc`.
- Produces: workflow `CI`, que a Task 11 exige verde.

- [ ] **Step 1: Criar `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Habilitar Corepack
        run: corepack enable

      - name: Instalar Node da .nvmrc
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Formatação
        run: pnpm format:check

      - name: Gates de qualidade
        run: pnpm check

      - name: Cache do Chromium
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Instalar Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E
        run: pnpm e2e

      - name: Publicar relatório do Playwright
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
```

Três pontos que não são preferência:

`node-version-file: .nvmrc` é obrigatório. Com `engineStrict: true` e `engines.node` em `>=24.19.0 <25`, um runner com Node fora da faixa mata **todo** comando pnpm com `ERR_PNPM_UNSUPPORTED_ENGINE`. É o débito D-07, fechado aqui.

`corepack enable` vem **antes** de `actions/setup-node`, porque `cache: pnpm` precisa do pnpm no PATH para localizar o store.

Todos os passos bloqueiam merge por padrão: falha em qualquer um reprova o job. `pnpm check` já carrega `agent:check`, lint, typecheck, unit e build; `format:check` e `e2e` são os dois gates adicionais.

- [ ] **Step 2: Validar a sintaxe do workflow localmente**

```bash
pnpm exec node -e "
const { readFileSync } = require('node:fs')
const src = readFileSync('.github/workflows/ci.yml', 'utf8')
if (!src.includes('node-version-file: .nvmrc')) throw new Error('faltou node-version-file')
if (src.indexOf('corepack enable') > src.indexOf('actions/setup-node')) throw new Error('corepack precisa vir antes do setup-node')
console.log('workflow OK')
"
```

Esperado: `workflow OK`.

- [ ] **Step 3: Reproduzir a sequência do CI localmente**

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm check
pnpm e2e
```

Esperado: os quatro verdes. Isto não substitui o CI — é a checagem de que a sequência é executável antes de gastar uma rodada de runner.

- [ ] **Step 4: Confirmar a versão de Node local contra a `.nvmrc`**

```bash
node -v && cat .nvmrc
```

Esperado: `v24.19.0` batendo com o conteúdo do arquivo. Divergência local aqui prevê exatamente a falha que `node-version-file` evita no runner.

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "feat(1.3.7): criar CI inicial no GitHub Actions

Workflow roda em pull request contra main: install frozen, format:check,
check (agent:check + lint + typecheck + unit + build), instalação do Chromium
e e2e. Todos os passos bloqueiam merge.

node-version-file: .nvmrc é obrigatório, não preferência: com engineStrict
ligado, runner fora de >=24.19.0 <25 mata todo comando pnpm com
ERR_PNPM_UNSUPPORTED_ENGINE. Fecha D-07.

corepack enable vem antes do setup-node porque cache: pnpm precisa do pnpm
no PATH. Relatório do Playwright vira artefato quando o job falha."
```

**Nota para a review:** o workflow só executa de fato quando o PR for aberto em `/fechar-site`. Até lá, o gate está escrito e reproduzido localmente, não provado em runner. A Task 11 é quem exige o verde real, e essa limitação é registrada, nunca simulada.

---

## Task 10: 1.3.8 · ADR-SITE-001 e README

**Files:**

- Create: `docs/adr/ADR-SITE-001.md`
- Modify: `README.md`

**Interfaces:**

- Consumes: todas as decisões das Tasks 1 a 9.
- Produces: nada consumido por código.

- [ ] **Step 1: Ler o README atual antes de sobrescrever**

```bash
cat README.md
```

O scaffold do Vite deixa um README genérico. Ele é substituído, não emendado — mas leia antes para não perder nada específico do projeto que tenha sido acrescentado.

- [ ] **Step 2: Criar `docs/adr/ADR-SITE-001.md`**

```markdown
# ADR-SITE-001 · Fundação técnica do lotus-site

- **Status:** aceito
- **Data:** 2026-08-24
- **Escopo:** Sprint 0, EAP 1.1.1 a 1.3.9

## Contexto

`lotus-site` reconstrói o site institucional público da Lotus OTEC. O primeiro marco é um
clone funcional e visualmente fiel de `https://lotusotec.cl/`; redesign vem depois de uma
baseline de paridade aprovada. Isso empurra a fundação para o mínimo que sustenta um clone
verificável, não para uma plataforma.

## Decisões

### Vite, não Next.js

O site é institucional, estático e de página única por âncoras. Não há renderização no
servidor, rota dinâmica nem camada de dados a justificar Next.js. Vite entrega HMR rápido e
build simples.

Várias tasks do roadmap no Notion ainda descrevem Next.js — App Router, Server Action,
`next/image`. São stale e estão registradas como débito D-01; não foram reconciliadas porque
escrita no Notion exige autorização explícita.

### pnpm com versão fixada, Node 24 fixado

`packageManager` no `package.json` via Corepack, `.nvmrc` com `24.19.0` e `engines` declarando
`node >=24.19.0 <25`. `engineStrict: true` no `pnpm-workspace.yaml` transforma runtime
incompatível em erro, não em aviso.

Consequência que morde: qualquer ambiente fora dessa faixa — inclusive um runner de CI mal
configurado — falha com `ERR_PNPM_UNSUPPORTED_ENGINE` em todo comando pnpm. Por isso o
workflow usa `node-version-file: .nvmrc` em vez de fixar a versão à mão.

### TypeScript estrito desde o começo

`strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`,
`erasableSyntaxOnly`, `verbatimModuleSyntax`. `any` e cast para calar o compilador são
proibidos: erro de tipo se resolve com guarda ou com tipo correto.

`tsconfig.json` é um solution file com três projetos: `tsconfig.app.json` para `src/`,
`tsconfig.node.json` para código de build e scripts, `tsconfig.e2e.json` para `e2e/`.
Arquivo de configuração novo precisa entrar no `include` de um deles, senão nasce fora do
`tsc -b`.

### Direção de dependências como catraca de lint

`content` não importa React nem componente. `lib` não importa React nem componente. `ui` não
importa `sections`. `components` não importa `integrations` — integração entra por prop ou
callback. Violação é erro de `pnpm lint`, não observação de revisão.

### Tailwind 4, sem arquivo de configuração

Tailwind 4 configura por CSS: `@import 'tailwindcss'` em `src/index.css` e
`@tailwindcss/vite` no `vite.config.ts`. Nenhum `tailwind.config.js` existe. `prettier-plugin-tailwindcss`
localiza o Tailwind pela opção `tailwindStylesheet` no `.prettierrc`.

Tailwind entrou dentro da EAP 1.2.5 porque nenhuma task do roadmap o instala, apesar de duas
dependerem dele. Débito D-08.

### Vitest para unidade, Playwright para E2E

Vitest com jsdom cobre lógica e componente; testes ficam ao lado do código, em
`src/**/*.test.ts`. Playwright cobre a aplicação real, com specs em `e2e/`, fora de `src/`.

`pnpm check` roda `agent:check`, lint, typecheck, testes unitários e build. E2E fica fora do
`check` e roda por `pnpm e2e` e no CI, porque precisa subir servidor.

### Estrutura de pastas por consumidor real

Diretório só existe quando há consumidor. Hoje existem `src/lib/` e `src/content/`, ambos
consumidos por `src/App.tsx`. `src/components/`, `src/app/` e `src/integrations/` nascem
quando houver código real — `components/` no Sprint 2, com o clone.

O critério da EAP 1.3.5 no Notion pedia também `features/contact`, `config`, `tests` e `docs`.
As rules do repositório venceram; a divergência é o débito D-09.

## Adiado, com motivo

| Adiado                                    | Motivo                                               | Volta em                                          |
| ----------------------------------------- | ---------------------------------------------------- | ------------------------------------------------- |
| Router                                    | navegação é uma página com âncoras                   | quando houver segunda rota real                   |
| Estado global                             | estado local e derivado bastam                       | quando dois ramos distantes compartilharem estado |
| WebKit e Firefox no Playwright            | sem clone, cobrir três motores custa sem retorno     | Sprint 5 · débito D-10                            |
| axe reprovando o build                    | o alvo é a home do scaffold, descartada no Sprint 2  | Sprint 4 · débito D-11                            |
| `eslint.config.js` sob `checkJs`          | tipagem própria pesada, fora do que a EAP 1.3.6 pede | quando houver necessidade real                    |
| `features/contact`                        | não há formulário até o Sprint 3                     | Sprint 3 · débito D-09                            |
| Limite numérico de tamanho e complexidade | sem amostra do clone não há como calibrar            | após o Sprint 2 · débito D-04                     |
| Teste de regressão visual, Lighthouse CI  | não há o que comparar antes do clone                 | Sprint 5                                          |

## Consequências

Um desenvolvedor novo clona, instala e roda `pnpm check` para saber se o repositório está
saudável. Um agente tem catraca automática para as regras que importam, em vez de depender de
revisão humana atenta.

O custo é rigidez: runtime fora da faixa não roda, tipo frouxo não compila, import fora da
direção declarada não passa no lint. É deliberado — o clone visual do Sprint 2 vai produzir
volume grande de código, e é mais barato barrar na entrada do que revisar depois.
```

- [ ] **Step 3: Reescrever o `README.md`**

````markdown
# lotus-site

Site institucional público da Lotus OTEC.

Primeiro marco: clone funcional e visualmente fiel de `https://lotusotec.cl/`. Redesign e
evolução só depois de uma baseline de paridade aprovada.

## Requisitos

- Node `24.19.0` — a versão está em `.nvmrc`; use `nvm use`
- pnpm 11, habilitado por Corepack

`engineStrict` está ligado: runtime fora da faixa declarada em `engines` falha com
`ERR_PNPM_UNSUPPORTED_ENGINE` em qualquer comando pnpm. Isso é proposital.

## Começar

```bash
nvm use
corepack enable
pnpm install --frozen-lockfile
pnpm dev
```
````

A aplicação sobe em `http://localhost:5173`.

## Comandos

| Comando             | O que faz                                                   |
| ------------------- | ----------------------------------------------------------- |
| `pnpm dev`          | servidor de desenvolvimento com HMR                         |
| `pnpm build`        | `tsc -b` seguido do build de produção                       |
| `pnpm preview`      | serve o `dist/` já gerado                                   |
| `pnpm typecheck`    | typecheck isolado dos três projetos TypeScript              |
| `pnpm lint`         | ESLint via flat config                                      |
| `pnpm format`       | aplica Prettier                                             |
| `pnpm format:check` | verifica formatação sem escrever                            |
| `pnpm test`         | testes unitários, uma passada                               |
| `pnpm test:watch`   | testes unitários em watch                                   |
| `pnpm e2e`          | testes de ponta a ponta no Chromium                         |
| `pnpm check`        | `agent:check` + lint + typecheck + testes unitários + build |
| `pnpm agent:check`  | valida o contrato do harness de agentes                     |

`pnpm check` é o gate. `pnpm e2e` fica fora dele porque precisa subir servidor, e roda
separado no CI.

Antes do primeiro `pnpm e2e`, instale o navegador:

```bash
pnpm exec playwright install --with-deps chromium
```

## Estrutura

```text
src/
├── lib/        helpers puros, sem React
├── content/    conteúdo institucional e dados estáticos
└── assets/     assets importados por componente
e2e/            testes de ponta a ponta
scripts/        ferramental do harness de agentes
docs/           ADR, specs, planos e estado do workflow
```

Diretório só nasce quando há consumidor real. `components/`, `app/` e `integrations/` chegam
com o clone. A árvore alvo e a direção de dependências estão em
`.claude/rules/architecture.md` — violação delas é erro de lint, não de revisão.

`src/assets/` guarda o que é importado por componente e ganha fingerprint do Vite; `public/`
é copiado verbatim e referenciado por URL absoluta.

## Decisões

`docs/adr/ADR-SITE-001.md` registra stack, princípios e o que foi deliberadamente adiado.

## Fluxo de trabalho

Cada bloco de trabalho roda em branch própria e vira um PR; `main` não recebe commit direto.
O estado corrente do fluxo vive em `docs/superpowers/state.md`, e as regras completas em
`CLAUDE.md`. Este README não as duplica.

````

- [ ] **Step 4: Provar o critério de aceite**

O critério é "novo dev consegue clonar, instalar, iniciar, testar e entender as decisões sem contexto oral". Executar exatamente o que o README manda, na ordem em que manda:

```bash
pnpm install --frozen-lockfile
pnpm dev --port 5174 &
sleep 5 && curl -s -o /dev/null -w '%{http_code}\n' http://localhost:5174
kill %1
pnpm check
````

Esperado: `200` do `curl` e `pnpm check` verde. Comando do README que não funciona é falha de aceite da EAP.

- [ ] **Step 5: Rodar a formatação**

```bash
pnpm format && pnpm format:check
```

Esperado: verde. Os arquivos Markdown novos precisam estar formatados como o resto.

- [ ] **Step 6: Commit**

```bash
git add docs/adr/ADR-SITE-001.md README.md
git commit -m "docs(1.3.8): registrar ADR-SITE-001 e README de desenvolvimento

ADR cobre Vite em vez de Next.js, runtime fixado, TypeScript estrito, direção
de dependências como catraca de lint, Tailwind 4 sem arquivo de configuração,
e a divisão entre Vitest e Playwright. Traz tabela do que foi adiado com
motivo e sprint de retorno.

README cobre requisitos, comandos, estrutura e fluxo de branch, apontando para
CLAUDE.md em vez de duplicá-lo. Cada comando foi executado antes de ser
documentado."
```

---

## Task 11: 1.3.9 · baseline técnico da Fundação

**Files:**

- Modify: `docs/superpowers/backlog.md`

**Interfaces:**

- Consumes: tudo.
- Produces: evidência de aceite do bloco, consumida por `/revisar-site` e `/fechar-site`.

- [ ] **Step 1: Registrar os débitos novos no backlog**

Em `docs/superpowers/backlog.md`, seção `# DÉBITOS`, acrescentar:

```markdown
- **D-08 · Tailwind entrou sem EAP própria** — nenhuma task do roadmap instala `tailwindcss`,
  mas `1.2.5` pede `tailwind-merge` e `1.3.1` pede `prettier-plugin-tailwindcss`. Tailwind foi
  absorvido por `1.2.5` por decisão de João em 2026-08-24. Reconciliar com o Notion quando
  houver autorização de escrita.
  **Gatilho:** junto de `D-01`, antes de planejar o Sprint 3.
- **D-09 · Estrutura de `1.3.5` divergiu do Notion** — a EAP pedia `features/contact`, `config`,
  `tests` e `docs`; `.claude/rules/architecture.md` venceu e esses diretórios não nasceram, por
  decisão de João em 2026-08-24. `components/`, `app/` e `integrations/` nascem com consumidor
  real.
  **Gatilho:** ao planejar o Sprint 3.
- **D-10 · Playwright cobre só Chromium** — WebKit e Firefox ficaram de fora do baseline por
  não haver clone a comparar. Paridade visual em outro motor não é verificada hoje.
  **Gatilho:** ao planejar o Sprint 5.
- **D-11 · axe reporta mas não reprova** — a auditoria de `1.3.4` executa e anexa
  `axe-home.json`, sem transformar violação em falha, porque o alvo é a home do scaffold Vite.
  **Gatilho:** ao planejar o Sprint 4.
```

Marcar também `D-06` e `D-07` como fechados por este bloco, e `D-05` como parcialmente fechado — `agent:check` passou a rodar em CI e o Playwright passou a existir.

Na seção `# DEPOIS`, corrigir a contagem do tema de tooling: são 11 tasks, não 12.

- [ ] **Step 2: Provar em árvore limpa**

```bash
git status --short
```

Esperado: só as mudanças do Step 1. Qualquer artefato solto significa que uma task anterior deixou lixo — resolver antes.

- [ ] **Step 3: Reinstalar do zero**

```bash
rm -rf node_modules
pnpm install --frozen-lockfile
```

Esperado: instalação conclui sem erro. `--frozen-lockfile` falhando significa `pnpm-lock.yaml` fora de sincronia com o `package.json`, o que reprovaria o CI.

- [ ] **Step 4: Rodar o gate completo**

```bash
pnpm check
```

Esperado: verde em `agent:check`, `lint`, `typecheck`, `test` e `build`. Copiar a saída — é a evidência do critério de aceite.

- [ ] **Step 5: Rodar o E2E**

```bash
pnpm exec playwright install chromium
pnpm e2e
```

Esperado: `2 passed`. Copiar a saída.

- [ ] **Step 6: Rodar a formatação**

```bash
pnpm format:check
```

Esperado: verde.

- [ ] **Step 7: Commit**

```bash
git add docs/superpowers/backlog.md
git commit -m "feat(1.3.9): fechar baseline técnico da Fundação

pnpm check e pnpm e2e provados em ambiente limpo, após rm -rf node_modules e
pnpm install --frozen-lockfile. Saída real registrada no relatório de execução.

Backlog registra os débitos abertos pelo bloco: D-08 (Tailwind sem EAP),
D-09 (estrutura de 1.3.5 divergindo do Notion), D-10 (só Chromium),
D-11 (axe reporta sem reprovar). D-06 e D-07 fechados; D-05 parcial.

CI verde ainda não pode ser provado: o workflow só dispara quando o PR for
aberto em /fechar-site. A limitação fica registrada, não simulada."
```

- [ ] **Step 8: Registrar a limitação do CI**

O critério da EAP exige "CI está verde". O workflow dispara em `pull_request`, e o PR só nasce em `/fechar-site`. Isso **não** é motivo para declarar o critério cumprido.

Registrar no relatório de execução: a sequência do CI foi reproduzida localmente e passou, mas o verde em runner fica pendente da abertura do PR. `/fechar-site` confirma antes de fechar o bloco. Nunca escrever que o CI passou sem ter visto o job verde.

---

## Handoff de execução

executor: claude
reviewer: codex
paths_autorizados:

- (não se aplica — executor é Claude)
