# Delegação de `lotusotec.cl` e certificado ACM — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** corrigir a cópia da zona contra o inventário fechado do painel, trocar a delegação de `lotusotec.cl` para os nameservers do Route 53 sem derrubar o e-mail corporativo, e emitir o certificado ACM até `ISSUED`.

**Architecture:** três defeitos de cópia entram primeiro no template e na catraca offline (`pop3`, e o `AAAA` de `www` e de `sistema`), porque delegar uma cópia incompleta é irreversível dentro do TTL. Em seguida a conferência ao vivo aprende um segundo modo — depois da delegação os dois lados passam a ser a mesma zona, e o veredito de cada linha inverte —, o João troca os nameservers no StackCP, a entrega de e-mail é provada com mensagem real, e só então o recurso `AWS::CertificateManager::Certificate` entra no mesmo stack `lotus-dns`. A ordem é a arquitetura: cada passo só começa com a prova do anterior na mão.

**Tech Stack:** CloudFormation (YAML), AWS CLI 2.36.38, Node 24.19.0 + pnpm, Vitest, `node:dns/promises`, DNS-over-HTTPS contra `dns.google`.

**Spec:** [`docs/superpowers/specs/2026-09-20-7.2.1-delegacao-certificado-design.md`](../specs/2026-09-20-7.2.1-delegacao-certificado-design.md)
**Branch:** `feat/7-2-1-delegacao-certificado` · **Base:** `fba854b` · **Spec commitada em:** `b64bb94`

---

## Global Constraints

- **Node 24.19.0.** O Node do PATH desta máquina é o **v22.23.1** (medido em 2026-09-20), e `engineStrict: true` transforma isso em `ERR_PNPM_UNSUPPORTED_ENGINE`. **Todo** comando `pnpm` leva o prefixo, sem exceção:
  ```bash
  export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
  ```
- **Nenhuma dependência nova** (Lei 7). Nem `yaml` nem `js-yaml` resolvem na árvore; a leitura do template continua textual, como `scripts/infra/lib/zona.mjs` já faz.
- **Aspas simples no YAML.** `.prettierrc` tem `"singleQuote": true`, e `infra/lotus-dns.yaml` passa hoje no `format:check` com aspas simples e com `!Sub`/`!GetAtt`/`!Ref` preservados.
- **`tsconfig.node.json` typecheca `scripts/**/*.mjs`** com `checkJs`, `strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`, `erasableSyntaxOnly`, `verbatimModuleSyntax`. Consequência prática: **todo parâmetro precisa de tipo JSDoc**, e **todo acesso por índice devolve `T | undefined`** e precisa de guarda. `arr[0]` sem guarda reprova `pnpm typecheck`.
- **`vitest.config.ts` inclui `scripts/**/*.test.mjs`**, então a catraca offline roda dentro de `pnpm check`, sem rede e sem credencial.
- **`pnpm check` verde antes de cada commit.** É `agent:check && format:check && lint && typecheck && test && build`.
- **`docs/superpowers/backlog.md` não pode conter a string `workflow_state`** — `checkBacklog` reprova. Escreva "estado" ou "fase".
- **Conventional Commits com a EAP no escopo:** `feat(7.2.1): …`.
- **Nada de `git push`, nada de `gh pr create`** (Lei 6). Só `/fechar-site`.
- **Não escrever no Notion** (Lei 6). Não há autorização nesta rodada, e `7.2.1` não fecha (spec §3 D7).
- **Não tocar `src/`, `public/`, `e2e/`, `index.html`, `infra/lotus-site.yaml`.** Nenhum pixel do produto muda; `pnpm e2e` não se aplica.
- **Não apagar a zona na StackDNS nem pedir remoção dela.** Ela é o rollback (spec §7).
- **Região do stack: `us-east-1`.** O runbook exporta `AWS_REGION=sa-east-1` na linha 50; **todo** comando deste bloco leva `--region us-east-1` explícito.
- **Perfil AWS: `lotus`.** `AWS_PROFILE=lotus aws sts get-caller-identity` responde a conta `760144413534`; sem o perfil a credencial do ambiente é inválida (`InvalidClientTokenId`).
- **Prova antes de afirmação** (Lei 8). Saída real colada no corpo do commit; nunca "o gate passou" sem a linha do comando.
- **Gate humano não se simula.** As Tasks 4 e 5 param e esperam o João. Executar o passo dele por conta própria é violação, não atalho.

### Paths autorizados

Os da spec §9, mais os dois da emenda **E1**:

```text
infra/lotus-dns.yaml
scripts/infra/lib/zona.mjs
scripts/infra/zona.test.mjs               ← E1
scripts/infra/conferir-zona.mjs           ← E1
docs/infra/zona-dns-lotusotec.md
docs/infra/runbook-aws.md
docs/infra/conferencia-zona-*.md
docs/infra/delegacao-2026-09-*.md
docs/superpowers/state.md
docs/superpowers/backlog.md
docs/superpowers/specs/2026-09-20-7.2.1-delegacao-certificado-design.md
docs/superpowers/plans/2026-09-20-7.2.1-delegacao-certificado.md
```

---

## Emenda E1 à spec — dois paths de `scripts/` a mais

A spec §9 autoriza `scripts/infra/lib/zona.mjs` e nada mais dentro de `scripts/`. Dois arquivos vizinhos precisam mudar, e nenhum dos dois é escolha de estilo:

**E1a — `scripts/infra/zona.test.mjs`.** A catraca tem hoje um teste chamado `não declara certificado`, escrito no bloco anterior justamente para impedir que o certificado entrasse antes da delegação. A Task 5 emite o certificado: ou esse teste vira o seu oposto no mesmo commit, ou `pnpm check` fica vermelho e a catraca passa a mentir sobre o que a zona contém.

**E1b — `scripts/infra/conferir-zona.mjs`.** O script afirma, numa linha própria do relatório, que a delegação **não** mudou (`NS_DA_STACKDNS`), e trata "nome inventado resolve na StackDNS e não na AWS" como a única divergência esperada. Depois da troca as duas afirmações se invertem: a delegação passa a ser a da AWS, e os dois lados passam a ser a mesma zona, então o nome inventado não resolve em lado nenhum. Sem a emenda, a conferência pós-delegação — que é a evidência central do bloco — sairia vermelha por estar certa.

Nenhuma dependência entra; Lei 7 continua respeitada.

## Emenda E2 à spec §5 — seis commits, não cinco

A spec §5 lista cinco commits e coloca a conferência pós-delegação dentro do commit 3. A mudança de E1b é **código**, com teste próprio, e precisa estar pronta e provada **antes** de a delegação acontecer — depois da troca não há como escrever o modo novo sem já precisar dele. Entra um commit entre o 2 e o 3 da spec:

| #   | Commit                                                           | Task |
| --- | ---------------------------------------------------------------- | ---- |
| 1   | `fix(7.2.1): acrescentar pop3 e o AAAA de www e sistema`         | 1    |
| 2   | `docs(7.2.1): registrar o inventario fechado do painel`          | 2    |
| 3   | `feat(7.2.1): ensinar a conferencia a rodar depois da delegacao` | 3    |
| 4   | `feat(7.2.1): delegar lotusotec.cl para o Route 53`              | 4    |
| 5   | `feat(7.2.1): emitir o certificado ACM da zona`                  | 5    |
| 6   | `docs(7.2.1): atualizar runbook e backlog`                       | 6    |

---

## Medições que este plano assume

| Fato                                                              | Quando     | Como                                                             |
| ----------------------------------------------------------------- | ---------- | ---------------------------------------------------------------- |
| a conta responde                                                  | 2026-09-20 | `AWS_PROFILE=lotus aws sts get-caller-identity` → `760144413534` |
| o stack `lotus-dns` existe em `us-east-1` com a zona criada       | 2026-09-20 | `describe-stacks`, outputs `NameServers` e `IdDaZona`            |
| os quatro nameservers da zona nova                                | 2026-09-21 | `docs/infra/conferencia-zona-2026-09-21.md`                      |
| a zona nova responde igual à StackDNS nos onze registros de então | 2026-09-21 | mesmo relatório, sem divergência não esperada                    |
| não há `dig` nesta máquina; `curl` e `aws` existem                | 2026-09-20 | `which dig` sem saída                                            |
| o painel do StackCP lista `pop3`, o wildcard duplo e três atalhos | 2026-09-20 | prints do João, transcritos na Task 2                            |
| `www` e `sistema` **não** são registro no painel                  | 2026-09-20 | mesmos prints                                                    |

Os quatro nameservers medidos, que são os que vão para o painel na Task 4:

```text
ns-904.awsdns-49.net     205.251.195.136
ns-31.awsdns-03.com      205.251.192.31
ns-1889.awsdns-44.co.uk  205.251.199.97
ns-1507.awsdns-60.org    205.251.197.227
```

**Eles não são chute nem cache: releia-os do stack antes de entregar ao João**, porque uma zona recriada troca os quatro (Task 4, Step 2).

---

## File Structure

| Arquivo                                 | Responsabilidade                                                 | Tasks   |
| --------------------------------------- | ---------------------------------------------------------------- | ------- |
| `infra/lotus-dns.yaml`                  | a zona e, a partir da Task 5, o certificado                      | 1, 5    |
| `scripts/infra/lib/zona.mjs`            | inventário fechado, leitura textual do template, vereditos puros | 1, 3, 5 |
| `scripts/infra/zona.test.mjs`           | catraca offline: o template não pode divergir do inventário      | 1, 3, 5 |
| `scripts/infra/conferir-zona.mjs`       | conferência ao vivo, antes e depois da delegação                 | 3       |
| `docs/infra/zona-dns-lotusotec.md`      | o que a zona contém, com fonte e data                            | 2, 5    |
| `docs/infra/conferencia-zona-<data>.md` | evidência gerada; não se edita à mão                             | 4       |
| `docs/infra/delegacao-<data>.md`        | evidência dos gates humanos: troca e prova de e-mail             | 4       |
| `docs/infra/runbook-aws.md`             | como operar, depois que tudo isto for passado                    | 6       |
| `docs/superpowers/backlog.md`           | `D-45`, `D-47`, débitos novos                                    | 6       |
| `docs/superpowers/state.md`             | fase                                                             | 6       |

A divisão de responsabilidade entre os dois arquivos de `scripts/infra/` continua a do bloco anterior: **`lib/zona.mjs` não faz E/S e é o que a catraca testa**; `conferir-zona.mjs` faz rede, CLI e escrita, e não é testado offline. Todo veredito novo da Task 3 nasce em `lib/`, por isso, e não dentro do script.

---

## Ordem e dependências

```text
Task 1 ──▶ Task 2 ──▶ Task 3 ──▶ Task 4 ──▶ Task 5 ──▶ Task 6
 codigo    inventario   modo      G1 + G2      G3        docs
                       pos-deleg  (Joao)     (Joao)
```

## Gate paralelo G0 — production access do SES

Fora da linha acima, e por isso escrito aqui: **peça ao João já na Task 1**, porque a resposta da AWS leva de um a alguns dias úteis. Não bloqueia nenhuma task deste bloco; bloqueia `B2` se ficar para depois.

> 1. Entre no console da AWS com a conta `760144413534`, região `sa-east-1`.
> 2. Serviço **Amazon SES** → menu lateral **Account dashboard**.
> 3. No aviso "Your account is in the sandbox", clique em **Request production access**.
> 4. Preencha: tipo de e-mail **Transactional**; URL do site `https://lotusotec.cl`; caso de uso —
>    formulário de contato do site institucional, mensagens enviadas só para caixas da própria
>    empresa; volume esperado — dezenas por mês.
> 5. Envie e guarde o número do caso.

O resultado dele **não** entra em nenhum commit deste bloco: é insumo de `B2`, e registrá-lo aqui como entrega seria afirmar aceite de outro item.

Nenhuma task pula. A Task 4 tem dois gates humanos e um tempo de espera que não controlamos (TTL da delegação no registro `.cl`, de horas a dois dias); a Task 5 tem um gate. Entre eles o executor **para**.

---

## Task 1: `pop3` e os dois `AAAA` entram na cópia

**Files:**

- Modify: `scripts/infra/lib/zona.mjs` (o array `INVENTARIO` e o JSDoc acima dele)
- Modify: `infra/lotus-dns.yaml` (três entradas em `RecordSets`)
- Test: `scripts/infra/zona.test.mjs` (**não muda**; ele itera sobre `INVENTARIO` e reprova sozinho)

**Interfaces:**

- Consumes: nada de tasks anteriores.
- Produces: `INVENTARIO` com **14** entradas, cobrindo nove nomes distintos (`lotusotec.cl`, `www`, `sistema`, `mail`, `smtp`, `imap`, `pop3`, `autodiscover`, `ftp`). As Tasks 3 e 4 contam com esse número.

- [ ] **Step 1: escrever a expectativa que falha — as três linhas novas do inventário**

Em `scripts/infra/lib/zona.mjs`, substitua o JSDoc de `INVENTARIO` por este:

```js
/**
 * Inventário fechado da zona, transcrito do painel do StackCP em 2026-09-20
 * e registrado em `docs/infra/zona-dns-lotusotec.md`. Nomes com ponto final,
 * como o Route 53 os guarda.
 *
 * A fonte deixou de ser a sondagem por DNS de 2026-09-09, que não podia
 * enxergar nome que ninguém adivinhasse: com wildcard na zona, todo palpite
 * responde. Foi essa cegueira que escondeu `pop3` — `pop` foi sondado,
 * devolveu o IP do apex e foi lido como wildcard falando; `pop3` nunca foi
 * perguntado.
 *
 * `www` e `sistema` continuam aqui, e agora por prova em vez de precaução: o
 * painel mostra que nenhum dos dois é registro do outro lado. Eles só
 * resolvem hoje por causa do wildcard, que não atravessa — sem estas linhas,
 * a troca de delegação apagaria os dois. O `AAAA` dos dois existe pelo mesmo
 * motivo: o wildcard do painel tem `A` **e** `AAAA`, então cliente
 * dual-stack perderia IPv6 na troca.
 * @type {readonly RegistroEsperado[]}
 */
```

E acrescente as três entradas, mantendo o agrupamento por nome:

```js
  { nome: 'www.lotusotec.cl.', tipo: 'A', valores: ['185.146.167.195'] },
  { nome: 'www.lotusotec.cl.', tipo: 'AAAA', valores: ['2a07:7800::195'] },
  { nome: 'sistema.lotusotec.cl.', tipo: 'A', valores: ['185.146.167.195'] },
  {
    nome: 'sistema.lotusotec.cl.',
    tipo: 'AAAA',
    valores: ['2a07:7800::195'],
  },
```

e, logo depois do bloco de `imap`:

```js
  {
    nome: 'pop3.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['pop3.stackmail.com.'],
  },
```

- [ ] **Step 2: rodar a catraca e ver reprovar**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **FAIL**, em cinco testes —
`declara AAAA www.lotusotec.cl.`, `declara AAAA sistema.lotusotec.cl.` e `declara CNAME pop3.lotusotec.cl.` com `não está no template`;
`acha exatamente um RecordSet por linha do inventário` com `expected 11 to be 14`;
`usa o TTL medido da zona atual em todos os registros` com listas de tamanhos diferentes.

Se algum desses cinco passar, pare: significa que o template já tem o registro e a premissa da task está errada.

- [ ] **Step 3: declarar os três registros no template**

Em `infra/lotus-dns.yaml`, dentro de `RecordSets`, logo abaixo do bloco `www` `A`:

```yaml
# O wildcard do painel tem A e AAAA. Sem este registro, cliente
# dual-stack perde IPv6 em www na troca de delegacao.
- Name: !Sub 'www.${NomeDaZona}.'
  Type: AAAA
  TTL: !Ref TtlPadrao
  ResourceRecords:
    - !Ref Ipv6DoWordPress
```

logo abaixo do bloco `sistema` `A`:

```yaml
- Name: !Sub 'sistema.${NomeDaZona}.'
  Type: AAAA
  TTL: !Ref TtlPadrao
  ResourceRecords:
    - !Ref Ipv6DoWordPress
```

e logo abaixo do bloco `imap`:

```yaml
# Existe no painel e a sondagem de 2026-09-09 nao podia te-lo achado:
# `pop` foi perguntado e devolveu o IP do apex pelo wildcard; `pop3`
# nao foi perguntado. Sem ele, a troca derruba POP3 de quem ainda usa
# o correio antigo.
- Name: !Sub 'pop3.${NomeDaZona}.'
  Type: CNAME
  TTL: !Ref TtlPadrao
  ResourceRecords:
    - pop3.stackmail.com.
```

- [ ] **Step 4: rodar a catraca e ver passar**

```bash
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **PASS**, com `acha exatamente um RecordSet por linha do inventário` comparando 14 contra 14.

- [ ] **Step 5: validar o template como CloudFormation**

```bash
export AWS_PROFILE=lotus
aws cloudformation validate-template --region us-east-1 \
  --template-body file://infra/lotus-dns.yaml
```

Esperado: JSON com os quatro `Parameters` e nenhum erro.

- [ ] **Step 6: implantar, ainda sem delegação**

```bash
aws cloudformation deploy --region us-east-1 --stack-name lotus-dns \
  --template-file infra/lotus-dns.yaml --tags Projeto=lotus-site
```

Esperado: `Successfully created/updated stack - lotus-dns`.

- [ ] **Step 7: provar que os três registros existem na zona**

```bash
aws route53 list-resource-record-sets --output text \
  --hosted-zone-id "$(aws cloudformation describe-stacks --region us-east-1 \
    --stack-name lotus-dns \
    --query "Stacks[0].Outputs[?OutputKey=='IdDaZona'].OutputValue" \
    --output text)" \
  --query "ResourceRecordSets[?Name=='pop3.lotusotec.cl.' || Type=='AAAA'].[Name,Type]"
```

Esperado: quatro linhas — `lotusotec.cl. AAAA`, `pop3.lotusotec.cl. CNAME`, `sistema.lotusotec.cl. AAAA`, `www.lotusotec.cl. AAAA`. Guarde a saída para o corpo do commit.

- [ ] **Step 8: gate de qualidade**

```bash
pnpm check
```

Esperado: verde de ponta a ponta. Se o `format:check` reprovar o YAML ou o `.mjs`, rode `pnpm format` e confira o diff antes de seguir — é `D-18`, não erro novo.

- [ ] **Step 9: commit**

```bash
git add infra/lotus-dns.yaml scripts/infra/lib/zona.mjs
git commit -m "$(cat <<'EOF'
fix(7.2.1): acrescentar pop3 e o AAAA de www e sistema

Os prints do painel do StackCP fecharam o inventario e mostraram tres
defeitos na copia feita a partir da sondagem por DNS de 2026-09-09:

- pop3.lotusotec.cl CNAME pop3.stackmail.com. existe e nao estava no
  template. A sondagem perguntou por `pop`, recebeu o IP do apex e leu
  isso como wildcard falando -- conclusao certa sobre `pop` e cega para
  `pop3`, que ninguem perguntou. Delegar sem ele derrubaria POP3 de quem
  ainda usa o correio antigo.
- O wildcard do painel tem A e AAAA; o inventario so registrara o A.
  www e sistema, que existem hoje so por causa dele, perderiam IPv6 na
  troca. Os dois ganham AAAA 2a07:7800::195.

A catraca offline reprovou os cinco testes esperados antes da correcao e
passou depois, com 14 RecordSets contra as 14 linhas do inventario.

<colar aqui a saida do list-resource-record-sets do Step 7>

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: o inventário fechado do painel

**Files:**

- Modify: `docs/infra/zona-dns-lotusotec.md`

**Interfaces:**

- Consumes: `INVENTARIO` da Task 1 (as 14 linhas precisam bater com a tabela deste documento).
- Produces: o documento que a spec §3 D1 nomeia como a fonte do inventário. A Task 6 aponta o runbook para ele.

O documento hoje diz, em três lugares, que o export BIND é condição de `B1` e que o inventário é "o piso da conferência, não o teto". Depois do painel isso deixou de ser verdade, e documento que afirma pendência resolvida é pior que documento vago.

- [ ] **Step 1: gate humano GT — confirmar a transcrição contra os prints**

**Este passo é do João.** A tabela do Step 2 foi transcrita dos dois prints de 2026-09-20, e transcrição manual admite erro de digitação que um arquivo não admitiria (spec §3 D1). Antes de commitar, peça:

> "Abra de novo a tela de gerenciamento de DNS do `lotusotec.cl` no StackCP e confira estas 17 linhas contra o painel, uma a uma. Se aparecer qualquer nome que não esteja nesta lista, me diga antes de eu seguir — é registro que a delegação apagaria."

Se aparecer registro fora da lista, **pare o bloco** e volte à Task 1 para acrescentá-lo ao template e ao inventário. Não siga para a Task 3 com a lista em dúvida.

- [ ] **Step 2: reescrever o documento**

Substitua as secções `## Método`, `## Delegação`, `## Apex`, `## Wildcard`, `## Subdomínios provadamente explícitos`, `## Nomes indistinguíveis do wildcard` e `## O que esta medição não pode saber` por:

````markdown
## Fonte

Duas fontes, com autoridades diferentes.

**O inventário fechado** veio do painel do StackCP em **2026-09-20**: João abriu a tela de
gerenciamento de DNS de `lotusotec.cl` e capturou a lista inteira, do cabeçalho até a linha vazia
de inserção, em dois prints que se sobrepõem em `smtp` — então não há corte no meio. Essa lista tem
a mesma autoridade do export BIND que se esperava do suporte, e o torna desnecessário: o problema
do wildcard (`D-45`) só existe para quem sonda a zona de fora.

**Os valores servidos** foram medidos em **2026-09-09** por DNS-over-HTTPS contra `dns.google`,
porque não há `dig` nesta máquina. Reproduzir uma linha:

```bash
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=MX' | python3 -m json.tool
```

O painel diz **quais registros existem**; a medição diz **o que eles respondem**. As duas concordam
em tudo que as duas enxergam. O TTL de 3600 vem da medição.

## Delegação

```text
SOA  ns1.stackdns.com. hostmaster.stackdns.com. 1753989299 1800 900 1209600 300
NS   ns1.stackdns.com.   ns2.stackdns.com.   ns3.stackdns.com.   ns4.stackdns.com.
```

O painel é o Stack Control Panel (`https://www.stackcp.com/`), revenda da BlueHosting, onde o
domínio foi comprado. João recuperou o acesso em 2026-09-20 (`D-44` fechado).

## Inventário fechado — o que o painel lista

Transcrito dos prints de 2026-09-20. Esta é a lista **completa**: nome que não está aqui não existe
na zona.

| Nome no painel   | Tipo    | Valor                                                           |
| ---------------- | ------- | --------------------------------------------------------------- |
| `lotusotec.cl`   | `A`     | `185.146.167.195`                                               |
| `lotusotec.cl`   | `AAAA`  | `2a07:7800::195`                                                |
| `*.lotusotec.cl` | `A`     | `185.146.167.195`                                               |
| `*.lotusotec.cl` | `AAAA`  | `2a07:7800::195`                                                |
| `lotusotec.cl`   | `MX`    | `1 ASPMX.L.GOOGLE.COM.`                                         |
| `lotusotec.cl`   | `MX`    | `5 ALT1.ASPMX.L.GOOGLE.COM.`                                    |
| `lotusotec.cl`   | `MX`    | `5 ALT2.ASPMX.L.GOOGLE.COM.`                                    |
| `lotusotec.cl`   | `MX`    | `10 ALT3.ASPMX.L.GOOGLE.COM.`                                   |
| `lotusotec.cl`   | `MX`    | `10 ALT4.ASPMX.L.GOOGLE.COM.`                                   |
| `lotusotec.cl`   | `TXT`   | `v=spf1 include:_spf.google.com include:spf.stackmail.com -all` |
| `mail`           | `CNAME` | `ghs.googlehosted.com.`                                         |
| `smtp`           | `CNAME` | `smtp.stackmail.com.`                                           |
| `imap`           | `CNAME` | `imap.stackmail.com.`                                           |
| `pop3`           | `CNAME` | `pop3.stackmail.com.`                                           |
| `autodiscover`   | `CNAME` | `autodiscover.stackmail.com.`                                   |
| `ftp`            | `CNAME` | `ftp.us.stackcp.com.`                                           |

Mais três CNAME cujo nome contém `://` e `/`, tratados abaixo.

`185.146.167.195` é o WordPress na BlueHosting. Os cinco MX são Google Workspace: é o e-mail
corporativo, e é o registro que mais exige conferência antes e depois da troca de nameserver.

**`mail.lotusotec.cl` já está ocupado.** O MAIL FROM do SES não pode usar esse nome; a
`ADR-SITE-005` reserva `ses.lotusotec.cl`.

### `pop3`, e o que a sondagem não podia saber

A medição de 2026-09-09 perguntou por `pop`, recebeu o IP do apex e concluiu que `pop` não existia
e era o wildcard respondendo. A conclusão estava certa sobre `pop` — ele não está no painel — e
cega para `pop3`, que ninguém perguntou. É exatamente a falha que o export BIND existia para pegar,
e é a prova concreta de que enumerar subdomínio por palpite não fecha inventário.

### `www` e `sistema` não são registro

Nenhum dos dois aparece no painel. Hoje eles resolvem só porque o wildcard existe. A cópia no Route
53 os declara explicitamente **porque** o wildcard não atravessa: sem essas linhas, a troca de
delegação apagaria os dois.

### O wildcard é duplo

```text
*.lotusotec.cl.  A     185.146.167.195
*.lotusotec.cl.  AAAA  2a07:7800::195
```

O inventário de 2026-09-09 só registrou o `A`, porque só o `A` tinha sido perguntado. A consequência
prática é que `www` e `sistema` respondem IPv6 hoje, e por isso os dois ganharam `AAAA` na cópia.

### Os três atalhos do painel, descartados de propósito

| Nome no painel                                 | Valor                     |
| ---------------------------------------------- | ------------------------- |
| `http://phpmyadmin.stackcp.com/.lotusotec.cl`  | `phpmyadmin.stackcp.com.` |
| `https://webbuilder.stackcp.com/.lotusotec.cl` | `webbuilder.stackcp.com.` |
| `https://www.stackcp.com/.lotusotec.cl`        | `cp.stackcp.com.`         |

Nome de host não pode conter `://` nem `/`. São atalhos da interface do StackCP gravados na tabela
de DNS, não registros que algum resolvedor consulte. O Route 53 só os aceitaria com escape octal, e
nada os consultaria mesmo assim. **Não migram**, e o descarte está escrito aqui para que a próxima
conferência não os leia como registro perdido. Morrem junto com o painel, em `B7`.

## Ausências medidas

| Registro                               | Estado         | Consequência                                       |
| -------------------------------------- | -------------- | -------------------------------------------------- |
| `_dmarc.lotusotec.cl` `TXT`            | **não existe** | e-mail sai sem política de alinhamento             |
| `google._domainkey.lotusotec.cl` `TXT` | **não existe** | Google Workspace envia sem assinatura DKIM         |
| `CAA` do apex                          | não existe     | ACM emite sem obstáculo                            |
| DNSSEC                                 | não existe     | troca de nameserver não quebra cadeia de confiança |

As duas primeiras são `D-46`. O `include:spf.stackmail.com` do SPF precisa de decisão junto: o MX é
Google, então esse include ou é resíduo do provedor antigo ou é caminho de envio ainda em uso — e
a diferença importa quando o SES entrar como remetente novo (`B2`).

Ausência de `CAA` é boa notícia agora e dívida depois: um CAA restritivo bloquearia a emissão do
certificado pelo ACM, mas não ter nenhum significa que qualquer CA do mundo pode emitir para este
domínio. Publicar `CAA` é bloco próprio, **depois** do primeiro `ISSUED`.

## O limite desta transcrição

Ela é manual. Se um dia aparecer divergência entre a zona do Route 53 e o painel, a primeira
hipótese é erro de digitação aqui, não registro perdido lá.
````

- [ ] **Step 3: atualizar a secção `## Cópia no Route 53`**

Substitua o parágrafo que começa em "A cópia tem duas diferenças deliberadas" por:

```markdown
A cópia tem três diferenças deliberadas em relação ao painel:

- **não tem wildcard** — `www` e `sistema` viraram registro explícito no lugar dele, com `A` e
  `AAAA`;
- **não declara `NS` nem `SOA`** do apex, que a própria zona gera;
- **não tem os três atalhos do StackCP**, cujos nomes não são nomes de host.
```

- [ ] **Step 4: formatar e conferir**

```bash
pnpm exec prettier --write docs/infra/zona-dns-lotusotec.md
git diff --stat docs/infra/zona-dns-lotusotec.md
```

- [ ] **Step 5: gate de qualidade**

```bash
pnpm check
```

Esperado: verde.

- [ ] **Step 6: commit**

```bash
git add docs/infra/zona-dns-lotusotec.md
git commit -m "$(cat <<'EOF'
docs(7.2.1): registrar o inventario fechado do painel

O export BIND deixou de ser pendencia: os prints da tela de DNS do
StackCP, capturados por Joao em 2026-09-20, listam a zona inteira e tem a
mesma autoridade. O documento passa a separar as duas fontes -- o painel
diz QUAIS registros existem, a medicao por DoH de 2026-09-09 diz O QUE
eles respondem -- em vez de afirmar que a enumeracao por DNS era piso.

Fica registrado o que a sondagem nao podia saber (pop3), o que ela
mediu pela metade (o wildcard tem A e AAAA), o que ela supunha e agora
esta provado (www e sistema nao sao registro) e o que nao migra (os tres
atalhos do painel, cujos nomes contem :// e /).

Joao conferiu a transcricao contra o painel antes deste commit.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: a conferência aprende a rodar depois da delegação

**Files:**

- Modify: `scripts/infra/lib/zona.mjs` (duas funções puras novas, no fim do arquivo)
- Modify: `scripts/infra/zona.test.mjs` (um `describe` novo)
- Modify: `scripts/infra/conferir-zona.mjs` (passa a usar as duas, e ganha a flag)

**Interfaces:**

- Consumes: `NS_DA_STACKDNS` e `NOMES_INVENTADOS`, que já existem em `lib/zona.mjs`.
- Produces:
  - `wildcardAusente(naAws: string[], naStack: string[], posDelegacao: boolean): boolean`
  - `delegacaoEsperada(nomesDeServidor: string[], posDelegacao: boolean): string[]`
  - a flag `--pos-delegacao` de `pnpm infra:conferir-zona`, usada pela Task 4.

Hoje o script afirma duas coisas que a troca inverte. Primeira: a linha `NS` só é "esperada" quando a delegação ainda é da StackDNS. Segunda: a linha de nome inventado só é "esperada" quando ele resolve na StackDNS e não na AWS — depois da troca os dois lados são a mesma zona, e o nome inventado não resolve em lugar nenhum. Sem esta task, a conferência pós-delegação sai vermelha **por estar certa**, e a evidência central do bloco vira ruído.

As duas decisões nascem em `lib/` e não no script porque `lib/` é o que a catraca offline testa: veredito dentro de `conferir-zona.mjs` só seria exercido com rede e com a delegação já trocada — isto é, tarde demais.

- [ ] **Step 1: escrever os testes que falham**

Acrescente ao fim de `scripts/infra/zona.test.mjs` um `describe` novo, e inclua `delegacaoEsperada` e `wildcardAusente` no `import` do topo do arquivo:

```js
describe('vereditos que a delegação inverte', () => {
  it('antes da troca, nome inventado só pode resolver do lado da StackDNS', () => {
    expect(wildcardAusente([], ['185.146.167.195'], false)).toBe(true)
    // Resolveu na AWS: o wildcard atravessou.
    expect(
      wildcardAusente(['185.146.167.195'], ['185.146.167.195'], false),
    ).toBe(false)
    // Não resolveu em lado nenhum: ou a StackDNS mudou, ou a pergunta não
    // chegou. Nos dois casos o relatório não pode dar verde.
    expect(wildcardAusente([], [], false)).toBe(false)
  })

  it('depois da troca, nome inventado não pode resolver em lado nenhum', () => {
    // Os dois lados passaram a ser a mesma zona; a assimetria de antes
    // deixaria de ser possível mesmo que tudo estivesse certo.
    expect(wildcardAusente([], [], true)).toBe(true)
    expect(wildcardAusente([], ['185.146.167.195'], true)).toBe(false)
    expect(wildcardAusente(['185.146.167.195'], [], true)).toBe(false)
  })

  it('antes da troca, a delegação esperada é a da StackDNS', () => {
    expect(delegacaoEsperada(['ns-31.awsdns-03.com'], false)).toEqual([
      'ns1.stackdns.com.',
      'ns2.stackdns.com.',
      'ns3.stackdns.com.',
      'ns4.stackdns.com.',
    ])
  })

  it('depois da troca, a delegação esperada são os nameservers do stack', () => {
    // Com ponto final, que é como a resposta DoH chega. Hardcodar os quatro
    // nomes aqui seria mentira a partir da primeira zona recriada.
    expect(
      delegacaoEsperada(['ns-31.awsdns-03.com', 'ns-904.awsdns-49.net.'], true),
    ).toEqual(['ns-31.awsdns-03.com.', 'ns-904.awsdns-49.net.'])
  })
})
```

- [ ] **Step 2: rodar e ver reprovar**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **FAIL** nos quatro testes novos, com `wildcardAusente is not a function` e `delegacaoEsperada is not a function`.

- [ ] **Step 3: escrever as duas funções**

No fim de `scripts/infra/lib/zona.mjs`:

```js
/**
 * Veredito da linha de nome inventado, que a delegação inverte.
 *
 * Antes da troca os dois lados são zonas diferentes, e a prova de que o
 * wildcard não atravessou é a assimetria: vazio na AWS, IP na StackDNS.
 * Depois da troca os dois lados são a MESMA zona lida por caminhos
 * diferentes, e essa assimetria deixa de ser possível — o único desfecho
 * correto passa a ser vazio dos dois lados. Manter a regra antiga daria
 * vermelho justamente quando tudo estivesse certo.
 *
 * Resolver na AWS reprova nos dois casos: é o wildcard tendo atravessado.
 * @param {string[]} naAws
 * @param {string[]} naStack
 * @param {boolean} posDelegacao
 */
export function wildcardAusente(naAws, naStack, posDelegacao) {
  if (naAws.length > 0) return false
  return posDelegacao ? naStack.length === 0 : naStack.length > 0
}

/**
 * Qual delegação o relatório espera encontrar. Depois da troca ela é a do
 * próprio stack, e não uma lista fixa: uma hosted zone recriada ganha
 * nameservers novos, e uma constante aqui viraria mentira silenciosa na
 * primeira vez que isso acontecesse.
 * @param {string[]} nomesDeServidor
 * @param {boolean} posDelegacao
 * @returns {string[]}
 */
export function delegacaoEsperada(nomesDeServidor, posDelegacao) {
  if (!posDelegacao) return [...NS_DA_STACKDNS]
  return nomesDeServidor.map((nome) => `${nome.replace(/\.$/, '')}.`)
}
```

- [ ] **Step 4: rodar e ver passar**

```bash
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **PASS**, incluindo os quatro testes novos.

- [ ] **Step 5: ligar as duas no script**

Em `scripts/infra/conferir-zona.mjs`:

Acrescente a flag à documentação de uso, no cabeçalho:

```js
//   --pos-delegacao                                      inverte os vereditos
```

Troque o `import` de `./lib/zona.mjs` por:

```js
import {
  INVENTARIO,
  NOMES_INVENTADOS,
  delegacaoEsperada,
  mesmoConjunto,
  wildcardAusente,
} from './lib/zona.mjs'
```

`NS_DA_STACKDNS` sai daqui — quem a usa agora é `delegacaoEsperada`.

Logo abaixo da função `argumento`:

```js
// Flag sem valor: `argumento()` leria o proximo argv como se fosse o dela.
const posDelegacao = process.argv.includes('--pos-delegacao')
```

Substitua o corpo do laço de `NOMES_INVENTADOS` por:

```js
for (const inventado of NOMES_INVENTADOS) {
  const naAws = await ladoAws(inventado, 'A')
  const naStack = await perguntarDoh(inventado, 'A')
  linhas.push({
    nome: inventado,
    tipo: 'A',
    inventario: [],
    aws: naAws,
    atual: naStack,
    // Nunca `igual`: para nome inventado nao existe lado certo a bater.
    igual: false,
    esperado: wildcardAusente(naAws, naStack, posDelegacao),
    nota: posDelegacao
      ? 'wildcard removido; os dois lados ja leem a zona nova'
      : 'wildcard removido de proposito',
  })
}
```

Substitua o bloco da linha `NS` por:

```js
const delegacao = await perguntarDoh('lotusotec.cl.', 'NS')
const esperadaNs = delegacaoEsperada(nomesDeServidor, posDelegacao)
linhas.push({
  nome: 'lotusotec.cl.',
  tipo: 'NS',
  inventario: esperadaNs,
  aws: [],
  atual: delegacao,
  // A coluna AWS fica vazia porque nada foi perguntado a ela: o que esta
  // linha afirma e sobre a delegacao, nao sobre a zona nova. Por isso ela
  // nunca e `igual`.
  igual: false,
  esperado: mesmoConjunto(delegacao, esperadaNs),
  nota: posDelegacao
    ? 'a linha afirma que a delegacao ja e a da AWS; AWS nao consultada'
    : 'a linha afirma a delegacao, nao a zona nova; AWS nao consultada',
})
```

No cabeçalho do relatório, a entrada que hoje é `porApi ? '…' : '> Lado AWS lido da **resposta servida**…'` vira:

```js
  porApi
    ? '> **Degradado.** UDP/53 não saiu desta máquina, então o lado AWS foi lido da' +
      ' **configuração** (`aws route53 list-resource-record-sets`), e não da resposta servida.' +
      ' São afirmações diferentes: esta prova que a zona está escrita assim, não que ela' +
      ' responde assim.'
    : posDelegacao
      ? '> Lado AWS lido da **resposta servida**, perguntando direto aos quatro nameservers da' +
        ' zona nova. A delegação já aponta para eles, então a coluna StackDNS deixou de ser um' +
        ' segundo lado: é a mesma zona, lida pelo caminho comum. As duas colunas baterem prova' +
        ' **convergência**, não paridade — e uma delas ainda pode carregar cache de antes da' +
        ' troca.'
      : '> Lado AWS lido da **resposta servida**, perguntando direto aos quatro nameservers da' +
        ' zona nova — a delegação ainda aponta para a StackDNS, então nenhuma consulta comum' +
        ' enxergaria esta zona.',
```

E as duas últimas linhas de texto do relatório viram uma só entrada:

```js
  posDelegacao
    ? 'Resolução correta não prova entrega de e-mail. A prova do MX é mensagem recebida, e ela fica' +
      ' em `docs/infra/delegacao-<data>.md`.'
    : 'Resolução correta não prova entrega de e-mail. A prova do MX é mensagem recebida, e ela só é' +
      ' possível depois da delegação.',
```

- [ ] **Step 6: provar que o script ainda roda, e ainda sabe reprovar**

```bash
pnpm infra:conferir-zona --saida /tmp/conferencia-descartavel.md
```

Esperado: saída 0 e a mesma tabela de sempre — a delegação ainda é a da StackDNS, então o modo padrão continua correto. As três linhas novas do inventário (`pop3` e os dois `AAAA`) devem aparecer com `sim`.

Agora a prova de que ele reprova:

```bash
pnpm infra:conferir-zona --pos-delegacao --saida /tmp/descartavel-2.md; echo "saida=$?"
```

Esperado: `saida=1`, com a linha `NS` marcada `**NÃO**` — a delegação ainda **não** é a da AWS, e é exatamente isso que o modo novo deve acusar. Guarde as duas saídas para o corpo do commit, e apague os dois arquivos de `/tmp`.

- [ ] **Step 7: gate de qualidade**

```bash
pnpm check
```

- [ ] **Step 8: commit**

```bash
git add scripts/infra/lib/zona.mjs scripts/infra/zona.test.mjs scripts/infra/conferir-zona.mjs
git commit -m "$(cat <<'EOF'
feat(7.2.1): ensinar a conferencia a rodar depois da delegacao

Duas afirmacoes do relatorio se invertem com a troca de nameservers, e
sem isto a conferencia pos-delegacao sairia vermelha por estar certa:

- a linha NS espera a delegacao da StackDNS. Depois da troca ela passa a
  esperar os nameservers do proprio stack -- lidos do stack, nao de uma
  constante, porque zona recriada ganha nameservers novos.
- a linha de nome inventado espera assimetria (vazio na AWS, IP na
  StackDNS). Depois da troca os dois lados sao a mesma zona, e o unico
  desfecho correto passa a ser vazio dos dois lados.

Os dois vereditos ficam em lib/zona.mjs, que e o que a catraca offline
testa; dentro do script eles so seriam exercidos com a delegacao ja
trocada, isto e, tarde demais.

<colar aqui as duas corridas do Step 6: modo padrao saindo 0 e
--pos-delegacao saindo 1 na linha NS>

Emenda E1b ao escopo da spec, justificada em
docs/superpowers/plans/2026-09-20-7.2.1-delegacao-certificado.md.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: a delegação

**Files:**

- Create: `docs/infra/conferencia-zona-<data>.md` (gerado, pré-troca)
- Create: `docs/infra/conferencia-zona-<data>-pos-delegacao.md` (gerado)
- Create: `docs/infra/delegacao-<data>.md` (escrito à mão: os gates humanos)

**Interfaces:**

- Consumes: a flag `--pos-delegacao` da Task 3; o inventário de 14 linhas da Task 1.
- Produces: a delegação convergida e a prova de e-mail, sem as quais a Task 5 não pode começar.

Esta é a task irreversível dentro do TTL. Ela tem **dois gates humanos** e uma espera que não controlamos.

- [ ] **Step 1: conferência pré-troca**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
export AWS_PROFILE=lotus
pnpm infra:conferir-zona
```

Esperado: saída 0, relatório em `docs/infra/conferencia-zona-<data-de-hoje>.md`, com as 14 linhas do inventário em `sim`, os dois nomes inventados em `divergência esperada` e a linha `NS` ainda na StackDNS.

**Se houver qualquer divergência não esperada, pare aqui.** A troca não acontece com o relatório vermelho — depois dela, diferença é serviço fora do ar para parte do mundo, e o rollback leva o mesmo TTL para valer.

- [ ] **Step 2: reler os quatro nameservers do stack**

```bash
aws cloudformation describe-stacks --region us-east-1 --stack-name lotus-dns \
  --query "Stacks[0].Outputs[?OutputKey=='NameServers'].OutputValue" --output text
```

Esperado: quatro nomes `ns-*.awsdns-*` separados por vírgula. Confira contra os quatro da secção "Medições" deste plano; se forem outros, **são estes que valem** — a zona foi recriada em algum momento, e os do plano estão velhos.

- [ ] **Step 3: gate humano G1 — o João troca os nameservers no StackCP**

**Este passo é do João. Não há comando nosso que o substitua.** Entregue a ele exatamente isto, com os quatro nomes do Step 2 no lugar:

> A conferência passou sem divergência inesperada — o relatório está em
> `docs/infra/conferencia-zona-<data>.md`. Pode trocar. Passo a passo:
>
> 1. Entre no StackCP (o painel da BlueHosting, o mesmo dos prints do inventário).
> 2. Abra o domínio `lotusotec.cl`. Procure a área de **nameservers** — o painel está em espanhol e
>    costuma chamar **Nombres de Servidor**, **Servidores de Nombres** ou **Name Servers**. É uma
>    tela **diferente** da de gerenciamento de DNS, que é onde você tirou os prints.
> 3. **Antes de mudar qualquer coisa, tire um print da tela como está.** Ele registra o valor
>    antigo e é a instrução de rollback: `ns1.stackdns.com`, `ns2.stackdns.com`,
>    `ns3.stackdns.com`, `ns4.stackdns.com`.
> 4. Substitua os quatro por estes, um por campo, na ordem em que vierem. Sem ponto final, a menos
>    que o painel já mostre os atuais com ponto:
>
>    ```text
>    <os quatro nomes do Step 2>
>    ```
>
> 5. Salve e tire um print da tela depois de salvar.
> 6. Me avise. Eu meço a convergência; ela não é instantânea.
> 7. **Não peça remoção da zona antiga ao suporte.** Ela é o rollback e fica de pé até `B7`.
>
> O tempo de convergência é do TTL da delegação no registro `.cl`, que não controlamos — de horas a
> dois dias. Nesse intervalo, resolvedores diferentes leem lados diferentes, e é por isso que os
> dois lados precisavam responder igual antes da troca.

**Pare e espere.** Não siga para o Step 4 sem o aviso dele.

- [ ] **Step 4: medir a convergência**

Não há `dig` nesta máquina; a leitura é por DNS-over-HTTPS:

```bash
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=NS' \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("\n".join(sorted(a["data"] for a in d.get("Answer",[]) if a["type"]==2)))'
```

Esperado, quando tiver convergido: os quatro `ns-*.awsdns-*.` do Step 2. Enquanto aparecerem os `ns*.stackdns.com.`, **ainda não convergiu** — repita mais tarde, sem pressa e sem pular para o Step 5.

Confira também que o e-mail resolve pelo lado novo antes de pedir o teste humano:

```bash
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=MX' \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("\n".join(sorted(a["data"] for a in d.get("Answer",[]) if a["type"]==15)))'
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=TXT' \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("\n".join(a["data"] for a in d.get("Answer",[]) if a["type"]==16))'
```

Esperado: os cinco MX do Google, sem sobra e sem falta, e o SPF idêntico ao do inventário.

**Se o MX ou o SPF divergirem, execute o rollback (spec §7) imediatamente e avise o João.** Não espere para ver se melhora.

- [ ] **Step 5: gate humano G2 — o João prova que o e-mail sobreviveu**

**Este passo é do João.** `dig` prova que o MX resolve; entrega é outra afirmação. Entregue a ele:

> A delegação convergiu: `NS lotusotec.cl` já devolve os quatro nameservers da AWS, e MX e SPF
> estão idênticos ao que a StackDNS servia. Agora a prova que só você pode dar:
>
> 1. De uma conta que **não** seja `@lotusotec.cl` — seu Gmail pessoal serve —, envie uma mensagem
>    para uma caixa real `@lotusotec.cl`.
> 2. Confirme que ela chegou, abrindo a caixa.
> 3. Na mensagem recebida, abra **Mostrar original** (Gmail) e confirme que o cabeçalho `SPF` diz
>    `PASS`. Isso prova que o SPF atravessou a troca.
> 4. Responda a mensagem e confirme que a resposta chega no remetente externo. A saída é Google e
>    não depende do MX, mas prova a caixa inteira de uma vez.
> 5. Me avise o resultado, inclusive se algo falhar.
>
> **Se a mensagem não chegar, ou o SPF reprovar:** me avise imediatamente. Eu executo o rollback, e
> ele é: voltar à mesma tela de antes e repor `ns1.stackdns.com`, `ns2.stackdns.com`,
> `ns3.stackdns.com`, `ns4.stackdns.com` — os mesmos do print que você tirou antes de mudar. Não
> espere para ver se melhora.

**Pare e espere.** Sem o "chegou, com SPF PASS" não há Task 5.

- [ ] **Step 6: conferência pós-delegação**

```bash
pnpm infra:conferir-zona --pos-delegacao \
  --saida "docs/infra/conferencia-zona-$(date +%F)-pos-delegacao.md"
```

Esperado: saída 0. As 14 linhas do inventário em `sim`, os dois nomes inventados em `divergência esperada — wildcard removido; os dois lados ja leem a zona nova`, e a linha `NS` em `divergência esperada` com os quatro nameservers da AWS na coluna StackDNS.

Se a linha `NS` sair `**NÃO**`, a convergência regrediu ou o cache do `dns.google` ainda carrega a resposta antiga: espere e rode de novo, sem editar o relatório.

- [ ] **Step 7: escrever a evidência dos gates humanos**

Crie `docs/infra/delegacao-<data-da-troca>.md`:

````markdown
# Delegação de `lotusotec.cl` para o Route 53 — <data>

> Evidência dos dois gates humanos do bloco `B1`. O que um comando podia provar está em
> `conferencia-zona-<data>-pos-delegacao.md`; o que só uma pessoa podia provar está aqui.

## Antes

Nameservers no StackCP, registrados em print antes de qualquer mudança:

```text
ns1.stackdns.com   ns2.stackdns.com   ns3.stackdns.com   ns4.stackdns.com
```

São eles o rollback, e a zona da StackDNS continua de pé e respondendo até `B7`.

## Depois

```text
<os quatro ns-*.awsdns-* efetivamente gravados>
```

Troca feita por João em <data e hora>, no painel do StackCP.

## Convergência

Medida por DNS-over-HTTPS contra `dns.google`, porque não há `dig` nesta máquina:

```text
<colar a saída do Step 4: NS, MX e TXT>
```

## Prova de entrega de e-mail

Resolução correta não prova entrega. O que foi feito, por João, em <data e hora>:

- mensagem enviada de <conta externa> para <caixa @lotusotec.cl>;
- recebida em <quanto tempo>;
- cabeçalho `SPF` da mensagem recebida: `PASS`;
- resposta enviada de volta e recebida no remetente externo.

Esta é a prova de aceite do `MX` deste bloco. O `dig` não é.

## Conferência pós-delegação

`conferencia-zona-<data>-pos-delegacao.md`, gerado por
`pnpm infra:conferir-zona --pos-delegacao`: as 14 linhas do inventário conferem, os dois nomes
inventados não resolvem em lado nenhum — o wildcard não atravessou — e a linha `NS` mostra a
delegação já na AWS.

## O que continua de pé de propósito

A zona na StackDNS **não** foi apagada e sua remoção **não** foi pedida ao suporte. Ela é o
rollback, e o custo de mantê-la é zero. Desligá-la é `B7`, junto com o WordPress.
````

- [ ] **Step 8: gate de qualidade**

```bash
pnpm check
```

Esperado: verde. Se o `format:check` reclamar do relatório gerado, **não o edite à mão** — quem o formata é o próprio script, e reformatá-lo por fora é editar evidência.

- [ ] **Step 9: commit**

```bash
git add docs/infra/conferencia-zona-*.md docs/infra/delegacao-*.md
git commit -m "$(cat <<'EOF'
feat(7.2.1): delegar lotusotec.cl para o Route 53

O registro .cl passou a apontar para os quatro nameservers da zona do
stack lotus-dns. A troca foi feita por Joao no painel do StackCP, com
print da tela antes e depois -- o print de antes e a instrucao de
rollback, e a zona da StackDNS continua de pe e respondendo.

Ordem cumprida: conferencia pre-troca sem divergencia inesperada, troca,
convergencia medida por DoH, prova de entrega de e-mail com mensagem
real, conferencia pos-delegacao.

A prova do MX e mensagem recebida numa caixa @lotusotec.cl com SPF PASS
no cabecalho, registrada em docs/infra/delegacao-<data>.md. Resolucao
correta nao prova entrega, e o dig nao substitui essa linha.

D-45 fecha: o wildcard nao atravessou, e os dois nomes inventados nao
resolvem mais em lado nenhum.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: o certificado ACM

**Files:**

- Modify: `scripts/infra/lib/zona.mjs` (uma linha de `lerRegistros`)
- Modify: `scripts/infra/zona.test.mjs` (um teste novo, um teste invertido)
- Modify: `infra/lotus-dns.yaml` (recurso `Certificado` e output `ArnDoCertificado`)
- Modify: `docs/infra/zona-dns-lotusotec.md` (secção do certificado)

**Interfaces:**

- Consumes: a delegação convergida da Task 4.
- Produces: o output `ArnDoCertificado` do stack `lotus-dns`, que `B5` consome como parâmetro do stack do site — CloudFormation não importa valor entre regiões, e o certificado é `us-east-1` enquanto a distribuição é `sa-east-1`.

Há um defeito latente no leitor do template que **precisa ser corrigido antes** do recurso entrar. `lerRegistros` para a leitura na primeira linha que começa na coluna zero (`/^\S/`), e o recurso seguinte dentro de `Resources:` começa com dois espaços. Com o `Certificado` no arquivo, o leitor entraria no bloco dele e acrescentaria `- !Sub 'www.${NomeDaZona}'` aos valores do último `RecordSet`. A catraca reprovaria o `ftp`, e a mensagem apontaria para o lugar errado.

- [ ] **Step 1: gate humano G3 — autorização para emitir**

**Peça ao João, e espere a resposta.** Um "pode ir" basta:

> A delegação convergiu e o e-mail está provado (`docs/infra/delegacao-<data>.md`). O próximo passo
> acrescenta o recurso `Certificado` ao stack `lotus-dns` e reimplanta. O passo é meu; a
> autorização é sua, porque é ele que trava o stack em `CREATE_IN_PROGRESS` se a delegação não
> tiver convergido de verdade — e um pedido pendente do ACM é cancelado em 72h. Posso ir?

- [ ] **Step 2: escrever o teste que pega o defeito do leitor**

Acrescente a `scripts/infra/zona.test.mjs`, dentro do `describe('leitura textual do template')`:

```js
it('para a lista de RecordSets no próximo recurso de topo', () => {
  // Com o Certificado no arquivo, um leitor que só para na coluna zero
  // entraria no bloco dele e somaria o SAN aos valores do último
  // RecordSet. A catraca reprovaria o ftp, apontando para o lugar errado.
  const fixture = [
    '',
    'Parameters:',
    '  NomeDaZona:',
    '    Default: exemplo.cl',
    'Resources:',
    '  Registros:',
    '    Properties:',
    '      RecordSets:',
    "        - Name: !Sub 'ftp.${NomeDaZona}.'",
    '          Type: CNAME',
    '          TTL: 3600',
    '          ResourceRecords:',
    '            - ftp.exemplo.',
    '  Certificado:',
    '    Properties:',
    '      SubjectAlternativeNames:',
    "        - !Sub 'www.${NomeDaZona}'",
    'Outputs:',
    '  Nada:',
    '    Value: x',
  ].join('\n')
  const lidos = lerRegistros(fixture)
  expect(lidos).toHaveLength(1)
  expect(lidos[0]?.nome).toBe('ftp.exemplo.cl.')
  expect(lidos[0]?.valores).toEqual(['ftp.exemplo.'])
})
```

- [ ] **Step 3: rodar e ver reprovar**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **FAIL** só nesse teste, com os valores lidos sendo `['ftp.exemplo.', 'www.exemplo.cl']`.

- [ ] **Step 4: corrigir o leitor**

Em `scripts/infra/lib/zona.mjs`, dentro de `lerRegistros`, troque:

```js
    // Chave de topo na coluna zero: a lista acabou.
    if (/^\S/.test(linha)) break
```

por:

```js
    // A lista acabou: ou uma chave de topo na coluna zero (`Outputs:`), ou o
    // proximo recurso dentro de `Resources:`, que entra com dois espacos. Os
    // itens de RecordSets vivem a partir da coluna oito, entao qualquer
    // conteudo em indentacao menor ja esta fora da lista.
    if (/^ {0,6}\S/.test(linha)) break
```

- [ ] **Step 5: rodar e ver passar**

```bash
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **PASS**, com os 14 registros do template ainda sendo lidos.

- [ ] **Step 6: inverter o teste que proibia o certificado**

O bloco anterior escreveu um teste para impedir que o certificado entrasse antes da delegação. Ele cumpriu o papel; agora ele mente. Substitua, em `scripts/infra/zona.test.mjs`:

```js
it('não declara certificado', () => {
  // Pedido pendente do ACM morre em 72h, e com HostedZoneId o stack fica em
  // CREATE_IN_PROGRESS até validar — com a zona ainda não delegada, isso é
  // stack travado até o timeout. Spec §3 D6.
  expect(TEMPLATE).not.toMatch(/AWS::CertificateManager::Certificate/)
})
```

por:

```js
it('declara o certificado da zona, com SAN explícito e sem wildcard', () => {
  // A proibição anterior existia enquanto a zona não estava delegada:
  // pedido pendente do ACM morre em 72h, e com HostedZoneId o stack fica em
  // CREATE_IN_PROGRESS até validar. A delegação convergiu, então o que a
  // catraca precisa travar agora é a forma do certificado.
  const de = TEMPLATE.indexOf('\n  Certificado:')
  expect(de, 'template sem o recurso Certificado').toBeGreaterThan(-1)
  const bloco = TEMPLATE.slice(de, TEMPLATE.indexOf('\nOutputs:'))
  expect(bloco).toMatch(/Type: AWS::CertificateManager::Certificate/)
  expect(bloco).toMatch(/ValidationMethod: DNS/)
  expect(bloco).toMatch(/- !Sub 'www\.\$\{NomeDaZona\}'/)
  // Os dois nomes validam na própria zona: sem isto o ACM não cria o CNAME
  // de validação e alguém teria de criá-lo à mão, fora do stack.
  expect(bloco.match(/HostedZoneId: !Ref Zona/g)).toHaveLength(2)
  // Wildcard amplia o raio de uma chave comprometida e esconde o inventário
  // de nomes. Spec §4.
  expect(bloco).not.toMatch(/\*/)
})

it('exporta o ARN do certificado para o bloco do site', () => {
  // B5 consome isto como parâmetro: CloudFormation não importa valor entre
  // regiões, e a distribuição é sa-east-1.
  expect(TEMPLATE).toMatch(/ArnDoCertificado:/)
})
```

- [ ] **Step 7: rodar e ver reprovar**

```bash
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **FAIL** nos dois testes novos, com `template sem o recurso Certificado`.

- [ ] **Step 8: declarar o certificado**

Em `infra/lotus-dns.yaml`, **depois** do recurso `Registros` e antes de `Outputs:`:

```yaml
Certificado:
  Type: AWS::CertificateManager::Certificate
  # Entra so depois da delegacao ter convergido. Com HostedZoneId o ACM cria
  # e remove o CNAME de validacao sozinho nesta zona -- mas se o mundo ainda
  # lesse a StackDNS, ninguem enxergaria esse CNAME e o stack ficaria em
  # CREATE_IN_PROGRESS ate o timeout.
  #
  # SANs explicitos, NUNCA *.lotusotec.cl: wildcard amplia o raio de uma
  # chave comprometida e esconde o inventario de nomes.
  Properties:
    DomainName: !Ref NomeDaZona
    SubjectAlternativeNames:
      - !Sub 'www.${NomeDaZona}'
    ValidationMethod: DNS
    DomainValidationOptions:
      - DomainName: !Ref NomeDaZona
        HostedZoneId: !Ref Zona
      - DomainName: !Sub 'www.${NomeDaZona}'
        HostedZoneId: !Ref Zona
    Tags:
      - Key: Projeto
        Value: lotus-site
```

E, em `Outputs:`:

```yaml
ArnDoCertificado:
  Description: >-
    Consumido por B5 como PARAMETRO do stack do site, nao por ImportValue:
    o certificado e us-east-1 e a distribuicao e sa-east-1, e o
    CloudFormation nao importa valor entre regioes.
  Value: !Ref Certificado
```

- [ ] **Step 9: rodar e ver passar**

```bash
pnpm vitest run scripts/infra/zona.test.mjs
```

Esperado: **PASS**, e os 14 `RecordSet` continuam sendo lidos — a correção do Step 4 é o que garante isso.

- [ ] **Step 10: validar o template**

```bash
export AWS_PROFILE=lotus
aws cloudformation validate-template --region us-east-1 \
  --template-body file://infra/lotus-dns.yaml
```

- [ ] **Step 11: implantar, e esperar a validação**

```bash
aws cloudformation deploy --region us-east-1 --stack-name lotus-dns \
  --template-file infra/lotus-dns.yaml --tags Projeto=lotus-site
```

O comando **não volta rápido**: ele fica parado enquanto o ACM cria o CNAME de validação na zona, espera a CA enxergá-lo e emite. Com a delegação convergida isso costuma levar poucos minutos.

**Se ficar preso além disso**, leia os eventos antes de concluir qualquer coisa:

```bash
aws cloudformation describe-stack-events --region us-east-1 --stack-name lotus-dns \
  --max-items 20 --query 'StackEvents[].[Timestamp,ResourceStatus,ResourceStatusReason]' \
  --output table
```

Se o stack entrar em rollback, **não tente de novo em seguida**: um pedido pendente do ACM é cancelado em 72h, e repetir empilha pedidos. Investigue a delegação primeiro, com o comando do Step 4 da Task 4.

- [ ] **Step 12: provar `ISSUED`**

```bash
ARN="$(aws cloudformation describe-stacks --region us-east-1 --stack-name lotus-dns \
  --query "Stacks[0].Outputs[?OutputKey=='ArnDoCertificado'].OutputValue" --output text)"

aws acm describe-certificate --region us-east-1 --certificate-arn "$ARN" \
  --query 'Certificate.[Status,DomainName,SubjectAlternativeNames,NotAfter,RenewalEligibility]' \
  --output json
```

Esperado: `"ISSUED"`, `"lotusotec.cl"`, `["lotusotec.cl","www.lotusotec.cl"]`, uma data cerca de treze meses à frente e `"ELIGIBLE"`. Guarde a saída para o corpo do commit.

- [ ] **Step 13: registrar o certificado no inventário**

Acrescente ao fim de `docs/infra/zona-dns-lotusotec.md`:

```markdown
## Certificado

Desde <data>, o stack `lotus-dns` também declara o certificado ACM da zona, em `us-east-1`:

| Campo                     | Valor              |
| ------------------------- | ------------------ |
| `DomainName`              | `lotusotec.cl`     |
| `SubjectAlternativeNames` | `www.lotusotec.cl` |
| `ValidationMethod`        | `DNS`              |
| `Status`                  | `ISSUED`           |

A validação é DNS-01 com `HostedZoneId` apontando para a própria zona: o ACM cria e remove o CNAME
de validação sozinho, e a renovação é automática — e silenciosa, que é justamente o motivo de a
`CAA` ser assunto de bloco próprio. Um `CAA` errado bloqueia a renovação sem aviso.

O ARN sai no output `ArnDoCertificado`. Ele é consumido por `B5` como **parâmetro** do stack do
site, não por `ImportValue`: o certificado é `us-east-1`, a distribuição é `sa-east-1`, e
CloudFormation não importa valor entre regiões.

**O certificado existe; ele ainda não é servido.** Nada o apresenta a um navegador enquanto a
distribuição não tiver `Aliases` e `ViewerCertificate` — isso é `B5`.
```

- [ ] **Step 14: gate de qualidade**

```bash
pnpm check
```

- [ ] **Step 15: commit**

```bash
git add infra/lotus-dns.yaml scripts/infra/lib/zona.mjs scripts/infra/zona.test.mjs \
  docs/infra/zona-dns-lotusotec.md
git commit -m "$(cat <<'EOF'
feat(7.2.1): emitir o certificado ACM da zona

Recurso AWS::CertificateManager::Certificate no proprio stack lotus-dns,
validacao DNS-01 com HostedZoneId apontando para a zona: o ACM cria e
remove o CNAME de validacao sozinho e renova sem intervencao. SANs
explicitos -- lotusotec.cl e www.lotusotec.cl --, nunca wildcard.

Sai o output ArnDoCertificado, que B5 consome como PARAMETRO do stack do
site: o certificado e us-east-1, a distribuicao e sa-east-1, e o
CloudFormation nao importa valor entre regioes.

Dois ajustes na catraca, no mesmo commit porque o certificado os exige:

- lerRegistros parava a lista de RecordSets so na coluna zero. Recurso
  irmao dentro de Resources: entra com dois espacos, entao o leitor
  entraria no bloco do Certificado e somaria o SAN aos valores do ftp.
  Teste com fixture proprio reprovou antes e passa depois.
- o teste `nao declara certificado`, escrito no bloco anterior para
  impedir a emissao antes da delegacao, cumpriu o papel e agora mente.
  Virou o seu oposto: trava a FORMA do certificado, e reprova wildcard.

<colar aqui a saida do describe-certificate do Step 12>

O certificado existe e esta ISSUED; ele ainda nao e servido. D-47 encolhe
para "HTTPS nao servido" e morre em B5.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: runbook, backlog e estado

**Files:**

- Modify: `docs/infra/runbook-aws.md` (secções 6.3 a 6.7)
- Modify: `docs/superpowers/backlog.md` (`B1`, PENDÊNCIAS, `D-45`, `D-47`, débito novo `D-49`)
- Modify: `docs/superpowers/state.md`

**Interfaces:**

- Consumes: tudo o que as Tasks 1 a 5 provaram.
- Produces: o estado em `ready_for_review`, que é o que `/revisar-site` aceita.

- [ ] **Step 1: runbook §6.3 — a flag nova**

Acrescente ao fim da secção `### 6.3 Conferir antes de trocar`:

````markdown
Depois da troca, a mesma conferência roda com `--pos-delegacao`:

```bash
pnpm infra:conferir-zona --pos-delegacao \
  --saida "docs/infra/conferencia-zona-$(date +%F)-pos-delegacao.md"
```

O modo inverte os dois vereditos que a delegação inverte: a linha `NS` passa a esperar os
nameservers do próprio stack, e o nome inventado passa a ter de não resolver em lado nenhum — os
dois lados são a mesma zona agora, e a assimetria de antes deixou de ser possível.
````

- [ ] **Step 2: runbook §6.4 e §6.5 — marcar feito e tirar o `dig`**

Acrescente no topo de `### 6.4 Trocar os nameservers`:

```markdown
**Feito em <data>.** Evidência em `docs/infra/delegacao-<data>.md`: print do painel antes e depois,
convergência medida e prova de entrega de e-mail. A zona da StackDNS continua de pé — é o rollback,
e desligá-la é `B7`.
```

E substitua o bloco `dig` de `### 6.5 Provar que o e-mail sobreviveu` por, já que não há `dig` nesta máquina:

```bash
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=MX' \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("\n".join(sorted(a["data"] for a in d.get("Answer",[]) if a["type"]==15)))'
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=TXT' \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("\n".join(a["data"] for a in d.get("Answer",[]) if a["type"]==16))'
```

- [ ] **Step 3: runbook §6.6 — marcar emitido e fechar `D-48`**

Acrescente no topo de `### 6.6 Certificado no ACM`:

```markdown
**Emitido em <data>**, pelo recurso do template. `Status: ISSUED`, para `lotusotec.cl` e
`www.lotusotec.cl`. O ARN sai no output `ArnDoCertificado` e é consumido por `B5` como parâmetro,
não por `ImportValue`.
```

E, logo antes do bloco de fallback com `aws acm request-certificate`, deixe explícito qual das duas descrições vale:

```markdown
**O caminho acima é o que está em uso.** O que vem abaixo é fallback declarado, nunca executado até
aqui, e um certificado emitido por ele **não** seria gerenciado pelo stack — o próximo deploy
tentaria criar outro. Se as duas descrições divergirem, a do template vence (`D-48`).
```

- [ ] **Step 4: runbook §6.7 — o inventário tem 14 linhas**

Substitua, em `### 6.7 Subdomínios`, a frase sobre `sistema.lotusotec.cl` por:

```markdown
`sistema.lotusotec.cl` e `www.lotusotec.cl` já nascem no template como `A` **e** `AAAA` para o
WordPress. Nenhum dos dois é registro no painel antigo — o inventário fechado de 2026-09-20 prova
isso —, e os dois só resolviam por causa do wildcard, que não atravessou.
```

- [ ] **Step 5: backlog — `B1`**

Substitua o bloco `**Entregue em 2026-09-20 (parcial):**` de `## B1` por:

```markdown
- **Entregue em 2026-09-20 (primeira rodada):** zona criada no Route 53 pelo stack `lotus-dns`,
  conferida contra a StackDNS registro a registro, catraca offline dentro do `pnpm check`, e filtro
  do Budget ampliado para Route 53. **Não** entregue: delegação, certificado, HTTPS, alias.
- **Entregue em <data> (segunda rodada):** inventário fechado a partir do painel do StackCP, que
  revelou `pop3` e o wildcard duplo; delegação trocada e convergida; entrega de e-mail provada com
  mensagem real e `SPF: PASS`; certificado ACM `ISSUED` para `lotusotec.cl` e `www.lotusotec.cl`.
  **Não** entregue: HTTPS servido e alias da distribuição — são `B5`, e é o que resta de `D-47`.
```

- [ ] **Step 6: backlog — PENDÊNCIAS**

Substitua o item 2 por:

```markdown
2. ~~**Export BIND da zona.**~~ Deixou de ser necessário: os prints do painel do StackCP, de
   2026-09-20, fecharam o inventário com a mesma autoridade. Ver `D-45`, fechado.
```

- [ ] **Step 7: backlog — débitos**

Acrescente ao fim de `D-45`:

```markdown
**Fechado em <data>.** Os prints do painel fecharam a outra metade — quais nomes existem —, e a
delegação trocada provou o resto: os dois nomes inventados não resolvem mais em lado nenhum. O
wildcard não atravessou, e o que ele escondia (`pop3`) entrou na cópia antes da troca.
```

Substitua o parágrafo final de `D-47` por:

```markdown
**Encolhido em <data>.** Delegação trocada, e-mail provado e certificado `ISSUED`. O que resta é
uma linha: **o HTTPS não é servido**. Nada apresenta o certificado a um navegador enquanto a
distribuição não tiver `Aliases` e `ViewerCertificate`, e o ARN precisa atravessar de `us-east-1`
para `sa-east-1` como parâmetro, porque CloudFormation não importa valor entre regiões.
A EAP continua **não** marcada no Notion.
**Gatilho:** `B5`.
```

E acrescente, depois de `D-48`:

```markdown
- **D-49 · a zona não publica `CAA`** — sem `CAA`, qualquer autoridade certificadora do mundo pode
  emitir certificado para `lotusotec.cl`. Restringir a emissão a `amazon.com` é o padrão, e não
  pôde entrar neste bloco por uma razão de ordem: `CAA` só se publica **depois** do primeiro
  `ISSUED`, e um `CAA` errado bloqueia a renovação do próprio certificado — que é automática, anual
  e silenciosa, então a falha apareceria como site fora do ar treze meses depois, sem ninguém ter
  tocado em nada. Merece bloco com prova própria: publicar, medir, e só então confiar.
  **Gatilho:** certificado `ISSUED` em <data>, neste bloco.
```

- [ ] **Step 7b: backlog — o que atravessou por fidelidade**

Acrescente a `D-46`, antes do `**Gatilho:**`:

```markdown
O `include:spf.stackmail.com` atravessou a troca de nameservers intacto, por fidelidade e não por
decisão de mantê-lo: mexer nele durante a delegação misturaria duas mudanças numa janela em que
metade do mundo lê cada lado.
```

E a `B7`, na lista de escopo:

```markdown
- **Decidir sobre os quatro nomes do StackMail** — `pop3`, `imap`, `smtp` e `autodiscover` apontam
  para o provedor de e-mail antigo. Foram copiados para o Route 53 por fidelidade, porque a regra
  do bloco da delegação era não mudar resposta nenhuma. Quem ainda os usa, se é que alguém usa, só
  aparece desligando-os — e é por isso que a decisão anda junto com o desligamento do WordPress.
```

- [ ] **Step 8: estado**

Em `docs/superpowers/state.md`, no front matter:

```yaml
workflow_state: ready_for_review
next_owner: codex
next_action: review_active_work_item
```

mais `updated_at` com a hora real e `state_basis_commit` com o hash do commit da Task 5.

- [ ] **Step 9: gate de qualidade**

```bash
pnpm check
```

Esperado: verde, incluindo `agent:check` — que reprova se `docs/superpowers/backlog.md` contiver a string `workflow_state`, e se `executor` e `reviewer` forem iguais.

- [ ] **Step 10: commit**

```bash
git add docs/infra/runbook-aws.md docs/superpowers/backlog.md docs/superpowers/state.md
git commit -m "$(cat <<'EOF'
docs(7.2.1): atualizar runbook e backlog

Runbook: 6.3 ganha o modo --pos-delegacao; 6.4 e 6.6 passam a dizer que
foram feitos, com a data e a evidencia; 6.5 troca o dig, que nao existe
nesta maquina, pela leitura por DoH; 6.7 corrige o inventario, que agora
tem 14 linhas e nove nomes.

Backlog: B1 ganha a segunda rodada; a pendencia do export BIND fecha,
substituida pelos prints do painel; D-45 fecha; D-47 encolhe para "HTTPS
nao servido" e aponta para B5; entra D-49, a CAA que so pode ser
publicada depois do primeiro ISSUED.

Estado vai para a fase de review, com o Codex como revisor.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## O que não é do executor, em nenhuma hipótese

- **Trocar os nameservers.** É o João, no painel dele, com print antes e depois (Task 4, Step 3).
- **Declarar que o e-mail funciona.** Só a mensagem recebida prova isso, e quem a recebe é o João (Task 4, Step 5). `dig`, DoH e `describe-certificate` não substituem essa linha.
- **Emitir o certificado sem o "pode ir".** Task 5, Step 1.
- **Apagar a zona na StackDNS, ou pedir a remoção dela ao suporte.** Ela é o rollback até `B7`.
- **Tocar `infra/lotus-site.yaml`.** `Aliases` e `ViewerCertificate` são `B5`.
- **Publicar `CAA`.** `D-49`, bloco próprio, depois do primeiro `ISSUED`.
- **Marcar `7.2.1` no Notion.** Lei 6, e o aceite não fecha (spec §3 D7).
- **`git push` ou `gh pr create`.** Só `/fechar-site`.
- **Editar um relatório gerado para deixar o `format:check` verde.** Quem formata o relatório é o script; editar evidência à mão é pior que gate vermelho.

## Prova de aceite

O critério da EAP no Notion — "domínio de staging/teste resolve corretamente, HTTPS está válido e canonical/redirects não criam loop" — **não fecha nesta rodada**. O certificado existe e está `ISSUED`, mas nada o apresenta a um navegador: isso exige o alias da distribuição, que é `B5`. `D-47` encolhe em vez de fechar.

O que este bloco prova, e onde:

| Afirmação                                     | Como se prova                                                                       | Onde fica                                  |
| --------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------ |
| o inventário está fechado                     | a transcrição do painel, conferida pelo João contra os prints                       | `zona-dns-lotusotec.md`, commit 2          |
| `pop3` e os dois `AAAA` entraram              | cinco testes reprovando antes e passando depois, e o `list-resource-record-sets`    | corpo do commit 1                          |
| a conferência sabe rodar dos dois lados       | modo padrão saindo 0 e `--pos-delegacao` saindo 1 na linha `NS`, antes da troca     | corpo do commit 3                          |
| os dois lados respondiam igual antes da troca | `pnpm infra:conferir-zona` sem divergência inesperada                               | `conferencia-zona-<data>.md`, commit 4     |
| a delegação mudou                             | `NS lotusotec.cl` por DoH devolvendo `ns-*.awsdns-*`                                | `delegacao-<data>.md`, commit 4            |
| os cinco MX do Google sobreviveram            | conferência pós-delegação, linha a linha                                            | `conferencia-zona-<data>-pos-delegacao.md` |
| o wildcard não atravessou                     | nome inventado sem resposta dos dois lados                                          | mesmo relatório                            |
| **o e-mail entrega**                          | **mensagem real recebida numa caixa `@lotusotec.cl`, com `SPF: PASS` no cabeçalho** | `delegacao-<data>.md`, commit 4            |
| o certificado existe e é válido               | `aws acm describe-certificate` com `Status: ISSUED` e os dois nomes                 | corpo do commit 5                          |
| nenhum registro do inventário se perdeu       | conferência pós-delegação cobrindo os nove nomes da zona, incluindo `pop3`          | `conferencia-zona-<data>-pos-delegacao.md` |

Gates genéricos: `pnpm check` verde a cada commit, e verde no HEAD da branch. `pnpm e2e` **não se aplica** — o bloco não toca `src/`, `public/`, `e2e/` nem `index.html`.

Prova que este bloco **não** pode dar, e que fica registrada como tal: **HTTPS servido**. Certificado emitido não é certificado apresentado.

## Handoff de execução

executor: claude
reviewer: codex
paths_autorizados:

- `infra/lotus-dns.yaml`
- `scripts/infra/lib/zona.mjs`
- `scripts/infra/zona.test.mjs`
- `scripts/infra/conferir-zona.mjs`
- `docs/infra/zona-dns-lotusotec.md`
- `docs/infra/runbook-aws.md`
- `docs/infra/conferencia-zona-*.md`
- `docs/infra/delegacao-2026-09-*.md`
- `docs/superpowers/state.md`
- `docs/superpowers/backlog.md`
- `docs/superpowers/specs/2026-09-20-7.2.1-delegacao-certificado-design.md`
- `docs/superpowers/plans/2026-09-20-7.2.1-delegacao-certificado.md`

`scripts/infra/zona.test.mjs` e `scripts/infra/conferir-zona.mjs` vêm da emenda **E1** e são o único acréscimo em relação à spec §9. O nome dos relatórios segue a data real da corrida; se a delegação escorregar de dia, o path muda junto e isso não é desvio.

Se o Codex CLI não iniciar — `D-32` registra `Error: failed to initialize in-process app-server client: Read-only file system (os error 30)` —, o desvio vira `reviewer_exception` declarada com motivo, data e quem autorizou, mais um débito no backlog. Nunca silêncio.

## Errata de execução (2026-09-26)

- **Direção do SPF no gate G2** — o roteiro pedia ler o `SPF` na mensagem recebida; a correção e a prova certa estão em `docs/infra/delegacao-2026-09-26.md` ("Correção do roteiro do plano").
- **Entrada de e-mail dispensada por João, às 2026-09-26T06:34Z** — ele seguiu o bloco sem esperar a confirmação. Registrado em `docs/superpowers/backlog.md`, PENDÊNCIAS item 6.
- **Sem print da tela depois de salvar** — o whois do NIC Chile confirmou o valor gravado pelo lado do registro. Ver `docs/infra/delegacao-2026-09-26.md`, secção "Depois".
- **O relatório pré-troca foi renomeado para a data UTC** — o script titula em UTC; o commit `66eac00` corrigiu o nome, com o conteúdo intacto.
- **Validade do certificado: 198 dias, não os ~13 meses esperados** — teto do ACM desde 2026-02-18. Ver `docs/infra/zona-dns-lotusotec.md`, secção "Certificado".
- **`RenewalEligibility: INELIGIBLE`, não `ELIGIBLE`** — o certificado só renova sozinho em uso, e `B5` ainda não o ligou à distribuição. Ver `docs/infra/zona-dns-lotusotec.md`, secção "Certificado".
- **O CNAME de validação fica na zona** — é criado pelo CloudFormation, não pelo ACM, e não é removido depois; é por ele que o ACM revalida a cada renovação. Ver `infra/lotus-dns.yaml`, comentário do recurso `Certificado`, e `docs/infra/zona-dns-lotusotec.md`, secção "Certificado".
- **Risco de renovação do certificado do WordPress não previsto** — a delegação corta o desafio DNS do wildcard Let's Encrypt que a 20i mantém para `lotusotec.cl`, `www` e `sistema`. Débito novo `D-51`, em `docs/superpowers/backlog.md`.
- **`delete-stack` de `lotus-dns` esvaziaria a zona viva** — `Registros` não tem `DeletionPolicy`, e o stack não tem termination protection. Débito novo `D-52`, em `docs/superpowers/backlog.md` e `docs/infra/runbook-aws.md`, §6.9.
- **A `CAA` de `D-49` precisaria das duas CAs, não só `amazon.com`** — o certificado do WordPress usa `letsencrypt.org`. `D-49` reescrito em `docs/superpowers/backlog.md`.
