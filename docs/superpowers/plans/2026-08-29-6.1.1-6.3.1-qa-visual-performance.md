# QA visual e performance — plano de implementação (`6.1.1-6.3.1`)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Provar a paridade visual e funcional do clone contra `https://lotusotec.cl/`, estender a suíte a três motores de renderização, medir e otimizar performance sobre build de produção, e assinar a homologação com os débitos abertos declarados.

**Architecture:** Scripts Node em `scripts/qa/` capturam referência e clone nos mesmos viewports e geram evidência comparável versionada em `docs/qa/paridade/2026-08-29/`. O Playwright ganha projetos por motor e um projeto que roda contra o build de produção. O Lighthouse entra como devDependency e roda contra `vite preview` por script próprio, com relatório versionado. A otimização age só sobre o gargalo medido, guardada por `toHaveScreenshot` do clone contra si mesmo.

**Tech Stack:** Node 24.19.0, pnpm 11, Vite 8, React 19, TypeScript 6, Playwright 1.62, Vitest 4, Lighthouse (novo).

## Global Constraints

- Spec: [`docs/superpowers/specs/2026-08-29-6.1.1-6.3.1-qa-visual-performance-design.md`](../specs/2026-08-29-6.1.1-6.3.1-qa-visual-performance-design.md). Packet: [`docs/superpowers/context-packets/6.1.1-6.3.1.md`](../context-packets/6.1.1-6.3.1.md).
- Branch `feat/6-1-1-6-3-1-qa-visual-performance`, base `9543ad0`. `main` não recebe commit.
- Um commit por EAP, oito no total, Conventional Commits com a EAP no escopo: `test(6.1.1): …`.
- Gerenciador é **pnpm**. Nunca `npm`, nunca `yarn`.
- Node fixado em 24.19.0 pelo `.nvmrc`; `engineStrict: true` transforma versão errada em `ERR_PNPM_UNSUPPORTED_ENGINE`. Se `pnpm` recusar a versão, rode `. "$HOME/.nvm/nvm.sh" && nvm use` antes.
- Única dependência nova autorizada em todo o bloco: `lighthouse`, em `devDependencies`, na Task 5 (D3 da spec). Qualquer outra é violação da Lei 7.
- Lei 1 — clone antes de redesign: nenhuma correção visual pode mudar o que a matriz de [`docs/inventario/README.md`](../../inventario/README.md) já registra como `fiel` ou como `divergência intencional` aprovada.
- Script novo em `scripts/**/*.mjs` já é coberto por `tsconfig.node.json` (`include`) e por `vitest.config.ts` (`scripts/**/*.test.mjs`). Arquivo `.ts` novo de build precisaria entrar no `include` à mão.
- Todo script que escreve JSON usa `JSON.stringify(valor, null, 2)` e é seguido de `pnpm format`, senão `pnpm format:check` fica vermelho.
- Gates: `pnpm check` (`agent:check` + `format:check` + `lint` + `typecheck` + `test` + `build`) e `pnpm e2e`. O aceite específico da EAP é provado antes dos gates genéricos.
- Não tocar `.claude/**`, `.agents/**`, `e2e/a11y-exceptions.ts`, nem escrever em Notion, Drive ou Figma.
  (Emenda E1 da spec, registrada na review de 2026-08-29: o seletor de uma exceção pode acompanhar a
  renomeação da classe que ele cita; nada mais em `e2e/a11y-exceptions.ts` muda.)
- A pasta de rodada é `docs/qa/paridade/2026-08-29/`. Regerar cria pasta nova datada; nunca sobrescreve.

---

### Task 1: Congelar a referência do WordPress (`6.1.1`)

**Files:**

- Create: `scripts/qa/lib/paridade.mjs`
- Create: `scripts/qa/lib/paridade.test.mjs`
- Create: `scripts/qa/capture-referencia.mjs`
- Modify: `package.json` (bloco `scripts`)
- Output: `docs/qa/paridade/2026-08-29/referencia/*.png`, `docs/qa/paridade/2026-08-29/referencia/manifest.json`

**Interfaces:**

- Consumes: `VIEWPORTS`, `openPage`, `SITE_URL` de `scripts/inventario/lib/site.mjs`.
- Produces: `STATES` (`{ name, width, height, fullPage, menu }[]`), `sha256File(path) → string`, `writeManifest({ dir, target, url, files }) → object`, `RUN_DIR` (`'docs/qa/paridade/2026-08-29'`).

- [ ] **Step 1: Escrever o teste que falha**

Crie `scripts/qa/lib/paridade.test.mjs`:

```js
import { mkdtempSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { describe, expect, it } from 'vitest'
import { RUN_DIR, STATES, sha256File, writeManifest } from './paridade.mjs'

describe('STATES', () => {
  it('cobre os quatro viewports do inventário mais o menu aberto em 375', () => {
    expect(STATES.map((state) => state.name)).toEqual([
      '375',
      '375-menu',
      '768',
      '1440',
      '1920',
    ])
  })

  it('captura página inteira em todo estado, menos no menu aberto', () => {
    const menu = STATES.find((state) => state.name === '375-menu')
    expect(menu).toMatchObject({
      width: 375,
      height: 812,
      fullPage: false,
      menu: true,
    })
    for (const state of STATES.filter(
      (candidate) => candidate.name !== '375-menu',
    )) {
      expect(state).toMatchObject({ fullPage: true, menu: false })
    }
  })

  it('aponta a rodada para uma pasta datada, não para um temporário', () => {
    expect(RUN_DIR).toBe('docs/qa/paridade/2026-08-29')
  })
})

describe('sha256File', () => {
  it('devolve o digest hexadecimal do conteúdo do arquivo', () => {
    const dir = mkdtempSync(join(tmpdir(), 'paridade-'))
    const file = join(dir, 'a.txt')
    writeFileSync(file, 'lotus')
    // Digest conhecido de 'lotus', conferido com `printf lotus | sha256sum`.
    expect(sha256File(file)).toMatch(/^[0-9a-f]{64}$/)
    expect(sha256File(file)).toBe(sha256File(file))
  })
})

describe('writeManifest', () => {
  it('grava alvo, URL, data e uma entrada por arquivo, ordenada por nome', () => {
    const dir = mkdtempSync(join(tmpdir(), 'paridade-'))
    writeFileSync(join(dir, 'home-768.png'), 'b')
    writeFileSync(join(dir, 'home-375.png'), 'a')

    const manifest = writeManifest({
      dir,
      target: 'referencia',
      url: 'https://lotusotec.cl/',
      files: ['home-768.png', 'home-375.png'],
    })

    expect(manifest.target).toBe('referencia')
    expect(manifest.url).toBe('https://lotusotec.cl/')
    // Sem acesso indexado: `noUncheckedIndexedAccess` está ligado e vale
    // nestes .mjs, porque `tsconfig.node.json` roda com `checkJs`.
    expect(manifest.files.map((entry) => entry.name)).toEqual([
      'home-375.png',
      'home-768.png',
    ])
    expect(manifest.files).toContainEqual(
      expect.objectContaining({ name: 'home-375.png', bytes: 1 }),
    )
    for (const entry of manifest.files) {
      expect(entry.sha256).toMatch(/^[0-9a-f]{64}$/)
    }
    expect(manifest.capturedAt).toMatch(/^\d{4}-\d{2}-\d{2}T/)
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `pnpm test scripts/qa/lib/paridade.test.mjs`
Expected: FAIL com `Failed to load url ./paridade.mjs`.

- [ ] **Step 3: Escrever a lib**

Crie `scripts/qa/lib/paridade.mjs`:

```js
// Ferramenta de QA: roda em Node, nunca entra no bundle da aplicação.
import { createHash } from 'node:crypto'
import { readFileSync, statSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'

/** Pasta datada da rodada. Regerar cria pasta nova, não sobrescreve esta. */
export const RUN_DIR = 'docs/qa/paridade/2026-08-29'

/** @typedef {{ name: string, width: number, height: number, fullPage: boolean, menu: boolean }} State */

/**
 * Os quatro viewports do inventário mais o estado de menu aberto em 375, que
 * só existe por clique e nunca apareceria numa captura de página inteira.
 * @type {State[]}
 */
export const STATES = [
  { name: '375', width: 375, height: 812, fullPage: true, menu: false },
  { name: '375-menu', width: 375, height: 812, fullPage: false, menu: true },
  { name: '768', width: 768, height: 1024, fullPage: true, menu: false },
  { name: '1440', width: 1440, height: 900, fullPage: true, menu: false },
  { name: '1920', width: 1920, height: 1080, fullPage: true, menu: false },
]

/**
 * @param {string} path
 * @returns {string}
 */
export function sha256File(path) {
  return createHash('sha256').update(readFileSync(path)).digest('hex')
}

/**
 * @param {{ dir: string, target: string, url: string, files: string[] }} input
 * @returns {{ target: string, url: string, capturedAt: string, files: { name: string, bytes: number, sha256: string }[] }}
 */
export function writeManifest({ dir, target, url, files }) {
  const manifest = {
    target,
    url,
    capturedAt: new Date().toISOString(),
    files: [...files]
      .sort((a, b) => a.localeCompare(b))
      .map((name) => ({
        name,
        bytes: statSync(join(dir, name)).size,
        sha256: sha256File(join(dir, name)),
      })),
  }
  writeFileSync(
    join(dir, 'manifest.json'),
    `${JSON.stringify(manifest, null, 2)}\n`,
  )
  return manifest
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `pnpm test scripts/qa/lib/paridade.test.mjs`
Expected: PASS, 5 testes.

- [ ] **Step 5: Escrever o capturador da referência**

Crie `scripts/qa/capture-referencia.mjs`:

```js
// Congela a referência visual do WordPress imediatamente antes da comparação.
import { mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { SITE_URL, openPage } from '../inventario/lib/site.mjs'
import { RUN_DIR, STATES, writeManifest } from './lib/paridade.mjs'

const dir = join(RUN_DIR, 'referencia')
mkdirSync(dir, { recursive: true })

const written = []

for (const state of STATES) {
  // `openPage` abre SITE_URL, espera `load` e rola a página inteira: é o
  // mesmo pré-processo do baseline da Sprint 1, então as duas rodadas são
  // comparáveis entre si.
  const { browser, page } = await openPage(state)

  if (state.menu) {
    const toggle = page
      .locator('.mobile_menu_bar, .elementor-menu-toggle')
      .first()
    if (!(await toggle.count())) {
      await browser.close()
      throw new Error('toggle do menu mobile não encontrado na referência')
    }
    await toggle.click()
    await page.waitForTimeout(800)
  }

  const name = `home-${state.name}.png`
  await page.screenshot({ path: join(dir, name), fullPage: state.fullPage })
  written.push(name)
  console.log(name)

  await browser.close()
}

const manifest = writeManifest({
  dir,
  target: 'referencia',
  url: SITE_URL,
  files: written,
})
console.log(`referencia: ${manifest.files.length} arquivos em ${dir}`)
```

- [ ] **Step 6: Registrar o script no `package.json`**

Em `package.json`, dentro de `scripts`, logo depois de `"inventario:baseline"`, acrescente:

```json
    "qa:referencia": "node scripts/qa/capture-referencia.mjs",
```

- [ ] **Step 7: Capturar de verdade**

Run: `pnpm qa:referencia`
Expected: cinco linhas (`home-375.png`, `home-375-menu.png`, `home-768.png`, `home-1440.png`, `home-1920.png`) e a linha final `referencia: 5 arquivos em docs/qa/paridade/2026-08-29/referencia`.

Se falhar com `Could not resolve host: lotusotec.cl`, o processo está sem rede — não simule a captura, pare e reporte. A delegação ao Codex falhou exatamente assim, e isso está registrado no packet.

- [ ] **Step 8: Conferir a evidência**

Run: `ls -la docs/qa/paridade/2026-08-29/referencia/ && cat docs/qa/paridade/2026-08-29/referencia/manifest.json`
Expected: cinco PNG mais `manifest.json`, com `capturedAt` de hoje e um `sha256` de 64 hexadecimais por arquivo.

- [ ] **Step 9: Formatar e rodar os gates**

Run: `pnpm format && pnpm check`
Expected: `All matched files use Prettier code style!` e todos os gates verdes.

- [ ] **Step 10: Commit**

```bash
git add scripts/qa package.json docs/qa/paridade/2026-08-29/referencia
git commit -m "test(6.1.1): capturar e versionar a referência datada do WordPress"
```

---

### Task 2: Capturar o clone e montar o contact-sheet (`6.1.2`)

**Files:**

- Modify: `scripts/qa/lib/paridade.mjs` (acrescenta `contactSheetHtml`)
- Modify: `scripts/qa/lib/paridade.test.mjs` (acrescenta o bloco de teste)
- Create: `scripts/qa/capture-clone.mjs`
- Create: `scripts/qa/contact-sheet.mjs`
- Modify: `package.json` (bloco `scripts`)
- Output: `docs/qa/paridade/2026-08-29/clone/*.png`, `docs/qa/paridade/2026-08-29/clone/manifest.json`, `docs/qa/paridade/2026-08-29/contact-sheet.html`

**Interfaces:**

- Consumes: `RUN_DIR`, `STATES`, `writeManifest` da Task 1.
- Produces: `contactSheetHtml(referencia, clone) → string`, onde os dois argumentos são o objeto devolvido por `writeManifest`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescente ao fim de `scripts/qa/lib/paridade.test.mjs`:

```js
describe('contactSheetHtml', () => {
  const referencia = {
    target: 'referencia',
    url: 'https://lotusotec.cl/',
    capturedAt: '2026-08-29T10:00:00.000Z',
    files: [
      { name: 'home-375.png', bytes: 1, sha256: 'a'.repeat(64) },
      { name: 'home-1440.png', bytes: 2, sha256: 'b'.repeat(64) },
    ],
  }
  const clone = {
    target: 'clone',
    url: 'http://localhost:5184/',
    capturedAt: '2026-08-29T10:05:00.000Z',
    files: [
      { name: 'home-375.png', bytes: 3, sha256: 'c'.repeat(64) },
      { name: 'home-1440.png', bytes: 4, sha256: 'd'.repeat(64) },
    ],
  }

  it('emite um par por estado, referência à esquerda e clone à direita', () => {
    const html = contactSheetHtml(referencia, clone)
    expect(html).toContain('referencia/home-375.png')
    expect(html).toContain('clone/home-375.png')
    expect(html.indexOf('referencia/home-375.png')).toBeLessThan(
      html.indexOf('clone/home-375.png'),
    )
  })

  it('ordena os pares pelo nome do estado e carimba as duas datas', () => {
    const html = contactSheetHtml(referencia, clone)
    expect(html.indexOf('home-1440.png')).toBeLessThan(
      html.indexOf('home-375.png'),
    )
    expect(html).toContain('2026-08-29T10:00:00.000Z')
    expect(html).toContain('2026-08-29T10:05:00.000Z')
  })

  it('reprova estado sem par, em vez de esconder a falta', () => {
    expect(() =>
      contactSheetHtml(referencia, { ...clone, files: clone.files.slice(1) }),
    ).toThrow('sem par no clone: home-1440.png')
  })
})
```

Acrescente `contactSheetHtml` ao `import` do topo do arquivo:

```js
import {
  RUN_DIR,
  STATES,
  contactSheetHtml,
  sha256File,
  writeManifest,
} from './paridade.mjs'
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `pnpm test scripts/qa/lib/paridade.test.mjs`
Expected: FAIL com `contactSheetHtml is not a function`.

- [ ] **Step 3: Implementar `contactSheetHtml`**

Acrescente ao fim de `scripts/qa/lib/paridade.mjs`:

```js
/**
 * Página estática com os pares lado a lado. Não decide paridade: só põe as
 * duas capturas do mesmo estado uma ao lado da outra, com peso e digest.
 * @param {{ target: string, url: string, capturedAt: string, files: { name: string, bytes: number, sha256: string }[] }} referencia
 * @param {{ target: string, url: string, capturedAt: string, files: { name: string, bytes: number, sha256: string }[] }} clone
 * @returns {string}
 */
export function contactSheetHtml(referencia, clone) {
  const pares = [...referencia.files]
    .sort((a, b) => a.name.localeCompare(b.name))
    .map((esquerda) => {
      const direita = clone.files.find((file) => file.name === esquerda.name)
      if (!direita) throw new Error(`sem par no clone: ${esquerda.name}`)
      return { esquerda, direita }
    })

  const linhas = pares
    .map(
      ({ esquerda, direita }) => `    <section id="${esquerda.name}">
      <h2>${esquerda.name}</h2>
      <div class="par">
        <figure>
          <img src="referencia/${esquerda.name}" alt="Referência ${esquerda.name}" loading="lazy" />
          <figcaption>referência · ${esquerda.bytes} B · ${esquerda.sha256.slice(0, 16)}…</figcaption>
        </figure>
        <figure>
          <img src="clone/${direita.name}" alt="Clone ${direita.name}" loading="lazy" />
          <figcaption>clone · ${direita.bytes} B · ${direita.sha256.slice(0, 16)}…</figcaption>
        </figure>
      </div>
    </section>`,
    )
    .join('\n')

  return `<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <title>Paridade — referência × clone</title>
    <style>
      body { font-family: system-ui, sans-serif; margin: 2rem; }
      .par { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; align-items: start; }
      img { width: 100%; border: 1px solid #ccc; }
      figcaption { font-size: 0.8rem; color: #555; }
    </style>
  </head>
  <body>
    <h1>Paridade — referência × clone</h1>
    <p>referência: ${referencia.url} · ${referencia.capturedAt}</p>
    <p>clone: ${clone.url} · ${clone.capturedAt}</p>
${linhas}
  </body>
</html>
`
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `pnpm test scripts/qa/lib/paridade.test.mjs`
Expected: PASS, 8 testes.

- [ ] **Step 5: Escrever o capturador do clone**

Crie `scripts/qa/capture-clone.mjs`:

```js
// Captura o clone nas mesmas condições da referência: mesmos viewports, mesmo
// scroll de página inteira, mesmo estado de menu aberto em 375.
import { mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { chromium } from '@playwright/test'
import { RUN_DIR, STATES, writeManifest } from './lib/paridade.mjs'

const CLONE_URL = process.env.CLONE_URL ?? 'http://localhost:5184/'
const dir = join(RUN_DIR, 'clone')
mkdirSync(dir, { recursive: true })

const written = []

for (const state of STATES) {
  const browser = await chromium.launch()
  const page = await browser.newPage({
    viewport: { width: state.width, height: state.height },
  })
  await page.goto(CLONE_URL, { waitUntil: 'load', timeout: 60_000 })
  // Mesmo passo de scroll da referência: o clone não tem lazy-load do Divi,
  // mas rolar e voltar mantém as duas capturas sob o mesmo estado de página.
  await page.evaluate(async () => {
    const step = window.innerHeight
    for (let y = 0; y < document.body.scrollHeight; y += step) {
      window.scrollTo(0, y)
      await new Promise((resolve) => setTimeout(resolve, 250))
    }
    window.scrollTo(0, 0)
  })
  await page.waitForTimeout(1000)

  if (state.menu) {
    await page.getByRole('button', { name: 'Abrir menú' }).click()
    await page.waitForTimeout(300)
  }

  const name = `home-${state.name}.png`
  await page.screenshot({ path: join(dir, name), fullPage: state.fullPage })
  written.push(name)
  console.log(name)

  await browser.close()
}

const manifest = writeManifest({
  dir,
  target: 'clone',
  url: CLONE_URL,
  files: written,
})
console.log(`clone: ${manifest.files.length} arquivos em ${dir}`)
```

- [ ] **Step 6: Escrever o gerador do contact-sheet**

Crie `scripts/qa/contact-sheet.mjs`:

```js
import { readFileSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { RUN_DIR, contactSheetHtml } from './lib/paridade.mjs'

/** @param {string} target */
const manifest = (target) =>
  JSON.parse(readFileSync(join(RUN_DIR, target, 'manifest.json'), 'utf8'))

const html = contactSheetHtml(manifest('referencia'), manifest('clone'))
const out = join(RUN_DIR, 'contact-sheet.html')
writeFileSync(out, html)
console.log(out)
```

- [ ] **Step 7: Registrar os scripts no `package.json`**

Logo depois de `"qa:referencia"`, acrescente:

```json
    "qa:clone": "node scripts/qa/capture-clone.mjs",
    "qa:contact-sheet": "node scripts/qa/contact-sheet.mjs",
```

- [ ] **Step 8: Subir o build de produção e capturar o clone**

Em um terminal:

```bash
pnpm build
VITE_WEB3FORMS_ACCESS_KEY=qa-fake-access-key pnpm preview --port 5184 --strictPort
```

Em outro:

Run: `pnpm qa:clone && pnpm qa:contact-sheet`
Expected: cinco PNG em `docs/qa/paridade/2026-08-29/clone/`, `clone: 5 arquivos …` e o caminho do `contact-sheet.html`.

A chave falsa é a mesma condição do E2E: o adapter precisa estar montado, e nenhum envio real sai porque nada é submetido nesta captura.

- [ ] **Step 9: Abrir o contact-sheet e conferir os cinco pares**

Run: `ls -la docs/qa/paridade/2026-08-29/ && grep -c '<section id=' docs/qa/paridade/2026-08-29/contact-sheet.html`
Expected: `5`.

- [ ] **Step 10: Formatar e rodar os gates**

Run: `pnpm format && pnpm check`
Expected: tudo verde.

- [ ] **Step 11: Commit**

```bash
git add scripts/qa package.json docs/qa/paridade/2026-08-29
git commit -m "test(6.1.2): capturar o clone nas mesmas condições e gerar o contact-sheet"
```

---

### Task 3: Classificar e corrigir as divergências (`6.1.3`)

**Files:**

- Create: `docs/qa/paridade/2026-08-29/classificacao.md`
- Modify: `docs/inventario/README.md` (seção `## Matriz de paridade`)
- Modify: arquivos de `src/` só onde houver divergência **não intencional** comprovada
- Modify: `docs/qa/paridade/2026-08-29/clone/*` só se houver recaptura após correção

**Interfaces:**

- Consumes: `contact-sheet.html` e os dois `manifest.json` da Task 2.
- Produces: `classificacao.md` com uma linha por diferença observada e a matriz de `README.md` sem `pendente decisão` aberta para este bloco.

- [ ] **Step 1: Percorrer os cinco pares e listar toda diferença observada**

Abra `docs/qa/paridade/2026-08-29/contact-sheet.html` no navegador. Para cada um dos cinco estados, anote toda diferença visível entre referência e clone. Não classifique ainda; só liste.

- [ ] **Step 2: Escrever a classificação**

Crie `docs/qa/paridade/2026-08-29/classificacao.md` com esta estrutura, uma linha por diferença observada no Step 1:

```markdown
# Classificação de diferenças — rodada 2026-08-29

> Evidência: `contact-sheet.html` desta pasta. Categorias: `layout`, `tipografia`, `spacing`,
> `asset`, `responsive`. Decisão: `fiel` (já reproduzido), `divergência intencional`
> (registrada na matriz) ou `corrigir` (não intencional, vira código nesta EAP).

| #   | estado | categoria | diferença observada | decisão | referência na matriz |
| --- | ------ | --------- | ------------------- | ------- | -------------------- |
| 1   | 1440   | asset     | …                   | …       | …                    |
```

Regra de classificação, sem exceção: diferença que a matriz de `docs/inventario/README.md` já registra como `divergência intencional` recebe `divergência intencional` e o ponteiro para a linha da matriz. Diferença nova que **não** está na matriz é `corrigir`, a menos que exista razão escrita — e razão nova exige ratificação de João (D2 da spec).

- [ ] **Step 3: Corrigir cada linha marcada `corrigir`**

Para cada uma, na ordem da tabela: escreva ou ajuste o teste que prova a correção (Vitest se for estrutura/conteúdo, Playwright se precisar de navegador), rode e veja falhar, corrija o componente em `src/`, rode e veja passar.

Se a tabela não tiver nenhuma linha `corrigir`, pule para o Step 5 e registre isso em `classificacao.md`: `Nenhuma divergência não intencional encontrada nesta rodada.`

- [ ] **Step 4: Recapturar o clone e refazer o contact-sheet**

Só se o Step 3 mudou código.

Run: `pnpm build && (VITE_WEB3FORMS_ACCESS_KEY=qa-fake-access-key pnpm preview --port 5184 --strictPort &) && sleep 5 && pnpm qa:clone && pnpm qa:contact-sheet`
Expected: cinco PNG novos e `manifest.json` com `sha256` diferente para os arquivos afetados.

- [ ] **Step 5: Atualizar a matriz de paridade**

Em `docs/inventario/README.md`, seção `## Matriz de paridade`: acrescente uma linha para cada item que este bloco decidiu e que ainda não estava lá, com a justificativa apontando para `docs/qa/paridade/2026-08-29/classificacao.md`. Nenhum item deste bloco pode ficar como `pendente decisão`.

- [ ] **Step 6: Apresentar a lista a João e esperar a ratificação**

Mostre a tabela de `classificacao.md` no chat e espere o "ok". D2 da spec: a decisão é de João, sobre o resumo.

- [ ] **Step 7: Rodar os gates**

Run: `pnpm format && pnpm check && pnpm e2e`
Expected: tudo verde.

- [ ] **Step 8: Commit**

```bash
git add docs/qa/paridade/2026-08-29 docs/inventario/README.md src
git commit -m "fix(6.1.3): corrigir divergências não intencionais e fechar a matriz"
```

Se nada em `src/` mudou, o mesmo comando funciona: `git add src` sobre árvore limpa é no-op, e o commit fica só com a evidência e a matriz. Nesse caso troque o tipo para `docs(6.1.3)`.

---

### Task 4: Estender a matriz a três motores (`6.1.4`)

**Files:**

- Modify: `playwright.config.ts`
- Modify: `.github/workflows/ci.yml:38-45`
- Test: `e2e/home.spec.ts`, `e2e/menu.spec.ts`, `e2e/contacto.spec.ts` (rodam nos motores novos, sem edição)

**Interfaces:**

- Consumes: nada das tasks anteriores.
- Produces: projetos `chromium`, `firefox`, `webkit`, `mobile-webkit` no Playwright. A Task 7 acrescenta o projeto `producao` a essa mesma lista.

- [ ] **Step 1: Instalar os motores que faltam**

Run: `pnpm exec playwright install --with-deps firefox webkit`
Expected: download concluído dos dois motores.

- [ ] **Step 2: Reescrever o bloco `projects` do `playwright.config.ts`**

Em `playwright.config.ts`, logo abaixo da linha `const baseURL = …`, acrescente:

```ts
// Fluxo principal do site. a11y e seo ficam fora: as exceções nominais de
// `e2e/a11y-exceptions.ts` foram medidas em Chromium (D-21), e replicá-las em
// três motores multiplicaria exceção sem ganho de sinal.
const FLUXO_PRINCIPAL = [
  '**/home.spec.ts',
  '**/menu.spec.ts',
  '**/contacto.spec.ts',
]
```

Substitua o array `projects` inteiro por:

```ts
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
      testMatch: FLUXO_PRINCIPAL,
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
      testMatch: FLUXO_PRINCIPAL,
    },
    {
      name: 'mobile-webkit',
      use: { ...devices['iPhone 13'] },
      testMatch: FLUXO_PRINCIPAL,
    },
  ],
```

- [ ] **Step 3: Rodar a suíte inteira nos quatro projetos**

Run: `pnpm e2e`
Expected: PASS. A contagem sobe de 7 arquivos em 1 projeto para 7 em Chromium mais 3 em cada um dos outros três projetos.

Falha em Firefox, WebKit ou mobile é achado de paridade, não licença para reduzir a matriz: corrija o clone ou registre a diferença na matriz de `docs/inventario/README.md` com justificativa, e só então siga.

- [ ] **Step 4: Ensinar o CI a instalar os três motores**

Em `.github/workflows/ci.yml`, troque o passo `Instalar Chromium` por:

```yaml
- name: Instalar navegadores
  run: pnpm exec playwright install --with-deps chromium firefox webkit
```

E renomeie o passo de cache para refletir o conteúdo:

```yaml
- name: Cache dos navegadores
```

- [ ] **Step 5: Rodar os gates**

Run: `pnpm format && pnpm check && pnpm e2e`
Expected: tudo verde.

- [ ] **Step 6: Commit**

```bash
git add playwright.config.ts .github/workflows/ci.yml
git commit -m "test(6.1.4): estender a matriz a Firefox, WebKit e viewport mobile"
```

---

### Task 5: Medir Lighthouse e Core Web Vitals (`6.2.1`)

**Files:**

- Modify: `package.json` (`devDependencies` e `scripts`)
- Create: `scripts/qa/perf.mjs`
- Create: `scripts/qa/lib/perf.mjs`
- Create: `scripts/qa/lib/perf.test.mjs`
- Output: `docs/qa/performance/2026-08-29/lighthouse.json`, `docs/qa/performance/2026-08-29/resumo.md`

**Interfaces:**

- Consumes: nada das tasks anteriores.
- Produces: `resumoMarkdown(lhr) → string`, consumido pela Task 6 para a segunda medição e pela Task 8 na homologação.

- [ ] **Step 1: Instalar o Lighthouse**

Run: `pnpm add -D lighthouse`
Expected: `lighthouse` em `devDependencies` do `package.json` com versão fixada pelo `pnpm-lock.yaml`.

Se o pnpm recusar por idade de publicação (`minimumReleaseAge`), acrescente o pacote em `minimumReleaseAgeExclude` no `pnpm-workspace.yaml` e repita. Nenhuma outra dependência entra neste bloco.

- [ ] **Step 2: Escrever o teste que falha**

Crie `scripts/qa/lib/perf.test.mjs`:

```js
import { describe, expect, it } from 'vitest'
import { resumoMarkdown } from './perf.mjs'

const lhr = {
  finalDisplayedUrl: 'http://localhost:5184/',
  fetchTime: '2026-08-29T12:00:00.000Z',
  categories: {
    performance: { title: 'Performance', score: 0.91 },
    accessibility: { title: 'Accessibility', score: 1 },
    'best-practices': { title: 'Best Practices', score: 0.96 },
    seo: { title: 'SEO', score: 1 },
  },
  audits: {
    'largest-contentful-paint': {
      title: 'LCP',
      displayValue: '1,2 s',
      numericValue: 1200,
    },
    'cumulative-layout-shift': {
      title: 'CLS',
      displayValue: '0',
      numericValue: 0,
    },
    'largest-contentful-paint-element': {
      title: 'Elemento do LCP',
      details: {
        items: [{ items: [{ node: { snippet: '<h1 id="hero-heading">' } }] }],
      },
    },
  },
}

describe('resumoMarkdown', () => {
  it('registra as quatro categorias com o score medido', () => {
    const md = resumoMarkdown(lhr)
    expect(md).toContain('| Performance | 91 |')
    expect(md).toContain('| Accessibility | 100 |')
    expect(md).toContain('| Best Practices | 96 |')
    expect(md).toContain('| SEO | 100 |')
  })

  it('registra LCP, CLS e o elemento do LCP', () => {
    const md = resumoMarkdown(lhr)
    expect(md).toContain('1,2 s')
    expect(md).toContain('<h1 id="hero-heading">')
  })

  it('diz o que a medição não prova, para o score não virar meta', () => {
    expect(resumoMarkdown(lhr)).toContain('laboratório')
  })

  it('não inventa elemento de LCP quando o audit não traz um', () => {
    const semElemento = {
      ...lhr,
      audits: { ...lhr.audits, 'largest-contentful-paint-element': undefined },
    }
    expect(resumoMarkdown(semElemento)).toContain(
      'elemento do LCP: não reportado',
    )
  })
})
```

- [ ] **Step 3: Rodar o teste e ver falhar**

Run: `pnpm test scripts/qa/lib/perf.test.mjs`
Expected: FAIL com `Failed to load url ./perf.mjs`.

- [ ] **Step 4: Implementar `resumoMarkdown`**

Crie `scripts/qa/lib/perf.mjs`:

```js
// Resumo legível do relatório do Lighthouse. Registra o que foi medido; não
// transforma score isolado em meta (aceite da EAP 6.2.1).

/**
 * @param {any} lhr
 * @returns {string}
 */
export function resumoMarkdown(lhr) {
  const categorias = Object.values(lhr.categories)
    .map(
      (categoria) =>
        `| ${categoria.title} | ${Math.round(categoria.score * 100)} |`,
    )
    .join('\n')

  const lcp = lhr.audits['largest-contentful-paint']
  const cls = lhr.audits['cumulative-layout-shift']
  const elemento =
    lhr.audits['largest-contentful-paint-element']?.details?.items?.[0]
      ?.items?.[0]?.node?.snippet ?? 'não reportado'

  return `# Lighthouse — build de produção

> URL medida: ${lhr.finalDisplayedUrl} · execução: ${lhr.fetchTime}
> Relatório cru: \`lighthouse.json\` nesta pasta.

## Categorias

| categoria | score |
| --------- | ----- |
${categorias}

## Core Web Vitals

- LCP: ${lcp.displayValue} (${Math.round(lcp.numericValue)} ms)
- CLS: ${cls.displayValue}
- elemento do LCP: ${elemento}

## Ressalva

Medição de **laboratório**, numa máquina só, contra \`vite preview\` local. Não é dado de campo e
não descreve o que um visitante real experimenta. Nenhum score aqui é meta: o que orienta a EAP
\`6.2.2\` é a causa apontada, não o número.
`
}
```

- [ ] **Step 5: Rodar o teste e ver passar**

Run: `pnpm test scripts/qa/lib/perf.test.mjs`
Expected: PASS, 4 testes.

- [ ] **Step 6: Escrever o runner**

Crie `scripts/qa/perf.mjs`:

```js
// Roda o Lighthouse contra o build de produção já servido por `vite preview`.
// O Chromium vem do Playwright, com porta de depuração aberta: assim o
// bloco não ganha `chrome-launcher` como segunda dependência.
import { mkdirSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { chromium } from '@playwright/test'
import lighthouse from 'lighthouse'
import { resumoMarkdown } from './lib/perf.mjs'

const URL_ALVO = process.env.CLONE_URL ?? 'http://localhost:5184/'
const OUT_DIR = 'docs/qa/performance/2026-08-29'
const DEBUG_PORT = 9222

mkdirSync(OUT_DIR, { recursive: true })

const browser = await chromium.launch({
  args: [`--remote-debugging-port=${DEBUG_PORT}`],
})

try {
  const resultado = await lighthouse(URL_ALVO, {
    port: DEBUG_PORT,
    output: 'json',
    logLevel: 'error',
  })
  if (!resultado) throw new Error('lighthouse não devolveu resultado')

  writeFileSync(
    join(OUT_DIR, 'lighthouse.json'),
    `${JSON.stringify(resultado.lhr, null, 2)}\n`,
  )
  writeFileSync(join(OUT_DIR, 'resumo.md'), resumoMarkdown(resultado.lhr))
  console.log(join(OUT_DIR, 'resumo.md'))
} finally {
  await browser.close()
}
```

- [ ] **Step 7: Registrar o script no `package.json`**

Logo depois de `"qa:contact-sheet"`, acrescente:

```json
    "qa:perf": "node scripts/qa/perf.mjs",
```

- [ ] **Step 8: Medir de verdade**

Em um terminal:

```bash
pnpm build
VITE_WEB3FORMS_ACCESS_KEY=qa-fake-access-key pnpm preview --port 5184 --strictPort
```

Em outro:

Run: `pnpm qa:perf && cat docs/qa/performance/2026-08-29/resumo.md`
Expected: o caminho do resumo e, no `cat`, as quatro categorias com score, LCP, CLS e o snippet do elemento do LCP.

- [ ] **Step 9: Formatar e rodar os gates**

Run: `pnpm format && pnpm check`
Expected: tudo verde. `lighthouse.json` é grande; conferir que o Prettier o aceitou.

- [ ] **Step 10: Commit**

```bash
git add package.json pnpm-lock.yaml pnpm-workspace.yaml scripts/qa docs/qa/performance
git commit -m "test(6.2.1): medir Lighthouse e Core Web Vitals do build de produção"
```

---

### Task 6: Atacar o gargalo medido (`6.2.2`)

**Files:**

- Create: `scripts/vite/preload-critical.mjs`
- Create: `scripts/vite/preload-critical.test.mjs`
- Modify: `vite.config.ts`
- Create: `e2e/regressao-visual.spec.ts`
- Output: `docs/qa/performance/2026-08-29/resumo-pos-otimizacao.md`

**Interfaces:**

- Consumes: `resumo.md` da Task 5 — a causa medida decide o que este commit faz.
- Produces: `preloadTags(ctx) → tags[]` e o plugin `preloadCritical()` para o `vite.config.ts`; snapshots de `e2e/regressao-visual.spec.ts` usados como guarda.

- [ ] **Step 1: Ler o que a medição apontou**

Run: `cat docs/qa/performance/2026-08-29/resumo.md`

Decida pela linha `elemento do LCP`:

- **snippet da foto do hero ou do `<div>` com `background-image`** → o alvo é o `preload` da imagem, Steps 2–9;
- **snippet do `<h1 id="hero-heading">` ou de outro texto** → o alvo é o `preload` das duas faces de fonte acima da dobra, mesmos Steps 2–9 (o plugin cobre os dois casos; só muda o que entra em `ALVOS`);
- **qualquer outra causa** → implemente a correção dessa causa com o mesmo ciclo TDD dos outros commits, registre no resumo pós-otimização por que o `preload` não era o gargalo, e siga para o Step 10.

Nenhuma troca de formato de imagem, otimizador novo ou dependência entra aqui (D10 da spec).

- [ ] **Step 2: Escrever o teste que falha**

Crie `scripts/vite/preload-critical.test.mjs`:

```js
import { describe, expect, it } from 'vitest'
import { preloadCritical, preloadTags } from './preload-critical.mjs'

const bundle = {
  'assets/index-a1b2c3.css': {
    type: 'asset',
    fileName: 'assets/index-a1b2c3.css',
  },
  'assets/montserrat-700-d4e5f6.woff2': {
    type: 'asset',
    fileName: 'assets/montserrat-700-d4e5f6.woff2',
  },
  'assets/open-sans-500-070809.woff2': {
    type: 'asset',
    fileName: 'assets/open-sans-500-070809.woff2',
  },
  'assets/shutterstock_1444636373-1-scaled-0a1b2c.jpg': {
    type: 'asset',
    fileName: 'assets/shutterstock_1444636373-1-scaled-0a1b2c.jpg',
  },
}

const injetar = (ctx) => preloadTags(ctx)

describe('preloadTags', () => {
  it('preloada as duas faces acima da dobra, com crossorigin', () => {
    const fontes = injetar({ bundle }).filter((tag) => tag.attrs.as === 'font')
    expect(fontes.map((tag) => tag.attrs.href)).toEqual([
      '/assets/montserrat-700-d4e5f6.woff2',
      '/assets/open-sans-500-070809.woff2',
    ])
    for (const fonte of fontes) {
      expect(fonte.attrs.crossorigin).toBe('anonymous')
      expect(fonte.attrs.type).toBe('font/woff2')
    }
  })

  it('preloada a foto do hero só acima do breakpoint desktop', () => {
    const imagens = injetar({ bundle }).filter(
      (tag) => tag.attrs.as === 'image',
    )
    // A foto é `desktop:block`: em 375 e 768 ela nunca pinta, e preloadar
    // sem `media` gastaria banda de mobile por nada.
    expect(imagens).toEqual([
      expect.objectContaining({
        attrs: expect.objectContaining({
          href: '/assets/shutterstock_1444636373-1-scaled-0a1b2c.jpg',
          media: '(min-width: 1000px)',
        }),
      }),
    ])
  })

  it('injeta as tags no head', () => {
    for (const tag of injetar({ bundle })) {
      expect(tag.injectTo).toBe('head-prepend')
      expect(tag.tag).toBe('link')
      expect(tag.attrs.rel).toBe('preload')
    }
  })

  it('não injeta nada no dev server, onde não há bundle', () => {
    expect(injetar({ bundle: undefined })).toEqual([])
  })

  it('não inventa tag para alvo ausente do bundle', () => {
    expect(
      injetar({
        bundle: {
          'assets/index-a1b2c3.css': bundle['assets/index-a1b2c3.css'],
        },
      }),
    ).toEqual([])
  })
})

describe('preloadCritical', () => {
  it('expõe o handler como plugin de build do Vite', () => {
    const plugin = preloadCritical()
    expect(plugin.name).toBe('lotus-preload-critical')
    expect(plugin.transformIndexHtml.order).toBe('post')
  })
})
```

- [ ] **Step 3: Rodar o teste e ver falhar**

Run: `pnpm test scripts/vite/preload-critical.test.mjs`
Expected: FAIL com `Failed to load url ./preload-critical.mjs`.

- [ ] **Step 4: Implementar o plugin**

Crie `scripts/vite/preload-critical.mjs`:

```js
// Plugin de build: injeta `<link rel="preload">` para os assets acima da
// dobra. O Vite fingerprinta o nome dos arquivos, então o href só existe
// depois do bundle — daí `transformIndexHtml` com acesso a `ctx.bundle`.

/** Padrão do arquivo no bundle e atributos do link gerado. */
const ALVOS = [
  {
    padrao: /montserrat-700-[^/]*\.woff2$/,
    attrs: { as: 'font', type: 'font/woff2', crossorigin: 'anonymous' },
  },
  {
    padrao: /open-sans-500-[^/]*\.woff2$/,
    attrs: { as: 'font', type: 'font/woff2', crossorigin: 'anonymous' },
  },
  {
    padrao: /shutterstock_1444636373-1-scaled-[^/]*\.jpg$/,
    // A foto do hero é `desktop:block`: abaixo de 1000px ela nunca pinta.
    attrs: { as: 'image', media: '(min-width: 1000px)' },
  },
]

/**
 * O handler mora fora do plugin para o teste poder chamá-lo sem depender do
 * tipo `Plugin` do Vite, que é uma união e derruba o `checkJs` do repositório.
 * @param {{ bundle?: Record<string, { fileName?: string }> }} ctx
 * @returns {{ tag: string, injectTo: string, attrs: Record<string, string> }[]}
 */
export function preloadTags(ctx) {
  if (!ctx.bundle) return []
  const arquivos = Object.values(ctx.bundle)
    .map((saida) => saida.fileName)
    .filter((fileName) => typeof fileName === 'string')

  return ALVOS.flatMap((alvo) => {
    const encontrado = arquivos.find((fileName) => alvo.padrao.test(fileName))
    if (!encontrado) return []
    return [
      {
        tag: 'link',
        injectTo: 'head-prepend',
        attrs: { rel: 'preload', href: `/${encontrado}`, ...alvo.attrs },
      },
    ]
  })
}

export function preloadCritical() {
  return {
    name: 'lotus-preload-critical',
    transformIndexHtml: {
      order: /** @type {const} */ ('post'),
      /**
       * @param {string} _html
       * @param {{ bundle?: Record<string, { fileName?: string }> }} ctx
       */
      handler: (_html, ctx) => preloadTags(ctx),
    },
  }
}
```

- [ ] **Step 5: Rodar o teste e ver passar**

Run: `pnpm test scripts/vite/preload-critical.test.mjs`
Expected: PASS, 6 testes.

- [ ] **Step 6: Capturar o snapshot de regressão ANTES de ligar o plugin**

Crie `e2e/regressao-visual.spec.ts`:

```ts
import { expect, test } from '@playwright/test'

// Guarda de `6.2.2`: compara o clone com ele mesmo, antes e depois da
// otimização. Não é diff contra o WordPress — as divergências intencionais
// aprovadas na matriz produziriam diferença alta e legítima (D9 da spec).
const VIEWPORTS = [
  { nome: '375', width: 375, height: 812 },
  { nome: '1440', width: 1440, height: 900 },
] as const

for (const viewport of VIEWPORTS) {
  test(`a home não muda um pixel em ${viewport.nome}`, async ({ page }) => {
    await page.setViewportSize({
      width: viewport.width,
      height: viewport.height,
    })
    await page.goto('/')
    await expect(page).toHaveScreenshot(`home-${viewport.nome}.png`, {
      fullPage: true,
      animations: 'disabled',
    })
  })
}
```

Run: `pnpm e2e --project=chromium regressao-visual.spec.ts --update-snapshots`
Expected: dois snapshots criados em `e2e/regressao-visual.spec.ts-snapshots/`.

Este passo tem que rodar **antes** do Step 7. O snapshot é a foto do estado pré-otimização.

- [ ] **Step 7: Ligar o plugin no `vite.config.ts`**

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { preloadCritical } from './scripts/vite/preload-critical.mjs'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss(), preloadCritical()],
})
```

- [ ] **Step 8: Provar que a regressão visual é zero**

Run: `pnpm e2e --project=chromium regressao-visual.spec.ts`
Expected: PASS sem diferença. Falha aqui significa que a otimização mexeu em pixel: reverta em vez de atualizar o snapshot.

- [ ] **Step 9: Medir de novo e escrever o resumo pós-otimização**

Com `pnpm build` refeito e o `preview` no ar:

Run: `pnpm qa:perf && cp docs/qa/performance/2026-08-29/resumo.md docs/qa/performance/2026-08-29/resumo-pos-otimizacao.md && git checkout docs/qa/performance/2026-08-29/resumo.md docs/qa/performance/2026-08-29/lighthouse.json`
Expected: `resumo-pos-otimizacao.md` com a segunda medição, e a primeira preservada intacta para comparação.

Acrescente ao fim de `resumo-pos-otimizacao.md`:

```markdown
## Delta contra a primeira medição

| métrica | antes | depois | mudança |
| ------- | ----- | ------ | ------- |
| LCP     | …     | …      | …       |
| CLS     | …     | …      | …       |

Mudança aplicada: `<link rel="preload">` para as duas faces acima da dobra e para a foto do hero
acima de 1000px, injetado no build por `scripts/vite/preload-critical.mjs`. Nenhum pixel mudou —
`e2e/regressao-visual.spec.ts` verde contra os snapshots pré-otimização.
```

Preencha as células com os números reais das duas medições. Se o delta for nulo ou negativo, reverta o plugin: mudança que não move métrica não fica "por precaução" (D10 da spec).

- [ ] **Step 10: Rodar os gates**

Run: `pnpm format && pnpm check && pnpm e2e`
Expected: tudo verde.

- [ ] **Step 11: Commit**

```bash
git add scripts/vite vite.config.ts e2e/regressao-visual.spec.ts e2e/regressao-visual.spec.ts-snapshots docs/qa/performance
git commit -m "perf(6.2.2): atacar o gargalo medido sem regressão visual"
```

---

### Task 7: Zerar erro de console e request quebrada (`6.2.3`)

**Files:**

- Create: `e2e/producao.spec.ts`
- Modify: `playwright.config.ts` (segundo `webServer` e projeto `producao`)

**Interfaces:**

- Consumes: os projetos criados na Task 4.
- Produces: projeto `producao` do Playwright, apontado para o build de produção em `http://localhost:5184`.

- [ ] **Step 1: Escrever o teste que falha**

Crie `e2e/producao.spec.ts`:

```ts
import { expect, test } from '@playwright/test'

// A EAP cita erro de hydration. A aplicação monta com `createRoot` em
// `src/main.tsx` e não tem SSR: não existe hydration a quebrar nesta
// arquitetura (D8 da spec). O que se prova aqui é console e network reais,
// sobre o build de produção — não sobre o dev server.
test('a jornada principal não produz erro de console nem request quebrada', async ({
  page,
}) => {
  const errosDeConsole: string[] = []
  const requestsQuebradas: string[] = []

  page.on('console', (mensagem) => {
    if (mensagem.type() === 'error') errosDeConsole.push(mensagem.text())
  })
  page.on('pageerror', (erro) => errosDeConsole.push(erro.message))
  page.on('response', (resposta) => {
    if (resposta.status() >= 400) {
      requestsQuebradas.push(`${resposta.status()} ${resposta.url()}`)
    }
  })

  await page.setViewportSize({ width: 1440, height: 900 })
  await page.goto('/')
  await expect(
    page.getByRole('heading', { level: 1, name: 'LOTUS OTEC' }),
  ).toBeVisible()

  for (const label of ['Quienes Somos', 'Cursos', 'Contacto'] as const) {
    await page
      .getByRole('navigation', { name: 'Principal' })
      .getByRole('link', { name: label })
      .click()
  }

  await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight))
  await page.waitForTimeout(1000)

  expect(errosDeConsole).toEqual([])
  expect(requestsQuebradas).toEqual([])
})
```

- [ ] **Step 2: Acrescentar o servidor de produção e o projeto `producao`**

Em `playwright.config.ts`, abaixo de `const baseURL = …`:

```ts
// Porta separada da do dev server: `6.2.3` exige o build de produção, não o
// bundle de desenvolvimento com HMR e sourcemaps.
const PREVIEW_PORT = Number(process.env.E2E_PREVIEW_PORT ?? 5184)
const previewURL = `http://localhost:${PREVIEW_PORT}`
```

Acrescente ao fim do array `projects`:

```ts
    {
      name: 'producao',
      use: { ...devices['Desktop Chrome'], baseURL: previewURL },
      testMatch: ['**/producao.spec.ts'],
    },
```

E acrescente `testIgnore` ao projeto `chromium`, para o spec de produção não rodar contra o dev server:

```ts
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
      testIgnore: ['**/producao.spec.ts'],
    },
```

Troque o `webServer` único por dois:

```ts
  webServer: [
    {
      command: `pnpm dev --port ${PORT} --strictPort`,
      url: baseURL,
      reuseExistingServer: false,
      timeout: 120_000,
      // Chave falsa: o E2E precisa do adapter Web3Forms montado para poder
      // interceptar a rota. Nenhum envio real sai — todo teste que submete
      // intercepta `api.web3forms.com` (D6 da spec: não há conta nesta rodada).
      env: { VITE_WEB3FORMS_ACCESS_KEY: 'e2e-fake-access-key' },
    },
    {
      command: `pnpm build && pnpm preview --port ${PREVIEW_PORT} --strictPort`,
      url: previewURL,
      reuseExistingServer: false,
      timeout: 180_000,
      // A chave entra no build: no Vite a variável é resolvida em tempo de
      // compilação, não em runtime.
      env: { VITE_WEB3FORMS_ACCESS_KEY: 'e2e-fake-access-key' },
    },
  ],
```

- [ ] **Step 3: Rodar e ver passar**

Run: `pnpm e2e --project=producao`
Expected: PASS, 1 teste.

- [ ] **Step 4: Provar que o teste reprova de verdade**

Acrescente temporariamente ao fim de `src/main.tsx`:

```ts
console.error('injeção temporária para provar o gate de 6.2.3')
```

Run: `pnpm e2e --project=producao`
Expected: FAIL, com `injeção temporária para provar o gate de 6.2.3` na lista de erros.

Remova a linha e rode de novo:

Run: `pnpm e2e --project=producao`
Expected: PASS.

- [ ] **Step 5: Rodar os gates**

Run: `pnpm format && pnpm check && pnpm e2e`
Expected: tudo verde, os cinco projetos do Playwright inclusos.

- [ ] **Step 6: Commit**

```bash
git add e2e/producao.spec.ts playwright.config.ts
git commit -m "test(6.2.3): reprovar erro de console e request quebrada em produção"
```

---

### Task 8: Homologar a paridade (`6.3.1`)

**Files:**

- Create: `docs/qa/homologacao-2026-08-29.md`
- Modify: `docs/superpowers/backlog.md` (débitos `D-10` e `D-17`)

**Interfaces:**

- Consumes: toda a evidência produzida pelas Tasks 1–7.
- Produces: o documento que `/fechar-site` cita como prova de aceite do bloco.

- [ ] **Step 1: Reunir a evidência**

Run: `pnpm check && pnpm e2e && ls docs/qa/paridade/2026-08-29 docs/qa/performance/2026-08-29`
Expected: gates verdes e as duas pastas de evidência completas. Anote a saída real — nenhum item da homologação pode citar teste que não foi rodado agora.

- [ ] **Step 2: Escrever a homologação**

Crie `docs/qa/homologacao-2026-08-29.md`:

```markdown
# Homologação de paridade — clone Lotus OTEC

> Bloco `6.1.1-6.3.1` · branch `feat/6-1-1-6-3-1-qa-visual-performance`
> Gate formal antes de preparar o domínio para produção (EAP `6.3.1`).

## Veredito

| item               | situação              | evidência                                                                    |
| ------------------ | --------------------- | ---------------------------------------------------------------------------- |
| Matriz de paridade | aprovado              | `docs/inventario/README.md` + `docs/qa/paridade/2026-08-29/classificacao.md` |
| Testes             | aprovado              | `pnpm check` e `pnpm e2e` verdes em <data e hora reais>                      |
| Formulário         | **parcial declarado** | provado contra a API documentada; envio real não provado — `D-17`            |
| SEO                | aprovado com ressalva | `e2e/seo.spec.ts` verde; validadores públicos pendentes — `D-22`             |
| Acessibilidade     | aprovado com ressalva | `e2e/a11y.spec.ts` verde; nove nós sob exceção nominal — `D-21`              |
| Performance        | aprovado              | `docs/qa/performance/2026-08-29/resumo.md` e `resumo-pos-otimizacao.md`      |

## Divergências intencionais

Todas registradas na matriz de `docs/inventario/README.md`. Nenhuma divergência deste bloco ficou
como `pendente decisão`.

## Débitos que seguem abertos

- `D-17` — envio real do formulário não provado: não há conta nem access key do Web3Forms.
  Decisão de João em 2026-08-29 (D5 da spec deste bloco): o bloco fecha com o débito aberto.
  **Gatilho:** antes do go-live.
- `D-21` — nove nós de `color-contrast` sob exceção nominal, cor medida do original.
  **Gatilho:** redesign.
- `D-22` — JSON-LD e tags sociais validados só localmente. **Gatilho:** primeiro deploy.
- Débito novo — `a11y.spec.ts` e `seo.spec.ts` rodam só em Chromium (D4 da spec).
  **Gatilho:** quando houver relato de falha de acessibilidade fora do Chromium.
- Débito novo — cada rodada de paridade versiona ~7 MB de PNG (D6 da spec).
  **Gatilho:** terceira rodada, ou quando o repositório incomodar.

## O que esta homologação não prova

Paridade medida em Chromium, Firefox e WebKit sobre a home. Não há segunda página. A performance é
laboratório local, não campo. Nenhuma mensagem de formulário chegou a uma caixa de entrada real.
```

Substitua `<data e hora reais>` pela saída do Step 1. Ajuste qualquer linha cuja situação real
divergir do rascunho — a tabela descreve o que foi medido, não o que se esperava medir.

- [ ] **Step 3: Fechar `D-10` no backlog**

Em `docs/superpowers/backlog.md`, na entrada `D-10`, acrescente ao fim:

```markdown
**Fechado por este bloco** (`6.1.1`–`6.3.1`) em 2026-08-29 — `playwright.config.ts` declara
`chromium`, `firefox`, `webkit` e `mobile-webkit`, e o CI instala os três motores.
```

E na entrada `D-17`, acrescente:

```markdown
Reafirmado em 2026-08-29 (D5 da spec do bloco `6.1.1-6.3.1`): a homologação `6.3.1` fecha com o
formulário como parcial declarado.
```

- [ ] **Step 4: Rodar os gates**

Run: `pnpm format && pnpm check && pnpm e2e`
Expected: tudo verde.

- [ ] **Step 5: Commit**

```bash
git add docs/qa/homologacao-2026-08-29.md docs/superpowers/backlog.md
git commit -m "docs(6.3.1): homologar a paridade e declarar os débitos abertos"
```

---

## Handoff de execução

executor: claude
reviewer: codex

Segunda lente, não independente: claude, sob o protocolo de confronto de D7 da spec — achado do
Codex só cai com contra-evidência executada; achado de Claude só vira correção depois de o Codex
confirmar.

paths_autorizados:

- não se aplica; o executor é Claude
