# Zona DNS de `lotusotec.cl` no Route 53 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** criar a zona de `lotusotec.cl` no Route 53 como cópia conferida da zona que a StackDNS serve hoje, provada registro a registro, **sem trocar a delegação**.

**Architecture:** um stack CloudFormation novo e separado (`infra/lotus-dns.yaml`, em `us-east-1`) declara a hosted zone com `Retain` e os onze `RecordSet` do inventário medido. Duas camadas de prova, com papéis distintos: uma catraca offline em Vitest lê o template e falha se ele divergir do inventário ou ganhar wildcard, e um script de conferência ao vivo pergunta direto aos quatro nameservers da AWS e compara com o lado ainda servido pela StackDNS. O filtro do Budget do stack do site passa a incluir Route 53 no mesmo bloco.

**Tech Stack:** CloudFormation (YAML), AWS CLI 2.36.38, Node 24.19.0 + pnpm, Vitest, `node:dns/promises`, DNS-over-HTTPS contra `dns.google`.

**Spec:** [`docs/superpowers/specs/2026-09-20-7.2.1-zona-dns-route53-design.md`](../specs/2026-09-20-7.2.1-zona-dns-route53-design.md)
**Branch:** `feat/7-2-1-zona-dns-route53` · **Base:** `c7137e0`

---

## Global Constraints

- **Node 24.19.0.** O Node do PATH desta máquina é o **v22.23.1** (medido em 2026-09-20), e `engineStrict: true` transforma isso em `ERR_PNPM_UNSUPPORTED_ENGINE`. **Todo** comando `pnpm` leva o prefixo, sem exceção:
  ```bash
  export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
  ```
- **Nenhuma dependência nova** (Lei 7). Medido em 2026-09-20: nem `yaml` nem `js-yaml` resolvem na árvore. A catraca offline lê o template como texto; a validação estrutural do YAML fica com `aws cloudformation validate-template`.
- **Aspas simples no YAML.** `.prettierrc` tem `"singleQuote": true`, e `infra/lotus-site.yaml` passa hoje no `format:check` com aspas simples e com `!Sub`/`!GetAtt`/`!Ref` preservados (medido em 2026-09-20: `prettier --check infra/lotus-site.yaml` → `All matched files use Prettier code style!`). Escreva YAML com aspas simples.
- **`eslint .` só olha `**/*.{ts,tsx}`** (`eslint.config.js`). `.mjs` e `.yaml` não são lintados — mas **são** typechecados.
- **`tsconfig.node.json` typecheca `scripts/**/*.mjs`** com `checkJs`, `strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`, `erasableSyntaxOnly`, `verbatimModuleSyntax`. Consequência prática: **todo parâmetro precisa de tipo JSDoc**, e **todo acesso por índice devolve `T | undefined`** e precisa de guarda. `arr[0]` sem guarda reprova `pnpm typecheck`.
- **`vitest.config.ts` inclui `scripts/**/*.test.mjs`**, então a catraca offline roda dentro de `pnpm check` e no CI, sem rede e sem credencial.
- **`pnpm check` verde antes de cada commit.** É `agent:check && format:check && lint && typecheck && test && build`.
- **`docs/superpowers/backlog.md` não pode conter a string `workflow_state`** — `checkBacklog` reprova. Escreva "estado" ou "fase".
- **Conventional Commits com a EAP no escopo:** `feat(7.2.1): …`.
- **Nada de `git push`, nada de `gh pr create`** (Lei 6). Só `/fechar-site`.
- **Não escrever no Notion** (Lei 6). Não há autorização nesta rodada.
- **Não tocar `src/`, `public/`, `e2e/`, `index.html`.** Nenhum pixel do produto muda; `pnpm e2e` não se aplica.
- **Não trocar os nameservers, não emitir certificado, não criar alias de CloudFront.** Spec §2. A Task 2 cria a zona; quem a delega é o bloco seguinte.
- **Região do stack novo: `us-east-1`.** O runbook exporta `AWS_REGION=sa-east-1` na linha 50; **todo** comando deste bloco leva `--region us-east-1` explícito.
- **Perfil AWS: `lotus`.** Medido em 2026-09-20: `AWS_PROFILE=lotus aws sts get-caller-identity` responde a conta `760144413534`; sem o perfil a credencial do ambiente é inválida (`InvalidClientTokenId`).
- **Prova antes de afirmação** (Lei 8). Saída real colada no corpo do commit; nunca "o gate passou" sem a linha do comando.

### Paths autorizados

Os da spec §7, mais os dois da emenda **E1**:

```text
infra/lotus-dns.yaml
infra/lotus-site.yaml
scripts/infra/lib/zona.mjs                ← E1
scripts/infra/zona.test.mjs
scripts/infra/conferir-zona.mjs
package.json                              ← E1
docs/infra/runbook-aws.md
docs/infra/zona-dns-lotusotec.md
docs/infra/conferencia-zona-<data>.md
docs/superpowers/state.md
docs/superpowers/backlog.md
docs/superpowers/specs/2026-09-20-7.2.1-zona-dns-route53-design.md
docs/superpowers/plans/2026-09-20-7.2.1-zona-dns-route53.md
```

---

## Emenda E1 à spec — por que dois paths a mais

A spec §7 autoriza `scripts/infra/zona.test.mjs` e `scripts/infra/conferir-zona.mjs`, e mais nada em `scripts/`. Escrever só esses dois arquivos obriga a **duplicar o inventário** — os onze registros, os cinco MX, os dois nomes inventados — dentro de cada um. Duplicado, ele sai de sincronia: a catraca offline aprovaria um template que a conferência ao vivo reprova, ou pior, o contrário.

**E1a — `scripts/infra/lib/zona.mjs`.** Fonte única do inventário e da leitura do template. Os dois arquivos da spec passam a importar daqui. Segue o padrão já existente na árvore (`scripts/qa/lib/*.mjs` + `*.test.mjs`, `scripts/inventario/lib/site.mjs`), e é o que torna a leitura do template testável isoladamente.

**E1b — `package.json`.** Uma linha em `"scripts"`: `"infra:conferir-zona": "node scripts/infra/conferir-zona.mjs"`. Todo script operacional da árvore tem alias (`qa:perf`, `inventario:dom`, `qa:espacamento`); sem ele o runbook passaria a citar um caminho cru que ninguém mais cita. **Nenhuma dependência entra** — Lei 7 continua respeitada.

Se João recusar a emenda, o caminho alternativo é escrever o inventário duas vezes e declarar a duplicação como débito. Não é recomendado.

---

## Medições que este plano assume, e quando foram feitas

Todas em **2026-09-20**, nesta máquina, com `AWS_PROFILE=lotus`:

| Fato                                           | Comando                                                                 | Resultado                                    |
| ---------------------------------------------- | ----------------------------------------------------------------------- | -------------------------------------------- |
| a conta responde                               | `aws sts get-caller-identity`                                           | `760144413534`                               |
| **não existe** zona de `lotusotec.cl` na conta | `aws route53 list-hosted-zones`                                         | só `komit.cl.`, 8 registros                  |
| não existe stack nenhum em `us-east-1`         | `aws cloudformation list-stacks --region us-east-1`                     | `[]`                                         |
| o nome exato do serviço no Cost Explorer       | `aws ce get-dimension-values --dimension SERVICE --search-string Route` | `Amazon Route 53`                            |
| não há parser YAML na árvore                   | `ls node_modules/yaml node_modules/js-yaml`                             | nenhum dos dois                              |
| Prettier aceita o YAML com aspas simples       | `pnpm exec prettier --check infra/lotus-site.yaml`                      | `All matched files use Prettier code style!` |

Duas consequências que o executor não pode esquecer:

1. **`komit.cl` já custa Route 53 nesta conta.** O Budget é da conta, não do stack; ao incluir `Amazon Route 53` no filtro, o teto passa a medir também a zona de terceiro. É o comportamento correto — o teto existe para a conta —, mas precisa ficar escrito, senão a primeira fatura parece erro.
2. **A conta é membro de organização de terceiro, com SCPs ativas e invisíveis.** Route 53 ou Budgets podem ser negados sem aviso legível. Por isso a Task 2 usa change set antes de executar.

---

## File Structure

| Arquivo                                 | Responsabilidade                                                                                                                                                                  | Task |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `scripts/infra/lib/zona.mjs`            | Fonte única do inventário esperado e leitor textual de `infra/lotus-dns.yaml`, com `!Ref`/`!Sub` resolvidos contra os defaults. Puro, sem rede, sem `fs` — recebe o texto pronto. | 1    |
| `infra/lotus-dns.yaml`                  | A zona e os onze `RecordSet`, com `Retain` nos dois sentidos. Stack próprio porque o ciclo de vida é outro.                                                                       | 1    |
| `scripts/infra/zona.test.mjs`           | A catraca: o template não pode divergir do inventário, nem ganhar wildcard, nem apontar para o CloudFront, nem perder o `Retain`.                                                 | 1    |
| `scripts/infra/conferir-zona.mjs`       | Conferência ao vivo, executada à mão. Lê os nameservers do stack, pergunta direto a cada um e compara com o lado da StackDNS. Grava a evidência.                                  | 3    |
| `package.json`                          | Ganha o alias `infra:conferir-zona` (emenda E1b).                                                                                                                                 | 3    |
| `docs/infra/conferencia-zona-<data>.md` | A evidência gerada pelo script. Não é escrita à mão.                                                                                                                              | 3    |
| `infra/lotus-site.yaml`                 | Só o filtro do Budget e a descrição do parâmetro do teto. Nada mais.                                                                                                              | 4    |
| `docs/infra/runbook-aws.md`             | Secção 6 reescrita: a zona passa a nascer de stack, a conferência passa a ser um comando, o certificado passa a ser recurso do template com a CLI como fallback declarado.        | 4    |
| `docs/infra/zona-dns-lotusotec.md`      | Ganha a secção da cópia no Route 53 e perde a frase de que João não tem acesso ao painel.                                                                                         | 4    |
| `docs/superpowers/backlog.md`           | `D-44` fecha; `D-45` ganha o desfecho desta rodada; `D-47` e `D-48` nascem; `B1` e as pendências são atualizados.                                                                 | 4    |
| `docs/superpowers/state.md`             | Fase vai para `ready_for_review`.                                                                                                                                                 | 4    |

---

## Sequência de commits

| #   | Commit                                                               | Task |
| --- | -------------------------------------------------------------------- | ---- |
| 1   | `feat(7.2.1): criar stack lotus-dns com a zona de lotusotec.cl`      | 1    |
| 2   | `test(7.2.1): travar o conteúdo da zona contra o inventário`         | 1    |
| 3   | `feat(7.2.1): conferir a zona nova contra a StackDNS`                | 3    |
| 4   | `chore(7.2.1): ampliar o Budget para Route 53 e atualizar o runbook` | 4    |

A Task 1 produz **dois** commits porque a ordem de escrita e a ordem de leitura são diferentes: escreve-se o teste primeiro, para vê-lo vermelho pelo motivo certo, mas o histórico fica mais legível com o template antes da catraca que o prende. `git add` por commit resolve; os dois ficam verdes em `pnpm check`, cada um na sua árvore.

A **Task 2 não gera commit**: ela implanta o stack. É o portão de mundo real deste bloco, e a Task 3 não existe sem ele.

---

## Task 1: a zona em CloudFormation e a catraca que a prende

**Files:**

- Create: `scripts/infra/lib/zona.mjs`
- Create: `scripts/infra/zona.test.mjs`
- Create: `infra/lotus-dns.yaml`

**Interfaces:**

- Consumes: nada de tasks anteriores. O inventário vem de `docs/infra/zona-dns-lotusotec.md`, medido em 2026-09-09.
- Produces, de `scripts/infra/lib/zona.mjs`:
  - `INVENTARIO: readonly RegistroEsperado[]` onde `RegistroEsperado = { nome: string, tipo: string, valores: string[] }`, com `nome` **sempre com ponto final**.
  - `NOMES_INVENTADOS: readonly string[]` — os dois nomes que só respondem hoje por causa do wildcard.
  - `lerParametros(texto: string): Map<string, string>`
  - `resolver(bruto: string, parametros: Map<string, string>): string`
  - `lerRegistros(texto: string): RegistroLido[]` onde `RegistroLido = { nome: string, tipo: string, ttl: number, valores: string[] }`
  - `lerPoliticasDaZona(texto: string): { deletionPolicy: string, updateReplacePolicy: string }`
  - `normalizar(valor: string): string`
  - `mesmoConjunto(a: string[], b: string[]): boolean`
  - A Task 3 importa `INVENTARIO`, `NOMES_INVENTADOS`, `normalizar` e `mesmoConjunto` — mais nada.

### Por que o leitor é textual

Não há parser YAML na árvore e instalar um só para esta catraca é dependência sem necessidade do work item (Lei 7). O template é escrito por nós, num formato estável, então a leitura aqui é textual e **assumida frágil**: se o formato mudar, ela quebra ruidosamente — nunca em silêncio. Duas guardas garantem isso: o teste exige que o leitor ache exatamente onze registros, e `resolver` lança exceção quando encontra um parâmetro sem `Default` em vez de devolver string vazia.

- [ ] **Step 1: criar a pasta e escrever a biblioteca**

```bash
mkdir -p scripts/infra/lib
```

`scripts/infra/lib/zona.mjs`:

```js
// O que a zona de lotusotec.cl precisa conter, e como ler isso de volta de
// `infra/lotus-dns.yaml` sem parser YAML.
//
// Medido em 2026-09-20: nem `yaml` nem `js-yaml` resolvem na árvore, e
// instalar um só para esta catraca é dependência sem necessidade do work item
// (Lei 7). O template é escrito por nós, num formato estável, então a leitura
// aqui é textual e assumida frágil: ela quebra ruidosamente se o formato
// mudar, nunca em silêncio. A validação estrutural do YAML fica com
// `aws cloudformation validate-template`, no runbook.

/**
 * @typedef {{ nome: string, tipo: string, valores: string[] }} RegistroEsperado
 * @typedef {{ nome: string, tipo: string, ttl: number, valores: string[] }} RegistroLido
 */

/**
 * Inventário medido em 2026-09-09 por DNS-over-HTTPS contra `dns.google` e
 * registrado em `docs/infra/zona-dns-lotusotec.md`. Nomes com ponto final,
 * como o Route 53 os guarda.
 * @type {readonly RegistroEsperado[]}
 */
export const INVENTARIO = Object.freeze([
  { nome: 'lotusotec.cl.', tipo: 'A', valores: ['185.146.167.195'] },
  { nome: 'lotusotec.cl.', tipo: 'AAAA', valores: ['2a07:7800::195'] },
  {
    nome: 'lotusotec.cl.',
    tipo: 'MX',
    valores: [
      '1 ASPMX.L.GOOGLE.COM.',
      '5 ALT1.ASPMX.L.GOOGLE.COM.',
      '5 ALT2.ASPMX.L.GOOGLE.COM.',
      '10 ALT3.ASPMX.L.GOOGLE.COM.',
      '10 ALT4.ASPMX.L.GOOGLE.COM.',
    ],
  },
  {
    nome: 'lotusotec.cl.',
    tipo: 'TXT',
    valores: [
      '"v=spf1 include:_spf.google.com include:spf.stackmail.com -all"',
    ],
  },
  { nome: 'www.lotusotec.cl.', tipo: 'A', valores: ['185.146.167.195'] },
  { nome: 'sistema.lotusotec.cl.', tipo: 'A', valores: ['185.146.167.195'] },
  {
    nome: 'mail.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['ghs.googlehosted.com.'],
  },
  {
    nome: 'smtp.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['smtp.stackmail.com.'],
  },
  {
    nome: 'imap.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['imap.stackmail.com.'],
  },
  {
    nome: 'autodiscover.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['autodiscover.stackmail.com.'],
  },
  {
    nome: 'ftp.lotusotec.cl.',
    tipo: 'CNAME',
    valores: ['ftp.us.stackcp.com.'],
  },
])

/**
 * Nomes que só respondem hoje porque o wildcard existe (`D-45`, medido em
 * 2026-09-09). A zona nova não os declara, então os dois lados divergirem
 * neles é a prova de que o wildcard não atravessou — e é a única divergência
 * esperada da conferência.
 * @type {readonly string[]}
 */
export const NOMES_INVENTADOS = Object.freeze([
  'zzz-nao-existe-19283.lotusotec.cl.',
  'outro-teste-aleatorio-77.lotusotec.cl.',
])

const ASPAS_EXTERNAS = /^(['"])([\s\S]*)\1$/

/** @param {string} bruto */
function semAspas(bruto) {
  const cru = bruto.trim()
  return cru.match(ASPAS_EXTERNAS)?.[2] ?? cru
}

/**
 * @param {Map<string, string>} parametros
 * @param {string} nome
 */
function exigirParametro(parametros, nome) {
  const valor = parametros.get(nome)
  if (valor === undefined) {
    throw new Error(
      `template usa o parâmetro ${nome}, que não tem Default declarado`,
    )
  }
  return valor
}

/**
 * Recorta o trecho entre duas chaves de topo do template.
 * @param {string} texto
 * @param {string} inicio
 * @param {string} fim
 */
function recortar(texto, inicio, fim) {
  const de = texto.indexOf(`\n${inicio}`)
  const ate = texto.indexOf(`\n${fim}`)
  if (de === -1) throw new Error(`template sem a secção ${inicio}`)
  if (ate === -1 || ate < de)
    throw new Error(`template sem ${fim} depois de ${inicio}`)
  return texto.slice(de, ate)
}

/**
 * Lê o `Default` de cada parâmetro declarado.
 * @param {string} texto
 * @returns {Map<string, string>}
 */
export function lerParametros(texto) {
  /** @type {Map<string, string>} */
  const parametros = new Map()
  let atual = ''
  for (const linha of recortar(texto, 'Parameters:', 'Resources:').split(
    '\n',
  )) {
    const nome = linha.match(/^ {2}(\w+):\s*$/)
    if (nome) {
      atual = nome[1] ?? ''
      continue
    }
    const padrao = linha.match(/^ {4}Default:\s*(.+)$/)
    if (padrao && atual) parametros.set(atual, semAspas(padrao[1] ?? ''))
  }
  return parametros
}

/**
 * Resolve `!Ref X` e `!Sub '...${X}...'` contra os defaults dos parâmetros.
 * Valor sem tag volta apenas sem as aspas do YAML.
 * @param {string} bruto
 * @param {Map<string, string>} parametros
 */
export function resolver(bruto, parametros) {
  const cru = bruto.trim()
  const referencia = cru.match(/^!Ref\s+(\w+)$/)
  if (referencia) return exigirParametro(parametros, referencia[1] ?? '')
  const substituicao = cru.match(/^!Sub\s+([\s\S]+)$/)
  const texto = semAspas(substituicao?.[1] ?? cru)
  return texto.replace(/\$\{(\w+)\}/g, (_todo, nome) =>
    exigirParametro(parametros, String(nome)),
  )
}

/**
 * Lê os `RecordSet` do template, com `!Ref` e `!Sub` já resolvidos.
 * @param {string} texto
 * @returns {RegistroLido[]}
 */
export function lerRegistros(texto) {
  const parametros = lerParametros(texto)
  const de = texto.indexOf('RecordSets:')
  if (de === -1) throw new Error('template sem RecordSets:')
  /** @type {RegistroLido[]} */
  const registros = []
  /** @type {RegistroLido | undefined} */
  let atual
  for (const linha of texto.slice(de).split('\n').slice(1)) {
    // Chave de topo na coluna zero: a lista acabou.
    if (/^\S/.test(linha)) break
    const abertura = linha.match(/^\s*- Name:\s*(.+)$/)
    if (abertura) {
      atual = {
        nome: resolver(abertura[1] ?? '', parametros),
        tipo: '',
        ttl: 0,
        valores: [],
      }
      registros.push(atual)
      continue
    }
    if (!atual) continue
    const tipo = linha.match(/^\s*Type:\s*(.+)$/)
    if (tipo) {
      atual.tipo = resolver(tipo[1] ?? '', parametros)
      continue
    }
    const ttl = linha.match(/^\s*TTL:\s*(.+)$/)
    if (ttl) {
      atual.ttl = Number(resolver(ttl[1] ?? '', parametros))
      continue
    }
    const valor = linha.match(/^\s*- (.+)$/)
    if (valor) atual.valores.push(resolver(valor[1] ?? '', parametros))
  }
  return registros
}

/**
 * As duas políticas do recurso da zona. Apagar a zona e recriá-la dá
 * nameservers novos, e são eles que o registrador aponta.
 * @param {string} texto
 */
export function lerPoliticasDaZona(texto) {
  const de = texto.indexOf('\n  Zona:')
  if (de === -1) throw new Error('template sem o recurso Zona')
  const politicas = { deletionPolicy: '', updateReplacePolicy: '' }
  for (const linha of texto.slice(de).split('\n').slice(1)) {
    // Próximo recurso de topo dentro de Resources:.
    if (/^ {2}\w/.test(linha)) break
    const apagar = linha.match(/^ {4}DeletionPolicy:\s*(\S+)/)
    if (apagar) politicas.deletionPolicy = apagar[1] ?? ''
    const substituir = linha.match(/^ {4}UpdateReplacePolicy:\s*(\S+)/)
    if (substituir) politicas.updateReplacePolicy = substituir[1] ?? ''
  }
  return politicas
}

/**
 * TXT chega com aspas de um lado e sem do outro; MX e CNAME chegam com caixa
 * variável. Normalizar aqui é o que evita falso positivo na comparação.
 * @param {string} valor
 */
export function normalizar(valor) {
  return semAspas(valor).trim().toLowerCase().replace(/\s+/g, ' ')
}

/**
 * @param {string[]} a
 * @param {string[]} b
 */
export function mesmoConjunto(a, b) {
  /** @param {string[]} lista */
  const chaves = (lista) => [...new Set(lista.map(normalizar))].sort()
  const esquerda = chaves(a)
  const direita = chaves(b)
  return (
    esquerda.length === direita.length &&
    esquerda.every((valor, i) => valor === direita[i])
  )
}
```

- [ ] **Step 2: escrever o teste, antes do template**

`scripts/infra/zona.test.mjs`:

```js
import { readFileSync } from 'node:fs'
import { describe, expect, it } from 'vitest'
import {
  INVENTARIO,
  lerParametros,
  lerPoliticasDaZona,
  lerRegistros,
  mesmoConjunto,
  normalizar,
  resolver,
} from './lib/zona.mjs'

// Caminho relativo ao arquivo, não ao diretório de trabalho: o teste roda
// igual pelo `pnpm test` da raiz e por um runner apontado para a pasta.
const TEMPLATE = readFileSync(
  new URL('../../infra/lotus-dns.yaml', import.meta.url),
  'utf8',
)

describe('leitura textual do template', () => {
  const parametros = lerParametros(TEMPLATE)

  it('lê o Default de cada parâmetro', () => {
    expect(parametros.get('NomeDaZona')).toBe('lotusotec.cl')
    expect(parametros.get('IpDoWordPress')).toBe('185.146.167.195')
    expect(parametros.get('Ipv6DoWordPress')).toBe('2a07:7800::195')
    expect(parametros.get('TtlPadrao')).toBe('3600')
  })

  it('resolve !Ref e !Sub contra os defaults', () => {
    expect(resolver('!Ref IpDoWordPress', parametros)).toBe('185.146.167.195')
    expect(resolver("!Sub 'www.${NomeDaZona}.'", parametros)).toBe(
      'www.lotusotec.cl.',
    )
    expect(resolver("'1 ASPMX.L.GOOGLE.COM.'", parametros)).toBe(
      '1 ASPMX.L.GOOGLE.COM.',
    )
  })

  it('reprova parâmetro sem Default em vez de resolver para vazio', () => {
    expect(() => resolver('!Ref NaoExiste', parametros)).toThrow(/NaoExiste/)
  })

  it('acha exatamente um RecordSet por linha do inventário', () => {
    // Guarda contra o leitor deixar de achar qualquer coisa e o resto do
    // arquivo ficar verde por vacuidade.
    expect(lerRegistros(TEMPLATE)).toHaveLength(INVENTARIO.length)
  })
})

describe('infra/lotus-dns.yaml contra o inventário medido', () => {
  const registros = lerRegistros(TEMPLATE)

  for (const esperado of INVENTARIO) {
    it(`declara ${esperado.tipo} ${esperado.nome}`, () => {
      const achado = registros.find(
        (registro) =>
          registro.nome === esperado.nome && registro.tipo === esperado.tipo,
      )
      expect(
        achado,
        `${esperado.tipo} ${esperado.nome} não está no template`,
      ).toBeDefined()
      expect(mesmoConjunto(achado?.valores ?? [], esperado.valores)).toBe(true)
    })
  }

  it('mantém os cinco MX do Google, com as prioridades medidas', () => {
    const mx = registros.find((registro) => registro.tipo === 'MX')
    const doInventario = INVENTARIO.find((registro) => registro.tipo === 'MX')
    expect(mx?.valores).toHaveLength(5)
    expect((mx?.valores ?? []).map(normalizar).sort()).toEqual(
      (doInventario?.valores ?? []).map(normalizar).sort(),
    )
  })

  it('não declara wildcard', () => {
    expect(registros.filter((registro) => registro.nome.includes('*'))).toEqual(
      [],
    )
  })

  it('não declara NS nem SOA do apex', () => {
    // A zona gera os dois; declará-los briga com o delegation set escolhido
    // pela AWS.
    expect(
      registros.filter(
        (registro) => registro.tipo === 'NS' || registro.tipo === 'SOA',
      ),
    ).toEqual([])
  })

  it('não aponta nada para o CloudFront', () => {
    // Alias do apex e de www são B5. Chegar aqui cedo aponta o domínio antes
    // de existir certificado.
    expect(TEMPLATE).not.toMatch(/cloudfront\.net/)
    expect(TEMPLATE).not.toMatch(/AliasTarget/)
  })

  it('não declara certificado', () => {
    // Pedido pendente do ACM morre em 72h, e com HostedZoneId o stack fica em
    // CREATE_IN_PROGRESS até validar — com a zona ainda não delegada, isso é
    // stack travado até o timeout. Spec §3 D6.
    expect(TEMPLATE).not.toMatch(/AWS::CertificateManager::Certificate/)
  })

  it('protege a zona contra delete-stack', () => {
    expect(lerPoliticasDaZona(TEMPLATE)).toEqual({
      deletionPolicy: 'Retain',
      updateReplacePolicy: 'Retain',
    })
  })

  it('usa o TTL medido da zona atual em todos os registros', () => {
    // Comparado contra o tamanho do INVENTARIO, e nao contra o proprio
    // `registros`: com os dois lados derivados da mesma lista, uma leitura
    // vazia passaria.
    expect(registros.map((registro) => registro.ttl)).toEqual(
      INVENTARIO.map(() => 3600),
    )
  })
})
```

- [ ] **Step 3: rodar o teste e vê-lo vermelho pelo motivo certo**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm exec vitest run scripts/infra/zona.test.mjs
```

Esperado: **FAIL** na leitura do arquivo, com `ENOENT` citando `infra/lotus-dns.yaml`. Qualquer outro erro — de import, de sintaxe — é bug do Step 1, não a ausência do template; corrija antes de seguir.

- [ ] **Step 4: escrever o template**

`infra/lotus-dns.yaml`:

```yaml
AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  Zona DNS de lotusotec.cl no Route 53, copia conferida da zona que a StackDNS
  serve hoje (inventario em docs/infra/zona-dns-lotusotec.md, ADR-SITE-006).
  Stack separado do lotus-site porque o ciclo de vida e outro: delete-stack do
  site nao pode arrastar a zona junto. NAO troca a delegacao, NAO emite
  certificado e NAO aponta nada para o CloudFront -- a zona nasce respondendo
  exatamente o que a zona atual responde.

  Regiao us-east-1 de proposito, embora o Route 53 seja global: o certificado
  do bloco seguinte entra neste mesmo stack, e o CloudFront so le certificado
  de us-east-1.

Parameters:
  NomeDaZona:
    Type: String
    Default: lotusotec.cl
    Description: Zona sem ponto final; o template acrescenta onde precisa.
    AllowedPattern: '^[a-z0-9][a-z0-9.-]*[a-z0-9]$'

  IpDoWordPress:
    Type: String
    Default: 185.146.167.195
    Description: >-
      WordPress na BlueHosting. Apex, www e sistema apontam para ele ate o
      cutover (B5). Medido em 2026-09-09.

  Ipv6DoWordPress:
    Type: String
    Default: '2a07:7800::195'
    Description: AAAA do apex. Medido em 2026-09-09.

  TtlPadrao:
    Type: Number
    Default: 3600
    MinValue: 60
    Description: >-
      TTL de toda a zona atual. Copia fiel: mudar o TTL aqui mudaria o
      comportamento durante a propagacao da troca de nameservers.

Resources:
  Zona:
    Type: AWS::Route53::HostedZone
    # Retain nos dois sentidos. Uma hosted zone recriada ganha nameservers
    # NOVOS, e sao eles que o registrador aponta: apagar esta zona por acidente
    # derruba o site e o e-mail corporativo junto, e nao basta recria-la.
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Name: !Ref NomeDaZona
      HostedZoneConfig:
        Comment: Zona do site e do e-mail corporativo; ADR-SITE-006, bloco B1.
      HostedZoneTags:
        - Key: Projeto
          Value: lotus-site

  Registros:
    Type: AWS::Route53::RecordSetGroup
    Properties:
      HostedZoneId: !Ref Zona
      # NS e SOA do apex nao entram: a zona ja nasce com os dois, e declara-los
      # brigaria com o delegation set que a AWS escolheu.
      #
      # O wildcard da zona atual (`*.lotusotec.cl`, D-45) tambem nao entra. Os
      # dois nomes que hoje so existem por causa dele e nao podem quebrar --
      # www e sistema -- nascem explicitos, com o mesmo IP que o wildcard
      # devolve.
      RecordSets:
        - Name: !Sub '${NomeDaZona}.'
          Type: A
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - !Ref IpDoWordPress

        - Name: !Sub '${NomeDaZona}.'
          Type: AAAA
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - !Ref Ipv6DoWordPress

        # Google Workspace: e o e-mail da empresa, e e o registro que mais
        # exige conferencia antes e depois da troca de nameservers.
        - Name: !Sub '${NomeDaZona}.'
          Type: MX
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - '1 ASPMX.L.GOOGLE.COM.'
            - '5 ALT1.ASPMX.L.GOOGLE.COM.'
            - '5 ALT2.ASPMX.L.GOOGLE.COM.'
            - '10 ALT3.ASPMX.L.GOOGLE.COM.'
            - '10 ALT4.ASPMX.L.GOOGLE.COM.'

        # O include:spf.stackmail.com atravessa como esta. Decidir sobre ele e
        # B2, junto com DKIM e DMARC (D-46).
        - Name: !Sub '${NomeDaZona}.'
          Type: TXT
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - '"v=spf1 include:_spf.google.com include:spf.stackmail.com -all"'

        - Name: !Sub 'www.${NomeDaZona}.'
          Type: A
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - !Ref IpDoWordPress

        # Registro de DNS e nada mais. Nao abre a 8.2.1, congelada por Joao em
        # 2026-09-09.
        - Name: !Sub 'sistema.${NomeDaZona}.'
          Type: A
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - !Ref IpDoWordPress

        - Name: !Sub 'mail.${NomeDaZona}.'
          Type: CNAME
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - ghs.googlehosted.com.

        - Name: !Sub 'smtp.${NomeDaZona}.'
          Type: CNAME
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - smtp.stackmail.com.

        - Name: !Sub 'imap.${NomeDaZona}.'
          Type: CNAME
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - imap.stackmail.com.

        - Name: !Sub 'autodiscover.${NomeDaZona}.'
          Type: CNAME
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - autodiscover.stackmail.com.

        - Name: !Sub 'ftp.${NomeDaZona}.'
          Type: CNAME
          TTL: !Ref TtlPadrao
          ResourceRecords:
            - ftp.us.stackcp.com.

Outputs:
  NameServers:
    Description: >-
      Os quatro nameservers desta zona. Sao eles que vao para o painel do
      registrador no bloco da delegacao, e recriar a zona troca os quatro.
    Value: !Join
      - ','
      - !GetAtt Zona.NameServers

  IdDaZona:
    Description: Entrada de aws route53 list-resource-record-sets --hosted-zone-id.
    Value: !Ref Zona
```

- [ ] **Step 5: rodar o teste e vê-lo verde**

```bash
pnpm exec vitest run scripts/infra/zona.test.mjs
```

Esperado: **PASS**, 22 testes: 4 de leitura, 11 do inventário e 7 de invariante. Se `acha exatamente um RecordSet por linha do inventário` falhar com número diferente de 11, o leitor textual perdeu o passo com a indentação do template — conserte o leitor, nunca o número esperado.

- [ ] **Step 6: provar que a catraca morde**

Prova de que o teste testa alguma coisa. Três mutações, uma de cada vez, cada uma desfeita antes
da seguinte. `git checkout <path>` restaura do índice, então o template precisa estar lá primeiro —
é `git add`, não `git commit`:

```bash
git add infra/lotus-dns.yaml

# 1. wildcard entra
sed -i "s|- Name: !Sub 'www.\${NomeDaZona}.'|- Name: !Sub '*.\${NomeDaZona}.'|" infra/lotus-dns.yaml
pnpm exec vitest run scripts/infra/zona.test.mjs   # espera FAIL em 'não declara wildcard'
git checkout infra/lotus-dns.yaml

# 2. um MX some
sed -i "/- '10 ALT4.ASPMX.L.GOOGLE.COM.'/d" infra/lotus-dns.yaml
pnpm exec vitest run scripts/infra/zona.test.mjs   # espera FAIL nos cinco MX
git checkout infra/lotus-dns.yaml

# 3. o Retain cai
sed -i '/^    DeletionPolicy: Retain$/d' infra/lotus-dns.yaml
pnpm exec vitest run scripts/infra/zona.test.mjs   # espera FAIL em 'protege a zona'
git checkout infra/lotus-dns.yaml
```

Cole as três linhas de falha no corpo do commit 2. A primeira mutação reprova dois testes ao mesmo tempo — o do wildcard e o de `www` — e isso é esperado.

- [ ] **Step 7: validar o YAML de verdade**

O leitor textual não valida estrutura de CloudFormation. Quem valida é a AWS:

```bash
AWS_PROFILE=lotus aws cloudformation validate-template --region us-east-1 \
  --template-body file://infra/lotus-dns.yaml
```

Esperado: JSON com `Parameters` listando `NomeDaZona`, `IpDoWordPress`, `Ipv6DoWordPress` e `TtlPadrao`, e `Description` não vazia. Erro aqui é erro de template, não de teste.

- [ ] **Step 8: gate completo**

```bash
pnpm check
```

Esperado: verde ponta a ponta. Se `format:check` reclamar do YAML ou do `.mjs`, rode `pnpm format` e confira o diff antes de aceitar — Prettier reescrevendo o template pode mudar a indentação que o leitor textual assume, e o `pnpm test` logo em seguida dirá se mudou.

- [ ] **Step 9: commit 1 — só o template**

```bash
git add infra/lotus-dns.yaml
git commit
```

Mensagem:

```text
feat(7.2.1): criar stack lotus-dns com a zona de lotusotec.cl

A zona nasce em stack proprio, e nao dentro de lotus-site: delete-stack do
site derrubaria a zona junto, e uma hosted zone recriada ganha nameservers
novos -- o que levaria o e-mail corporativo no mesmo movimento. Dai o Retain
em DeletionPolicy e UpdateReplacePolicy.

us-east-1 embora o Route 53 seja global, porque o certificado do bloco
seguinte entra neste mesmo stack e o CloudFront so le certificado de la.

Copia fiel do inventario de 2026-09-09, com duas diferencas deliberadas: o
wildcard nao atravessa, e www e sistema -- que hoje so respondem por causa
dele -- nascem explicitos. NS e SOA nao sao declarados; a zona gera os dois.

aws cloudformation validate-template --region us-east-1 aceitou o template.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

- [ ] **Step 10: commit 2 — a catraca**

```bash
git add scripts/infra/lib/zona.mjs scripts/infra/zona.test.mjs
git commit
```

Mensagem, com a saída real dos Steps 5 e 6 no corpo:

```text
test(7.2.1): travar o conteudo da zona contra o inventario

pnpm check nao fala DNS, e nenhum gate existente impediria o template de
perder um MX numa edicao futura. A catraca le infra/lotus-dns.yaml e exige
os onze registros do inventario medido, os cinco MX com as prioridades
certas, ausencia de wildcard, ausencia de NS/SOA declarados, ausencia de
alias de CloudFront e de certificado, e o Retain da zona.

Leitura textual, sem parser YAML: nem `yaml` nem `js-yaml` resolvem na
arvore e instalar um so para isto seria dependencia sem necessidade do work
item. A fragilidade e aceita de proposito -- ela quebra ruidosamente, e o
teste que exige exatamente 11 registros impede verde por vacuidade. A
validacao estrutural fica com aws cloudformation validate-template.

<saida do pnpm exec vitest run scripts/infra/zona.test.mjs>

Catraca provada por mutacao, tres vezes, cada uma revertida:
<as tres linhas de FAIL>

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

---

## Task 2: o change set, e a fronteira de quem executa

**Files:** nenhum arquivo do repositório muda. Esta task não gera commit.

**Interfaces:**

- Consumes: `infra/lotus-dns.yaml` da Task 1.
- Produces: o stack `lotus-dns` em `us-east-1`, e dois valores que a Task 3 lê — o output `NameServers` (quatro hostnames separados por vírgula) e o output `IdDaZona`.

### A fronteira, que não é deste plano e não se negocia aqui

O cabeçalho de `docs/infra/runbook-aws.md` fixa, desde 2026-09-03:

> Quem executa os comandos desta página é o João, no terminal dele ou no CloudShell. O agente
> escreve os templates, valida estrutura e cria change set; **não** executa change set e **não**
> cria recurso.

Vale aqui inteiro. O executor tem credencial funcionando nesta máquina — medido em 2026-09-20,
`AWS_PROFILE=lotus aws sts get-caller-identity` responde a conta `760144413534` —, e isso é
capacidade, não autorização. **O `deploy` sem `--no-execute-changeset` é de João.**

O que essa zona custa e o que ela não faz, para João decidir com o número na mão:

- **Faz:** cria uma hosted zone real, que passa a custar **US$ 0,50/mês** e que responde nos
  quatro nameservers da AWS para quem perguntar diretamente a eles.
- **Não faz:** não muda a delegação. Enquanto o registro `.cl` apontar para
  `ns1..ns4.stackdns.com`, **ninguém no mundo consulta esta zona por acidente**. Site e e-mail
  continuam servidos pelo lado antigo, byte a byte.
- **Não desfaz fácil:** `DeletionPolicy: Retain` faz `delete-stack` **deixar a zona para trás**.
  Removê-la é um `delete-hosted-zone` manual, e uma zona recriada ganha nameservers novos.

- [ ] **Step 1: conferir que não há zona duplicada** _(agente; leitura)_

Uma segunda hosted zone do mesmo domínio recebe nameservers diferentes, e o bloco da delegação
apontaria o registrador para o conjunto errado.

```bash
export AWS_PROFILE=lotus
aws route53 list-hosted-zones --query 'HostedZones[].Name' --output text
```

Esperado: `komit.cl.` e mais nada — foi o que respondeu em 2026-09-20. Se `lotusotec.cl.` já
aparecer, **pare** e leve o bloco a `blocked`: alguém criou a zona fora deste fluxo, e importá-la
para o stack é decisão de João.

- [ ] **Step 2: criar o change set, sem executar** _(agente)_

```bash
aws cloudformation deploy \
  --region us-east-1 \
  --stack-name lotus-dns \
  --template-file infra/lotus-dns.yaml \
  --tags Projeto=lotus-site \
  --no-execute-changeset
```

A saída imprime o comando `describe-change-set` com o ARN. Rode-o e leia a lista:

```bash
aws cloudformation describe-change-set --region us-east-1 \
  --change-set-name <arn-impresso-acima> \
  --query 'Changes[].ResourceChange.{Acao:Action,Tipo:ResourceType,Id:LogicalResourceId}' \
  --output table
```

Esperado: exatamente **duas** linhas — `Add` para `AWS::Route53::HostedZone` (`Zona`) e `Add`
para `AWS::Route53::RecordSetGroup` (`Registros`). Qualquer terceira linha é template errado.

Se o change set for **negado por SCP** — a conta é membro de organização de terceiro, com SCPs
ativas e invisíveis —, o bloco vai para `blocked` com a mensagem de erro literal no `blocker`.
Não tente contornar.

- [ ] **Step 3: entregar a João, com o que ele precisa para decidir** _(portão humano)_

Entregue estes dois comandos e o resumo de custo e reversibilidade acima:

```bash
export AWS_PROFILE=lotus

aws cloudformation deploy \
  --region us-east-1 \
  --stack-name lotus-dns \
  --template-file infra/lotus-dns.yaml \
  --tags Projeto=lotus-site

aws cloudformation describe-stacks --region us-east-1 --stack-name lotus-dns \
  --query 'Stacks[0].Outputs' --output table
```

Esperado da primeira: `Successfully created/updated stack - lotus-dns`. Esperado da segunda:
`NameServers` com quatro hostnames `ns-*.awsdns-*` separados por vírgula, e `IdDaZona` com um id
`Z...`.

**Enquanto João não executar, a Task 3 não tem o que conferir.** Não é motivo para `blocked` — é
espera declarada. A Task 4 é independente do stack `lotus-dns` e pode andar antes, ao custo de
inverter a ordem dos commits 3 e 4, o que é decisão de João e não do executor.

- [ ] **Step 4: prova mínima de que a zona responde** _(agente; leitura)_

Antes de confiar no script da Task 3, uma pergunta manual confirma que o caminho existe:

```bash
NS=$(aws cloudformation describe-stacks --region us-east-1 --stack-name lotus-dns \
  --query "Stacks[0].Outputs[?OutputKey=='NameServers'].OutputValue" --output text | cut -d, -f1)

export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
node --input-type=module -e "
import { Resolver, resolve4 } from 'node:dns/promises'
const [hostname] = process.argv.slice(1)
const [ip] = await resolve4(hostname)
const resolvedor = new Resolver()
resolvedor.setServers([ip])
console.log(hostname, ip, await resolvedor.resolveMx('lotusotec.cl'))
" "$NS"
```

Esperado: os cinco MX do Google, vindos do nameserver da AWS.

Se isto falhar com `ETIMEOUT` ou `ECONNREFUSED`, **UDP/53 não sai desta máquina**. Anote: é
exatamente o caso de degradação que a Task 3 trata, e a evidência sairá da API em vez da resposta
servida — afirmação mais fraca, e declarada como tal no relatório.

- [ ] **Step 5: confirmar que a delegação continua onde estava** _(agente; leitura)_

```bash
curl -s 'https://dns.google/resolve?name=lotusotec.cl&type=NS' | python3 -m json.tool | grep data
```

Esperado: os quatro `ns1..ns4.stackdns.com`. Se aparecer `awsdns`, alguém trocou os nameservers
fora deste fluxo — **pare**. Isso muda o risco de tudo o que vem depois, e o relatório da Task 3
passaria a significar outra coisa.

---

## Task 3: a conferência ao vivo, e a evidência

**Files:**

- Create: `scripts/infra/conferir-zona.mjs`
- Modify: `package.json` (uma linha em `"scripts"`, emenda E1b)
- Create: `docs/infra/conferencia-zona-<data>.md` (gerado, não escrito à mão)

**Interfaces:**

- Consumes: `INVENTARIO`, `NOMES_INVENTADOS`, `normalizar` e `mesmoConjunto` de `scripts/infra/lib/zona.mjs` (Task 1); os outputs `NameServers` e `IdDaZona` do stack (Task 2).
- Produces: o relatório versionado, que é a prova de aceite do bloco.

### Por que não basta perguntar "ao DNS"

A delegação de `lotusotec.cl` ainda aponta para a StackDNS. Uma consulta normal, de qualquer resolvedor, devolve o lado antigo — inclusive quando a zona nova está perfeita, e inclusive quando ela está errada. A única pergunta que enxerga a zona nova é a pergunta feita **diretamente aos nameservers dela**. É isso que o script faz, e é por isso que ele não pode ser substituído por `curl` ao `dns.google`.

Do outro lado, o script lê a zona atual por DNS-over-HTTPS contra `dns.google`, como o inventário de 2026-09-09 já fez — não há `dig` nesta máquina, medido em 2026-09-03 e ainda verdade em 2026-09-20.

- [ ] **Step 1: escrever o script**

`scripts/infra/conferir-zona.mjs`:

```js
// Confere a zona nova do Route 53 contra a zona que a StackDNS ainda serve,
// registro a registro, e grava a evidencia.
//
// Perguntar "ao DNS" nao serve aqui: a delegacao de lotusotec.cl ainda aponta
// para ns1..ns4.stackdns.com, entao qualquer consulta normal devolve o lado
// antigo -- esteja a zona nova certa ou errada. Este script pergunta DIRETO
// aos nameservers da zona nova e compara com o lado atual, lido por
// DNS-over-HTTPS porque nao ha `dig` nesta maquina.
//
// Uso: `pnpm infra:conferir-zona`
//   --nameservers ns-1.awsdns-01.org,ns-2.awsdns-02.com  pula a leitura do stack
//   --saida docs/infra/conferencia-zona-2026-09-20.md    muda o destino
//
// Sai com codigo 1 quando ha divergencia fora das esperadas.
import { execFileSync } from 'node:child_process'
import { Resolver, resolve4 } from 'node:dns/promises'
import { writeFileSync } from 'node:fs'
import {
  INVENTARIO,
  NOMES_INVENTADOS,
  mesmoConjunto,
  normalizar,
} from './lib/zona.mjs'

const STACK = 'lotus-dns'
const REGIAO = 'us-east-1'
const HOJE = new Date().toISOString().slice(0, 10)
const CODIGO_DOH = { A: 1, AAAA: 28, CNAME: 5, MX: 15, TXT: 16, NS: 2 }

/**
 * @typedef {{
 *   nome: string,
 *   tipo: string,
 *   inventario: string[],
 *   aws: string[],
 *   atual: string[],
 *   igual: boolean,
 *   esperado: boolean,
 *   nota: string,
 * }} Linha
 */

/** @param {string} nome */
function argumento(nome) {
  const posicao = process.argv.indexOf(`--${nome}`)
  return posicao === -1 ? undefined : process.argv[posicao + 1]
}

/** @param {string[]} args */
function aws(args) {
  return execFileSync('aws', args, { encoding: 'utf8' }).trim()
}

/** @param {string} chave */
function saidaDoStack(chave) {
  return aws([
    'cloudformation',
    'describe-stacks',
    '--region',
    REGIAO,
    '--stack-name',
    STACK,
    '--query',
    `Stacks[0].Outputs[?OutputKey=='${chave}'].OutputValue`,
    '--output',
    'text',
  ])
}

/**
 * `setServers` exige IP; o stack devolve hostname.
 * @param {string[]} hostnames
 */
async function enderecos(hostnames) {
  /** @type {{ hostname: string, ip: string }[]} */
  const lista = []
  for (const hostname of hostnames) {
    const [ip] = await resolve4(hostname)
    if (!ip) throw new Error(`${hostname} nao resolveu para nenhum IPv4`)
    lista.push({ hostname, ip })
  }
  return lista
}

/**
 * NXDOMAIN e "o nome existe mas nao tem esse tipo" sao resposta, nao falha:
 * viram lista vazia. Erro de rede continua subindo, porque ele muda a
 * natureza do relatorio.
 * @param {() => Promise<string[]>} consulta
 */
async function tolerandoAusencia(consulta) {
  try {
    return await consulta()
  } catch (erro) {
    const codigo = /** @type {NodeJS.ErrnoException} */ (erro).code
    if (codigo === 'ENOTFOUND' || codigo === 'ENODATA') return []
    throw erro
  }
}

/**
 * @param {Resolver} resolvedor
 * @param {string} nome
 * @param {string} tipo
 * @returns {Promise<string[]>}
 */
async function perguntarDireto(resolvedor, nome, tipo) {
  const alvo = nome.replace(/\.$/, '')
  if (tipo === 'A') return resolvedor.resolve4(alvo)
  if (tipo === 'AAAA') return resolvedor.resolve6(alvo)
  if (tipo === 'CNAME')
    return (await resolvedor.resolveCname(alvo)).map((v) => `${v}.`)
  if (tipo === 'NS')
    return (await resolvedor.resolveNs(alvo)).map((v) => `${v}.`)
  if (tipo === 'MX') {
    return (await resolvedor.resolveMx(alvo)).map(
      (v) => `${v.priority} ${v.exchange}.`,
    )
  }
  if (tipo === 'TXT')
    return (await resolvedor.resolveTxt(alvo)).map((p) => p.join(''))
  throw new Error(`tipo nao suportado: ${tipo}`)
}

/**
 * @param {string} nome
 * @param {string} tipo
 * @returns {Promise<string[]>}
 */
async function perguntarDoh(nome, tipo) {
  const url = `https://dns.google/resolve?name=${encodeURIComponent(nome)}&type=${tipo}`
  const resposta = await fetch(url, {
    headers: { accept: 'application/dns-json' },
  })
  if (!resposta.ok) {
    throw new Error(
      `dns.google respondeu ${resposta.status} para ${tipo} ${nome}`,
    )
  }
  /** @type {{ Answer?: { type: number, data: string }[] }} */
  const corpo = await resposta.json()
  const codigo = CODIGO_DOH[/** @type {keyof typeof CODIGO_DOH} */ (tipo)]
  return (corpo.Answer ?? [])
    .filter((registro) => registro.type === codigo)
    .map((registro) => registro.data)
}

/**
 * Caminho degradado: le a CONFIGURACAO da zona, nao a resposta servida. Sao
 * afirmacoes diferentes, e o relatorio diz qual delas ele carrega.
 * @returns {Map<string, string[]>}
 */
function zonaPelaApi() {
  const bruto = aws([
    'route53',
    'list-resource-record-sets',
    '--hosted-zone-id',
    saidaDoStack('IdDaZona'),
    '--output',
    'json',
  ])
  /** @type {{ ResourceRecordSets: { Name: string, Type: string, ResourceRecords?: { Value: string }[] }[] }} */
  const corpo = JSON.parse(bruto)
  /** @type {Map<string, string[]>} */
  const mapa = new Map()
  for (const conjunto of corpo.ResourceRecordSets) {
    mapa.set(
      `${conjunto.Name}|${conjunto.Type}`,
      (conjunto.ResourceRecords ?? []).map((registro) => registro.Value),
    )
  }
  return mapa
}

/** @param {string[]} valores */
function celula(valores) {
  return valores.length === 0
    ? '—'
    : valores.map((v) => `\`${v}\``).join('<br>')
}

const nomesDeServidor = (
  argumento('nameservers') ?? saidaDoStack('NameServers')
)
  .split(',')
  .map((valor) => valor.trim())
  .filter(Boolean)

if (nomesDeServidor.length === 0) {
  throw new Error(
    'nenhum nameserver: passe --nameservers ou implante o stack lotus-dns',
  )
}

const servidores = await enderecos(nomesDeServidor)
const primeiro = servidores[0]
if (!primeiro) throw new Error('nenhum nameserver resolveu para IPv4')

// Sonda unica: se UDP/53 nao sai desta maquina, o lado AWS inteiro passa a
// ser lido pela API, e o relatorio declara a troca.
/** @type {Map<string, string[]> | undefined} */
let porApi
try {
  const sonda = new Resolver()
  sonda.setServers([primeiro.ip])
  await tolerandoAusencia(() => perguntarDireto(sonda, 'lotusotec.cl.', 'A'))
} catch (erro) {
  console.error(
    `UDP/53 indisponivel (${String(erro)}); lendo o lado AWS pela API`,
  )
  porApi = zonaPelaApi()
}

/**
 * @param {string} nome
 * @param {string} tipo
 * @returns {Promise<string[]>}
 */
async function ladoAws(nome, tipo) {
  if (porApi) return porApi.get(`${nome}|${tipo}`) ?? []
  /** @type {string[][]} */
  const respostas = []
  for (const servidor of servidores) {
    const resolvedor = new Resolver()
    resolvedor.setServers([servidor.ip])
    respostas.push(
      await tolerandoAusencia(() => perguntarDireto(resolvedor, nome, tipo)),
    )
  }
  const primeira = respostas[0] ?? []
  if (!respostas.every((resposta) => mesmoConjunto(resposta, primeira))) {
    throw new Error(
      `os nameservers da AWS discordam entre si em ${tipo} ${nome}`,
    )
  }
  return primeira
}

/** @type {Linha[]} */
const linhas = []

for (const registro of INVENTARIO) {
  const naAws = await ladoAws(registro.nome, registro.tipo)
  const naStack = await perguntarDoh(registro.nome, registro.tipo)
  linhas.push({
    nome: registro.nome,
    tipo: registro.tipo,
    inventario: [...registro.valores],
    aws: naAws,
    atual: naStack,
    igual:
      mesmoConjunto(naAws, naStack) &&
      mesmoConjunto(naAws, [...registro.valores]),
    esperado: false,
    nota: '',
  })
}

// O wildcard nao atravessou: nome inventado nao resolve na AWS e resolve na
// StackDNS. E a unica divergencia esperada do relatorio.
for (const inventado of NOMES_INVENTADOS) {
  const naAws = await ladoAws(inventado, 'A')
  const naStack = await perguntarDoh(inventado, 'A')
  linhas.push({
    nome: inventado,
    tipo: 'A',
    inventario: [],
    aws: naAws,
    atual: naStack,
    igual: mesmoConjunto(naAws, naStack),
    esperado: naAws.length === 0 && naStack.length > 0,
    nota: 'wildcard removido de proposito',
  })
}

// A delegacao nao e deste bloco. Se ela ja tiver mudado, o relatorio inteiro
// muda de significado.
const delegacao = await perguntarDoh('lotusotec.cl.', 'NS')
const delegacaoIntacta = delegacao.every((valor) =>
  normalizar(valor).includes('stackdns.com'),
)
linhas.push({
  nome: 'lotusotec.cl.',
  tipo: 'NS',
  inventario: ['ns1..ns4.stackdns.com.'],
  aws: [],
  atual: delegacao,
  igual: delegacaoIntacta,
  esperado: delegacaoIntacta,
  nota: 'delegacao inalterada, como o bloco exige',
})

const problemas = linhas.filter((linha) => !linha.igual && !linha.esperado)

const relatorio = [
  `# Conferência da zona de \`lotusotec.cl\` — ${HOJE}`,
  '',
  '> Gerado por `pnpm infra:conferir-zona`. Não editar à mão.',
  '>',
  porApi
    ? '> **Degradado.** UDP/53 não saiu desta máquina, então o lado AWS foi lido da' +
      ' **configuração** (`aws route53 list-resource-record-sets`), e não da resposta servida.' +
      ' São afirmações diferentes: esta prova que a zona está escrita assim, não que ela' +
      ' responde assim.'
    : '> Lado AWS lido da **resposta servida**, perguntando direto aos quatro nameservers da' +
      ' zona nova — a delegação ainda aponta para a StackDNS, então nenhuma consulta comum' +
      ' enxergaria esta zona.',
  '>',
  '> Lado atual lido por DNS-over-HTTPS contra `dns.google`, mesmo método do inventário de' +
    ' 2026-09-09.',
  '',
  `Nameservers da zona nova: ${servidores
    .map((servidor) => `\`${servidor.hostname}\` (${servidor.ip})`)
    .join(', ')}`,
  '',
  '| Nome | Tipo | Inventário | AWS | StackDNS | Confere? |',
  '| ---- | ---- | ---------- | --- | -------- | -------- |',
  ...linhas.map((linha) => {
    const veredito = linha.igual
      ? 'sim'
      : linha.esperado
        ? `divergência esperada — ${linha.nota}`
        : '**NÃO**'
    return `| \`${linha.nome}\` | \`${linha.tipo}\` | ${celula(linha.inventario)} | ${celula(
      linha.aws,
    )} | ${celula(linha.atual)} | ${veredito} |`
  }),
  '',
  problemas.length === 0
    ? 'Nenhuma divergência fora das esperadas.'
    : `**${problemas.length} divergência(s) não esperada(s).** A troca de nameservers não pode` +
      ' acontecer enquanto elas existirem.',
  '',
  'Resolução correta não prova entrega de e-mail. A prova do MX é mensagem recebida, e ela só é',
  'possível depois da delegação — que não é deste bloco.',
  '',
].join('\n')

const saida = argumento('saida') ?? `docs/infra/conferencia-zona-${HOJE}.md`
writeFileSync(saida, relatorio)
console.log(relatorio)
console.error(`relatório em ${saida}`)

if (problemas.length > 0) process.exit(1)
```

- [ ] **Step 2: alias no `package.json`**

Depois de `"qa:espacamento": "node scripts/qa/medir-espacamento.mjs",`, acrescente:

```json
    "infra:conferir-zona": "node scripts/infra/conferir-zona.mjs",
```

- [ ] **Step 3: typecheck antes de rodar**

`scripts/**/*.mjs` entra em `tsconfig.node.json` com `checkJs` e `strict`. Rodar o script antes de typechecá-lo desperdiça uma ida à rede:

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm typecheck
```

Esperado: sem saída. Os erros prováveis, todos de `noUncheckedIndexedAccess`: acesso a `servidores[0]`, a `respostas[0]` e ao grupo de captura de um `match`. Todos já têm guarda no código acima; se aparecer um novo, resolva com guarda, **nunca** com `!` ou `any`.

- [ ] **Step 4: rodar contra a zona real**

```bash
export AWS_PROFILE=lotus
pnpm infra:conferir-zona
```

Esperado: a tabela com 14 linhas — os onze do inventário, os dois nomes inventados e a delegação —, `Nenhuma divergência fora das esperadas.` e saída 0. O relatório fica em `docs/infra/conferencia-zona-<data>.md`.

Leia a tabela linha a linha antes de aceitar. Em especial:

- os **cinco** MX, com `1`, `5`, `5`, `10`, `10`;
- os dois nomes inventados com `—` na coluna AWS e o IP do WordPress na coluna StackDNS;
- a linha `NS` mostrando `stackdns.com`, não `awsdns`.

- [ ] **Step 5: provar que o script reprova**

Um script de conferência que nunca reprovou não é evidência de nada:

```bash
pnpm infra:conferir-zona --nameservers ns-1.awsdns-01.org
echo "saida=$?"
```

Um nameserver que não é desta zona responde `REFUSED`/vazio para tudo, então a coluna AWS vem vazia contra uma coluna StackDNS cheia. Esperado: `**NÃO**` em várias linhas e `saida=1`. **Desfaça o relatório sujo** que esta corrida gravou:

```bash
git checkout docs/infra/conferencia-zona-*.md 2>/dev/null || true
```

e rode o Step 4 de novo para regravar o relatório bom.

- [ ] **Step 6: gate completo**

```bash
pnpm check
```

Esperado: verde. `pnpm test` continua sem rede — `conferir-zona.mjs` não tem teste próprio porque sua parte pura (`normalizar`, `mesmoConjunto`) já é testada em `scripts/infra/zona.test.mjs`, e o resto é rede e `aws`, que não se testa sem mock de valor duvidoso.

- [ ] **Step 7: commit 3**

```bash
git add scripts/infra/conferir-zona.mjs package.json docs/infra/conferencia-zona-*.md
git commit
```

Mensagem, com a tabela real no corpo:

```text
feat(7.2.1): conferir a zona nova contra a StackDNS

A delegacao de lotusotec.cl ainda aponta para a StackDNS, entao consulta
comum devolve o lado antigo -- esteja a zona nova certa ou errada. O script
pergunta direto aos quatro nameservers da zona nova, compara com o lado
atual lido por DNS-over-HTTPS (nao ha dig nesta maquina) e grava a
evidencia. Sai 1 na primeira divergencia nao esperada.

Divergencia esperada, e unica: nome inventado nao resolve na AWS e resolve
na StackDNS. E a prova de que o wildcard de D-45 nao atravessou.

Se UDP/53 nao sair da maquina, o lado AWS cai para
route53 list-resource-record-sets e o relatorio declara a troca no
cabecalho: configuracao escrita nao e resposta servida.

<tabela do relatorio>

Reprovacao provada apontando o script para um nameserver de outra zona:
saida 1.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

---

## Task 4: o teto de custo, o runbook e a dívida declarada

**Files:**

- Modify: `infra/lotus-site.yaml` (só o filtro do Budget e a descrição do parâmetro do teto)
- Modify: `docs/infra/runbook-aws.md` (secção 6 inteira)
- Modify: `docs/infra/zona-dns-lotusotec.md`
- Modify: `docs/superpowers/backlog.md`
- Modify: `docs/superpowers/state.md`

**Interfaces:**

- Consumes: os outputs da Task 2 e o relatório da Task 3.
- Produces: nada que outra task deste bloco leia. É a última.

- [ ] **Step 1: ampliar o filtro do Budget**

Em `infra/lotus-site.yaml`, na descrição do parâmetro do teto:

```yaml
TetoMensalUSD:
  Type: Number
  Default: 30
  MinValue: 1
  Description: Teto mensal em USD somando S3, CloudFront e Route 53.
```

E no recurso `Teto`, **uma linha** a mais no filtro, mais o comentário que explica o efeito
colateral:

```yaml
# Filtro por SERVICO e nao por tag: tag de alocacao de custo precisa
# ser ativada no painel de billing e leva ate 24h para valer, e um
# budget que nao mede nada e pior que nenhum.
#
# Route 53 entra em 7.2.1, com a zona de lotusotec.cl. O filtro e da
# CONTA, nao do stack: a zona komit.cl, que ja existia nesta conta
# antes de nos, passa a ser contada tambem. E o comportamento correto
# -- o teto existe para a conta -- mas a primeira fatura filtrada
# parece erro se isto nao estiver escrito.
CostFilters:
  Service:
    - Amazon Simple Storage Service
    - Amazon CloudFront
    - Amazon Route 53
```

`Amazon Route 53` é a string exata da dimensão `SERVICE` do Cost Explorer, medida em 2026-09-20
com `aws ce get-dimension-values --dimension SERVICE --search-string Route`. Nome errado **não dá
erro**: o filtro simplesmente deixa de medir, em silêncio — que é o fracasso que este passo
existe para evitar.

- [ ] **Step 2: change set do stack do site, e o portão de João**

Mesma fronteira da Task 2: o agente cria o change set, João executa.

```bash
export AWS_PROFILE=lotus

aws cloudformation validate-template --region sa-east-1 \
  --template-body file://infra/lotus-site.yaml

aws cloudformation describe-stacks --region sa-east-1 --stack-name lotus-site \
  --query 'Stacks[0].Parameters' --output table
```

O segundo comando existe por um motivo específico: `EmailDeAlerta` **não tem default**. Um deploy
sem `--parameter-overrides` usa o valor anterior, mas se algo forçar a repassar parâmetros, o
destinatário do alerta pode ser trocado por engano. Leia o valor atual antes.

```bash
aws cloudformation deploy \
  --region sa-east-1 \
  --stack-name lotus-site \
  --template-file infra/lotus-site.yaml \
  --capabilities CAPABILITY_IAM \
  --no-execute-changeset
```

Confira, no `describe-change-set`, que o change set toca **apenas** `Teto`. Distribuição, bucket,
OAC e role não podem aparecer; se aparecerem, o template foi editado além do combinado.

Entregue a João o mesmo comando sem `--no-execute-changeset`. Depois que ele executar, prove
(leitura, agente):

```bash
aws budgets describe-budget --region us-east-1 \
  --account-id 760144413534 \
  --budget-name lotus-site-teto \
  --query 'Budget.CostFilters' --output json
```

Esperado: os três serviços na lista, `Amazon Route 53` inclusive. Cole a saída no corpo do
commit 4. A API de Budgets é global com endpoint em `us-east-1`, como o runbook já registra.

- [ ] **Step 3: reescrever a secção 6 do runbook**

Substitua o corpo das subsecções abaixo em `docs/infra/runbook-aws.md`. A secção 6 deixa de
descrever cliques e change batch e passa a descrever o stack; o que sobra de CLI é o que continua
sendo feito à mão.

**6.0** — o item 1 fecha, o item 2 vira o bloqueio real:

```markdown
### 6.0 Antes de qualquer comando

1. ~~Recuperar o acesso ao painel~~ — **feito**. João confirmou em 2026-09-20 que tem acesso à
   tela de nameservers (`D-44` fechado). É declaração, não medição nossa: quem executar a troca
   comprova na hora.
2. **Pedir o export BIND da zona** ao suporte da BlueHosting. **Ainda pendente, e é ele que
   autoriza a troca.** Com wildcard na zona (`D-45`), enumerar por DNS devolve resposta para
   qualquer palpite: não há como afirmar que a cópia está completa sem o export, e um registro
   esquecido só aparece depois da troca, como falha intermitente, para parte do mundo.

A EAP `7.2.1` cobre da criação da zona até o HTTPS válido. O bloco de 2026-09-20 entregou só o
primeiro pedaço: a zona existe, está conferida e **não** está delegada. A ordem do que falta é
export BIND, troca de nameservers (6.4), certificado (6.6) e alias do CloudFront (`B5`).

Em paralelo, e num lugar diferente: abrir o pedido de **production access do SES** no suporte da
**AWS** — não no da BlueHosting. Ele tem espera e trava `B2` se ficar para depois.
```

**6.1 e 6.2** — viram uma só, porque a zona deixou de ser feita à mão:

````markdown
### 6.1 Criar e popular a zona

A zona é um stack, `infra/lotus-dns.yaml`, e não uma sequência de `change-resource-record-sets`.
Editar registros à mão funciona e **é desfeito pelo próximo deploy**: o template é a fonte.

```bash
export AWS_PROFILE=lotus

aws cloudformation deploy \
  --region us-east-1 \
  --stack-name lotus-dns \
  --template-file infra/lotus-dns.yaml \
  --tags Projeto=lotus-site

aws cloudformation describe-stacks --region us-east-1 --stack-name lotus-dns \
  --query 'Stacks[0].Outputs' --output table
```

`us-east-1` de propósito, embora o Route 53 seja global: o certificado de 6.6 entra neste mesmo
stack, e o CloudFront só lê certificado de lá.

O output `NameServers` traz os quatro `ns-*.awsdns-*` que vão para o registrador no passo 6.4.
`IdDaZona` é a entrada de `list-resource-record-sets`.

Três regras que o template já aplica e que edição manual não deve desfazer:

- **O MX do Google entra idêntico.** É o e-mail da empresa.
- **O wildcard `*` não entra.** `www` e `sistema`, que hoje só respondem por causa dele, nascem
  explícitos. Se o export BIND revelar outro nome nessa situação, ele vira registro explícito no
  template, um a um — não um wildcard de volta.
- **O apontamento para o CloudFront não entra agora.** `B1` move a zona sem mudar o que ela
  responde; apontar o site é `B5`.

A catraca `scripts/infra/zona.test.mjs` roda em `pnpm check` e reprova se qualquer uma das três
for violada no template.
````

**6.3** — a conferência vira um comando:

````markdown
### 6.3 Conferir antes de trocar

```bash
pnpm infra:conferir-zona
```

Pergunta direto aos quatro nameservers da zona nova — consulta comum devolveria o lado antigo,
porque a delegação ainda aponta para a StackDNS —, compara com esse lado atual por
DNS-over-HTTPS, grava `docs/infra/conferencia-zona-<data>.md` e sai 1 na primeira divergência não
esperada.

A única divergência esperada: nome inventado não resolve na AWS e resolve na StackDNS. É a prova
de que o wildcard não atravessou.

Se UDP/53 não sair da máquina, o lado AWS cai para `route53 list-resource-record-sets` e o
relatório **declara a troca no cabeçalho**: configuração escrita não é resposta servida.

Diferença aqui é barata; diferença depois do passo 6.4 é serviço fora do ar para parte do mundo.
````

**6.6** — o certificado passa a ser recurso do template:

````markdown
### 6.6 Certificado no ACM

**Só depois de 6.4.** A validação DNS-01 precisa que o mundo leia a zona da AWS; com a delegação
ainda na StackDNS, o CNAME de validação existe e ninguém o enxerga.

Caminho padrão: um `AWS::CertificateManager::Certificate` no stack `lotus-dns`, com
`DomainValidationOptions.HostedZoneId` apontando para a própria zona — o ACM cria e remove o CNAME
de validação sozinho, e a renovação é automática e silenciosa.

```yaml
Certificado:
  Type: AWS::CertificateManager::Certificate
  Properties:
    DomainName: !Ref NomeDaZona
    SubjectAlternativeNames:
      - !Sub 'www.${NomeDaZona}'
    ValidationMethod: DNS
    DomainValidationOptions:
      - DomainName: !Ref NomeDaZona
        HostedZoneId: !Ref Zona
      - DomainName: !Sub 'www.${NomeDaZona}'
        HostedZoneId: !Ref Zona
```

SANs explícitos, **nunca** `*.lotusotec.cl`: o wildcard amplia o raio de uma chave comprometida e
esconde o inventário de nomes.

Três armadilhas, todas de ordem:

1. **Pedido pendente do ACM é cancelado em 72h.** Pedir antes de a delegação funcionar queima o
   pedido.
2. **Com `HostedZoneId`, o stack fica em `CREATE_IN_PROGRESS` até validar.** Zona não delegada
   significa stack travado até o timeout, e depois rollback.
3. **`CAA` restringindo emissão a `amazon.com` entra depois do primeiro `ISSUED`, nunca antes.**
   CAA errado bloqueia a própria renovação. A zona não tem CAA hoje (medido em 2026-09-09), e é
   por isso que o ACM emite sem obstáculo.

Fallback declarado, para o caso de o recurso do template travar e ser preciso emitir à mão:

```bash
aws acm request-certificate --region us-east-1 \
  --domain-name lotusotec.cl \
  --subject-alternative-names www.lotusotec.cl \
  --validation-method DNS \
  --query CertificateArn --output text

aws acm describe-certificate --region us-east-1 --certificate-arn <arn> \
  --query 'Certificate.DomainValidationOptions[].ResourceRecord'

aws acm wait certificate-validated --region us-east-1 --certificate-arn <arn>
```

Um certificado emitido por este caminho **não** é gerenciado pelo stack, e o próximo deploy
tentaria criar outro. Se as duas descrições divergirem, a do template vence (`D-48`).
````

**6.7** — encolhe, porque o registro já está no template:

```markdown
### 6.7 Subdomínios

Com a zona no Route 53 não há limite de um subdomínio. `sistema.lotusotec.cl` já nasce no template
como `A` para o WordPress — hoje ele só resolve por causa do wildcard, e a troca o apagaria se ele
não estivesse declarado.

Subdomínio novo é uma entrada a mais em `RecordSets` **e** uma linha a mais no inventário de
`scripts/infra/lib/zona.mjs`, senão a catraca reprova. Não é `change-resource-record-sets` à mão.

Criar esse registro é DNS e nada mais. A integração com a API do Lotus (`8.2.1`) está congelada
por decisão de João em 2026-09-09 e não é aberta por este comando.
```

**6.8** — passa a registrar o que foi feito:

````markdown
### 6.8 Budget

**Feito em `7.2.1`.** `CostFilters.Service` em `infra/lotus-site.yaml` lista
`Amazon Simple Storage Service`, `Amazon CloudFront` e `Amazon Route 53`. SES e Lambda entram no
mesmo lugar em `B2`.

O filtro é da **conta**, não do stack: a zona `komit.cl`, que já existia nesta conta antes de nós,
passa a ser contada também.

```bash
aws budgets describe-budget --region us-east-1 --account-id 760144413534 \
  --budget-name lotus-site-teto --query 'Budget.CostFilters' --output json
```
````

**6.9, novo** — o desmonte da zona, que a secção 7 não cobre:

````markdown
### 6.9 Desmontar a zona (leia antes de pensar em fazer)

`delete-stack` de `lotus-dns` **não apaga a zona**: `DeletionPolicy: Retain` a deixa para trás, de
propósito. Apagá-la é um ato manual e separado:

```bash
aws route53 delete-hosted-zone --id <IdDaZona>
```

Uma hosted zone recriada ganha **nameservers novos**. Se a zona já estiver delegada nessa altura,
apagá-la derruba o site e o e-mail corporativo no mesmo movimento, e recriá-la não conserta — é
preciso voltar ao painel do registrador com os quatro nomes novos e esperar a propagação outra
vez.
````

- [ ] **Step 4: atualizar o inventário**

Em `docs/infra/zona-dns-lotusotec.md`:

1. Na secção "Delegação", trocar a frase final por:

```markdown
O painel é o Stack Control Panel (`https://www.stackcp.com/`), revenda da BlueHosting, onde o
domínio foi comprado. João confirmou em 2026-09-20 que recuperou o acesso (`D-44` fechado); o
export BIND continua pendente, e é ele que autoriza a troca.
```

2. Acrescentar, antes da secção "Conferência antes e depois da troca":

```markdown
## Cópia no Route 53

Desde 2026-09-20 a zona existe também no Route 53, criada pelo stack `lotus-dns`
(`infra/lotus-dns.yaml`, `us-east-1`), **sem delegação**: o registro `.cl` continua apontando para
`ns1..ns4.stackdns.com`, então este arquivo continua descrevendo o que o mundo lê.

A cópia tem duas diferenças deliberadas em relação ao que está medido acima:

- **não tem wildcard** — `www` e `sistema` viraram registro explícito no lugar dele;
- **não declara `NS` nem `SOA`** do apex, que a própria zona gera.

Conferência registro a registro em `conferencia-zona-2026-09-20.md`, gerada por
`pnpm infra:conferir-zona`. A catraca `scripts/infra/zona.test.mjs` impede o template de divergir
desta medição sem reprovar `pnpm check`.
```

3. Na secção "Conferência antes e depois da troca", trocar o bloco de `dig` — que não existe nesta
   máquina — pelo comando real:

````markdown
```bash
# antes da troca: perguntar direto aos nameservers da AWS, sem depender da delegação
pnpm infra:conferir-zona

# depois da troca: recebimento real, não só resolução
# enviar uma mensagem de fora para uma caixa @lotusotec.cl e confirmar a entrega
```
````

- [ ] **Step 5: atualizar o backlog**

Quatro edições em `docs/superpowers/backlog.md`. **Nenhuma pode escrever a string
`workflow_state`** — `checkBacklog` reprova o `pnpm agent:check`.

1. **`D-44` sai de Abertos e entra em Fechados**, com o desfecho:

```markdown
- **D-44 · o acesso ao painel de DNS foi perdido** — o domínio é comprado na BlueHosting e a zona
  é gerenciada em `https://www.stackcp.com/` (Stack Control Panel). João informou em 2026-09-09
  que não tinha mais acesso; travava `B1` inteiro, e `B1` é o gargalo de `B2` a `B7`.
  **Fechado em 2026-09-20**, por confirmação de João de que o acesso à tela de nameservers
  voltou. É declaração, não medição nossa: quem executar a troca comprova na hora.
```

2. **`D-45` ganha o desfecho desta rodada**, substituindo o `**Gatilho:**` da ficha:

```markdown
Desfecho parcial em 2026-09-20 (`7.2.1`): a zona nova nasceu **sem** wildcard, e os dois nomes
que dependiam dele e não podiam quebrar — `www` e `sistema` — nasceram explícitos. A decisão de
remover está tomada e provada no template; o que continua aberto é a outra metade, a que só o
export BIND resolve: **quais outros nomes existem**. Por isso a delegação não foi trocada.
**Gatilho:** export BIND na mão de João.
```

3. **Dois débitos novos**, no fim de Abertos:

```markdown
- **D-47 · `7.2.1` fechou parcial: sem delegação, sem certificado, sem HTTPS** — o critério de
  aceite da EAP no Notion pede domínio resolvendo e HTTPS válido. O bloco de 2026-09-20 entregou
  só a zona: criada no Route 53, conferida registro a registro contra a StackDNS e travada por
  catraca, mas **não delegada**. Sem delegação não há validação DNS-01, sem ela não há
  certificado, e sem certificado não há HTTPS. A EAP **não** foi marcada como concluída, e não
  havia autorização de escrita no Notion nesta rodada de qualquer forma. O que falta, em ordem:
  export BIND, troca de nameservers, `Certificate` no stack `lotus-dns`, alias do CloudFront
  (`B5`).
  **Gatilho:** export BIND na mão de João.
- **D-48 · o runbook descreve duas formas de emitir o certificado** — a secção 6.6 passou a
  descrever o recurso `AWS::CertificateManager::Certificate` no stack `lotus-dns` como caminho
  padrão, e manteve `aws acm request-certificate` como fallback declarado. Duas descrições da
  mesma coisa divergem com o tempo, e um certificado emitido pela CLI não é gerenciado pelo
  stack: o próximo deploy tentaria criar outro. Enquanto as duas convivem, **a do template
  vence**.
  **Gatilho:** ao emitir o certificado, no bloco da delegação.
```

4. **O bloco `B1` e as pendências**:

- em `## B1`, acrescentar ao fim:

```markdown
- **Entregue em 2026-09-20 (parcial):** zona criada no Route 53 pelo stack `lotus-dns`, conferida
  contra a StackDNS registro a registro, catraca offline dentro do `pnpm check`, e filtro do
  Budget ampliado para Route 53. **Não** entregue: delegação, certificado, HTTPS, alias. Ver
  `D-47`.
```

- em `# PENDÊNCIAS`, trocar os itens 1, 2 e 5:

```markdown
1. ~~**Acesso ao painel de DNS.**~~ Recuperado; confirmado por João em 2026-09-20. Ver `D-44`,
   fechado.
2. **Export BIND da zona**, a pedir ao suporte da BlueHosting. **É o que falta para a troca de
   nameservers acontecer.** A enumeração por DNS não enxerga registro que ninguém adivinhou, e o
   wildcard (`D-45`) faz qualquer palpite responder.
```

```markdown
5. **Autorização de escrita no Notion.** Concedida em 2026-09-09 para registrar as decisões
   daquele bloco. **Não vale para esta rodada**: a EAP `7.2.1` não foi marcada no Notion, e o
   aceite parcial está declarado em `D-47`.
```

- [ ] **Step 6: mover o estado para review**

Em `docs/superpowers/state.md`, no front matter:

```yaml
workflow_state: ready_for_review
work_class: architectural
active_work_item: 7.2.1
active_notion_eap: 7.2.1
active_title: Configurar domínio, DNS, HTTPS e redirects necessários
active_branch: feat/7-2-1-zona-dns-route53
bounded_design: null
authorized_paths: null
next_owner: codex
next_action: review_active_work_item
context_packet: null
active_spec: docs/superpowers/specs/2026-09-20-7.2.1-zona-dns-route53-design.md
active_plan: docs/superpowers/plans/2026-09-20-7.2.1-zona-dns-route53.md
executor: claude
reviewer: codex
reviewer_exception: null
state_basis_commit: <sha do commit 3>
updated_at: <data real>
```

`executor` e `reviewer` diferentes e `reviewer_exception` limpa: com os dois iguais e o campo
vazio, `pnpm agent:check` reprova; com os dois diferentes e o campo preenchido, também.

- [ ] **Step 7: gate completo**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm check
```

Esperado: verde. `agent:check` é quem valida o front matter acima e a ausência de `workflow_state`
no backlog.

- [ ] **Step 8: commit 4**

```bash
git add infra/lotus-site.yaml docs/infra/runbook-aws.md docs/infra/zona-dns-lotusotec.md \
        docs/superpowers/backlog.md docs/superpowers/state.md
git commit
```

Mensagem:

```text
chore(7.2.1): ampliar o Budget para Route 53 e atualizar o runbook

Com a zona criada, o teto de US$ 30 deixaria de medir parte da conta.
`Amazon Route 53` e a string exata da dimensao SERVICE do Cost Explorer,
medida com aws ce get-dimension-values -- nome errado nao da erro, o filtro
so para de medir, em silencio. O filtro e da conta: a zona komit.cl, que ja
existia aqui antes de nos, passa a ser contada tambem.

<saida de aws budgets describe-budget>

A seccao 6 do runbook deixa de descrever cliques e change batch: a zona e um
stack, a conferencia e um comando, e o certificado passa a ser recurso do
template com a CLI como fallback declarado (D-48). Entra o 6.9, que faltava:
Retain deixa a zona para tras no delete-stack, e apagar zona ja delegada
derruba site e e-mail sem conserto rapido.

D-44 fecha: o acesso ao painel voltou. D-45 fica aberto pela metade que so o
export BIND resolve. D-47 registra que a 7.2.1 fecha parcial -- sem
delegacao, sem certificado, sem HTTPS -- e que a EAP nao foi marcada no
Notion.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

---

## Ordem e dependências entre as tasks

```text
Task 1  template + catraca          ── sem rede, sem credencial, sem portão
   │
   └─▶ Task 2  change set + deploy   ── PORTÃO: o deploy é de João (runbook, 2026-09-03)
          │                             sem ele, a zona não existe
          │
          └─▶ Task 3  conferência    ── precisa da zona implantada e de rede
                                        UDP/53 bloqueado degrada a prova, não a impede

Task 4  budget + runbook + dívida    ── só o Step 2 tem portão de João
                                        independente das Tasks 2 e 3
```

A **Task 2** é o único ponto do bloco que depende de alguém fora desta máquina. Se João demorar a
executar, a Task 4 continua executável — mas isso inverte a ordem dos commits 3 e 4, e inverter é
decisão de João, não do executor.

A **Task 3** é a única que depende de rede. Se UDP/53 não sair do WSL, ela **não trava**: cai para
a leitura pela API, e o relatório declara a degradação no cabeçalho. O que ela não pode fazer é
degradar em silêncio.

## O que não é do executor, em nenhuma hipótese

- **Trocar os nameservers no painel da BlueHosting.** É o passo irreversível na prática — durante
  a propagação, parte do mundo lê um lado e parte lê o outro — e ele não acontece sem o export
  BIND. Não é deste bloco.
- **Emitir certificado, por qualquer caminho.** Pedido pendente do ACM morre em 72h, e com
  `HostedZoneId` o stack trava em `CREATE_IN_PROGRESS` até validar. Sem delegação, os dois falham.
- **Apontar qualquer coisa para o CloudFront.** Alias do apex e de `www` são `7.2.5` / `B5`.
- **Executar change set ou criar recurso na AWS.** O runbook fixa a fronteira desde 2026-09-03; o
  agente escreve template, valida e cria change set.
- **Escrever no Notion** (Lei 6). Não há autorização nesta rodada, e o aceite da EAP não fecha de
  qualquer forma (`D-47`).
- **`git push` ou `gh pr create`** (Lei 6). Só `/fechar-site`.
- **Instalar dependência** (Lei 7), inclusive um parser YAML "só para o teste".
- **Tocar `src/`, `public/`, `e2e/` ou `index.html`.** Nenhum pixel do produto muda.
- **Mexer no stack `lotus-oidc-github` ou na distribuição.** Em `infra/lotus-site.yaml`, a única
  coisa que muda é o filtro do Budget e a descrição do parâmetro do teto.

## Prova de aceite

O critério de aceite da EAP no Notion — "domínio de staging/teste resolve corretamente, HTTPS está
válido e canonical/redirects não criam loop" — **não fecha nesta rodada**. Sem delegação não há
certificado, e sem certificado não há HTTPS. O bloco fecha como **parcial declarado**, e `D-47`
nomeia o que falta.

O que este bloco prova, e onde:

| Afirmação                                                   | Como se prova                                                                     | Onde fica                                         |
| ----------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------- |
| o template descreve a zona medida, e não pode divergir dela | `pnpm test` com `scripts/infra/zona.test.mjs`, mais as três mutações que reprovam | corpo do commit 2                                 |
| o template é CloudFormation válido                          | `aws cloudformation validate-template --region us-east-1`                         | corpo do commit 1                                 |
| a zona existe na AWS                                        | `describe-stacks` com os outputs `NameServers` e `IdDaZona`                       | corpo do commit 3                                 |
| ela responde igual à zona atual, registro a registro        | `pnpm infra:conferir-zona` sem divergência não esperada                           | `docs/infra/conferencia-zona-<data>.md`, commit 3 |
| os cinco MX do Google estão intactos                        | linha `MX` do mesmo relatório, com as cinco prioridades                           | mesmo relatório                                   |
| o wildcard não atravessou                                   | nome inventado com `—` na coluna AWS e IP na coluna StackDNS                      | mesmo relatório                                   |
| a delegação não mudou                                       | linha `NS` do relatório devolvendo `stackdns.com`                                 | mesmo relatório                                   |
| o script de conferência sabe reprovar                       | corrida contra um nameserver de outra zona, saída 1                               | corpo do commit 3                                 |
| o teto de custo mede Route 53                               | `aws budgets describe-budget --query 'Budget.CostFilters'`                        | corpo do commit 4                                 |

Gates genéricos: `pnpm check` verde a cada commit, e verde no HEAD da branch. `pnpm e2e` **não se
aplica** — o bloco não toca `src/`, `public/`, `e2e/` nem `index.html`, e nenhum pixel do produto
muda.

Prova que este bloco **não** pode dar, e que fica registrada como tal: **recebimento de e-mail**.
Resolução correta não prova entrega; a prova do MX é mensagem recebida, e ela só existe depois da
delegação.

## Handoff de execução

executor: claude
reviewer: codex
paths_autorizados:

- `infra/lotus-dns.yaml`
- `infra/lotus-site.yaml`
- `scripts/infra/lib/zona.mjs`
- `scripts/infra/zona.test.mjs`
- `scripts/infra/conferir-zona.mjs`
- `package.json`
- `docs/infra/runbook-aws.md`
- `docs/infra/zona-dns-lotusotec.md`
- `docs/infra/conferencia-zona-2026-09-20.md`
- `docs/superpowers/state.md`
- `docs/superpowers/backlog.md`
- `docs/superpowers/specs/2026-09-20-7.2.1-zona-dns-route53-design.md`
- `docs/superpowers/plans/2026-09-20-7.2.1-zona-dns-route53.md`

`scripts/infra/lib/zona.mjs` e `package.json` vêm da emenda **E1** e são o único acréscimo em
relação à spec §7. O nome do relatório de conferência segue a data real da corrida; se o deploy
escorregar de dia, o path muda junto e isso não é desvio.

Se o Codex CLI não iniciar — `D-32` registra
`Error: failed to initialize in-process app-server client: Read-only file system (os error 30)` —,
o desvio vira `reviewer_exception` declarada com motivo, data e quem autorizou, mais um débito no
backlog. Nunca silêncio.
