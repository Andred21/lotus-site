# Clone estático — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar o scaffold Vite na home estática da Lotus OTEC — header, hero, `Somos`, `Cursos`, `Contacto` e rodapé — com paridade visual e de conteúdo contra `https://lotusotec.cl/`, medida sobre a evidência congelada da Sprint 1 e sem nenhuma requisição a host do WordPress em runtime.

**Architecture:** Uma única página React composta em `src/app/App.tsx`: `Header` fixo, `<main>` com cinco seções e `Footer`. Todo texto vem de `src/content/site.ts`; toda cor, fonte, escala e caixa vêm de tokens declarados em `@theme` no `src/index.css`, que o Tailwind v4 emite como custom properties em `:root` e como utilitários. `Row` é a única primitiva de layout — 80% de largura travada em 1080px. Nenhum componente tem estado, exceto `MobileMenu`.

**Tech Stack:** Vite 8, React 19, TypeScript 6 (`strict`, `noUncheckedIndexedAccess`, `erasableSyntaxOnly`, `verbatimModuleSyntax`), Tailwind CSS 4 via `@tailwindcss/vite`, `lucide-react`, Vitest + Testing Library (jsdom), Playwright (Chromium), pnpm 11, Node 24.19.0.

## Global Constraints

- Gerenciador é `pnpm`; `engineStrict: true` derruba qualquer comando fora de Node `>=24.19.0 <25`.
- `paths_autorizados` do bloco: `src/**`, `public/**`, `e2e/**`, `index.html`, `docs/inventario/04-tipografia.md`, `docs/inventario/05-layout.md`, `docs/inventario/08-seo.md`, `docs/inventario/README.md`, `scripts/inventario/**`. **Nenhuma task deste plano toca `package.json`, `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts` ou `eslint.config.js`** — os scripts novos rodam por `node <caminho>`, não por `pnpm run`.
- Nenhuma dependência nova. `lucide-react`, `clsx` e `tailwind-merge` já estão no `package.json` desde o bloco `1.2.4-1.3.9`.
- Um commit por EAP, Conventional Commits com a EAP no escopo: `feat(3.2.1): ...`. O commit `0` usa `chore(3.1.2)`.
- Todo texto institucional é **verbatim** do inventário, em espanhol, sem tradução, sem correção ortográfica e sem completar o que o original não publica (D2 da spec): `NUESTRos cursos`, `888 horas` sob o rótulo `ALUMNOS`, `Copyright © 2022`.
- Valor arbitrário do Tailwind (`bg-[#000000]`, `max-w-[1080px]`) é proibido para qualquer valor que apareça **duas vezes** — vira token em `@theme` (D5). Valor que aparece uma vez só pode ficar arbitrário.
- Componente não chama `fetch` nem `XMLHttpRequest`; `src/content/**` não importa React, componente nem integração. As duas direções são catraca de `pnpm lint`.
- `pnpm format` roda antes de cada commit; `pnpm check` precisa ficar verde ao fim de **cada** task.
- `pnpm e2e` não faz parte de `pnpm check`, mas não fica vermelho de propósito em nenhum ponto: as tasks que mudam o DOM que o E2E afirma ajustam o spec no mesmo commit.
- Nada é escrito em Notion, Google Drive, Figma ou no site público. Nenhum push, nenhum PR — isso é `/fechar-site`.
- O formulário **nunca** simula envio: sem mensagem de sucesso, sem erro, sem carregamento (D10).

### Divergências da spec, deliberadas e registradas

1. **A correção de medição cresceu e mudou de commit.** A spec previa corrigir só `#Contacto p` dentro do commit `2`. Amostrando os pixels de `docs/inventario/baseline/home-1440.png` — evidência congelada e versionada — aparecem cinco contradições entre `04-tipografia.md` e o que o site realmente pinta:

   | elemento         | `04-tipografia.md` | pixel do baseline |
   | ---------------- | ------------------ | ----------------- |
   | `#main-header`   | `#000000`          | `#f8f8f8`         |
   | `#Intrucción h1` | `#f7f7f7`          | `#24a2e0`         |
   | `#Intrucción h3` | `#333333`          | `#f0f0f0`         |
   | `#Cursos h2`     | `#333333`          | `#24a2e0`         |
   | `#main-footer`   | `#222222`          | `#323232`         |

   As duas causas estão em `extract-styles.mjs`: `rgbToHex` descarta o canal alpha (um fundo `rgba(0,0,0,0.03)` sobre branco vira `#000000` na tabela e `#f8f8f8` na tela), e `section.querySelector('h1')` alcança o nó-eco escondido que o Divi duplica (`hero.title_subtitle_echo` em `02-conteudo.md`), que carrega a cor default do tema, não a cor aplicada. A Task 1 corrige a régua e o Task 3 escreve os tokens já corrigidos. A spec pede "os valores medidos"; medir certo serve a intenção dela.

2. **Os três ícones dos destaques entram por `lucide-react`.** `Somos` tem um ícone azul `#24a2e0` acima de cada rótulo (raio, pessoa, medalha) e o menu mobile tem um hambúrguer. São glifos da fonte `ETmodules` do tema Divi: não aparecem em `dom.json` nem no manifesto de assets, e a spec não os menciona. Decisão de João em 2026-08-25: usar `lucide-react`, que já é dependência, com o desenho aproximado. Divergência intencional, registrada na matriz pelo Task 16.

3. **`background-texture.jpg` não é portado.** D3 manda 7 imagens para `src/assets/`, a textura de seção inclusa. A amostragem do baseline mostra o fundo do hero como `#000000` em **100%** dos pixels de duas regiões limpas (`120,690,400,110` e `60,760,600,60`): a textura não pinta nada visível. Portá-la sem importar deixaria peso morto no repositório, e importá-la sem usar quebra `noUnusedLocals`. Vão 6 imagens para `src/assets/`, e a textura entra na matriz como divergência intencional, mesma classe do `preloader.gif`.

4. **Nenhum arquivo novo de evidência.** `docs/inventario/` só tem quatro arquivos autorizados, todos `.md`. Os scripts da Task 1 imprimem o resultado em stdout; os números entram nas tabelas de `04-tipografia.md` e `05-layout.md`, com o comando que os reproduz. A proveniência continua sendo o `sha256` dos PNG de baseline, já citado em `06-baseline.md`.

5. **O conteúdo guarda texto, o componente guarda binário.** A spec descreve `institucional` como "corpo + logo". `src/content/site.ts` carrega o **texto alternativo** de cada imagem; o `import` do binário fica no componente que o renderiza. A rule diz "string institucional vive em `src/content/`" — `alt` é string institucional; `.png` não é.

---

## File Structure

| Arquivo                                      | Responsabilidade                                                                          |
| -------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `scripts/inventario/lib/site.mjs`            | ganha `cssColor` (preserva alpha) e `BREAKPOINT_WIDTHS`                                   |
| `scripts/inventario/sample-baseline.mjs`     | lê os PNG de baseline versionados e imprime a cor real de cada região — offline, sem rede |
| `scripts/inventario/measure-breakpoints.mjs` | mede o site real em 900–1400 e imprime as duas viradas — exige rede                       |
| `docs/inventario/04-tipografia.md`           | tabela de escala e paleta corrigida pela amostragem                                       |
| `docs/inventario/05-layout.md`               | seção de breakpoints com os valores medidos, não derivados                                |
| `docs/inventario/08-seo.md`                  | linha de `lang` passa de `preservar` para `criar` (`es-CL`)                               |
| `docs/inventario/README.md`                  | matriz de paridade fechada ao fim do bloco                                                |
| `index.html`                                 | `lang="es-CL"`, `<title>`, `canonical`, os 4 ícones de `<head>`                           |
| `src/index.css`                              | `@font-face` das 5 faces self-hosted, bloco `@theme` com todos os tokens, regras de base  |
| `src/main.tsx`                               | monta `App` em `#root`                                                                    |
| `src/app/App.tsx`                            | shell: `Header` + `<main>` com as seções + `Footer`                                       |
| `src/components/layout/Row.tsx`              | linha de 80% travada em 1080px — a única primitiva do bloco                               |
| `src/components/layout/Header.tsx`           | cabeçalho fixo: logo + nav desktop + `MobileMenu`                                         |
| `src/components/layout/MobileMenu.tsx`       | toggle e painel do menu mobile — único componente com `useState`                          |
| `src/components/layout/Footer.tsx`           | faixa de copyright                                                                        |
| `src/components/sections/Hero.tsx`           | `#Intrucción`: texto à esquerda, foto à direita só no desktop                             |
| `src/components/sections/QuienesSomos.tsx`   | `#Somos` linha 1: logo institucional + corpo                                              |
| `src/components/sections/Destaques.tsx`      | `#Somos` linha 2: os 3 destaques com ícone                                                |
| `src/components/sections/Cursos.tsx`         | `#Cursos`: título, intro, 3 cards e o CTA `See More`                                      |
| `src/components/sections/Contacto.tsx`       | `#Contacto`: painel de texto + painel de formulário                                       |
| `src/content/site.ts`                        | todo texto institucional, tipado, verbatim                                                |
| `src/assets/fonts/*.woff2`                   | `Open Sans` 500/600 e `Montserrat` 400/500/700, subset `latin`                            |
| `src/assets/*.png\|*.jpg\|*.jpeg`            | 6 imagens de conteúdo, fingerprintadas pelo Vite                                          |
| `public/cropped-Logo-*.png`                  | os 4 ícones de `<head>`, com URL estável                                                  |
| `e2e/home.spec.ts`                           | smoke da home; ganha âncoras, viewports e a asserção de host externo                      |
| `e2e/menu.spec.ts`                           | menu mobile: clique, teclado, foco, scroll                                                |

---

### Task 1: Régua de medição corrigida e breakpoints reais (commit 0 — `chore(3.1.2)`)

Existe porque D7 é pré-requisito medido da `3.1.2` e não tem EAP própria, e porque a amostragem do baseline mostrou que a paleta de `04-tipografia.md` descreve nós escondidos, não o que a página pinta. Nenhum token nasce antes desta task.

**Files:**

- Modify: `scripts/inventario/lib/site.mjs`
- Modify: `scripts/inventario/lib/site.test.mjs`
- Create: `scripts/inventario/sample-baseline.mjs`
- Create: `scripts/inventario/measure-breakpoints.mjs`
- Modify: `docs/inventario/04-tipografia.md`
- Modify: `docs/inventario/05-layout.md`

**Interfaces:**

- Consumes: `SITE_URL`, `OUT_DIR`, `openPage(viewport)` de `lib/site.mjs`; os PNG de `docs/inventario/baseline/`.
- Produces: `cssColor(value: string): string` em `lib/site.mjs`; `BREAKPOINT_WIDTHS: number[]`; a saída em stdout dos dois scripts; as tabelas corrigidas de `04-tipografia.md` e `05-layout.md`, que o Task 3 lê para escrever os tokens.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `scripts/inventario/lib/site.test.mjs`, mantendo os blocos que já existem:

```js
import { cssColor } from './site.mjs'

describe('cssColor', () => {
  it('devolve hex quando a cor é opaca', () => {
    expect(cssColor('rgb(36, 162, 224)')).toBe('#24a2e0')
    expect(cssColor('rgba(0, 0, 0, 1)')).toBe('#000000')
  })

  it('preserva o alpha em vez de fingir opacidade', () => {
    expect(cssColor('rgba(0, 0, 0, 0.03)')).toBe('rgba(0, 0, 0, 0.03)')
    expect(cssColor('rgba(84, 84, 84, 0.32)')).toBe('rgba(84, 84, 84, 0.32)')
  })

  it('trata alpha zero como transparente', () => {
    expect(cssColor('rgba(255, 255, 255, 0)')).toBe('transparent')
  })

  it('devolve o valor original quando não reconhece o formato', () => {
    expect(cssColor('currentColor')).toBe('currentColor')
  })
})
```

Adicionar `cssColor` ao `import` que já traz `assetFileName`, `normalizeText` e `rgbToHex`.

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use
pnpm test -- scripts/inventario/lib/site.test.mjs
```

Esperado: FAIL com `cssColor is not a function`.

> O `export NVM_DIR…; nvm use` precisa ser repetido em **toda** nova invocação de shell antes de qualquer `pnpm` — sem ele o pnpm aborta com `ERR_PNPM_UNSUPPORTED_ENGINE`.

- [ ] **Step 3: Implementar `cssColor` e `BREAKPOINT_WIDTHS`**

Acrescentar a `scripts/inventario/lib/site.mjs`, sem tocar em `rgbToHex` (a suíte existente e `styles.json` dependem do comportamento atual):

```js
/**
 * Larguras intermediárias que faltavam ao inventário: os quatro viewports da
 * Sprint 1 só provam que as duas viradas estão entre 768 e 1440.
 * @type {number[]}
 */
export const BREAKPOINT_WIDTHS = [900, 1000, 1100, 1200, 1300, 1400]

/**
 * Cor CSS preservando transparência. `rgbToHex` descarta o alpha, e um fundo
 * `rgba(0,0,0,0.03)` sobre branco vira `#000000` na tabela e `#f8f8f8` na
 * tela — foi assim que a paleta do cabeçalho saiu errada no inventário.
 * @param {string} value
 * @returns {string}
 */
export function cssColor(value) {
  const match = value.match(
    /^rgba?\((\d+),\s*(\d+),\s*(\d+)(?:,\s*([\d.]+))?\)/,
  )
  if (!match) return value
  const [, r, g, b, a] = match
  if (r === undefined || g === undefined || b === undefined) return value
  const alpha = a === undefined ? 1 : Number(a)
  if (alpha === 0) return 'transparent'
  if (alpha < 1) return `rgba(${r}, ${g}, ${b}, ${alpha})`
  const hex = [r, g, b]
    .map((channel) => Number(channel).toString(16).padStart(2, '0'))
    .join('')
  return `#${hex}`
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

```bash
pnpm test -- scripts/inventario/lib/site.test.mjs
```

Esperado: PASS, incluindo os blocos de `assetFileName`, `normalizeText` e `rgbToHex` que já existiam.

- [ ] **Step 5: Escrever o amostrador do baseline**

Criar `scripts/inventario/sample-baseline.mjs`. Ele não abre a rede: lê os PNG versionados, desenha num canvas do Chromium local e reporta, por região, a cor dominante e a cor mais distante do fundo — que é a cor do texto.

```js
// Ferramenta de inventário: roda em Node, nunca entra no bundle da aplicação.
/// <reference lib="dom" />
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { chromium } from '@playwright/test'
import { OUT_DIR } from './lib/site.mjs'

/**
 * `bg` é a cor de fundo da região: a cor do texto é o pixel mais distante
 * dela. Coordenadas em pixel do PNG, que é 1:1 com o viewport capturado.
 * @typedef {{ name: string, x: number, y: number, w: number, h: number, bg: string }} Region
 */

/** @type {Array<{ file: string, regions: Region[] }>} */
const TARGETS = [
  {
    file: 'home-1440.png',
    regions: [
      { name: 'header-fundo', x: 300, y: 10, w: 700, h: 70, bg: '#ffffff' },
      { name: 'menu-link', x: 850, y: 36, w: 400, h: 20, bg: '#f8f8f8' },
      { name: 'hero-fundo', x: 120, y: 690, w: 400, h: 110, bg: '#ffffff' },
      { name: 'hero-kicker', x: 110, y: 230, w: 400, h: 50, bg: '#000000' },
      { name: 'hero-h1', x: 110, y: 344, w: 260, h: 52, bg: '#000000' },
      { name: 'hero-h3', x: 110, y: 490, w: 420, h: 60, bg: '#000000' },
      { name: 'hero-corpo', x: 110, y: 585, w: 380, h: 90, bg: '#000000' },
      { name: 'somos-fundo', x: 60, y: 1000, w: 200, h: 60, bg: '#000000' },
      { name: 'somos-corpo', x: 600, y: 1165, w: 640, h: 120, bg: '#f0f0f0' },
      { name: 'destaque-icone', x: 300, y: 1440, w: 90, h: 80, bg: '#f0f0f0' },
      {
        name: 'destaque-rotulo',
        x: 250,
        y: 1540,
        w: 200,
        h: 32,
        bg: '#f0f0f0',
      },
      {
        name: 'destaque-corpo',
        x: 225,
        y: 1580,
        w: 250,
        h: 110,
        bg: '#f0f0f0',
      },
      { name: 'cursos-fundo', x: 60, y: 1820, w: 200, h: 60, bg: '#ffffff' },
      { name: 'cursos-h2', x: 500, y: 1846, w: 440, h: 50, bg: '#000000' },
      { name: 'cursos-intro', x: 380, y: 1930, w: 680, h: 65, bg: '#000000' },
      { name: 'card-legenda', x: 180, y: 2315, w: 340, h: 50, bg: '#000000' },
      {
        name: 'contacto-painel',
        x: 200,
        y: 2580,
        w: 200,
        h: 40,
        bg: '#000000',
      },
      { name: 'contacto-h2', x: 560, y: 2606, w: 320, h: 48, bg: '#f0f0f0' },
      { name: 'contacto-corpo', x: 400, y: 2690, w: 660, h: 70, bg: '#f0f0f0' },
      { name: 'campo-texto', x: 330, y: 2830, w: 240, h: 34, bg: '#f0f0f0' },
      { name: 'enviar-borda', x: 980, y: 3240, w: 120, h: 50, bg: '#f0f0f0' },
      { name: 'rodape-faixa', x: 100, y: 3400, w: 700, h: 41, bg: '#ffffff' },
      { name: 'rodape-texto', x: 175, y: 3400, w: 400, h: 41, bg: '#323232' },
    ],
  },
  {
    file: 'home-375.png',
    regions: [
      {
        name: 'header-fundo-mobile',
        x: 120,
        y: 20,
        w: 180,
        h: 40,
        bg: '#000000',
      },
      {
        name: 'hero-fundo-mobile',
        x: 30,
        y: 640,
        w: 300,
        h: 60,
        bg: '#ffffff',
      },
      {
        name: 'rodape-faixa-mobile',
        x: 20,
        y: 5400,
        w: 300,
        h: 40,
        bg: '#ffffff',
      },
    ],
  },
  {
    file: 'home-375-menu.png',
    regions: [
      { name: 'menu-painel', x: 100, y: 90, w: 150, h: 40, bg: '#000000' },
      { name: 'menu-borda-topo', x: 150, y: 80, w: 200, h: 3, bg: '#ffffff' },
      { name: 'menu-separador', x: 100, y: 141, w: 150, h: 1, bg: '#000000' },
    ],
  },
]

const browser = await chromium.launch()
const page = await browser.newPage({ viewport: { width: 400, height: 300 } })

for (const target of TARGETS) {
  const base64 = readFileSync(join(OUT_DIR, 'baseline', target.file)).toString(
    'base64',
  )
  // data: URI em vez de file://: canvas com imagem file:// fica "tainted" e
  // getImageData lança SecurityError.
  await page.setContent(
    `<style>*{margin:0;padding:0}</style><img id="i" src="data:image/png;base64,${base64}">`,
  )
  await page.waitForFunction(() => {
    const img = /** @type {HTMLImageElement | null} */ (
      document.querySelector('img')
    )
    return Boolean(img && img.complete && img.naturalWidth > 0)
  })

  const rows = await page.evaluate((regions) => {
    const img = /** @type {HTMLImageElement | null} */ (
      document.querySelector('img')
    )
    if (!img) throw new Error('imagem ausente na página de amostragem')
    const canvas = document.createElement('canvas')
    canvas.width = img.naturalWidth
    canvas.height = img.naturalHeight
    const ctx = canvas.getContext('2d')
    if (!ctx) throw new Error('canvas 2d indisponível')
    ctx.drawImage(img, 0, 0)

    /** @param {number} r @param {number} g @param {number} b */
    const hex = (r, g, b) =>
      `#${[r, g, b].map((c) => c.toString(16).padStart(2, '0')).join('')}`

    return regions.map((region) => {
      const br = Number.parseInt(region.bg.slice(1, 3), 16)
      const bg = Number.parseInt(region.bg.slice(3, 5), 16)
      const bb = Number.parseInt(region.bg.slice(5, 7), 16)
      const data = ctx.getImageData(region.x, region.y, region.w, region.h).data
      /** @type {Map<string, number>} */
      const counts = new Map()
      let farthest = 0
      for (let i = 0; i < data.length; i += 4) {
        const r = data[i] ?? 0
        const g = data[i + 1] ?? 0
        const b = data[i + 2] ?? 0
        const key = hex(r, g, b)
        counts.set(key, (counts.get(key) ?? 0) + 1)
        const distance = (r - br) ** 2 + (g - bg) ** 2 + (b - bb) ** 2
        if (distance > farthest) farthest = distance
      }
      const entries = [...counts.entries()]
      const dominant = entries.sort((a, b) => b[1] - a[1])[0]
      const core = entries
        .filter(([key]) => {
          const r = Number.parseInt(key.slice(1, 3), 16)
          const g = Number.parseInt(key.slice(3, 5), 16)
          const b = Number.parseInt(key.slice(5, 7), 16)
          return (r - br) ** 2 + (g - bg) ** 2 + (b - bb) ** 2 > farthest * 0.75
        })
        .sort((a, b) => b[1] - a[1])[0]
      const total = region.w * region.h
      const share = dominant ? ((dominant[1] / total) * 100).toFixed(1) : '0.0'
      return `${region.name.padEnd(22)} dominante=${dominant?.[0] ?? '—'} (${share}%)  núcleo=${core?.[0] ?? '—'}`
    })
  }, target.regions)

  console.log(`--- ${target.file} ---`)
  console.log(rows.join('\n'))
}

await browser.close()
```

- [ ] **Step 6: Rodar o amostrador e conferir contra a tabela abaixo**

```bash
node scripts/inventario/sample-baseline.mjs
```

Esperado — os valores que o Task 3 transforma em token. Se algum divergir, **pare e registre**: os PNG são congelados e versionados, então divergência aqui significa que o baseline mudou, não que a régua está errada.

| região                | núcleo    | é o quê                                     |
| --------------------- | --------- | ------------------------------------------- |
| `header-fundo`        | `#f8f8f8` | fundo do cabeçalho desktop                  |
| `menu-link`           | `#24a2e0` | link do menu                                |
| `hero-fundo`          | `#000000` | fundo do hero, 100% dos pixels              |
| `hero-kicker`         | `#f0f0f0` | `ENTRENAMIENTO PARA TRABAJOS…`              |
| `hero-h1`             | `#24a2e0` | `LOTUS OTEC`                                |
| `hero-h3`             | `#f0f0f0` | `SERVICIOS DE CAPACITACIÓN Y CERTIFICACIÓN` |
| `hero-corpo`          | `#24a2e0` | `Somos especialistas…`                      |
| `somos-corpo`         | `#545454` | corpo institucional                         |
| `destaque-icone`      | `#24a2e0` | os três ícones                              |
| `destaque-rotulo`     | `#353740` | `ENERGIZADAS` / `ALUMNOS` / `CERTIFICACIÓN` |
| `destaque-corpo`      | `#747d88` | corpo dos três destaques                    |
| `cursos-h2`           | `#24a2e0` | `NUESTROS CURSOS`                           |
| `cursos-intro`        | `#f0f0f0` | intro dos cursos                            |
| `card-legenda`        | `#f0f0f0` | nome de cada curso                          |
| `contacto-h2`         | `#333333` | `CONTÁCTENOS`                               |
| `contacto-corpo`      | `#353740` | corpo de contato                            |
| `campo-texto`         | `#353740` | texto do campo                              |
| `enviar-borda`        | `#545454` | borda e rótulo do botão                     |
| `rodape-faixa`        | `#323232` | faixa do rodapé (dominante)                 |
| `rodape-texto`        | `#24a2e0` | copyright                                   |
| `header-fundo-mobile` | `#ffffff` | fundo do cabeçalho mobile                   |
| `menu-borda-topo`     | `#24a2e0` | borda superior do painel mobile             |
| `menu-separador`      | `#f7f7f7` | divisória entre itens do menu               |

- [ ] **Step 7: Escrever o medidor de breakpoints**

Criar `scripts/inventario/measure-breakpoints.mjs`:

```js
// Ferramenta de inventário: roda em Node, nunca entra no bundle da aplicação.
/// <reference lib="dom" />
import { BREAKPOINT_WIDTHS, openPage } from './lib/site.mjs'

/** @type {Array<{ width: number, menuVisible: boolean, rowWidth: number }>} */
const rows = []

for (const width of BREAKPOINT_WIDTHS) {
  const { browser, page } = await openPage({
    name: String(width),
    width,
    height: 900,
  })
  const measured = await page.evaluate(() => {
    const menu = document.getElementById('top-menu')
    const row = document.querySelector('#Somos .et_pb_row')
    return {
      menuWidth: menu ? menu.getBoundingClientRect().width : 0,
      rowWidth: row ? Math.round(row.getBoundingClientRect().width) : 0,
    }
  })
  await browser.close()

  const menuVisible = measured.menuWidth > 0
  rows.push({ width, menuVisible, rowWidth: measured.rowWidth })
  console.log(
    `${String(width).padStart(4)}px  menu=${menuVisible ? 'desktop' : 'mobile '}  row=${measured.rowWidth}px`,
  )
}

const firstDesktop = rows.findIndex((row) => row.menuVisible)
const firstCapped = rows.findIndex((row) => row.rowWidth >= 1080)

/**
 * @param {number} index
 * @returns {string}
 */
const between = (index) => {
  if (index < 0) return 'acima de 1400px — nenhuma largura medida virou'
  const turned = rows[index]
  const previous = rows[index - 1]
  if (!turned) return 'indeterminado'
  return previous
    ? `entre ${previous.width}px e ${turned.width}px`
    : `em ${turned.width}px ou abaixo (768px é o piso conhecido)`
}

console.log('')
console.log(`menu vira desktop ${between(firstDesktop)}`)
console.log(`container trava em 1080px ${between(firstCapped)}`)
```

- [ ] **Step 8: Rodar o medidor**

```bash
node scripts/inventario/measure-breakpoints.mjs
```

Esperado: seis linhas, uma por largura, e as duas conclusões. **Este é o único passo do plano que precisa de rede.** Se `lotusotec.cl` estiver fora do ar, a task para aqui: registre o bloqueio, leve `state.md` para `blocked` com `resume_state: executing` e não invente número — a spec proíbe breakpoint por chute (D7).

- [ ] **Step 9: Corrigir `04-tipografia.md`**

Substituir, em `docs/inventario/04-tipografia.md`:

1. Na tabela **Escala**, as linhas de `#Intrucción h1`, `#Intrucción h3`, `#Cursos h2` e `#Contacto p` passam a citar a cor amostrada, e o parágrafo que hoje explica a medição de `#Contacto p` é trocado por este:

```markdown
Três medições desta página descreviam o nó-eco escondido que o Divi duplica (`hero.title_subtitle_echo`, `02-conteudo.md`), não o elemento pintado: `#Intrucción h1`, `#Intrucción h3` e `#Cursos h2` foram medidos com a cor default do tema (`#f7f7f7`/`#333333`), enquanto a página pinta `#24a2e0`, `#f0f0f0` e `#24a2e0`. `#Contacto p` alcança um parágrafo vazio de altura `0px`, cujo estilo computado é o do `h2` vizinho; o corpo real de `#Contacto` é `#353740`. As quatro correções vêm de `node scripts/inventario/sample-baseline.mjs`, que amostra os PNG de `baseline/` — evidência congelada, com `sha256` citado em `06-baseline.md`.
```

2. Na tabela **Paleta**, trocar as duas últimas linhas de cabeçalho e a de rodapé:

```markdown
| `#f8f8f8` | fundo de `#main-header` em 1440/1920 | cor de fundo — cabeçalho desktop |
| `#ffffff` | fundo de `#main-header` em 375/768 | cor de fundo — cabeçalho mobile |
| `#323232` | faixa visível de `#main-footer`/`#footer-bottom` | cor de fundo — rodapé |
| `#747d88` | corpo dos três destaques de `#Somos` | texto — destaque secundário |
```

e o parágrafo sobre a troca de cor do cabeçalho passa a dizer `#f8f8f8` no lugar de `#000000`, acrescentando:

```markdown
`getComputedStyle` devolve `rgba(0, 0, 0, 0.03)` para o fundo do cabeçalho e `rgbToHex` descartava o alpha, publicando `#000000` numa faixa que a tela pinta `#f8f8f8`. `cssColor` em `lib/site.mjs` preserva o alpha; a faixa do rodapé caía no mesmo erro (`#545454` na tabela, `#323232` na tela).
```

- [ ] **Step 10: Corrigir `05-layout.md`**

Na seção **Breakpoints**, substituir os dois bullets e a ressalva final pela saída real do Step 8, no formato:

```markdown
Medição em seis larguras intermediárias (`node scripts/inventario/measure-breakpoints.mjs`, 2026-08-25):

| viewport | menu    | `.et_pb_row` |
| -------- | ------- | ------------ |
| 900px    | <valor> | <valor>      |
| 1000px   | <valor> | <valor>      |
| 1100px   | <valor> | <valor>      |
| 1200px   | <valor> | <valor>      |
| 1300px   | <valor> | <valor>      |
| 1400px   | <valor> | <valor>      |

- **Menu desktop → menu mobile**: vira <conclusão impressa pelo script>.
- **Container `1080px`**: trava <conclusão impressa pelo script>.

`1350px` (`1080 / 0,8`) continua sendo derivação da regra dos 80%; a tabela acima é medição. O clone usa o valor medido.
```

Preencher `<valor>` e `<conclusão>` com o que o Step 8 imprimiu — nada aqui é estimado.

- [ ] **Step 11: Gate e commit**

```bash
pnpm format && pnpm check
```

Esperado: `agent:check`, `format:check`, `lint`, `typecheck`, `test` e `build` verdes. `inventario.test.mjs` continua passando: nenhum arquivo novo entrou em `docs/inventario/assets/` e nenhum `sha256` novo foi citado.

```bash
git add scripts/inventario docs/inventario/04-tipografia.md docs/inventario/05-layout.md
git commit -m "chore(3.1.2): medir breakpoints intermediários e corrigir a régua de cor"
```

---

### Task 2: Documento raiz, idioma e fontes (commit 1 — `feat(3.1.1)`)

**Files:**

- Modify: `index.html`
- Create: `src/assets/fonts/montserrat-400.woff2`, `montserrat-500.woff2`, `montserrat-700.woff2`, `open-sans-500.woff2`, `open-sans-600.woff2`
- Modify: `src/index.css`
- Create: `src/app/App.tsx`
- Modify: `src/main.tsx`
- Delete: `src/App.tsx`, `src/App.css`, `src/assets/react.svg`, `src/assets/vite.svg`, `src/assets/hero.png`, `public/favicon.svg`, `public/icons.svg`
- Modify: `docs/inventario/08-seo.md`
- Modify: `e2e/home.spec.ts`

**Interfaces:**

- Consumes: nada.
- Produces: `App` (named export) em `src/app/App.tsx`; as famílias `'Open Sans'` e `Montserrat` disponíveis por `@font-face` local; `<html lang="es-CL">`.

- [ ] **Step 1: Baixar as fontes**

D6 fixa os pesos observados no DOM, não a faixa inteira que o Google Fonts carrega: `Open Sans` 500/600 e `Montserrat` 400/500/700. Subset `latin` — cobre `á é í ó ú ñ ¿ ¡`, que é tudo que o conteúdo em espanhol usa.

```bash
mkdir -p src/assets/fonts
UA='Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'
curl -sS -A "$UA" -o /tmp/lotus-fonts.css \
  'https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&family=Open+Sans:wght@500;600&display=swap'
node --input-type=module -e "
import { readFileSync } from 'node:fs'
import { writeFileSync } from 'node:fs'
const css = readFileSync('/tmp/lotus-fonts.css', 'utf8')
const faces = [...css.matchAll(/\/\* latin \*\/\s*@font-face \{([^}]*)\}/g)]
for (const [, body] of faces) {
  const family = body.match(/font-family: '([^']+)'/)?.[1]
  const weight = body.match(/font-weight: (\d+)/)?.[1]
  const url = body.match(/url\((https:[^)]+\.woff2)\)/)?.[1]
  if (!family || !weight || !url) throw new Error('face latin incompleta: ' + body)
  const slug = family.toLowerCase().replace(/ /g, '-')
  const file = 'src/assets/fonts/' + slug + '-' + weight + '.woff2'
  const bytes = Buffer.from(await (await fetch(url)).arrayBuffer())
  writeFileSync(file, bytes)
  console.log(file, bytes.byteLength + ' B')
}
"
ls -la src/assets/fonts/
```

Esperado: exatamente cinco `.woff2`, nenhum com 0 byte. Se o Google devolver menos de cinco faces `latin`, pare — significa que a API mudou de formato e o passo precisa ser refeito, não contornado.

- [ ] **Step 2: Reescrever `index.html`**

`lang` é `es-CL` (D8): o site publica `es`, mas o critério de aceite da `3.1.1` pede `es-CL`, e o site é referência visual/de conteúdo, não autoridade arquitetural. `<title>` e `canonical` ficam verbatim. Os quatro `<link>` de ícone entram no Task 5, junto com os arquivos.

```html
<!doctype html>
<html lang="es-CL">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LOTUS | OTEC</title>
    <link rel="canonical" href="https://lotusotec.cl/" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 3: Reescrever `src/index.css`**

O tema do scaffold sai inteiro — paleta roxa, `prefers-color-scheme: dark`, `#root { width: 1126px }` e as regras de `h1`/`h2`/`code`/`.counter`. O site de referência não tem dark mode; manter o bloco criaria divergência que nenhuma EAP pede. O bloco `@theme` nasce no Task 3.

```css
@import 'tailwindcss';

@font-face {
  font-family: 'Open Sans';
  font-style: normal;
  font-weight: 500;
  font-display: swap;
  src: url('./assets/fonts/open-sans-500.woff2') format('woff2');
}

@font-face {
  font-family: 'Open Sans';
  font-style: normal;
  font-weight: 600;
  font-display: swap;
  src: url('./assets/fonts/open-sans-600.woff2') format('woff2');
}

@font-face {
  font-family: 'Montserrat';
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('./assets/fonts/montserrat-400.woff2') format('woff2');
}

@font-face {
  font-family: 'Montserrat';
  font-style: normal;
  font-weight: 500;
  font-display: swap;
  src: url('./assets/fonts/montserrat-500.woff2') format('woff2');
}

@font-face {
  font-family: 'Montserrat';
  font-style: normal;
  font-weight: 700;
  font-display: swap;
  src: url('./assets/fonts/montserrat-700.woff2') format('woff2');
}
```

- [ ] **Step 4: Criar o shell e apontar o `main.tsx` para ele**

`src/app/App.tsx`:

```tsx
/** Shell da home. As seções entram uma por task, a partir da 3.2.1. */
export function App() {
  return <main />
}
```

`src/main.tsx`:

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import { App } from './app/App'

const root = document.getElementById('root')
if (!root) throw new Error('elemento #root ausente em index.html')

createRoot(root).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 5: Apagar o scaffold**

```bash
git rm src/App.tsx src/App.css src/assets/react.svg src/assets/vite.svg src/assets/hero.png public/favicon.svg public/icons.svg
```

`public/icons.svg` era o sprite do scaffold Vite e seu único consumidor era o `App.tsx` que acabou de morrer. `.claude/rules/architecture.md:12` descreve esse sprite; o Task 16 registra o débito da rule que ficou órfã.

- [ ] **Step 6: Afrouxar o E2E que o commit quebra**

`e2e/home.spec.ts` afirma um `h1` `Lotus OTEC` que este commit apagou e o Task 8 recria com o texto real. Substituir o arquivo por:

```ts
import { expect, test } from '@playwright/test'

test('home responde 200 e declara o idioma do clone', async ({ page }) => {
  const response = await page.goto('/')

  expect(response?.status()).toBe(200)
  await expect(page.locator('html')).toHaveAttribute('lang', 'es-CL')
})
```

- [ ] **Step 7: Corrigir `08-seo.md`**

Na tabela, a linha de `lang` passa de `preservar` para `criar`, e ganha nota:

```markdown
| `lang` do `<html>` | `es` | `criar` (`es-CL`) |
```

```markdown
- **`lang` `es` → `es-CL`**: o site publica `es` e o critério de aceite da EAP `3.1.1` pede `es-CL`. O critério é requisito escrito e o site é referência visual e de conteúdo, não autoridade arquitetural (`CLAUDE.md`). Divergência intencional sem efeito visual, na matriz de `README.md`.
```

- [ ] **Step 8: Provar e commitar**

```bash
pnpm format && pnpm check && pnpm e2e
```

Esperado: `pnpm check` verde e o spec de home passando com a asserção nova. A página está em branco — é o esperado neste commit.

```bash
git add -A
git commit -m "feat(3.1.1): configurar documento raiz, idioma es-CL e fontes self-hosted"
```

---

### Task 3: Tokens e a primitiva `Row` (commit 2 — `feat(3.1.2)`)

**Files:**

- Modify: `src/index.css`
- Create: `src/components/layout/Row.tsx`
- Create: `src/components/layout/Row.test.tsx`

**Interfaces:**

- Consumes: os valores corrigidos por `04-tipografia.md` e `05-layout.md` (Task 1); `cn` de `src/lib/cn.ts`.
- Produces: os tokens de `@theme` — utilitários `bg-ink`, `bg-surface`, `bg-header`, `bg-header-mobile`, `text-brand`, `text-link`, `text-title-light`, `text-accent-ink`, `text-body-ink`, `text-muted-ink`, `text-neutral-ink`, `bg-footer`, `font-sans`, `font-display`, `text-hero`, `text-section`, `text-subtitle`, `text-highlight`, `text-lead`, `text-menu`, `text-body`, `text-kicker`, `text-button`, `text-field`, `max-w-row`, `max-w-lead`, `gap-gutter`, `h-header`, `h-header-desktop`, `pt-header-offset`, `rounded-pill`, `shadow-header`, `shadow-field`, `px-button-x`, `py-button-y` e a variante `desktop:`; e `Row({ children, className })`.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/layout/Row.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { Row } from './Row'

describe('Row', () => {
  it('reproduz a calha medida: 80% travado em 1080px e centrado', () => {
    render(<Row>conteúdo</Row>)
    const row = screen.getByText('conteúdo')

    expect(row.className).toContain('w-4/5')
    expect(row.className).toContain('max-w-row')
    expect(row.className).toContain('mx-auto')
  })

  it('aceita classe extra sem perder a calha', () => {
    render(<Row className="py-8">conteúdo</Row>)
    const row = screen.getByText('conteúdo')

    expect(row.className).toContain('py-8')
    expect(row.className).toContain('max-w-row')
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/layout/Row.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Row"`.

- [ ] **Step 3: Declarar os tokens**

Acrescentar a `src/index.css`, depois do `@import` e antes dos `@font-face` — o Tailwind v4 emite cada entrada de `@theme` como custom property em `:root` e gera o utilitário correspondente, o que satisfaz `.claude/rules/frontend.md` literalmente e dá consumidor ao `prettier-plugin-tailwindcss`, instalado pela `1.3.1` e até aqui sem uso.

```css
@theme {
  /* Cor — docs/inventario/04-tipografia.md, corrigida por sample-baseline.mjs.
     #2ea3f2 (link de conteúdo) e #24a2e0 (marca) ficam separados: a medição
     encontrou dois azuis distintos, e juntá-los seria escolha estética. */
  --color-ink: #000000;
  --color-surface: #f0f0f0;
  --color-header: #f8f8f8;
  --color-header-mobile: #ffffff;
  --color-brand: #24a2e0;
  --color-link: #2ea3f2;
  --color-title-light: #333333;
  --color-accent-ink: #353740;
  --color-body-ink: #545454;
  --color-muted-ink: #747d88;
  --color-neutral-ink: #666666;
  --color-divider: #f7f7f7;
  --color-footer: #323232;

  /* Família — stacks de fallback como medidas, não como preferência. */
  --font-sans: 'Open Sans', Arial, sans-serif;
  --font-display: Montserrat, Helvetica, Arial, Lucida, sans-serif;

  /* Escala tipográfica */
  --text-hero: 60px;
  --text-hero--line-height: 78px;
  --text-section: 42px;
  --text-section--line-height: 54.6px;
  --text-subtitle: 22px;
  --text-subtitle--line-height: 22px;
  --text-highlight: 22px;
  --text-highlight--line-height: 30.8px;
  --text-lead: 19px;
  --text-lead--line-height: 34.2px;
  --text-menu: 18px;
  --text-menu--line-height: 14px;
  --text-body: 16px;
  --text-body--line-height: 28.8px;
  --text-kicker: 16px;
  --text-kicker--line-height: 23.8px;
  --text-kicker--letter-spacing: 3px;
  --text-button: 20px;
  --text-button--line-height: 34px;
  --text-button--letter-spacing: 2px;
  --text-field: 16px;

  /* Caixa — docs/inventario/05-layout.md */
  --container-row: 1080px;
  --container-lead: 700px;
  --container-form: 800px;
  --spacing-gutter: 59.39px;
  --spacing-header: 80px;
  --spacing-header-desktop: 94px;
  --spacing-header-offset: 79px;
  --spacing-button-x: 20px;
  --spacing-button-y: 6px;
  --breakpoint-desktop: <valor medido no Task 1>;

  /* Borda e sombra */
  --radius-pill: 100px;
  --shadow-header: 0 1px 0 0 rgba(0, 0, 0, 0.1);
  --shadow-field: inset 0 -1px 0 0 rgba(0, 0, 0, 0.18);
}
```

`--breakpoint-desktop` recebe o número que `measure-breakpoints.mjs` imprimiu e que ficou registrado na tabela de `05-layout.md`. Nenhum outro ponto do plano repete esse número: toda regra responsiva usa a variante `desktop:`.

- [ ] **Step 4: Implementar `Row`**

`src/components/layout/Row.tsx`:

```tsx
import type { ReactNode } from 'react'
import { cn } from '../../lib/cn'

type RowProps = {
  children: ReactNode
  className?: string
}

/**
 * Linha do layout. Reproduz a regra medida em `05-layout.md`: 80% da largura
 * do viewport enquanto isso couber em 1080px, margem lateral automática e
 * calha lateral zero — a calha vem da própria linha, não de padding.
 * O hero é exceção e não usa `Row`.
 */
export function Row({ children, className }: RowProps) {
  return (
    <div className={cn('max-w-row mx-auto w-4/5', className)}>{children}</div>
  )
}
```

- [ ] **Step 5: Rodar o teste e ver passar**

```bash
pnpm test -- src/components/layout/Row.test.tsx
```

Esperado: PASS, 2 testes.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
```

O `prettier-plugin-tailwindcss` reordena as classes de `Row` — é esperado, e por isso `format` roda antes de `check`.

```bash
git add src/index.css src/components/layout/Row.tsx src/components/layout/Row.test.tsx
git commit -m "feat(3.1.2): criar tokens de design e a primitiva de container"
```

---

### Task 4: Camada de conteúdo institucional (commit 3 — `feat(3.1.3)`)

**Files:**

- Modify: `src/content/site.ts`
- Create: `src/content/site.test.ts`

**Interfaces:**

- Consumes: `docs/inventario/02-conteudo.md`, `07-formulario.md`, `09-dados.md`.
- Produces: `site` — objeto `as const` com `name`, `locale`, `logoAlt`, `nav`, `hero`, `institucional`, `destaques`, `cursos`, `contacto`, `footer`. Todo componente das tasks seguintes lê daqui; nenhum JSX carrega string institucional.

- [ ] **Step 1: Escrever o teste que falha**

`src/content/site.test.ts` — trava o que D2 proíbe mexer: a inconsistência `ALUMNOS`/`888 horas`, a caixa de `NUESTRos cursos` e o ano `2022`.

```ts
import { describe, expect, it } from 'vitest'
import { site } from './site'

describe('conteúdo institucional', () => {
  it('mantém os quatro itens de menu com os destinos medidos', () => {
    expect(site.nav.map((item) => [item.label, item.href])).toEqual([
      ['Inicio', '/'],
      ['Quienes Somos', '#Somos'],
      ['Cursos', '#Cursos'],
      ['Contacto', '#Contacto'],
    ])
  })

  it('preserva a inconsistência do original em ALUMNOS', () => {
    const alumnos = site.destaques[1]
    expect(alumnos.label).toBe('ALUMNOS')
    expect(alumnos.body).toContain('888 horas de capacitación')
  })

  it('preserva a caixa inconsistente do título de cursos', () => {
    expect(site.cursos.heading).toBe('NUESTRos cursos')
  })

  it('preserva o ano de copyright publicado', () => {
    expect(site.footer.copyright).toBe(
      'Diseñado por Lotus OTEC | Copyright © 2022. OTEC Lotus.',
    )
  })

  it('lista os três cursos com nome e texto alternativo', () => {
    expect(site.cursos.items).toHaveLength(3)
    for (const curso of site.cursos.items) {
      expect(curso.nombre.length).toBeGreaterThan(0)
      expect(curso.imageAlt.length).toBeGreaterThan(0)
    }
  })

  it('descreve os quatro campos do formulário sem exigir nenhum', () => {
    expect(site.contacto.form.fields).toHaveLength(4)
    expect(site.contacto.form.fields.map((field) => field.name)).toEqual([
      'nombre',
      'email',
      'empresa',
      'mensaje',
    ])
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/content/site.test.ts
```

Esperado: FAIL — `site.nav` é `undefined`.

- [ ] **Step 3: Escrever o conteúdo**

`src/content/site.ts`. O eco `hero.title_subtitle_echo` **não** é portado: é artefato do Divi Builder, não conteúdo. Os campos ocultos do Divi (`_wpnonce`, `_wp_http_referer`, `et_pb_contactform_submit_0`) também não — não têm função sem WordPress.

```ts
/**
 * Conteúdo institucional, verbatim de `docs/inventario/02-conteudo.md`. Não
 * importa React, componente nem integração — a direção de dependências é
 * catraca de lint em eslint.config.js. Nada aqui é traduzido, corrigido ou
 * completado: a inconsistência entre o rótulo `ALUMNOS` e o corpo sobre
 * `888 horas`, a caixa de `NUESTRos cursos` e o ano `2022` do rodapé são do
 * site original e ficam como estão (decisão D2 da spec).
 */
export const site = {
  name: 'Lotus OTEC',
  locale: 'es-CL',
  logoAlt: 'LOTUS',

  nav: [
    { label: 'Inicio', href: '/' },
    { label: 'Quienes Somos', href: '#Somos' },
    { label: 'Cursos', href: '#Cursos' },
    { label: 'Contacto', href: '#Contacto' },
  ],

  hero: {
    kicker: 'ENTRENAMIENTO PARA TRABAJOS EN INSTALACIONES ENERGIZADAS',
    title: 'LOTUS OTEC',
    subtitle: 'SERVICIOS DE CAPACITACIÓN Y CERTIFICACIÓN',
    body: 'Somos especialistas en entrenamiento en servicios de Alta y Media Tensión para líneas de transmisión y subestaciones.',
    cta: { label: 'Learn More', href: '' },
  },

  institucional: {
    body: 'En LOTUS OTEC tenemos una oferta especializada en satisfacer las necesidades de capacitación de la industria eléctrica. Somos expertos en las áreas de Seguridad, Entrenamiento y Certificación en métodos de trabajo con líneas energizadas a contacto, distancia y a potencial.',
    logoAlt: 'Logotipo de LOTUS OTEC',
  },

  destaques: [
    {
      label: 'ENERGIZADAS',
      body: 'Somos especialistas en entrenamiento de métodos de trabajo en Instalaciones Energizadas.',
    },
    {
      label: 'ALUMNOS',
      body: 'A la fecha hemos realizado un total de 888 horas de capacitación para la industria de la energía.',
    },
    {
      label: 'CERTIFICACIÓN',
      body: 'Estamos certificados bajo la norma NCH 2728:2015 como consta en certificado N° CA-751 y registro INN: A-10981.',
    },
  ],

  cursos: {
    heading: 'NUESTRos cursos',
    intro:
      'A continuación presentamos los principales curso de nuestra oferta. Si desea conocer toda nuestra oferta de cursos, por favor contáctenos.',
    items: [
      {
        nombre: 'Curso Especialistas Líneas Vivas en Media Tensión',
        imageAlt: 'Puesto de trabajo con computador portátil',
      },
      {
        nombre: 'Curso Especialistas en Líneas Vivas en Alta Tensión',
        imageAlt: 'Especialista trabajando en una línea de alta tensión',
      },
      {
        nombre: 'Curso Supervisor de Trabajos de Líneas Vivas',
        imageAlt: 'Supervisor de trabajos en líneas vivas sobre un poste',
      },
    ],
    cta: { label: 'See More', href: '#' },
  },

  contacto: {
    heading: 'CONTÁCTENOS',
    body: 'Contactese con nosotros, déjenos mensaje describiendo su requerimiento y le contactaremos a la brevedad o escribanos al siguiente correo: ',
    email: 'contacto@lotusotec.cl',
    form: {
      fields: [
        { name: 'nombre', label: 'Nombre Completo', type: 'text' },
        { name: 'email', label: 'Correo Electrónico', type: 'text' },
        { name: 'empresa', label: 'Empresa', type: 'text' },
        { name: 'mensaje', label: 'Mensaje', type: 'textarea' },
      ],
      submit: 'Enviar',
    },
  },

  footer: {
    copyright: 'Diseñado por Lotus OTEC | Copyright © 2022. OTEC Lotus.',
  },
} as const
```

`contacto.body` termina em espaço porque o `mailto:` do original é a continuação da frase, dentro do mesmo parágrafo — o componente renderiza o link logo depois. A string completa medida em `02-conteudo.md` é reconstituída por `body + email`.

`type: 'text' | 'textarea'` sai do `as const`, então o componente discrimina pelo valor sem cast.

- [ ] **Step 4: Rodar o teste e ver passar**

```bash
pnpm test -- src/content/site.test.ts
```

Esperado: PASS, 6 testes.

- [ ] **Step 5: Gate e commit**

```bash
pnpm format && pnpm check
git add src/content
git commit -m "feat(3.1.3): criar camada de conteúdo institucional tipada"
```

---

### Task 5: Assets no repositório (commit 4 — `feat(3.1.4)`)

O título da EAP diz "migrar assets para public", mas `.claude/rules/architecture.md` e `CLAUDE.md` reservam `public/` para arquivo que precisa de URL estável. A regra do repositório vence (D3): imagem importada por componente vai para `src/assets/` e ganha fingerprint do Vite; só os quatro ícones de `<head>`, referenciados por `<link>`, vão para `public/`. O conflito vira o débito `D-12`.

**Files:**

- Create: `src/assets/LOTUS_TRANSP_Fondo-Negro-REC2.png`, `LOTUS-G2_TRANSP_Fondo-Blanco.png`, `home-office-12.jpg`, `LLVV_00-v1-BN2.jpeg`, `LLVV_Mantas02-BN2.jpeg`, `shutterstock_1444636373-1-scaled.jpg`
- Create: `public/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-32x32.png`, `-180x180.png`, `-192x192.png`, `-270x270.png`
- Modify: `index.html`

**Interfaces:**

- Consumes: `docs/inventario/assets/` — os binários já baixados e conferidos por `sha256` na Sprint 1.
- Produces: os seis caminhos de `src/assets/` que os Tasks 6, 8, 9 e 11 importam; as quatro URLs estáveis de `public/`.

- [ ] **Step 1: Copiar os binários, deduplicando por `sha256`**

Quatro dos sete assets de staging têm cópia byte-idêntica em `lotusotec.cl`; o clone guarda **um** arquivo por binário, com o nome do host principal. Três existem só no staging — a foto do hero, a textura e **o logo do cabeçalho**.

```bash
cp docs/inventario/assets/staging-LOTUS_TRANSP_Fondo-Negro-REC2.png      src/assets/LOTUS_TRANSP_Fondo-Negro-REC2.png
cp docs/inventario/assets/staging-shutterstock_1444636373-1-scaled.jpg   src/assets/shutterstock_1444636373-1-scaled.jpg
cp docs/inventario/assets/LOTUS-G2_TRANSP_Fondo-Blanco.png              src/assets/
cp docs/inventario/assets/home-office-12.jpg                            src/assets/
cp docs/inventario/assets/LLVV_00-v1-BN2.jpeg                           src/assets/
cp docs/inventario/assets/LLVV_Mantas02-BN2.jpeg                        src/assets/
cp docs/inventario/assets/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-*.png public/
```

`background-texture.jpg` não é copiado: a amostragem do Task 1 mostra o fundo do hero como `#000000` em 100% dos pixels, então a textura não pinta nada e um arquivo sem `import` seria peso morto (com `import`, `noUnusedLocals` derruba o build). Vira divergência intencional no Task 16, mesma classe do `preloader.gif`. As variantes `-150x150`, `-300x225` e `-480x480` também ficam de fora — D4 dispensa `srcset`.

- [ ] **Step 2: Conferir que os bytes chegaram íntegros**

```bash
sha256sum src/assets/*.png src/assets/*.jpg src/assets/*.jpeg public/*.png
node -e "
const m = require('./docs/inventario/assets/manifest.json').assets
for (const a of m) console.log(a.sha256.slice(0, 8), a.file)
" | sort
```

Esperado: cada `sha256` dos dez arquivos copiados aparece no manifesto. `staging-…` e a cópia do host principal têm o mesmo hash — é o que autoriza a deduplicação.

- [ ] **Step 3: Referenciar os ícones no `<head>`**

Acrescentar a `index.html`, dentro de `<head>`, depois do `canonical` — os quatro `rel`/`sizes` são os medidos em `dom.json`:

```html
<link
  rel="icon"
  sizes="32x32"
  href="/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-32x32.png"
/>
<link
  rel="icon"
  sizes="192x192"
  href="/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-192x192.png"
/>
<link
  rel="apple-touch-icon"
  href="/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-180x180.png"
/>
<meta
  name="msapplication-TileImage"
  content="/cropped-Logo-LOTUS-_Fondo-Negro_Recortado-270x270.png"
/>
```

- [ ] **Step 4: Provar que o build carrega os ícones**

```bash
pnpm format && pnpm check
ls dist/*.png
```

Esperado: os quatro `cropped-Logo-*.png` aparecem em `dist/` com o nome intacto — `public/` é copiado verbatim, sem fingerprint. As seis imagens de `src/assets/` ainda **não** aparecem: nenhum componente as importa até o Task 6.

- [ ] **Step 5: Commit**

```bash
git add src/assets public index.html
git commit -m "feat(3.1.4): migrar assets do site para o repositório"
```

---

### Task 6: Header desktop (commit 5 — `feat(3.2.1)`)

**Files:**

- Create: `src/components/layout/Header.tsx`
- Create: `src/components/layout/Header.test.tsx`
- Modify: `src/app/App.tsx`
- Modify: `src/index.css`

**Interfaces:**

- Consumes: `Row`, `site.nav`, `site.logoAlt`, `src/assets/LOTUS_TRANSP_Fondo-Negro-REC2.png`.
- Produces: `Header()`; o deslocamento de topo do `<main>`, que o Task 14 reaproveita como `scroll-margin`.

Medidas de `05-layout.md` e da amostragem: altura `80px` em 375/768 e `94px` em 1440/1920; fundo `#ffffff` mobile e `#f8f8f8` desktop; sombra `rgba(0,0,0,0.1) 0 1px 0 0`; logo `34×43` mobile e `63×80` desktop; links do menu `18px/600/14px` em `#24a2e0`. O DOM original renderiza o menu duas vezes (header fixo + estado sticky do Divi); o clone renderiza uma vez — divergência estrutural sem efeito visual.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/layout/Header.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { Header } from './Header'

describe('Header', () => {
  it('expõe o logo com o texto alternativo medido', () => {
    render(<Header />)
    expect(screen.getByAltText('LOTUS')).toBeTruthy()
  })

  it('publica os quatro itens de navegação com os destinos medidos', () => {
    render(<Header />)
    const nav = screen.getByRole('navigation', { name: 'Principal' })

    expect(
      screen.getByRole('link', { name: 'Quienes Somos' }).getAttribute('href'),
    ).toBe('#Somos')
    expect(nav.querySelectorAll('a')).toHaveLength(4)
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/layout/Header.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Header"`.

- [ ] **Step 3: Implementar `Header`**

`src/components/layout/Header.tsx`:

```tsx
import logo from '../../assets/LOTUS_TRANSP_Fondo-Negro-REC2.png'
import { site } from '../../content/site'
import { Row } from './Row'

/**
 * Cabeçalho fixo. O original é o único elemento que troca de cor de fundo
 * entre viewports — `#ffffff` em 375/768 e `#f8f8f8` em 1440/1920 — e a
 * troca é reproduzida, não resolvida por escolha estética.
 */
export function Header() {
  return (
    <header className="bg-header-mobile shadow-header desktop:h-header-desktop desktop:bg-header h-header fixed inset-x-0 top-0 z-50">
      <Row className="flex h-full items-center justify-between">
        <a href="/" className="flex items-center">
          <img
            src={logo}
            alt={site.logoAlt}
            width={63}
            height={80}
            className="desktop:h-[80px] h-[43px] w-auto"
          />
        </a>
        <nav aria-label="Principal" className="desktop:block hidden">
          <ul className="flex items-center gap-8">
            {site.nav.map((item) => (
              <li key={item.href}>
                <a
                  href={item.href}
                  className="text-menu text-brand font-sans font-semibold"
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </Row>
    </header>
  )
}
```

O `<nav>` fica `hidden` abaixo do breakpoint desktop porque `top-menu` mede `0×0` em 375 e 768 — o menu mobile é o Task 7.

- [ ] **Step 4: Compor no shell e reservar o topo**

`src/app/App.tsx`:

```tsx
import { Header } from '../components/layout/Header'

export function App() {
  return (
    <>
      <Header />
      <main className="pt-header-offset desktop:pt-header-desktop" />
    </>
  )
}
```

`#page-container` reserva `79px` em 375/768 e `94px` em 1440/1920 (`05-layout.md`) — o valor muda porque o próprio cabeçalho muda de altura.

E acrescentar a `src/index.css`, depois do `@theme`, a base tipográfica que o Divi aplica no `body` e que a preflight do Tailwind zera:

```css
@layer base {
  body {
    font-family: var(--font-sans);
    color: var(--color-neutral-ink);
  }
}
```

- [ ] **Step 5: Rodar o teste e ver passar**

```bash
pnpm test -- src/components/layout/Header.test.tsx
```

Esperado: PASS, 2 testes.

- [ ] **Step 6: Conferir no navegador**

```bash
pnpm dev
```

Abrir em 1440 e em 375. Esperado: faixa clara no topo, logo à esquerda alinhado à calha da linha, quatro links azuis à direita só no desktop, e a linha de sombra de 1px na base. `.claude/rules/testing.md`: build verde não prova paridade visual.

- [ ] **Step 7: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/layout/Header.tsx src/components/layout/Header.test.tsx src/app/App.tsx src/index.css
git commit -m "feat(3.2.1): implementar cabeçalho fixo com navegação desktop"
```

---

### Task 7: Navegação mobile acessível (commit 6 — `feat(3.2.2)`)

**Files:**

- Create: `src/components/layout/MobileMenu.tsx`
- Create: `src/components/layout/MobileMenu.test.tsx`
- Modify: `src/components/layout/Header.tsx`
- Create: `e2e/menu.spec.ts`

**Interfaces:**

- Consumes: `site.nav`; `Menu` e `X` de `lucide-react`.
- Produces: `MobileMenu()` — único componente com `useState` no bloco.

Medidas de `home-375-menu.png`: painel branco de 80% da largura, alinhado à calha da linha, começando logo abaixo do cabeçalho de `80px`; borda superior de `3px` em `#24a2e0`; quatro itens de ~44px separados por 1px de `#f7f7f7`. O hambúrguer do Divi é glifo da fonte `ETmodules`, que o clone não carrega — entra `lucide-react`, já dependência (divergência 2 do plano).

- [ ] **Step 1: Escrever o teste que falha**

`src/components/layout/MobileMenu.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { MobileMenu } from './MobileMenu'

describe('MobileMenu', () => {
  it('começa fechado e anuncia o estado', () => {
    render(<MobileMenu />)
    const toggle = screen.getByRole('button', { name: 'Abrir menú' })

    expect(toggle.getAttribute('aria-expanded')).toBe('false')
    expect(screen.queryByRole('navigation', { name: 'Mobile' })).toBeNull()
  })

  it('abre por clique e mostra os quatro itens', async () => {
    const user = userEvent.setup()
    render(<MobileMenu />)

    await user.click(screen.getByRole('button', { name: 'Abrir menú' }))

    const nav = screen.getByRole('navigation', { name: 'Mobile' })
    expect(nav.querySelectorAll('a')).toHaveLength(4)
    expect(
      screen
        .getByRole('button', { name: 'Cerrar menú' })
        .getAttribute('aria-expanded'),
    ).toBe('true')
  })

  it('fecha com Escape e devolve o foco ao botão', async () => {
    const user = userEvent.setup()
    render(<MobileMenu />)
    const toggle = screen.getByRole('button', { name: 'Abrir menú' })

    await user.click(toggle)
    await user.keyboard('{Escape}')

    expect(screen.queryByRole('navigation', { name: 'Mobile' })).toBeNull()
    expect(document.activeElement).toBe(
      screen.getByRole('button', { name: 'Abrir menú' }),
    )
  })

  it('fecha ao escolher um item', async () => {
    const user = userEvent.setup()
    render(<MobileMenu />)

    await user.click(screen.getByRole('button', { name: 'Abrir menú' }))
    await user.click(screen.getByRole('link', { name: 'Cursos' }))

    expect(screen.queryByRole('navigation', { name: 'Mobile' })).toBeNull()
  })
})
```

`@testing-library/user-event` acompanha `@testing-library/react`, já instalado; não há dependência nova.

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/layout/MobileMenu.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./MobileMenu"`.

- [ ] **Step 3: Implementar `MobileMenu`**

`src/components/layout/MobileMenu.tsx`:

```tsx
import { Menu, X } from 'lucide-react'
import { useRef, useState } from 'react'
import { site } from '../../content/site'

/**
 * Menu mobile. Único componente com estado no bloco: só aberto/fechado.
 * O painel não prende o scroll da página — o original também não prende, e
 * travar `overflow` seria comportamento novo, não paridade.
 */
export function MobileMenu() {
  const [open, setOpen] = useState(false)
  const toggleRef = useRef<HTMLButtonElement>(null)

  const close = () => {
    setOpen(false)
    toggleRef.current?.focus()
  }

  return (
    <div
      className="desktop:hidden"
      onKeyDown={(event) => {
        if (event.key === 'Escape' && open) close()
      }}
    >
      <button
        ref={toggleRef}
        type="button"
        aria-expanded={open}
        aria-controls="menu-mobile"
        aria-label={open ? 'Cerrar menú' : 'Abrir menú'}
        onClick={() => setOpen(!open)}
        className="text-accent-ink flex h-11 w-11 items-center justify-center"
      >
        {open ? <X aria-hidden size={28} /> : <Menu aria-hidden size={28} />}
      </button>

      {open && (
        <nav
          id="menu-mobile"
          aria-label="Mobile"
          className="border-brand bg-header-mobile top-header absolute inset-x-0 mx-auto w-4/5 border-t-[3px]"
        >
          <ul>
            {site.nav.map((item) => (
              <li key={item.href} className="border-divider border-b">
                <a
                  href={item.href}
                  onClick={close}
                  className="text-neutral-ink text-body block px-6 py-3 font-sans"
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      )}
    </div>
  )
}
```

O `onKeyDown` fica no contêiner, não no `document`: `useEffect` só existe para sincronizar com sistema externo, e aqui o evento nasce dentro da própria árvore, já que o foco fica no botão ou num link do painel.

- [ ] **Step 4: Pendurar no `Header`**

Em `src/components/layout/Header.tsx`, acrescentar o import e o componente logo depois do `<nav>` desktop, dentro do `Row`:

```tsx
import { MobileMenu } from './MobileMenu'
```

```tsx
<MobileMenu />
```

A classe do `<header>` **não muda**: `fixed` já faz dele o bloco contentor do painel `absolute inset-x-0 top-header`, e é isso que ancora o painel em 80% da largura, centrado, começando na base do cabeçalho — a geometria medida em `home-375-menu.png` (`x` de 38 a 337 num viewport de 375).

- [ ] **Step 5: Rodar o teste e ver passar**

```bash
pnpm test -- src/components/layout/MobileMenu.test.tsx src/components/layout/Header.test.tsx
```

Esperado: PASS, 6 testes ao todo.

- [ ] **Step 6: Escrever o E2E do menu**

`e2e/menu.spec.ts`:

```ts
import { expect, test } from '@playwright/test'

test.use({ viewport: { width: 375, height: 812 } })

test('menu mobile abre, fecha e não prende o scroll', async ({ page }) => {
  await page.goto('/')

  const toggle = page.getByRole('button', { name: 'Abrir menú' })
  await toggle.click()

  const nav = page.getByRole('navigation', { name: 'Mobile' })
  await expect(nav).toBeVisible()
  await expect(nav.getByRole('link')).toHaveCount(4)

  const overflow = await page.evaluate(
    () => getComputedStyle(document.body).overflow,
  )
  expect(overflow).not.toBe('hidden')

  await page.keyboard.press('Escape')
  await expect(nav).toBeHidden()
  await expect(toggle).toBeFocused()
})

test('menu mobile é operável só pelo teclado', async ({ page }) => {
  await page.goto('/')

  await page.getByRole('button', { name: 'Abrir menú' }).focus()
  await page.keyboard.press('Enter')
  await expect(page.getByRole('navigation', { name: 'Mobile' })).toBeVisible()

  await page.keyboard.press('Tab')
  await expect(page.getByRole('link', { name: 'Inicio' })).toBeFocused()
})
```

- [ ] **Step 7: Rodar o E2E**

```bash
pnpm e2e
```

Primeira execução na máquina precisa de `pnpm exec playwright install --with-deps chromium`. Esperado: `home.spec.ts`, `a11y.spec.ts` e `menu.spec.ts` verdes.

- [ ] **Step 8: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/layout e2e/menu.spec.ts
git commit -m "feat(3.2.2): implementar navegação mobile acessível"
```

---

### Task 8: Seção Hero (commit 7 — `feat(3.2.3)`)

**Files:**

- Create: `src/components/sections/Hero.tsx`
- Create: `src/components/sections/Hero.test.tsx`
- Modify: `src/app/App.tsx`
- Modify: `e2e/home.spec.ts`

**Interfaces:**

- Consumes: `site.hero`, `src/assets/shutterstock_1444636373-1-scaled.jpg`.
- Produces: `Hero()`; a âncora `#Intrucción`, com a grafia do original.

Medidas: seção `#000000` full-bleed, `.et_pb_row` com `max-width: 100%` — o hero é a **exceção** que não usa `Row`. Coluna de texto com padding `10vw` vertical e `8vw` horizontal (`37.5/30` em 375, `76.8/61.44` em 768, `144/115.2` em 1440, `192/153.6` em 1920 — todos exatamente 10% e 8% do viewport). Duas colunas de `720px` em 1440; a foto ocupa a segunda e **não aparece** em 375/768. `h1` `60px/700/78px` em `#24a2e0`; `h3` `22px/500/22px` em `#f0f0f0`; kicker `16px/500/23.8px` com `letter-spacing: 3px` em `#f0f0f0`; corpo em `#24a2e0`.

O CTA `Learn More` é reproduzido fiel (D1): `color`, `background-color` e `border-color` iguais a `#000000`, borda `4px solid`, raio `100px`, `href=""`. Ele é invisível no site publicado e continua invisível no clone; dar contraste é evolução pós-clone, não task desta Sprint.

O subtítulo é `h3` logo depois do `h1`, sem `h2` entre eles. A hierarquia quebrada é do original e **não** é corrigida aqui: `08-seo.md` marca o item como `pendente decisão` e a spec põe a correção de headings na Sprint 4. Trocar a tag mudaria estrutura de conteúdo, não fidelidade.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/sections/Hero.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { site } from '../../content/site'
import { Hero } from './Hero'

describe('Hero', () => {
  it('renderiza kicker, título, subtítulo e corpo vindos do conteúdo', () => {
    render(<Hero />)

    expect(screen.getByText(site.hero.kicker)).toBeTruthy()
    expect(
      screen.getByRole('heading', { level: 1, name: 'LOTUS OTEC' }),
    ).toBeTruthy()
    expect(
      screen.getByRole('heading', { level: 3, name: site.hero.subtitle }),
    ).toBeTruthy()
    expect(screen.getByText(site.hero.body)).toBeTruthy()
  })

  it('mantém o CTA sem destino, como no original', () => {
    render(<Hero />)
    expect(
      screen.getByRole('link', { name: 'Learn More' }).getAttribute('href'),
    ).toBe('')
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/sections/Hero.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Hero"`.

- [ ] **Step 3: Implementar `Hero`**

`src/components/sections/Hero.tsx`:

```tsx
import heroPhoto from '../../assets/shutterstock_1444636373-1-scaled.jpg'
import { site } from '../../content/site'

/**
 * Hero. Única seção full-bleed: a `.et_pb_row` do original mede
 * `max-width: 100%`, então `Row` não entra aqui. Os paddings da coluna de
 * texto são 10% e 8% do viewport nos quatro tamanhos medidos.
 * A foto só aparece no desktop — em 375/768 ela não é reposicionada, some.
 */
export function Hero() {
  return (
    <section id="Intrucción" className="bg-ink desktop:grid-cols-2 grid">
      <div className="px-[8vw] py-[10vw]">
        <p className="text-kicker font-display text-surface font-medium">
          {site.hero.kicker}
        </p>
        <h1 className="text-hero font-display text-brand font-bold">
          {site.hero.title}
        </h1>
        <h3 className="text-subtitle text-surface font-sans font-medium">
          {site.hero.subtitle}
        </h3>
        <p className="text-body font-display text-brand mt-8 font-medium">
          {site.hero.body}
        </p>
        <a
          href={site.hero.cta.href}
          className="border-ink bg-ink text-ink rounded-pill text-button font-display px-button-x py-button-y mt-8 inline-block border-4 border-solid font-bold"
        >
          {site.hero.cta.label}
        </a>
      </div>
      <div
        aria-hidden
        className="desktop:block hidden bg-cover bg-center"
        style={{ backgroundImage: `url(${heroPhoto})` }}
      />
    </section>
  )
}
```

A foto entra por `background-image`, como no original, e por isso é decorativa: sem `alt`, com `aria-hidden`. `style` inline é o caminho para uma URL que o Vite fingerprinta — o valor não existe em tempo de escrita do CSS.

- [ ] **Step 4: Compor no shell**

`src/app/App.tsx`:

```tsx
import { Header } from '../components/layout/Header'
import { Hero } from '../components/sections/Hero'

export function App() {
  return (
    <>
      <Header />
      <main className="pt-header-offset desktop:pt-header-desktop">
        <Hero />
      </main>
    </>
  )
}
```

- [ ] **Step 5: Restaurar a asserção de `h1` no E2E**

O Task 2 afrouxou `e2e/home.spec.ts` porque o `h1` do scaffold morreu. Agora existe o `h1` real, em caixa alta:

```ts
import { expect, test } from '@playwright/test'

test('home responde 200, declara o idioma e mostra o título real', async ({
  page,
}) => {
  const response = await page.goto('/')

  expect(response?.status()).toBe(200)
  await expect(page.locator('html')).toHaveAttribute('lang', 'es-CL')
  await expect(
    page.getByRole('heading', { level: 1, name: 'LOTUS OTEC' }),
  ).toBeVisible()
})
```

- [ ] **Step 6: Rodar teste, E2E e conferir**

```bash
pnpm test -- src/components/sections/Hero.test.tsx
pnpm e2e
pnpm dev
```

Esperado: unitários e E2E verdes. No navegador em 1440, o hero é preto na metade esquerda com a foto na direita; em 375 é só o bloco preto com texto. O CTA `Learn More` **não** deve aparecer — é o comportamento medido, não um defeito da implementação.

- [ ] **Step 7: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/sections/Hero.tsx src/components/sections/Hero.test.tsx src/app/App.tsx e2e/home.spec.ts
git commit -m "feat(3.2.3): implementar seção hero"
```

---

### Task 9: Seção Quienes Somos (commit 8 — `feat(3.2.4)`)

**Files:**

- Create: `src/components/sections/QuienesSomos.tsx`
- Create: `src/components/sections/QuienesSomos.test.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**

- Consumes: `site.institucional`, `src/assets/LOTUS-G2_TRANSP_Fondo-Blanco.png`, `Row`.
- Produces: `QuienesSomos()`; a `<section id="Somos">` no `App`, que o Task 10 completa com a segunda linha.

Medidas: `#Somos` fundo `#f0f0f0`, padding `110px` no topo e `16px` na base. Linha 1 é `320px` (logo) + `700px` (texto) com vão de `59.39px` em 1440/1920, empilhada em 375/768; padding vertical da linha `30px` no topo e `1px` na base. Corpo `19px/500/34.2px` em `#545454`. A imagem mede `320×320` no desktop.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/sections/QuienesSomos.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { site } from '../../content/site'
import { QuienesSomos } from './QuienesSomos'

describe('QuienesSomos', () => {
  it('renderiza o corpo institucional verbatim', () => {
    render(<QuienesSomos />)
    expect(screen.getByText(site.institucional.body)).toBeTruthy()
  })

  it('dá texto alternativo real ao logotipo institucional', () => {
    render(<QuienesSomos />)
    expect(screen.getByAltText(site.institucional.logoAlt)).toBeTruthy()
  })
})
```

O segundo teste é D9: o original publica `alt=""` nessa imagem, e `.claude/rules/frontend.md` exige `alt` em imagem com conteúdo. A troca não muda um pixel.

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/sections/QuienesSomos.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./QuienesSomos"`.

- [ ] **Step 3: Implementar**

`src/components/sections/QuienesSomos.tsx`:

```tsx
import institucional from '../../assets/LOTUS-G2_TRANSP_Fondo-Blanco.png'
import { site } from '../../content/site'
import { Row } from '../layout/Row'

/** Primeira linha de `#Somos`: logotipo e corpo institucional lado a lado. */
export function QuienesSomos() {
  return (
    <Row className="desktop:grid-cols-[320px_700px] gap-gutter grid items-center pt-[30px] pb-px">
      <img
        src={institucional}
        alt={site.institucional.logoAlt}
        width={320}
        height={320}
        className="mx-auto w-full max-w-[320px]"
      />
      <p className="text-lead text-body-ink font-sans font-medium">
        {site.institucional.body}
      </p>
    </Row>
  )
}
```

- [ ] **Step 4: Abrir a seção `#Somos` no shell**

`src/app/App.tsx` — a âncora `#Somos` é uma `<section>` que envolve **duas** linhas Divi distintas, que são duas EAP distintas:

```tsx
<main className="pt-header-offset desktop:pt-header-desktop">
  <Hero />
  <section id="Somos" className="bg-surface pt-[110px] pb-4">
    <QuienesSomos />
  </section>
</main>
```

- [ ] **Step 5: Rodar o teste e ver passar**

```bash
pnpm test -- src/components/sections/QuienesSomos.test.tsx
```

Esperado: PASS, 2 testes.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/sections/QuienesSomos.tsx src/components/sections/QuienesSomos.test.tsx src/app/App.tsx
git commit -m "feat(3.2.4): implementar seção quienes somos"
```

---

### Task 10: Destaques e certificação (commit 9 — `feat(3.2.5)`)

**Files:**

- Create: `src/components/sections/Destaques.tsx`
- Create: `src/components/sections/Destaques.test.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**

- Consumes: `site.destaques`; `Zap`, `ShieldUser`, `Medal` de `lucide-react`; `Row`.
- Produces: `Destaques()`.

Medidas: linha 2 de `#Somos` com três colunas de `320px` e vão de `59.39px` em 1440/1920, empilhadas em 375/768; padding vertical `25px`. Rótulo `h4` `22px/700/30.8px` em `#353740`; corpo `19px/500/34.2px` em `#747d88`; ícone `#24a2e0`. Os três textos são verbatim (D2), inclusive a inconsistência entre o rótulo `ALUMNOS` e o corpo sobre `888 horas`.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/sections/Destaques.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { site } from '../../content/site'
import { Destaques } from './Destaques'

describe('Destaques', () => {
  it('renderiza os três destaques com rótulo e corpo', () => {
    render(<Destaques />)

    for (const destaque of site.destaques) {
      expect(
        screen.getByRole('heading', { level: 4, name: destaque.label }),
      ).toBeTruthy()
      expect(screen.getByText(destaque.body)).toBeTruthy()
    }
  })

  it('mantém a certificação exatamente como publicada', () => {
    render(<Destaques />)
    expect(screen.getByText(/NCH 2728:2015/)).toBeTruthy()
    expect(screen.getByText(/N° CA-751/)).toBeTruthy()
    expect(screen.getByText(/INN: A-10981/)).toBeTruthy()
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/sections/Destaques.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Destaques"`.

- [ ] **Step 3: Implementar**

`src/components/sections/Destaques.tsx`:

```tsx
import { Medal, ShieldUser, Zap } from 'lucide-react'
import { site } from '../../content/site'
import { Row } from '../layout/Row'

/**
 * Segunda linha de `#Somos`: três destaques em colunas de 320px.
 * Os ícones do original são glifos da fonte ETmodules do tema Divi, que não
 * acompanham o clone; entram os equivalentes de `lucide-react`, já
 * dependência do projeto. Divergência intencional registrada na matriz.
 */
const ICONS = [Zap, ShieldUser, Medal]

export function Destaques() {
  return (
    <Row className="desktop:grid-cols-3 gap-gutter grid py-[25px]">
      {site.destaques.map((destaque, index) => {
        const Icon = ICONS[index] ?? Zap
        return (
          <div key={destaque.label} className="text-center">
            <Icon aria-hidden size={48} className="text-brand mx-auto" />
            <h4 className="text-highlight font-display text-accent-ink mt-4 font-bold">
              {destaque.label}
            </h4>
            <p className="text-lead text-muted-ink font-sans font-medium">
              {destaque.body}
            </p>
          </div>
        )
      })}
    </Row>
  )
}
```

`ICONS[index] ?? Zap` existe porque `noUncheckedIndexedAccess` faz o acesso indexado devolver `Icon | undefined`; o fallback nunca dispara com três destaques e três ícones, mas o compilador precisa da guarda.

- [ ] **Step 4: Completar a seção no shell**

```tsx
<section id="Somos" className="bg-surface pt-[110px] pb-4">
  <QuienesSomos />
  <Destaques />
</section>
```

- [ ] **Step 5: Rodar o teste e conferir**

```bash
pnpm test -- src/components/sections/Destaques.test.tsx
pnpm dev
```

Esperado: PASS, 2 testes. No navegador em 1440, três colunas com ícone azul, rótulo escuro e corpo cinza-azulado; empilhadas em 375.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/sections/Destaques.tsx src/components/sections/Destaques.test.tsx src/app/App.tsx
git commit -m "feat(3.2.5): implementar destaques e certificação"
```

---

### Task 11: Seção Cursos (commit 10 — `feat(3.2.6)`)

**Files:**

- Create: `src/components/sections/Cursos.tsx`
- Create: `src/components/sections/Cursos.test.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**

- Consumes: `site.cursos`; as três imagens de curso em `src/assets/`; `Row`.
- Produces: `Cursos()`; a âncora `#Cursos`.

Medidas: `#Cursos` fundo `#000000`, padding `0` no topo e `110px` na base. Três linhas Divi: título+intro (1080), três cards de `320px` com vão de `59.39px`, e o CTA. Imagem do card `320×240` no desktop, `400×300` em 768 e `300×225` em 375 — proporção 4:3 em todos. `h2` `42px/700/54.6px` em `#24a2e0`; intro `16px/500/28.8px` em `#f0f0f0`, largura ~700px centrada; legenda do card em `#f0f0f0`.

Os três nomes de curso são a única informação publicada na home — carga horária, público-alvo e descrição longa **não** existem e não são inventados (D2). O CTA `See More` recebe o mesmo tratamento fiel do `Learn More` (D1), com `href="#"`.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/sections/Cursos.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { site } from '../../content/site'
import { Cursos } from './Cursos'

describe('Cursos', () => {
  it('renderiza o título com a caixa do original', () => {
    render(<Cursos />)
    expect(
      screen.getByRole('heading', { level: 2, name: 'NUESTRos cursos' }),
    ).toBeTruthy()
  })

  it('renderiza os três cursos com nome e imagem descrita', () => {
    render(<Cursos />)

    for (const curso of site.cursos.items) {
      expect(screen.getByText(curso.nombre)).toBeTruthy()
      expect(screen.getByAltText(curso.imageAlt)).toBeTruthy()
    }
  })

  it('mantém o CTA apontando para # como no original', () => {
    render(<Cursos />)
    expect(
      screen.getByRole('link', { name: 'See More' }).getAttribute('href'),
    ).toBe('#')
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/sections/Cursos.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Cursos"`.

- [ ] **Step 3: Implementar**

`src/components/sections/Cursos.tsx`:

```tsx
import mediaTension from '../../assets/home-office-12.jpg'
import altaTension from '../../assets/LLVV_00-v1-BN2.jpeg'
import supervisor from '../../assets/LLVV_Mantas02-BN2.jpeg'
import { site } from '../../content/site'
import { Row } from '../layout/Row'

/** Ordem medida em `dom.json`: a imagem do card i acompanha o curso i. */
const IMAGES = [mediaTension, altaTension, supervisor]

export function Cursos() {
  return (
    <section id="Cursos" className="bg-ink pb-[110px]">
      <Row className="py-[27px] text-center">
        <h2 className="text-section font-display text-brand font-bold">
          {site.cursos.heading}
        </h2>
        <p className="text-body font-display text-surface max-w-lead mx-auto font-medium">
          {site.cursos.intro}
        </p>
      </Row>

      <Row className="desktop:grid-cols-3 gap-gutter grid py-[27px]">
        {site.cursos.items.map((curso, index) => (
          <article key={curso.nombre} className="text-center">
            <img
              src={IMAGES[index] ?? mediaTension}
              alt={curso.imageAlt}
              width={320}
              height={240}
              className="aspect-[4/3] w-full object-cover"
            />
            <p className="text-body font-display text-surface mt-6 font-medium">
              {curso.nombre}
            </p>
          </article>
        ))}
      </Row>

      <Row className="py-[27px] text-center">
        <a
          href={site.cursos.cta.href}
          className="border-ink bg-ink text-ink rounded-pill text-button font-display px-button-x py-button-y inline-block border-4 border-solid font-bold"
        >
          {site.cursos.cta.label}
        </a>
      </Row>
    </section>
  )
}
```

- [ ] **Step 4: Compor no shell**

```tsx
<Cursos />
```

logo depois do `</section>` de `#Somos`.

- [ ] **Step 5: Rodar o teste e conferir**

```bash
pnpm test -- src/components/sections/Cursos.test.tsx
pnpm dev
```

Esperado: PASS, 3 testes. No navegador, três cards lado a lado em 1440, empilhados em 375; o `See More` invisível sobre o preto, como no baseline.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/sections/Cursos.tsx src/components/sections/Cursos.test.tsx src/app/App.tsx
git commit -m "feat(3.2.6): implementar seção cursos"
```

---

### Task 12: Apresentação do Contacto (commit 11 — `feat(3.2.7)`)

**Files:**

- Create: `src/components/sections/Contacto.tsx`
- Create: `src/components/sections/Contacto.test.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**

- Consumes: `site.contacto`; `Row`.
- Produces: `Contacto()`; a âncora `#Contacto`.

Medidas: `#Contacto` fundo `#000000`, padding `57px`/`58px`. Duas linhas de `1080px` com fundo `#f0f0f0` e padding vertical `27px` (desktop) / `30px` (mobile), separadas por uma faixa preta de 9px. O formulário trava em `800px` e é centrado dentro da linha. Campo: altura `51px`, padding `16px`, fundo **transparente**, texto `16px` em `#353740`, sombra `inset 0 -1px 0 0 rgba(0,0,0,0.18)`, sem borda. `h2` `42px/700/54.6px` em `#333333`; corpo em `#353740`. Botão `Enviar`: borda `5px solid #545454`, raio `100px`, texto `20px/700/34px` com `letter-spacing: 2px` em `#545454`.

O `#f0f0f0` visível atrás dos campos é do painel, não do campo: pintar o input de branco quebra paridade.

Duas regras deste commit, que são o coração de D9 e D10:

- cada campo ganha um `<label>` visualmente oculto com o mesmo texto do `placeholder`; o `placeholder` continua sendo o rótulo visível, exatamente como hoje. Zero pixel de diferença, e o campo deixa de ser anônimo para leitor de tela;
- o formulário **não simula envio**: `<form>` sem `action`, `onSubmit` chama `preventDefault` e não exibe nada. Sem mensagem de sucesso, sem erro, sem carregamento. Nenhum campo é `required`, como medido.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/sections/Contacto.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it } from 'vitest'
import { site } from '../../content/site'
import { Contacto } from './Contacto'

describe('Contacto', () => {
  it('renderiza título, corpo e o email como mailto', () => {
    render(<Contacto />)

    expect(
      screen.getByRole('heading', { level: 2, name: 'CONTÁCTENOS' }),
    ).toBeTruthy()
    expect(
      screen
        .getByRole('link', { name: site.contacto.email })
        .getAttribute('href'),
    ).toBe(`mailto:${site.contacto.email}`)
  })

  it('rotula os quatro campos sem mudar o rótulo visível', () => {
    render(<Contacto />)

    for (const field of site.contacto.form.fields) {
      const input = screen.getByLabelText(field.label)
      expect(input.getAttribute('placeholder')).toBe(field.label)
      expect(input.hasAttribute('required')).toBe(false)
    }
  })

  it('não anuncia envio nenhum ao submeter', async () => {
    const user = userEvent.setup()
    render(<Contacto />)

    await user.type(screen.getByLabelText('Nombre Completo'), 'Ana')
    await user.click(screen.getByRole('button', { name: 'Enviar' }))

    expect(screen.queryByRole('status')).toBeNull()
    expect(screen.queryByRole('alert')).toBeNull()
    expect(screen.getByLabelText('Nombre Completo')).toHaveProperty(
      'value',
      'Ana',
    )
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/sections/Contacto.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Contacto"`.

- [ ] **Step 3: Implementar**

`src/components/sections/Contacto.tsx`:

```tsx
import { site } from '../../content/site'
import { cn } from '../../lib/cn'
import { Row } from '../layout/Row'

const FIELD_CLASS =
  'text-field font-sans text-accent-ink shadow-field h-[51px] w-full bg-transparent p-4'

/**
 * Apresentação do contato. A EAP 3.2.7 constrói só a apresentação: o
 * formulário não tem `action`, não posta e não exibe mensagem alguma —
 * sucesso, erro ou carregamento. O backend é escopo da Sprint 3, com o
 * contrato aberto em `docs/inventario/07-formulario.md`.
 */
export function Contacto() {
  return (
    <section id="Contacto" className="bg-ink pt-[57px] pb-[58px]">
      <Row className="bg-surface px-6 py-[27px] text-center">
        <h2 className="text-section font-display text-title-light font-bold">
          {site.contacto.heading}
        </h2>
        <p className="text-body font-display text-accent-ink max-w-lead mx-auto font-medium">
          {site.contacto.body}
          <a href={`mailto:${site.contacto.email}`} className="text-link">
            {site.contacto.email}
          </a>
        </p>
      </Row>

      <div className="h-[9px]" />

      <Row className="bg-surface px-6 py-[27px]">
        <form
          className="max-w-form mx-auto"
          onSubmit={(event) => event.preventDefault()}
        >
          {site.contacto.form.fields.map((field) => (
            <p key={field.name} className="mb-4">
              <label htmlFor={field.name} className="sr-only">
                {field.label}
              </label>
              {field.type === 'textarea' ? (
                <textarea
                  id={field.name}
                  name={field.name}
                  placeholder={field.label}
                  className={cn(FIELD_CLASS, 'h-[150px] resize-y')}
                />
              ) : (
                <input
                  id={field.name}
                  name={field.name}
                  type="text"
                  placeholder={field.label}
                  className={FIELD_CLASS}
                />
              )}
            </p>
          ))}
          <p className="text-right">
            <button
              type="submit"
              className="border-body-ink text-body-ink rounded-pill text-button font-display px-button-x py-button-y border-[5px] border-solid font-bold"
            >
              {site.contacto.form.submit}
            </button>
          </p>
        </form>
      </Row>
    </section>
  )
}
```

`sr-only` é utilitário nativo do Tailwind: o `<label>` existe para a árvore de acessibilidade e não ocupa pixel. O `placeholder` continua sendo o rótulo visível, que some ao digitar — comportamento do original, preservado.

- [ ] **Step 4: Compor no shell**

```tsx
<Contacto />
```

logo depois de `<Cursos />`.

- [ ] **Step 5: Rodar o teste e conferir**

```bash
pnpm test -- src/components/sections/Contacto.test.tsx
pnpm dev
```

Esperado: PASS, 3 testes. No navegador, dois painéis claros de 1080px sobre o preto, separados por uma faixa fina; campos sem fundo próprio, com a linha inferior de 1px; botão em pílula à direita.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/sections/Contacto.tsx src/components/sections/Contacto.test.tsx src/app/App.tsx
git commit -m "feat(3.2.7): implementar apresentação da seção contacto"
```

---

### Task 13: Footer (commit 12 — `feat(3.2.8)`)

**Files:**

- Create: `src/components/layout/Footer.tsx`
- Create: `src/components/layout/Footer.test.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**

- Consumes: `site.footer`, `Row`.
- Produces: `Footer()`.

Medidas: faixa `#323232`, altura `54px` em 768/1440/1920 e `78px` em 375, padding `15px` no topo e `5px` na base, texto em `#24a2e0`.

O ano `2022` fica como publicado. O aceite da `3.2.8` condiciona regra dinâmica de ano a aprovação prévia, que não foi dada — `new Date().getFullYear()` seria conteúdo novo, não clone.

- [ ] **Step 1: Escrever o teste que falha**

`src/components/layout/Footer.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { Footer } from './Footer'

describe('Footer', () => {
  it('renderiza o copyright verbatim, com o ano publicado', () => {
    render(<Footer />)
    expect(
      screen.getByText(
        'Diseñado por Lotus OTEC | Copyright © 2022. OTEC Lotus.',
      ),
    ).toBeTruthy()
  })

  it('usa a landmark de rodapé', () => {
    render(<Footer />)
    expect(screen.getByRole('contentinfo')).toBeTruthy()
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

```bash
pnpm test -- src/components/layout/Footer.test.tsx
```

Esperado: FAIL com `Failed to resolve import "./Footer"`.

- [ ] **Step 3: Implementar**

`src/components/layout/Footer.tsx`:

```tsx
import { site } from '../../content/site'
import { Row } from './Row'

/**
 * Rodapé. O ano `2022` é o publicado no site e fica como está: gerar o ano
 * pelo relógio seria conteúdo novo, e o aceite da EAP condiciona isso a
 * aprovação que não foi dada.
 */
export function Footer() {
  return (
    <footer className="bg-footer pt-[15px] pb-[5px]">
      <Row>
        <p className="text-body text-brand font-sans font-medium">
          {site.footer.copyright}
        </p>
      </Row>
    </footer>
  )
}
```

- [ ] **Step 4: Compor no shell**

```tsx
import { Footer } from '../components/layout/Footer'
```

```tsx
      </main>
      <Footer />
    </>
```

- [ ] **Step 5: Rodar o teste e ver passar**

```bash
pnpm test -- src/components/layout/Footer.test.tsx
```

Esperado: PASS, 2 testes.

- [ ] **Step 6: Gate e commit**

```bash
pnpm format && pnpm check
git add src/components/layout/Footer.tsx src/components/layout/Footer.test.tsx src/app/App.tsx
git commit -m "feat(3.2.8): implementar rodapé"
```

---

### Task 14: Navegação por âncoras (commit 13 — `feat(3.2.9)`)

**Files:**

- Modify: `src/index.css`
- Modify: `e2e/home.spec.ts`

**Interfaces:**

- Consumes: os `id` `Somos`, `Cursos`, `Contacto` e `Intrucción`, já criados; os tokens `--spacing-header` e `--spacing-header-desktop`.
- Produces: rolagem suave com o cabeçalho compensado, e a prova E2E das três âncoras.

Os `id` de âncora são preservados verbatim porque os links do menu dependem deles. `Intrucción` mantém a grafia do original, inclusive o erro — nenhum link aponta para ele, mas a matriz marca o hero como `fiel`. Os `id` de wrapper do Divi (`page-container`, `et-main-area`, `main-content`, `post-47805`, `et_pb_*`, `main-header`, `top-menu*`, `menu-item-*`, `main-footer`, `footer-bottom`) não existem no clone — já registrados como divergência intencional.

- [ ] **Step 1: Escrever o E2E que falha**

Acrescentar a `e2e/home.spec.ts`:

```ts
test('os três links de âncora levam à seção certa, com o cabeçalho compensado', async ({
  page,
}) => {
  // Viewport explícito: o menu desktop só existe acima de --breakpoint-desktop,
  // e o default do projeto Chromium (1280px) pode ficar abaixo dele.
  await page.setViewportSize({ width: 1440, height: 900 })
  await page.goto('/')

  for (const [label, id] of [
    ['Quienes Somos', 'Somos'],
    ['Cursos', 'Cursos'],
    ['Contacto', 'Contacto'],
  ] as const) {
    await page
      .getByRole('navigation', { name: 'Principal' })
      .getByRole('link', { name: label })
      .click()
    await expect(page).toHaveURL(new RegExp(`#${id}$`))

    const top = await page
      .locator(`#${id}`)
      .evaluate((element) => element.getBoundingClientRect().top)
    // A seção para abaixo do cabeçalho de 94px, não atrás dele.
    expect(top).toBeGreaterThanOrEqual(0)
    expect(top).toBeLessThan(120)
  }
})
```

- [ ] **Step 2: Rodar o E2E e ver falhar**

```bash
pnpm e2e -- home.spec.ts
```

Esperado: FAIL — sem `scroll-margin-top`, a seção para atrás do cabeçalho fixo e `top` fica negativo.

- [ ] **Step 3: Implementar**

Acrescentar as regras abaixo **dentro** do bloco `@layer base` que o Task 6 criou em `src/index.css`, ao lado da regra de `body` — não abrir um segundo bloco:

```css
@layer base {
  /* a regra de `body` do Task 6 continua aqui */

  html {
    scroll-behavior: smooth;
  }

  /* O cabeçalho é fixo: sem esta margem a âncora para atrás dele. */
  [id] {
    scroll-margin-top: var(--spacing-header);
  }

  @media (width >= theme(--breakpoint-desktop)) {
    [id] {
      scroll-margin-top: var(--spacing-header-desktop);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }
  }
}
```

- [ ] **Step 4: Rodar o E2E e ver passar**

```bash
pnpm e2e
```

Esperado: os quatro specs verdes.

- [ ] **Step 5: Gate e commit**

```bash
pnpm format && pnpm check
git add src/index.css e2e/home.spec.ts
git commit -m "feat(3.2.9): implementar navegação por âncoras com cabeçalho compensado"
```

---

### Task 15: Responsividade nos viewports-alvo (commit 14 — `fix(3.2.10)`)

**Files:**

- Modify: os componentes que a conferência apontar, dentro de `src/components/**`
- Modify: `e2e/home.spec.ts`

**Interfaces:**

- Consumes: os cinco PNG de `docs/inventario/baseline/`.
- Produces: a prova de que nenhum dos quatro viewports tem overflow horizontal.

- [ ] **Step 1: Escrever o E2E que falha**

Acrescentar a `e2e/home.spec.ts`:

```ts
for (const width of [375, 768, 1440, 1920]) {
  test(`não há rolagem horizontal em ${width}px`, async ({ page }) => {
    await page.setViewportSize({ width, height: 900 })
    await page.goto('/')

    const overflow = await page.evaluate(() => ({
      scrollWidth: document.documentElement.scrollWidth,
      clientWidth: document.documentElement.clientWidth,
    }))

    expect(overflow.scrollWidth).toBeLessThanOrEqual(overflow.clientWidth)
  })
}
```

- [ ] **Step 2: Rodar e ver o estado real**

```bash
pnpm e2e -- home.spec.ts
```

Se os quatro passarem de primeira, registre isso — a task continua pelos Steps 3 e 4, que são conferência visual, não código.

- [ ] **Step 3: Conferir os quatro viewports contra o baseline**

```bash
pnpm dev
```

Comparar, lado a lado, com `docs/inventario/baseline/`:

| viewport | arquivo             | o que precisa bater                                                                    |
| -------- | ------------------- | -------------------------------------------------------------------------------------- |
| 375      | `home-375.png`      | cabeçalho branco de 80px, hero sem foto, tudo empilhado, rodapé de 78px                |
| 375      | `home-375-menu.png` | painel do menu com borda azul no topo e 4 itens separados por 1px                      |
| 768      | `home-768.png`      | ainda menu mobile, ainda empilhado, linha em 80% do viewport                           |
| 1440     | `home-1440.png`     | menu desktop, hero em duas metades com foto, `Somos` 320+700, 3 cards, painéis de 1080 |
| 1920     | `home-1920.png`     | idêntico ao 1440 com margem lateral maior — a linha continua travada em 1080           |

- [ ] **Step 4: Corrigir o que divergir**

Cada ajuste usa token existente ou a variante `desktop:`; nenhum número novo entra sem estar em `04-tipografia.md` ou `05-layout.md`. Se aparecer divergência que precise de valor não medido, **pare**: medir é o Task 1, não este.

- [ ] **Step 5: Gate e commit**

```bash
pnpm format && pnpm check && pnpm e2e
git add src e2e/home.spec.ts
git commit -m "fix(3.2.10): ajustar responsividade nos quatro viewports-alvo"
```

---

### Task 16: Smoke tests do clone e fechamento da matriz (commit 15 — `test(3.2.11)`)

**Files:**

- Create: `src/app/App.test.tsx`
- Modify: `e2e/home.spec.ts`
- Modify: `docs/inventario/README.md`

**Interfaces:**

- Consumes: tudo que os Tasks 6 a 15 produziram.
- Produces: o teste que falha se uma seção desaparecer — o aceite literal da `3.2.11` — e a matriz de paridade fechada.

- [ ] **Step 1: Escrever o teste de composição**

`src/app/App.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { site } from '../content/site'
import { App } from './App'

describe('App', () => {
  it('compõe as cinco seções da home, na ordem do original', () => {
    const { container } = render(<App />)
    const ids = [...container.querySelectorAll('section[id]')].map(
      (section) => section.id,
    )

    expect(ids).toEqual(['Intrucción', 'Somos', 'Cursos', 'Contacto'])
  })

  it('expõe o conteúdo de cada seção', () => {
    render(<App />)

    expect(
      screen.getByRole('heading', { level: 1, name: site.hero.title }),
    ).toBeTruthy()
    expect(screen.getByText(site.institucional.body)).toBeTruthy()
    expect(screen.getAllByRole('heading', { level: 4 })).toHaveLength(3)
    for (const curso of site.cursos.items) {
      expect(screen.getByText(curso.nombre)).toBeTruthy()
    }
    expect(screen.getAllByRole('textbox')).toHaveLength(4)
    expect(screen.getByText(site.footer.copyright)).toBeTruthy()
  })

  it('mantém as landmarks de cabeçalho, conteúdo e rodapé', () => {
    render(<App />)

    expect(screen.getByRole('banner')).toBeTruthy()
    expect(screen.getByRole('main')).toBeTruthy()
    expect(screen.getByRole('contentinfo')).toBeTruthy()
  })
})
```

- [ ] **Step 2: Rodar e ver passar**

```bash
pnpm test
```

Esperado: toda a suíte verde. Se o teste de ordem falhar, é sinal de que uma seção saiu de lugar — que é exatamente o que ele existe para pegar.

- [ ] **Step 3: Escrever a prova de independência de host**

Acrescentar a `e2e/home.spec.ts` — aceite literal da `3.1.4` e de D6:

```ts
test('nenhuma requisição sai para host do WordPress ou do Google', async ({
  page,
}) => {
  const foreign: string[] = []
  page.on('request', (request) => {
    const url = request.url()
    if (
      url.includes('lotusotec.cl') ||
      url.includes('stackstaging') ||
      url.includes('fonts.googleapis.com') ||
      url.includes('fonts.gstatic.com')
    ) {
      foreign.push(url)
    }
  })

  await page.goto('/')
  await page.waitForLoadState('networkidle')

  expect(foreign).toEqual([])
})
```

- [ ] **Step 4: Rodar o E2E completo**

```bash
pnpm e2e
```

Esperado: `home.spec.ts`, `menu.spec.ts` e `a11y.spec.ts` verdes. `a11y.spec.ts` continua **reportando sem reprovar** — o gate que reprova violação nasce na Sprint 4, débito `D-11`.

- [ ] **Step 5: Fechar a matriz de paridade**

Em `docs/inventario/README.md`, as linhas `pendente decisão` resolvidas por este bloco mudam de estado, e as divergências novas entram:

| item                           | novo estado               | motivo                                                                       |
| ------------------------------ | ------------------------- | ---------------------------------------------------------------------------- |
| CTA do hero (`Learn More`)     | `fiel`                    | reproduzido como medido, invisibilidade inclusa (D1)                         |
| CTA `See More`                 | `fiel`                    | mesma condição (D1)                                                          |
| Destaque `ALUMNOS`             | `fiel`                    | rótulo e corpo verbatim, inconsistência preservada (D2)                      |
| Rodapé (`Copyright © 2022`)    | `fiel`                    | ano publicado mantido (D2)                                                   |
| Imagens responsivas (`srcset`) | `divergência intencional` | variantes fora; cada imagem entra cheia (D4)                                 |
| Host de assets `stackstaging`  | `divergência intencional` | os 6 assets passam a ser servidos localmente, deduplicados por `sha256` (D3) |
| Google Fonts                   | `divergência intencional` | 5 faces self-hosted em `src/assets/fonts/` (D6)                              |
| `lang` do `<html>`             | `divergência intencional` | `es` → `es-CL`, pedido pelo aceite da `3.1.1` (D8)                           |
| `<label>` dos campos           | `divergência intencional` | rótulo oculto por campo, `placeholder` visível intacto — zero pixel (D9)     |
| `alt` das imagens de conteúdo  | `divergência intencional` | `alt=""` → descrição real em 4 imagens — zero pixel (D9)                     |
| Ícones dos destaques           | `divergência intencional` | glifo ETmodules do Divi → `lucide-react`, desenho aproximado                 |
| `background-texture.jpg`       | `divergência intencional` | catalogado, sem pixel visível no baseline; não portado                       |
| Menu duplicado no DOM          | `divergência intencional` | o clone renderiza o menu uma vez                                             |

A página residual `/http-18-230-15-185/` **continua** `pendente decisão`: está fora do escopo deste bloco.

Acrescentar à lista de divergências intencionais os itens novos, e à seção **Pendências de João** o registro de que os itens 2, 4 e 6 foram decididos em 2026-08-25 (manter verbatim, manter `2022`, reproduzir os CTAs como estão).

- [ ] **Step 6: Gate final e commit**

```bash
pnpm format && pnpm check && pnpm e2e
```

Anexar a saída real dos dois comandos ao relatório da task. Build verde não prova paridade visual — a conferência humana do Step 3 do Task 15 é parte do aceite, não substituível por gate.

```bash
git add src/app/App.test.tsx e2e/home.spec.ts docs/inventario/README.md
git commit -m "test(3.2.11): adicionar smoke tests do clone e fechar a matriz de paridade"
```

---

## Handoff

```yaml
executor: claude
reviewer: codex
work_class: architectural
active_work_item: 3.1.1-3.2.11
active_branch: feat/3-1-1-3-2-11-clone-estatico
paths_autorizados:
  - 'src/**'
  - 'public/**'
  - 'e2e/**'
  - 'index.html'
  - 'docs/inventario/04-tipografia.md'
  - 'docs/inventario/05-layout.md'
  - 'docs/inventario/08-seo.md'
  - 'docs/inventario/README.md'
  - 'scripts/inventario/**'
```

O revisor é Codex, agente diferente do executor, e revisa sobre `main..HEAD` da branch. Pontos que a review precisa checar por diff, não por relatório:

1. nenhum token de `@theme` carrega valor que não esteja em `04-tipografia.md` ou `05-layout.md` **depois** da correção do Task 1;
2. nenhum valor arbitrário do Tailwind repete um valor que já é token (D5);
3. todo texto institucional bate caractere a caractere com `02-conteudo.md`, inclusive `NUESTRos cursos`, `888 horas` sob `ALUMNOS` e `Copyright © 2022`;
4. o formulário não tem `action`, não posta e não renderiza mensagem de sucesso, erro ou carregamento (D10);
5. nenhum arquivo fora de `paths_autorizados` foi tocado — em especial `package.json`, `vitest.config.ts` e `eslint.config.js`;
6. `pnpm check` e `pnpm e2e` foram executados de verdade, com saída anexada.

## Débitos criados por este bloco

- **`D-12`** — a EAP `3.1.4` manda os assets para `public/`, a rule manda para `src/assets/`; a regra do repositório venceu (D3). O Notion fica stale até reconciliação autorizada.
- **`D-13`** — o critério da `3.1.1` usa "Client Component", vocabulário de Next.js que não existe em Vite. Terceira instância de `D-01`.
- **`D-14`** — breakpoints exatos não medidos. **Fechado** pelo Task 1.
- **`D-15`** — `.claude/rules/architecture.md` descreve `public/icons.svg` como o sprite onde ícone novo entra; o sprite era do scaffold Vite e morreu no Task 2, e os ícones deste bloco vêm de `lucide-react`. A rule ficou órfã e precisa de revisão numa task própria — rules não estão em `paths_autorizados`.
- **`D-16`** — `extract-styles.mjs` continua medindo o primeiro nó de cada seletor por seção, então ainda alcança nós-eco escondidos do Divi em qualquer seção nova. O Task 1 corrige a cor (`cssColor`) e documenta os quatro casos conhecidos, mas não corrige a seleção do nó. Regerar `styles.json` sem tratar isso reintroduz o erro.
