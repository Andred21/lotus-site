# Inventário do site — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produzir o inventário versionado de `https://lotusotec.cl/` — estrutura, copy, assets, tokens, layout, baseline visual, formulário, SEO, dados institucionais e matriz de paridade — para que a Sprint 2 clone contra fatos medidos, não contra memória.

**Architecture:** Quatro scripts Node em `scripts/inventario/` medem o site real (Chromium do Playwright e `fetch`) e gravam evidência bruta em JSON, binários e PNG. Os dez documentos de `docs/inventario/` são redigidos sobre essa evidência e citam data de captura e hash. Uma suíte Vitest valida o inventário como artefato: manifesto íntegro, matriz cobrindo todas as seções, nenhum placeholder.

**Tech Stack:** Node 24.19.0, pnpm 11, `@playwright/test` (Chromium), Vitest, Prettier, ESLint flat config, TypeScript via `checkJs` sobre `.mjs`.

## Global Constraints

- Gerenciador é `pnpm`; `engineStrict: true` derruba qualquer script fora de Node `>=24.19.0 <25`.
- Scripts novos são `.mjs` com JSDoc: `erasableSyntaxOnly` proíbe sintaxe de tipo em runtime e `verbatimModuleSyntax` exige `import type` explícito quando houver tipo.
- `tsconfig.node.json` já tem `"scripts/**/*.mjs"` no `include`; nenhum arquivo novo de script fica fora do `tsc -b`.
- Compilador roda com `strict`, `noUncheckedIndexedAccess`, `noUnusedLocals` e `noUnusedParameters`: todo acesso indexado precisa de guarda.
- Um commit por EAP, Conventional Commits com a EAP no escopo: `docs(2.1.1): ...`.
- Nada é escrito em Notion, Google Drive, Figma ou no site público.
- O formulário de contato **nunca** é submetido (decisão D5 da spec).
- Todo documento de `docs/inventario/` cita a data de captura e o commit-base da evidência que usou.
- Todo texto em espanhol é transcrito do DOM, nunca digitado à mão.
- `pnpm format` roda antes de cada commit; `pnpm check` precisa ficar verde ao fim de cada task.

**Divergências da spec, deliberadas e registradas:**

1. A spec listou três scripts (`fetch-assets`, `extract-tokens`, `capture-baseline`). O plano usa quatro: `extract-dom.mjs` entra porque `2.1.1`, `2.1.2`, `2.1.7`, `2.1.8` e `2.1.9` precisam de extração de DOM reprodutível — a própria spec exige texto extraído do DOM —, e `extract-tokens` vira `extract-styles.mjs` porque tipografia (`2.1.4`) e caixa/grid (`2.1.5`) saem da mesma passagem de medição.
2. A numeração dos documentos muda em relação à tabela da spec: o baseline visual ganhou documento próprio (`06-baseline.md`), então formulário, SEO e dados institucionais viram `07-formulario.md`, `08-seo.md` e `09-dados.md`. O conteúdo de cada EAP é o mesmo.
3. A spec autorizou `tsconfig.node.json` (apenas `include`); o arquivo já cobre `scripts/**/*.mjs`, então ele não é tocado. Em troca, `vitest.config.ts` entra nos paths tocados para que os testes dos helpers e do inventário rodem dentro de `pnpm test`.

---

## File Structure

| Arquivo                                           | Responsabilidade                                                                                                                                               |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/inventario/lib/site.mjs`                 | constantes do alvo (URL, viewports, diretório de saída), abertura de página com scroll completo e helpers puros (`assetFileName`, `normalizeText`, `rgbToHex`) |
| `scripts/inventario/lib/site.test.mjs`            | testes unitários dos helpers puros                                                                                                                             |
| `scripts/inventario/extract-dom.mjs`              | grava `docs/inventario/dom.json`: `head`, seções, âncoras, links, headings, blocos de texto e campos de formulário                                             |
| `scripts/inventario/extract-styles.mjs`           | grava `docs/inventario/styles.json`: tipografia, cor, borda, sombra e caixa por seção, nos quatro viewports                                                    |
| `scripts/inventario/fetch-assets.mjs`             | baixa os assets da home para `docs/inventario/assets/` e grava `assets/manifest.json` com origem, host, bytes e `sha256`                                       |
| `scripts/inventario/capture-baseline.mjs`         | grava `docs/inventario/baseline/*.png` full-page nos quatro viewports e o estado de menu mobile aberto                                                         |
| `scripts/inventario/inventario.test.mjs`          | valida o inventário como artefato: manifesto íntegro, matriz completa, sem placeholder                                                                         |
| `docs/inventario/01-estrutura.md` … `08-dados.md` | os oito documentos redigidos                                                                                                                                   |
| `docs/inventario/README.md`                       | índice e matriz de paridade                                                                                                                                    |
| `package.json`                                    | scripts `inventario:dom`, `inventario:styles`, `inventario:assets`, `inventario:baseline`                                                                      |
| `vitest.config.ts`                                | passa a incluir `scripts/**/*.test.mjs`                                                                                                                        |

---

### Task 1: Ferramenta base e mapa de estrutura (EAP 2.1.1)

**Files:**

- Create: `scripts/inventario/lib/site.mjs`
- Create: `scripts/inventario/lib/site.test.mjs`
- Create: `scripts/inventario/extract-dom.mjs`
- Create: `docs/inventario/dom.json` (gerado)
- Create: `docs/inventario/01-estrutura.md`
- Modify: `package.json` (bloco `scripts`)
- Modify: `vitest.config.ts` (campo `include`)

**Interfaces:**

- Consumes: nada.
- Produces: `SITE_URL: string`, `OUT_DIR: string`, `VIEWPORTS: Viewport[]`, `openPage(viewport): Promise<{ browser, page }>`, `assetFileName(url: string): string`, `normalizeText(text: string): string`, `rgbToHex(value: string): string`, e o arquivo `docs/inventario/dom.json` com o formato `{ capturedAt, url, head, sections[], forms[] }`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `scripts/inventario/lib/site.test.mjs`:

```js
import { describe, expect, it } from 'vitest'
import { assetFileName, normalizeText, rgbToHex } from './site.mjs'

describe('assetFileName', () => {
  it('mantém o basename de um asset do domínio principal', () => {
    expect(
      assetFileName('https://lotusotec.cl/wp-content/uploads/2022/08/logo.png'),
    ).toBe('logo.png')
  })

  it('prefixa o host de staging para não colidir com o domínio principal', () => {
    expect(
      assetFileName('https://lotusotec-cl.us.stackstaging.com/img/logo.png'),
    ).toBe('staging-logo.png')
  })

  it('decodifica caracteres escapados na URL', () => {
    expect(
      assetFileName('https://lotusotec.cl/wp-content/uploads/Logo%20LOTUS.png'),
    ).toBe('Logo LOTUS.png')
  })
})

describe('normalizeText', () => {
  it('colapsa espaços e quebras de linha do DOM', () => {
    expect(normalizeText('  Formación\n   de   excelencia  ')).toBe(
      'Formación de excelencia',
    )
  })
})

describe('rgbToHex', () => {
  it('converte rgb do getComputedStyle para hex', () => {
    expect(rgbToHex('rgb(15, 23, 42)')).toBe('#0f172a')
  })

  it('preserva o valor original quando não é rgb', () => {
    expect(rgbToHex('transparent')).toBe('transparent')
  })
})
```

- [ ] **Step 2: Incluir os testes de `scripts/` no Vitest**

Editar `vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.{ts,tsx}', 'scripts/**/*.test.mjs'],
    exclude: ['e2e/**', 'node_modules/**', 'dist/**'],
  },
})
```

- [ ] **Step 3: Rodar o teste e confirmar que falha**

Run: `pnpm test`
Expected: FAIL — `Failed to resolve import "./site.mjs"`.

- [ ] **Step 4: Escrever `lib/site.mjs`**

```js
// Ferramenta de inventário: roda em Node, nunca entra no bundle da aplicação.
import { chromium } from '@playwright/test'

export const SITE_URL = 'https://lotusotec.cl/'
export const OUT_DIR = 'docs/inventario'

/** @typedef {{ name: string, width: number, height: number }} Viewport */

/** @type {Viewport[]} */
export const VIEWPORTS = [
  { name: '375', width: 375, height: 812 },
  { name: '768', width: 768, height: 1024 },
  { name: '1440', width: 1440, height: 900 },
  { name: '1920', width: 1920, height: 1080 },
]

/**
 * Viewport de referência para medições que não variam por largura.
 * @returns {Viewport}
 */
export function desktopViewport() {
  const viewport = VIEWPORTS.find((candidate) => candidate.name === '1440')
  if (!viewport) throw new Error('viewport 1440 ausente em VIEWPORTS')
  return viewport
}

/**
 * Nome local estável para um asset remoto. O host de staging ganha prefixo
 * para que dois hosts com o mesmo basename não colidam no diretório local.
 * @param {string} url
 * @returns {string}
 */
export function assetFileName(url) {
  const parsed = new URL(url)
  const segments = parsed.pathname.split('/')
  const base = decodeURIComponent(segments[segments.length - 1] ?? 'asset')
  return parsed.hostname.includes('stackstaging') ? `staging-${base}` : base
}

/**
 * @param {string} text
 * @returns {string}
 */
export function normalizeText(text) {
  return text.replace(/\s+/g, ' ').trim()
}

/**
 * @param {string} value
 * @returns {string}
 */
export function rgbToHex(value) {
  const match = value.match(/^rgba?\((\d+),\s*(\d+),\s*(\d+)/)
  if (!match) return value
  const [, r, g, b] = match
  if (r === undefined || g === undefined || b === undefined) return value
  const hex = [r, g, b]
    .map((channel) => Number(channel).toString(16).padStart(2, '0'))
    .join('')
  return `#${hex}`
}

/**
 * Abre a home no viewport pedido, espera a rede parar e rola a página inteira
 * para disparar o lazy-load do Divi antes de qualquer medição ou captura.
 * @param {Viewport} viewport
 * @returns {Promise<{ browser: import('@playwright/test').Browser, page: import('@playwright/test').Page }>}
 */
export async function openPage(viewport) {
  const browser = await chromium.launch()
  const page = await browser.newPage({
    viewport: { width: viewport.width, height: viewport.height },
  })
  await page.goto(SITE_URL, { waitUntil: 'networkidle', timeout: 60_000 })
  await page.evaluate(async () => {
    const step = window.innerHeight
    for (let y = 0; y < document.body.scrollHeight; y += step) {
      window.scrollTo(0, y)
      await new Promise((resolve) => setTimeout(resolve, 250))
    }
    window.scrollTo(0, 0)
  })
  await page.waitForTimeout(1000)
  return { browser, page }
}
```

- [ ] **Step 5: Rodar o teste e confirmar que passa**

Run: `pnpm test`
Expected: PASS — 6 testes em `scripts/inventario/lib/site.test.mjs`.

- [ ] **Step 6: Escrever `extract-dom.mjs`**

```js
import { mkdirSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { OUT_DIR, SITE_URL, desktopViewport, openPage } from './lib/site.mjs'

const { browser, page } = await openPage(desktopViewport())

const inventory = await page.evaluate(() => {
  const clean = (text) => text.replace(/\s+/g, ' ').trim()

  const head = {
    title: document.title,
    description:
      document
        .querySelector('meta[name="description"]')
        ?.getAttribute('content') ?? null,
    canonical:
      document.querySelector('link[rel="canonical"]')?.getAttribute('href') ??
      null,
    lang: document.documentElement.lang || null,
    openGraph: [...document.querySelectorAll('meta[property^="og:"]')].map(
      (tag) => ({
        property: tag.getAttribute('property'),
        content: tag.getAttribute('content'),
      }),
    ),
    generators: [...document.querySelectorAll('meta[name="generator"]')].map(
      (tag) => tag.getAttribute('content'),
    ),
  }

  const sections = [...document.querySelectorAll('section, [id]')]
    .filter(
      (element) => element.id && element.getBoundingClientRect().height > 40,
    )
    .map((element) => ({
      id: element.id,
      tag: element.tagName.toLowerCase(),
      headings: [...element.querySelectorAll('h1, h2, h3, h4, h5, h6')].map(
        (heading) => ({
          level: heading.tagName.toLowerCase(),
          text: clean(heading.textContent ?? ''),
        }),
      ),
      texts: [
        ...element.querySelectorAll(
          'p, li, span.elementor-button-text, .et_pb_text_inner',
        ),
      ]
        .map((node) => clean(node.textContent ?? ''))
        .filter((text) => text.length > 0),
      links: [...element.querySelectorAll('a[href]')].map((anchor) => ({
        text: clean(anchor.textContent ?? ''),
        href: anchor.getAttribute('href'),
      })),
      images: [...element.querySelectorAll('img')].map((image) => ({
        src: image.currentSrc || image.src,
        alt: image.getAttribute('alt'),
      })),
    }))

  const forms = [...document.querySelectorAll('form')].map((form) => ({
    action: form.getAttribute('action'),
    method: form.getAttribute('method'),
    fields: [...form.querySelectorAll('input, textarea, select')].map(
      (field) => ({
        name: field.getAttribute('name'),
        type: field.getAttribute('type') ?? field.tagName.toLowerCase(),
        placeholder: field.getAttribute('placeholder'),
        required: field.hasAttribute('required'),
        ariaLabel: field.getAttribute('aria-label'),
      }),
    ),
    submitLabel: clean(
      form.querySelector('button, input[type="submit"]')?.textContent ?? '',
    ),
  }))

  const nav = [...document.querySelectorAll('a[href*="#"]')].map((anchor) => ({
    text: clean(anchor.textContent ?? ''),
    href: anchor.getAttribute('href'),
  }))

  return { head, nav, sections, forms }
})

await browser.close()

mkdirSync(OUT_DIR, { recursive: true })
writeFileSync(
  join(OUT_DIR, 'dom.json'),
  `${JSON.stringify({ capturedAt: new Date().toISOString(), url: SITE_URL, ...inventory }, null, 2)}\n`,
)
console.log('dom.json gravado')
```

- [ ] **Step 7: Registrar o script no `package.json`**

Adicionar ao bloco `scripts`, logo após `agent:check`:

```json
"inventario:dom": "node scripts/inventario/extract-dom.mjs",
```

- [ ] **Step 8: Rodar a extração**

Run: `pnpm inventario:dom`
Expected: `dom.json gravado`, e `docs/inventario/dom.json` com `head.title` igual a `LOTUS | OTEC`.

- [ ] **Step 9: Redigir `docs/inventario/01-estrutura.md`**

O documento é escrito a partir de `dom.json` e cobre, nesta ordem: ordem das seções da home com id e âncora; itens de menu `Inicio`, `Quienes Somos`, `Cursos`, `Contacto` com o destino real de cada um; CTAs e para onde levam; comportamento de navegação (âncora na mesma página, sem rota nova); a página residual `/http-18-230-15-185/`; e os endpoints WordPress que respondem hoje e não terão equivalente no clone (`/wp-json/`, `/xmlrpc.php`, `/feed/`, `/comments/feed/`). Cabeçalho do arquivo cita `capturedAt` de `dom.json` e o commit-base.

- [ ] **Step 10: Formatar e commitar**

```bash
pnpm format
pnpm check
git add scripts/inventario package.json vitest.config.ts docs/inventario
git commit -m "docs(2.1.1): mapear estrutura da LP e navegação por âncoras"
```

---

### Task 2: Inventário de textos (EAP 2.1.2)

**Files:**

- Create: `docs/inventario/02-conteudo.md`
- Read: `docs/inventario/dom.json`

**Interfaces:**

- Consumes: `dom.json` (`sections[].headings`, `sections[].texts`, `sections[].links`, `forms[].fields`).
- Produces: chaves estáveis de conteúdo (`hero.title`, `hero.subtitle`, `institucional.body`, `destaques[n].label`, `cursos[n].nombre`, `contacto.form.<campo>`, `footer.copyright`) que a Sprint 2 usará em `src/content/`.

- [ ] **Step 1: Conferir cobertura da extração**

Run: `node -e "const d=require('./docs/inventario/dom.json');console.log(d.sections.map(s=>[s.id,s.headings.length,s.texts.length].join(' ')).join('\n'))"`
Expected: uma linha por seção, nenhuma com `0 0`.

- [ ] **Step 2: Redigir `docs/inventario/02-conteudo.md`**

Uma tabela por seção, colunas `chave | texto (es) | origem no DOM`. O texto vai verbatim do `dom.json`, em espanhol, sem tradução e sem correção ortográfica. Ao final, uma seção `Não transcrito` que lista explicitamente o que ficou de fora e por quê (texto puramente decorativo, `aria-label`, texto de imagem). Se nada ficou de fora, a seção diz isso.

- [ ] **Step 3: Verificar que nenhuma string visível ficou órfã**

Run: `node -e "const d=require('./docs/inventario/dom.json');const t=d.sections.flatMap(s=>s.texts);const doc=require('node:fs').readFileSync('docs/inventario/02-conteudo.md','utf8');const miss=t.filter(x=>x.length>3&&!doc.includes(x));console.log(miss.length?miss.join('\n'):'todas transcritas')"`
Expected: `todas transcritas`.

- [ ] **Step 4: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario/02-conteudo.md
git commit -m "docs(2.1.2): inventariar textos e conteúdo institucional atual"
```

---

### Task 3: Assets baixados e catalogados (EAP 2.1.3)

**Files:**

- Create: `scripts/inventario/fetch-assets.mjs`
- Create: `docs/inventario/assets/**` (gerado)
- Create: `docs/inventario/03-assets.md`
- Modify: `package.json` (bloco `scripts`)
- Modify: `scripts/inventario/inventario.test.mjs` (criado nesta task)

**Interfaces:**

- Consumes: `assetFileName` de `lib/site.mjs`; `dom.json` para saber onde cada imagem aparece.
- Produces: `docs/inventario/assets/manifest.json`, uma lista de `{ url, host, file, bytes, sha256, usedIn }`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `scripts/inventario/inventario.test.mjs`:

```js
import { createHash } from 'node:crypto'
import { existsSync, readFileSync } from 'node:fs'
import { join } from 'node:path'
import { describe, expect, it } from 'vitest'

const OUT = 'docs/inventario'

/** @returns {Array<{url: string, host: string, file: string, bytes: number, sha256: string, usedIn: string[]}>} */
const manifest = () =>
  JSON.parse(readFileSync(join(OUT, 'assets', 'manifest.json'), 'utf8')).assets

describe('manifesto de assets', () => {
  it('tem entrada para todo asset com arquivo local presente', () => {
    for (const asset of manifest()) {
      expect(existsSync(join(OUT, 'assets', asset.file))).toBe(true)
    }
  })

  it('registra bytes e sha256 conferentes com o arquivo em disco', () => {
    for (const asset of manifest()) {
      const bytes = readFileSync(join(OUT, 'assets', asset.file))
      expect(bytes.byteLength).toBe(asset.bytes)
      expect(createHash('sha256').update(bytes).digest('hex')).toBe(
        asset.sha256,
      )
    }
  })

  it('não deixa asset sem origem declarada', () => {
    for (const asset of manifest()) {
      expect(asset.url).toMatch(/^https:\/\//)
      expect(asset.host.length).toBeGreaterThan(0)
    }
  })
})
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `pnpm test`
Expected: FAIL — `ENOENT: no such file or directory, open 'docs/inventario/assets/manifest.json'`.

- [ ] **Step 3: Escrever `fetch-assets.mjs`**

```js
import { createHash } from 'node:crypto'
import { mkdirSync, readFileSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { OUT_DIR, assetFileName } from './lib/site.mjs'

/** @type {{ sections: Array<{ id: string, images: Array<{ src: string }> }> }} */
const dom = JSON.parse(readFileSync(join(OUT_DIR, 'dom.json'), 'utf8'))

/** @type {Map<string, string[]>} */
const usage = new Map()
for (const section of dom.sections) {
  for (const image of section.images) {
    if (!image.src.startsWith('https://')) continue
    const url = image.src.split('?')[0] ?? image.src
    usage.set(url, [...(usage.get(url) ?? []), section.id])
  }
}

const assetsDir = join(OUT_DIR, 'assets')
mkdirSync(assetsDir, { recursive: true })

const assets = []
for (const [url, usedIn] of [...usage.entries()].sort()) {
  const response = await fetch(url)
  if (!response.ok)
    throw new Error(`falha ao baixar ${url}: ${response.status}`)
  const bytes = Buffer.from(await response.arrayBuffer())
  const file = assetFileName(url)
  writeFileSync(join(assetsDir, file), bytes)
  assets.push({
    url,
    host: new URL(url).hostname,
    file,
    bytes: bytes.byteLength,
    sha256: createHash('sha256').update(bytes).digest('hex'),
    usedIn,
  })
  console.log(`${file} ${bytes.byteLength} B`)
}

writeFileSync(
  join(assetsDir, 'manifest.json'),
  `${JSON.stringify({ capturedAt: new Date().toISOString(), assets }, null, 2)}\n`,
)
```

- [ ] **Step 4: Registrar o script e rodar**

Adicionar ao `package.json`: `"inventario:assets": "node scripts/inventario/fetch-assets.mjs",`

Run: `pnpm inventario:assets`
Expected: uma linha por asset e `docs/inventario/assets/manifest.json` gravado.

- [ ] **Step 5: Rodar o teste e confirmar que passa**

Run: `pnpm test`
Expected: PASS — três testes de manifesto verdes.

- [ ] **Step 6: Provar o determinismo exigido pelo aceite**

```bash
cp docs/inventario/assets/manifest.json /tmp/manifest-antes.json
pnpm inventario:assets
node -e "const a=require('/tmp/manifest-antes.json').assets,b=require('./docs/inventario/assets/manifest.json').assets;const k=x=>x.map(y=>y.url+y.sha256).join('|');console.log(k(a)===k(b)?'determinístico':'DIVERGIU')"
```

Expected: `determinístico`.

- [ ] **Step 7: Redigir `docs/inventario/03-assets.md`**

Tabela com `arquivo | origem | host | bytes | sha256 (8 primeiros) | usado em | candidato local`. O candidato local segue a regra do repositório: imagem importada por componente vai para `src/assets/`; arquivo que precisa de URL estável (favicon, og-image) vai para `public/`. Uma seção final registra que o host `lotusotec-cl.us.stackstaging.com` deixa de ser dependência do clone.

- [ ] **Step 8: Formatar e commitar**

```bash
pnpm format
pnpm check
git add scripts/inventario package.json docs/inventario
git commit -m "docs(2.1.3): inventariar e baixar assets visuais do WordPress"
```

---

### Task 4: Tipografia, cores e tokens (EAP 2.1.4)

**Files:**

- Create: `scripts/inventario/extract-styles.mjs`
- Create: `docs/inventario/styles.json` (gerado)
- Create: `docs/inventario/04-tipografia.md`
- Modify: `package.json` (bloco `scripts`)

**Interfaces:**

- Consumes: `VIEWPORTS`, `openPage`, `rgbToHex` de `lib/site.mjs`; `dom.json` para a lista de seções.
- Produces: `docs/inventario/styles.json` no formato `{ capturedAt, viewports: { "375": Measurement[], ... } }`, com `Measurement = { sectionId, selector, fontFamily, fontSize, fontWeight, lineHeight, letterSpacing, color, backgroundColor, borderRadius, boxShadow, width, paddingTop, paddingBottom, marginBottom, gap, height }`.

- [ ] **Step 1: Escrever `extract-styles.mjs`**

```js
import { readFileSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { OUT_DIR, VIEWPORTS, openPage, rgbToHex } from './lib/site.mjs'

/** @type {{ sections: Array<{ id: string }> }} */
const dom = JSON.parse(readFileSync(join(OUT_DIR, 'dom.json'), 'utf8'))
const sectionIds = dom.sections.map((section) => section.id)

/** @type {Record<string, unknown[]>} */
const byViewport = {}

for (const viewport of VIEWPORTS) {
  const { browser, page } = await openPage(viewport)
  const measurements = await page.evaluate((ids) => {
    const read = (element, sectionId, selector) => {
      const style = window.getComputedStyle(element)
      const box = element.getBoundingClientRect()
      return {
        sectionId,
        selector,
        fontFamily: style.fontFamily,
        fontSize: style.fontSize,
        fontWeight: style.fontWeight,
        lineHeight: style.lineHeight,
        letterSpacing: style.letterSpacing,
        color: style.color,
        backgroundColor: style.backgroundColor,
        borderRadius: style.borderRadius,
        boxShadow: style.boxShadow,
        width: Math.round(box.width),
        height: Math.round(box.height),
        paddingTop: style.paddingTop,
        paddingBottom: style.paddingBottom,
        marginBottom: style.marginBottom,
        gap: style.gap,
      }
    }

    const out = []
    for (const id of ids) {
      const section = document.getElementById(id)
      if (!section) continue
      out.push(read(section, id, `#${id}`))
      for (const selector of ['h1', 'h2', 'h3', 'p', 'a', 'button', 'img']) {
        const child = section.querySelector(selector)
        if (child) out.push(read(child, id, `#${id} ${selector}`))
      }
    }
    return out
  }, sectionIds)
  await browser.close()

  byViewport[viewport.name] = measurements.map((measurement) => ({
    ...measurement,
    color: rgbToHex(measurement.color),
    backgroundColor: rgbToHex(measurement.backgroundColor),
  }))
  console.log(`${viewport.name}px: ${measurements.length} medições`)
}

writeFileSync(
  join(OUT_DIR, 'styles.json'),
  `${JSON.stringify({ capturedAt: new Date().toISOString(), viewports: byViewport }, null, 2)}\n`,
)
```

- [ ] **Step 2: Registrar o script e rodar**

Adicionar ao `package.json`: `"inventario:styles": "node scripts/inventario/extract-styles.mjs",`

Run: `pnpm inventario:styles`
Expected: quatro linhas (`375px`, `768px`, `1440px`, `1920px`), cada uma com contagem maior que zero.

- [ ] **Step 3: Conferir as famílias medidas**

Run: `node -e "const s=require('./docs/inventario/styles.json');console.log([...new Set(s.viewports['1440'].map(m=>m.fontFamily))].join('\n'))"`
Expected: as famílias reais, incluindo `Montserrat` e `Open Sans`.

- [ ] **Step 4: Redigir `docs/inventario/04-tipografia.md`**

Seções: **Famílias** (nome, pesos observados, origem Google Fonts, `font-display`); **Escala** (tabela `papel | seletor | font-size | font-weight | line-height | letter-spacing`, com o papel nomeado — `h1 hero`, `h2 seção`, `corpo`, `botão`); **Paleta** (tabela `hex | onde aparece | papel`, agrupando cor de texto, de fundo e de destaque); **Borda e sombra** (valores de `border-radius` e `box-shadow` observados). Cada valor cita o seletor de origem em `styles.json`. Nenhum valor entra sem medição correspondente.

- [ ] **Step 5: Formatar e commitar**

```bash
pnpm format
pnpm check
git add scripts/inventario package.json docs/inventario
git commit -m "docs(2.1.4): medir tipografia, cores e tokens visuais atuais"
```

---

### Task 5: Containers, grid, espaçamento e breakpoints (EAP 2.1.5)

**Files:**

- Create: `docs/inventario/05-layout.md`
- Read: `docs/inventario/styles.json`

**Interfaces:**

- Consumes: `styles.json` (`width`, `height`, `paddingTop`, `paddingBottom`, `marginBottom`, `gap` por seção e viewport).
- Produces: a tabela de larguras e alturas por viewport que `2.1.6` usa para justificar diferenças de captura.

- [ ] **Step 1: Extrair a tabela por viewport**

Run: `node -e "const s=require('./docs/inventario/styles.json');for(const [vp,list] of Object.entries(s.viewports)){console.log('##',vp);for(const m of list.filter(x=>x.selector.startsWith('#')&&!x.selector.includes(' '))){console.log(m.sectionId,m.width,m.height,m.paddingTop,m.paddingBottom)}}"`
Expected: quatro blocos, um por viewport, com número em todas as colunas.

- [ ] **Step 2: Redigir `docs/inventario/05-layout.md`**

Seções: **Container** (largura máxima de conteúdo por viewport e o padding lateral que a produz); **Altura por seção** (tabela `seção | 375 | 768 | 1440 | 1920`); **Espaçamento vertical** (padding e margin entre seções, por viewport); **Grid interno** (colunas observadas em destaques e cursos, com `gap`); **Breakpoints** (as larguras em que os números mudam de patamar, derivadas da comparação entre viewports, com a ressalva de que são breakpoints observados e não declarados). Cada número cita a chave de `styles.json`.

- [ ] **Step 3: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario/05-layout.md
git commit -m "docs(2.1.5): medir containers, grid, espaçamentos e breakpoints"
```

---

### Task 6: Baseline visual (EAP 2.1.6)

**Files:**

- Create: `scripts/inventario/capture-baseline.mjs`
- Create: `docs/inventario/baseline/*.png` (gerado)
- Create: `docs/inventario/06-baseline.md`
- Modify: `package.json` (bloco `scripts`)

**Interfaces:**

- Consumes: `VIEWPORTS`, `openPage` de `lib/site.mjs`.
- Produces: `docs/inventario/baseline/home-<viewport>.png` e `home-375-menu.png`.

- [ ] **Step 1: Escrever `capture-baseline.mjs`**

```js
import { mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { OUT_DIR, VIEWPORTS, openPage } from './lib/site.mjs'

const baselineDir = join(OUT_DIR, 'baseline')
mkdirSync(baselineDir, { recursive: true })

for (const viewport of VIEWPORTS) {
  const { browser, page } = await openPage(viewport)
  await page.screenshot({
    path: join(baselineDir, `home-${viewport.name}.png`),
    fullPage: true,
  })
  console.log(`home-${viewport.name}.png`)

  if (viewport.name === '375') {
    const toggle = page
      .locator('.mobile_menu_bar, .elementor-menu-toggle')
      .first()
    if (await toggle.count()) {
      await toggle.click()
      await page.waitForTimeout(800)
      await page.screenshot({
        path: join(baselineDir, 'home-375-menu.png'),
        fullPage: false,
      })
      console.log('home-375-menu.png')
    }
  }

  await browser.close()
}
```

- [ ] **Step 2: Registrar o script e rodar**

Adicionar ao `package.json`: `"inventario:baseline": "node scripts/inventario/capture-baseline.mjs",`

Run: `pnpm inventario:baseline`
Expected: cinco linhas, uma por arquivo gravado.

- [ ] **Step 3: Conferir que as capturas são de página inteira**

Run: `node -e "const{readdirSync,statSync}=require('node:fs');for(const f of readdirSync('docs/inventario/baseline'))console.log(f,statSync('docs/inventario/baseline/'+f).size)"`
Expected: cinco arquivos, nenhum com tamanho zero.

- [ ] **Step 4: Redigir `docs/inventario/06-baseline.md`**

Registra: comando que gera cada arquivo; viewport, altura total capturada e peso de cada PNG; o que muda entre viewports (ordem de blocos, menu que vira hambúrguer, imagem que troca de posição), cada diferença descrita em uma linha; o estado `home-375-menu.png` e por que ele existe; e a ressalva de que o baseline é referência humana, não teste de diff automático — o Playwright do repositório cobre só Chromium, e o clone ainda não existe.

- [ ] **Step 5: Formatar e commitar**

```bash
pnpm format
pnpm check
git add scripts/inventario package.json docs/inventario
git commit -m "docs(2.1.6): capturar baseline visual do site atual nos viewports-alvo"
```

---

### Task 7: Contrato do formulário (EAP 2.1.7)

**Files:**

- Create: `docs/inventario/07-formulario.md`
- Read: `docs/inventario/dom.json`

**Interfaces:**

- Consumes: `dom.json` (`forms[]`).
- Produces: o contrato funcional que a Sprint 3 implementa.

- [ ] **Step 1: Ler os campos extraídos**

Run: `node -e "const d=require('./docs/inventario/dom.json');console.log(JSON.stringify(d.forms,null,2))"`
Expected: ao menos um formulário, com `fields` nomeando `Nombre Completo`, `Correo Electrónico`, `Empresa` e `Mensaje`.

- [ ] **Step 2: Redigir `docs/inventario/07-formulario.md`**

Três seções separadas e rotuladas: **Observado** (campos, `name`, `type`, `placeholder`, `required`, rótulo visível, texto do botão, `action` e `method` do form, plugin responsável); **Não observado** (o que só apareceria após envio — mensagem de sucesso, mensagem de erro, validação server-side, destinatário real do email — declarado como não verificado porque o formulário não foi submetido, decisão D5); **Decisão futura** (o que a Sprint 3 precisa escolher: endpoint próprio, serviço de terceiro, honeypot ou captcha, campos obrigatórios finais). Nenhuma linha da primeira seção pode conter suposição; nenhuma da segunda pode conter afirmação.

- [ ] **Step 3: Confirmar que nenhum envio foi feito**

Run: `git log --oneline -20 && grep -rn "form.submit\|click()" scripts/inventario | grep -v menu_bar`
Expected: nenhuma ocorrência de submissão de formulário nos scripts.

- [ ] **Step 4: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario/07-formulario.md
git commit -m "docs(2.1.7): documentar comportamento atual do formulário de contato"
```

---

### Task 8: Baseline de SEO (EAP 2.1.8)

**Files:**

- Create: `docs/inventario/08-seo.md`
- Read: `docs/inventario/dom.json`

**Interfaces:**

- Consumes: `dom.json` (`head`, `sections[].headings`).
- Produces: checklist migratório consumido por `2.1.10` e pela Sprint 4.

- [ ] **Step 1: Coletar os artefatos remotos de indexação**

```bash
curl -sS https://lotusotec.cl/robots.txt
curl -sS https://lotusotec.cl/wp-sitemap.xml
curl -sS "https://lotusotec.cl/wp-json/wp/v2/pages?per_page=100&_fields=id,slug,link,status"
```

Expected: `robots.txt` com `Disallow: /wp-admin/` e a linha `Sitemap:`; o sitemap listando os sub-sitemaps; o REST retornando duas páginas.

- [ ] **Step 2: Redigir `docs/inventario/08-seo.md`**

Tabela `item | valor atual | ação no clone`, cobrindo: `<title>`, `meta description` (ausente hoje), `canonical`, `lang` do `<html>`, tags `og:` (ausentes hoje), `twitter:`, hierarquia de headings por seção com marcação de quantos `h1` existem, `robots.txt`, sitemap, URLs publicadas e a página residual `/http-18-230-15-185/`. A coluna `ação no clone` só admite três valores: `preservar`, `criar` ou `pendente decisão`.

- [ ] **Step 3: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario/08-seo.md
git commit -m "docs(2.1.8): levantar baseline de SEO e metadados do site atual"
```

---

### Task 9: Dados institucionais e links externos (EAP 2.1.9)

**Files:**

- Create: `docs/inventario/09-dados.md`
- Read: `docs/inventario/dom.json`, `docs/inventario/02-conteudo.md`

**Interfaces:**

- Consumes: `dom.json` (`sections[].links`, `sections[].texts`).
- Produces: a lista de pendências que `2.1.10` transforma em decisão de João.

- [ ] **Step 1: Extrair links externos e contatos**

Run: `node -e "const d=require('./docs/inventario/dom.json');const l=d.sections.flatMap(s=>s.links).filter(x=>x.href&&!x.href.startsWith('#')&&!x.href.includes('lotusotec.cl'));console.log(JSON.stringify(l,null,2))"`
Expected: a lista de destinos externos (redes sociais, `mailto:`, `tel:`, WhatsApp), possivelmente vazia — se vazia, o documento registra isso.

- [ ] **Step 2: Verificar cada destino externo**

```bash
node -e "const d=require('./docs/inventario/dom.json');const l=[...new Set(d.sections.flatMap(s=>s.links).map(x=>x.href).filter(h=>h&&h.startsWith('http')&&!h.includes('lotusotec.cl')))];console.log(l.join('\n'))" > /tmp/links.txt
while read -r url; do printf '%s %s\n' "$(curl -sS -o /dev/null -m 20 -w '%{http_code}' -L "$url")" "$url"; done < /tmp/links.txt
```

Expected: um código HTTP por link. Qualquer código diferente de `200` vira pendência no documento, nunca correção silenciosa.

- [ ] **Step 3: Redigir `docs/inventario/09-dados.md`**

Tabela `dado | valor publicado | onde aparece | status`, com `status` restrito a `verificado` (o próprio site é a origem e o destino responde) ou `pendente João` (número, certificação, credencial ou email cuja validade só a Lotus confirma). Cobre no mínimo: email de contato, telefone se houver, certificações e selos citados, números institucionais (anos, alunos, horas), nomes e cargas horárias dos cursos, e cada link externo com seu código HTTP.

- [ ] **Step 4: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario/09-dados.md
git commit -m "docs(2.1.9): validar dados institucionais e links externos publicados"
```

---

### Task 10: Matriz de paridade e fechamento (EAP 2.1.10)

**Files:**

- Create: `docs/inventario/README.md`
- Modify: `scripts/inventario/inventario.test.mjs`
- Read: todos os documentos anteriores

**Interfaces:**

- Consumes: `dom.json`, `01-estrutura.md` … `09-dados.md`.
- Produces: o critério de aceite visual e funcional que a Sprint 2 usa como contrato.

- [ ] **Step 1: Escrever os testes que falham**

Acrescentar a `scripts/inventario/inventario.test.mjs`:

```js
describe('inventário como artefato', () => {
  const docs = [
    '01-estrutura.md',
    '02-conteudo.md',
    '03-assets.md',
    '04-tipografia.md',
    '05-layout.md',
    '06-baseline.md',
    '07-formulario.md',
    '08-seo.md',
    '09-dados.md',
    'README.md',
  ]

  it('não deixa placeholder em nenhum documento', () => {
    for (const doc of docs) {
      const text = readFileSync(join(OUT, doc), 'utf8')
      expect(text).not.toMatch(/\bTBD\b|\bTODO\b|\bpreencher\b/i)
    }
  })

  it('cobre na matriz toda seção mapeada no DOM', () => {
    const dom = JSON.parse(readFileSync(join(OUT, 'dom.json'), 'utf8'))
    const matrix = readFileSync(join(OUT, 'README.md'), 'utf8')
    for (const section of dom.sections) {
      expect(matrix).toContain(section.id)
    }
  })
})
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `pnpm test`
Expected: FAIL — `ENOENT ... docs/inventario/README.md`.

- [ ] **Step 3: Escrever `docs/inventario/README.md`**

Estrutura do arquivo: **Índice** (link para os nove documentos, uma linha de propósito cada); **Como regerar** (os quatro comandos `pnpm inventario:*`, na ordem `dom → assets → styles → baseline`, com a observação de que `dom` precisa vir primeiro porque os outros o consomem); **Matriz de paridade**, tabela `item | seção/id | decisão | justificativa`, com `decisão` restrita a `fiel`, `divergência intencional` ou `pendente decisão`, cobrindo toda seção presente em `dom.json` mais os itens transversais; **Divergências intencionais propostas**, uma linha por item da lista da spec: Google Fonts remoto vs self-host, assets saindo de `lotusotec-cl.us.stackstaging.com`, ausência de `/wp-json`, `/xmlrpc.php` e feeds, destino da página residual `/http-18-230-15-185/`, criação de `meta description` e tags `og:`, e o `preloader.gif` do Divi; **Pendências de João**, consolidando o que `09-dados.md` marcou como `pendente João`.

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `pnpm test`
Expected: PASS — placeholder e cobertura da matriz verdes.

- [ ] **Step 5: Provar o aceite completo do bloco**

```bash
pnpm inventario:dom && pnpm inventario:assets && pnpm inventario:styles && pnpm inventario:baseline
git status --short docs/inventario
pnpm check
pnpm e2e
```

Expected: os quatro scripts terminam com código zero; `git status` mostra no máximo mudança de `capturedAt`, nunca de `sha256`; `pnpm check` verde; `pnpm e2e` com 2 testes aprovados.

- [ ] **Step 6: Formatar e commitar**

```bash
pnpm format
pnpm check
git add docs/inventario scripts/inventario
git commit -m "docs(2.1.10): criar matriz de paridade e divergências intencionais do clone"
```

---

## Handoff de execução

executor: claude
reviewer: codex+claude

paths_autorizados:

- `docs/inventario/**`
- `scripts/inventario/**`
- `package.json` (apenas o bloco `scripts`, chaves `inventario:*`)
- `vitest.config.ts` (apenas o campo `include`)
- `docs/superpowers/state.md`
- `docs/superpowers/backlog.md`
- `docs/superpowers/historico/progress.md`

A revisão roda em duas lentes: Codex sob `site-review-task` e Claude em subagente sem contexto de execução, ambos sobre `main..HEAD`. Claude no thread principal reconcilia os dois relatórios e confere cada achado material no diff real antes de aceitá-lo.
